# Automove Ideas

This is the live decision board for automove work. Keep it short.

Use `HOW_TO_ITERATE_ON_AUTOMOVE.md` for the operator flow, `docs/automove-knowledge.md` for durable rules, and `docs/automove-archive.md` for retired wave detail.

## Current State

- Public Pro routes through `frontier_pro_v2_guarded`.
- `shipping_pro_search` remains the retained search-only baseline.
- The live experiment surface is Pro-only and multi-variant: 2 retained profiles and 6 canonical stages.
- The default operator entrypoint is `./scripts/run-automove-canonical-loop.sh`.
- Current major reset handoff: read `docs/automove-major-reset-plan.md` before starting more automove source work. There is no live runtime hypothesis; future implementation should be either outcome-corpus V2, a test-only ProV4 unified root policy, or a corpus-calibrated utility feature.
- The current work is in structural-reset mode. Read `docs/automove-strategy.md` and run `./scripts/run-automove-structural-scout.sh <candidate>` before spending runtime code on another broad Pro change.
- This iteration did not produce a promotable challenger and retained no runtime source. The retained harness change adds exact-pressure and exact-timing mechanism-class axes to `pro-policy-outcome-corpus` and lets portfolio stoplights report repeated mechanism-class evidence above the known-bad count-2 threshold. A one-state sampled-Pro smoke verified the new axes on baseline-save records; a four-state full-portfolio sampled-Pro run was stopped as cost-blocked before stoplights, so the next source spend still needs a cheaper/repeated corpus discriminator before another selector.
- This iteration did not produce a promotable challenger and retained no runtime source. A test-only ProV4 unified-root prototype over guarded/head/legacy/top roots plus one allowed-head continuation probe repaired the first sampled Pro `inner_wedge_mana_rows` slice from guarded `0-2` to `1-1`, but regressed `center_spoke_mana_rows` from guarded `2-0` to `1-1`; total stayed `2-2` while average move time rose to `206.82ms` vs guarded `130.08ms`. Attribution showed singleton black `frontier_execute` tradeoffs: turn-six `window=0/deny=0` inner-wedge repair versus turn-eight `window=1/deny=1` center-spoke regression. Do not retry root-origin or post-root continuation comparators without a corpus-trained feature that separates those contexts.
- This iteration did not produce a promotable challenger and retained no runtime source. A broad sampled cross-budget reset portfolio was stopped after roughly five minutes because it only reached the first state and reproduced a budget conflict, so future cross-budget scouting should stay one policy family at a time. Shipping-control alone was also not a source path: sampled four-state replay was `mixed_cross_budget` with one clean repair, one budget conflict, and one no-help state; active four-state replay had one non-regressing repair, two no-help states, a baseline-only all-budget save, and no repeated stable mechanism. The retained harness change is clearer cross-budget stoplights and all-budget save counts (`coverage_gap`, `partial_repair_coverage_gap`, `baseline_save_risk`), so singleton repairs are not mistaken for source permission.
- This follow-up did not produce a promotable challenger and retained no runtime source. Conflict-class cross-budget replay showed the tempting safe-step-to-spirit class is not separable: active `alternating_mana_rows` white had a `budget_conflict` with the same safe `ManaTempo` safe-step to safe `SpiritImpact` development shape. The opposite-direction scoped full-scored candidate, `frontier_pro_v4_spirit_to_mana_step_reply_guard`, was killed on sampled Pro at `6-6` with `inner_wedge_mana_rows=0-2`, `forward_bridge_mana_rows=1-3`, `15` overrides, and `222.34ms` average candidate turns. Do not retry bidirectional safe-step/spirit reply-guard routing; the next source spend needs a different measured feature, not another filter over full-scored reply guard.
- This iteration did not produce a promotable challenger and retained no runtime source. The useful retained change is cross-budget mechanism-class visibility: `pro-policy-cross-budget` can now emit `PRO_POLICY_CROSS_BUDGET_STOPLIGHT` and stable repair mechanism classes behind clean/non-regressing policies. A sampled one-state smoke showed `frontier_pro_v3_full_scored_reply_guard` as a non-regressing `inner_wedge_mana_rows` repair with safe `ManaTempo` safe-step progress losing to safe `SpiritImpact` development, but an active-blocker four-state slice was mixed: one clean all-budget outer-edge repair, two budget conflicts, one baseline-only all-budget save, and no repeated stable mechanism (`max_stable_mechanism_class_states=1`). Do not retry full-scored reply guard or safe-spirit/safe-step routing from this evidence; the next candidate still needs a repeated cross-budget-stable utility feature or a real ProV4 unified root policy.
- This iteration did not produce a promotable challenger and retained no source. A bounded active Pro outcome corpus showed tempting safe-step-to-spirit class evidence (`candidate_only_wins=3`, `baseline_only_wins=0`, `no_policy_wins=0`, `max_mechanism_class_games=3`), but exact winner policy/context/pair maxima were still `1`. Two discarded ProV4-style safe-spirit/full-scored reply-guard candidates stayed active-Pro `3-3` with high cost, and the existing `frontier_pro_v3_full_scored_reply_guard` also measured active Pro `3-3` across outer-edge, alternating, and forward-bridge splits. Do not retry safe `SpiritImpact` over safe-step progress or safe full-scored reply-guard filtering from this evidence; it is another selector-pressure branch unless a new cross-budget corpus feature separates those wins from rotated losses.
- This iteration did not produce a promotable challenger. A bounded active-blocker outcome corpus again found only selector-pressure evidence: active Pro and Fast had candidate-only wins but singleton winner policy/context/pair aggregates, while active Normal was shared-only. Widening the tempting active Fast lower-live `ManaTempo` class to six states killed the source path: the portfolio had `candidate_only_wins=4` but `baseline_only_wins=1`, `baseline_save_risk`, and mixed deltas for the surviving candidates. The retained harness change is baseline-save visibility: portfolio mechanism-class mode now also emits `PRO_POLICY_MATRIX_PORTFOLIO_BASELINE_BETTER_MECHANISM_CLASS` and `max_baseline_better_mechanism_class_games`, so future iterations can compare candidate-only repair classes against guarded save classes before writing runtime code. Do not encode lower-live or no-selected-followup routing unless a new corpus-side feature separates wins from baseline saves across Pro/Normal/Fast.
- This iteration did not produce a promotable challenger. A refreshed structural scout kept retained guarded `not_promising`: sampled Pro/Normal/Fast `7-5 / 7-5 / 6-6`, active blockers `3-3 / 5-1 / 2-4`, and the bounded policy corpus again had exact context/pair/mechanism maxima of `1` with only a broad safe-step-to-spirit mechanism-class repeat. A mechanism-class active outcome matrix reached active Pro only before it became too expensive for all duels; that partial result still showed `singleton_selector_pressure` (`candidate_only_wins=3`, `no_policy_wins=0`, winner policy/context/pair maxima all `1`, `max_mechanism_class_games=2`). The retained harness change is bounded outcome-corpus support: structural scout `--outcome-corpus` now defaults to `SMART_PRO_POLICY_MATRIX_STATE_LIMIT=2` and candidate-only portfolio mechanism classes. Do not write runtime code from this signal; widen the bounded outcome corpus only if it reports repeated winner context/pair/class evidence.
- This iteration did not produce a promotable challenger. A filtered sampled-Normal `pro-policy-outcome-corpus` made raw ProV2 and no-selected-followup look non-regressing on a tiny slice, but seed-aligned `pro-policy-cross-budget` over guarded/raw/no-selected classified the set as `0` clean repairs, `1` nonregressing repair, and `2` `budget_conflict` states. A retained guarded Normal/Fast decision-record refresh with the new `SMART_PRO_SWEEP_DECISION_RECORD_INCLUDE_MECHANISM_CLASS=true` flag stayed singleton-heavy: Normal `5` nonwins / `4` regressions with `max_mechanism_class_games=2`, Fast `6` / `4` with `max_mechanism_class_games=2`. The useful retained change is harness visibility: `pro-sweep-decision-record` now emits `PRO_SWEEP_DECISION_RECORD_MECHANISM_CLASS` and includes `max_mechanism_class_games` in its stoplight. Do not retry raw early-white, no-selected-followup, or count-2 class routing from this signal; require a repeated mechanism class above the known-bad count-2 threshold or a new cross-budget-stable utility feature before runtime code.
- This iteration did not produce a promotable challenger and retained no source. Rechecking the sampled-pass iterative-deepening row composite again failed active blockers (`3-3 / 3-3 / 5-1`), and adding outer-edge shipping-window fallbacks only moved the active panel to `3-3 / 4-2 / 5-1`. A snapshot-based advisor reply-floor policy then failed the sampled dashboard at Pro/Normal/Fast `5-7 / 9-3 / 8-4` after `100+` overrides per duel. Do not retry hand-tuned root reranking over existing primary utility, progress/setup, and reply-floor fields without a corpus-trained discriminator.
- This iteration did not produce a promotable challenger. A refreshed structural scout kept retained guarded `not_promising`: sampled Pro/Normal/Fast `7-5 / 7-5 / 6-6`, active blockers `3-3 / 5-1 / 2-4`, with sampled weak rows spread across inner-wedge, forward-bridge, corner-chain, offset-arc, and center-spoke. The bounded policy corpus now works as the default scout corpus path and found only partial singleton-style routing evidence (`max_mechanism_games=1`; global `max_mechanism_class_games=3` from broad safe-step-to-spirit-development class). A discarded safe `SpiritImpact` over safe-step `ManaTempo` test selector fired only a few times and was already non-promotable on sampled Pro/Normal (`6-6 / 7-5`) before Fast completed. `run-automove-structural-scout.sh --corpus` now defaults to `SMART_PRO_POLICY_WINNER_STATE_LIMIT=2` and `SMART_PRO_POLICY_WINNER_CANDIDATE_TRACE_LIMIT=64`; widen only after a bounded corpus shows a stronger repeated mechanism.
- This iteration did not produce a promotable challenger. A capped active-blocker Fast policy-winner slice again found only shipping-control repairs on `outer_edge_mana_rows`, and exact seed-aligned cross-budget replay split them into one white `budget_conflict` and one black non-regressing repair. A discarded test-only black outer-edge action+mana shipping-control selector improved the targeted active Fast panel only to `3-3`, while stacking it with known sampled opening components was cost-blocked before sampled dashboard summaries. The retained harness now prints effective `seed_tag` in policy-winner and cross-budget summaries/records; copy that value into `SMART_PRO_POLICY_CROSS_BUDGET_SEED_TAG` for exact follow-ups.
- A broader reset review now lives in `docs/automove-reset-review.md`, and the current whole-project review lives in `docs/automove-structural-review.md`. The next major path should be either an outcome-corpus diagnostic that ranks repeated mechanisms before runtime work, or a test-only ProV4 unified root policy that ranks guarded, raw, preserved, omitted, head, fallback, and shipping-control roots in one comparator.
- In reset mode, use `./scripts/run-automove-structural-scout.sh --corpus frontier_pro_v2_guarded` as the default no-live-hypothesis command before writing runtime code.
- This iteration did not produce a promotable challenger. The new `axis=rank_score_delta` winner-corpus view was widened to sampled Fast and still ended as `singleton_residue` (`baseline_wins=6`, `policy_wins=6`, `no_policy_wins=0`, `max_mechanism_class_games=2`); every rank/score-delta bucket was singleton and the only repeat was a broad safe-step safety/progress bucket. A discarded cached utility/root tiebreak used a new opt-in test snapshot to avoid rerunning scored roots after guarded selection, but even the widened version only reached `1-1` on the one-opening Pro/Normal/Fast micro sweep. Keep the snapshot harness for future cheap root-feature probes, but do not retry utility/rank tiebreaking without a new repeated corpus feature.
- This iteration did not produce a promotable challenger. A discarded test-only `frontier_pro_v4_utility_eval_tiebreak` selector was cost-blocked even after narrowing to low-rank selected roots and top-3 alternatives, and even a one-opening profile sweep did not return quickly. The retained harness now adds `axis=rank_score_delta` to mechanism-class policy-winner output so future corpus work can see coarse winner-vs-baseline rank and score gaps without selector-time utility recomputation. A full sampled Normal winner slice still ended as `singleton_residue` (`baseline_wins=7`, `policy_wins=5`, `no_policy_wins=0`, `max_mechanism_class_games=2`); the new rank/score-delta axis had no repeats, while the only repeated classes were broad role/safety buckets. Do not build a selector from those broad repeats.
- This iteration did not produce a promotable challenger. The retained harness now supports `SMART_PRO_POLICY_MATRIX_INCLUDE_MECHANISM_CLASS=true`, emitting `PRO_POLICY_MATRIX_MECHANISM_CLASS` records for nonzero candidate-vs-baseline deltas so outcome-corpus slices can compare candidate wins against baseline saves/regressions by coarse root/advisor/head/utility class. A narrow active Fast run showed raw ProV2 is non-regressing on that slice (`candidate_better=2`, `baseline_better=0`), but a discarded test-only relaxation of the exposed early-white and late-black fallback surfaces failed sampled Pro at `7-5` and spread losses across center, alternating, inner-wedge, and forward-bridge rows. Do not retry fallback relaxation from active Fast raw wins without a new feature that also clears sampled Pro/Normal/Fast.
- This iteration did not produce a promotable challenger. A focused active Fast mechanism-class matrix again showed portfolio oracle coverage but `singleton_selector_pressure`; raw was non-regressing on that slice (`candidate_better=2`, `baseline_better=0`), while no-selected-followup had mixed deltas. A discarded exact active-Fast no-followup window candidate still failed sampled Pro at `7-5` with `inner_wedge_mana_rows` `0-2`. The useful retained change is harness visibility: `PRO_POLICY_MATRIX_PORTFOLIO_STOPLIGHT` now reports `max_mechanism_class_games` when mechanism-class classification is enabled.
- This iteration did not produce a promotable challenger. The new mechanism-class field showed the active Fast portfolio still had `singleton_selector_pressure`, with only coarse `max_mechanism_class_games=2`. A discarded test-only lower-live safe-step `ManaTempo` recovery used that repeated class directly and failed sampled Pro at `5-7` after `142` overrides, with center-spoke and forward-bridge regressions. Treat repeated mechanism classes at count `2` as routing evidence only; require a narrower cross-budget feature before source work.
- This iteration did not produce a promotable challenger. The retained harness now prints `PRO_POLICY_WINNER_GLOBAL_*` aggregates and a `repeated_mechanism_class` stoplight only when mechanism-class evidence exceeds the known-bad count-2 threshold. A full/default mechanism-enabled policy-winner corpus was cost-blocked before summaries, and a tiny sampled Pro smoke still showed singleton residue on two `inner_wedge_mana_rows` policy wins. Keep mechanism-enabled winner runs filtered by panel/duel/sample size before using them for feature design.
- This iteration did not produce a promotable challenger. The retained harness now supports `SMART_PRO_POLICY_WINNER_STATE_LIMIT`, `SMART_PRO_POLICY_CROSS_BUDGET_STATE_LIMIT`, and `SMART_PRO_POLICY_CROSS_BUDGET_SEED_OPPONENT_MODE` so broad policy-winner hints can be followed by exact seed-aligned cross-budget checks. A focused active Fast slice repeated only the known coarse lower-live safe-step `ManaTempo` class at count `2`; the aligned cross-budget replay split `frontier_pro_v2_no_selected_followup_projection` into one white clean repair and one black `budget_conflict`. Do not turn active Fast no-followup/ManaTempo count-2 evidence into source code.
- This iteration did not produce a promotable challenger. A discarded test-only cross-budget utility admission selector preserved guarded wrapper branches and only considered non-regressing Pro/Normal/Fast primary utility axes, but even the sampled-only dashboard was cost-blocked before output after more than two minutes; narrowing to vulnerable/nearby roots did not fix the cost. Capped sampled Normal/Fast policy-winner checks also stayed singleton-heavy: Normal split into two one-off shipping-control SpiritImpact repairs, and Fast exposed only one omitted shipping-control root. Do not run cross-budget utility evaluation inside the selector; this needs corpus-side/precomputed features first.
- This iteration did not produce a promotable challenger. A discarded test-only spirit/rank utility selector improved sampled Pro to `10-2`, and stacking two known early-white opening winners reached sampled Pro `11-1` (`strict_passes=true`, confidence `0.9968`), but the full sampled dashboard failed Normal `6-6` and Fast `6-6`. The follow-up policy-winner pass showed all six sampled Normal misses had existing winners, but every winner policy/context/pair aggregate was singleton (`singleton_residue`). Do not turn sampled-Pro opening/utility winner records into static exact gates; the next useful path still needs a cross-budget repeated mechanism or a new utility feature below policy labels.
- This iteration did not produce a promotable challenger. The retained harness now emits `PRO_POLICY_WINNER_MECHANISM_CLASS` alongside exact `PRO_POLICY_WINNER_MECHANISM` records so future corpus work can distinguish true singleton residue from over-keyed exact mechanism strings. Narrow corpus checks still did not justify runtime code: sampled Pro had exact singleton mechanisms and only a broad safe-step class repeated `2x`; active Fast had a `2x` selected-head vs pre-accept/legacy class, but a discarded test-only legacy-preaccept head veto failed sampled Pro at `8-4` (`not_promising`) and rotated losses into center-spoke, alternating, inner-wedge, and forward-bridge. Do not retry pre-accept/legacy head vetoes unless a new feature separates harmful accepted heads from saved head continuations across sampled and active panels.
- This iteration did not produce a promotable challenger. The structural scout reconfirmed retained guarded as `not_promising`: sampled Pro/Normal/Fast `7-5 / 7-5 / 6-6`, active blockers `3-3 / 5-1 / 2-4`; the full policy corpus again had `no_policy_wins=0` but `singleton_residue` in every sampled and active Pro/Normal/Fast duel. Wrapper-preserving config retests still missed sampled Pro (`no_selected_followup_projection` `6-6`; head-rerank, expansion-224, and no-low-budget each `7-5`). A discarded test-only Normal/Fast budget-consensus selector was too broad (`5-7`), and even primary-axis utility gating only reached `6-6` while raising average move time above `220ms`. Do not retry budget-consensus routing unless it introduces a new feature below policy output agreement.
- This iteration did not produce a promotable challenger. A test-only online policy-rollout portfolio over guarded, shipping-control, raw, no-selected-followup, and full-scored reply guard was killed on cost: even after tightening to early `frontier_execute` turns, two-ply Pro/Fast continuation, and sampled Pro-only validation, it still did not complete the quick dashboard in a practical window. Dormant wrapper-preserving toggles also did not move the sampled Pro shape: enabling mid-turn progress guard and enabling late-black setup-progress rescue both reproduced `7-5`, `inner_wedge_mana_rows` `0-2`, and `not_promising`. No source candidate was retained.
- This iteration did not produce a promotable challenger. A fresh structural scout reconfirmed retained guarded as `not_promising`: sampled Pro/Normal/Fast `7-5 / 7-5 / 6-6`, active blockers `3-3 / 5-1 / 2-4`; policy corpus again had `no_policy_wins=0` with `singleton_residue` in every sampled and active Pro/Normal/Fast duel. Discarded test-only shallow ProV4 root-value comparators topped out at sampled Pro `9-3`: the latest opening-gated version fixed the alternating row but still missed center, inner-wedge, and forward-bridge; `pro-sweep-decision-record` labeled the residue `singleton_regression_pressure`, and policy-winner repair labels split one each across guarded, raw, and shipping-control with no repeated context. Do not retry a single shallow root-value tuple over existing score/utility/progress/safety fields; the next ProV4 attempt needs corpus-trained features or a retained harness change that explains repeated mechanisms below these singleton records.
- This iteration did not produce a promotable challenger. Existing dormant sweep candidates failed the sampled Pro quick gate: `frontier_pro_v2_no_spirit_family` went `5-7` with `inner_wedge_mana_rows` and `forward_bridge_mana_rows` both `0-2`, and `frontier_pro_v2_no_mid_tactical_guard` reproduced `7-5` with `inner_wedge_mana_rows` `0-2`. A discarded test-only continuation-stability root selector that preserved guarded wrappers and projected one more turn-engine plan for candidate roots was also non-promotable: the loose version reached only `8-4` with `41` overrides and `272.56ms` average move time, while the strict primary-axis version fell back to `7-5` with `37` overrides and `265.99ms`. Do not retry post-root turn-engine continuation utility as a root selector without a new lower-cost discriminator below the current utility axes.
- This iteration did not produce a promotable challenger. Raw config-feature scouts for local attack-reach scoring context and child/two-stage ordering missed the sampled Pro gate (`6-6` then `5-7`, and `8-4` respectively); wrapper-preserving scouts for lazy score-window projection and walk-threat prefilter both reproduced guarded sampled Pro `7-5` with `inner_wedge_mana_rows` at `0-2`. The retained useful work is harness hygiene: config-toggle sweep candidates now preserve the public guarded wrapper unless the candidate is explicitly `frontier_pro_v2_raw`, and search-preferability cache keys now include local scoring-context flags.
- This iteration killed and discarded a test-only policy-timing composite. Stacking existing opening components with inner-wedge SpiritImpact opening, forward-bridge pre-accept timing, black window rank-0 selection, and late black head timing passed sampled Pro (`11-1`, confidence `0.9968`, avg `~274ms`) but failed the sampled dashboard on Normal (`8-4`) and Fast (`7-5`) with singleton action+mana/non-action residuals across forward-bridge, inner-wedge, corner-chain, offset-arc, and center-spoke rows. Do not spend further on sampled-Pro policy-timing exact gates unless a corpus shows the same mechanism is cross-budget stable.
- This iteration killed and discarded a test-only ProV4 unified root-value comparator. Broad sampled Pro reached only `8-4` (`0.6667`, confidence `0.8062`, `242` overrides) with center/alternating/split regressions; tighter and opening-guarded variants fell to `7-5`. The retained useful change is harness guidance: `pro-promotion-dashboard` now emits `PRO_PROMOTION_DASHBOARD_STOPLIGHT`, and `pro-policy-corpus` now emits `PRO_POLICY_WINNER_STOPLIGHT`, so future reset iterations should follow those labels before source edits.
- This iteration stayed structural and did not produce a runtime challenger. Retained guarded still failed sampled dashboard shape (Pro `7-5`, Normal `7-5`, Fast `6-6`) and active blockers (Pro `3-3`, Normal `5-1`, Fast `2-4`), with weakest rows rotating by panel and budget. The full default policy corpus had `no_policy_wins=0` for sampled and active-blocker Pro/Normal/Fast duels, but every policy and mechanism aggregate was singleton (`games=1`), so coverage still does not justify a static policy-label selector. Next useful spend remains outcome-corpus stoplight / ProV4 unified root policy / new measured utility feature. `run-automove-structural-scout.sh` now skips the redundant guarded self-delta when the candidate is exactly `frontier_pro_v2_guarded`.
- This iteration stayed harness-first and did not produce a runtime challenger. `./scripts/run-automove-experiment.sh pro-policy-corpus <portfolio>` now runs policy-winner with `PRO_POLICY_WINNER_MECHANISM` aggregates tying first winning policies back to guarded root/advisor/head/utility status. Use it before spending on another selector from policy labels.
- Quick automove iteration uses seeded sampled game variants; final promotion confirmation uses all current variants.
- There is no second live challenger today.
- This iteration completed the active-blocker `pro-policy-outcome-corpus` validation across Pro/Normal/Fast for the full reset portfolio. Every duel produced `singleton_selector_pressure`: Pro had `candidate_only_wins=3`, Normal `1`, Fast `4`; all had `baseline_only_wins=0`, `no_policy_wins=0`, and winner policy/context/pair maxima of `1`. This closes the current static selector branch; do not enable decision probes or write a selector unless a new corpus first produces a repeated winner context or pair.
- This iteration stayed harness-first and did not produce a runtime challenger. `pro-policy-outcome-corpus` is now a first-class runner/scout path for the policy-matrix outcome corpus and stoplights. Use it with `SMART_PRO_POLICY_MATRIX_PANEL_FILTER` / `SMART_PRO_POLICY_MATRIX_DUEL_FILTER` before expensive decision probes or another selector.
- This iteration stayed harness-first after an active Fast policy matrix with decision probes proved too slow to use as a default scouting primitive. `pro-policy-matrix` now emits cheap candidate and portfolio stoplights from already-computed outcome summaries; future work should read those labels before enabling `SMART_PRO_POLICY_MATRIX_INCLUDE_DECISION_PROBE=true`. Validation on the active Fast portfolio produced `singleton_selector_pressure`: `no_policy_wins=0` and `candidate_only_wins=4`, but `max_winner_policy_games=1`, `max_winner_context_games=1`, and `max_winner_pair_games=1`.
- This iteration stayed harness-first and did not produce a promotable runtime candidate. `pro-sweep-decision-record` now emits `PRO_SWEEP_DECISION_RECORD_STOPLIGHT`; a full retained guarded sampled validation still had Pro `5` nonwins / `3` regressions, Normal `5` / `4`, and Fast `6` / `4`, with `max_context_games=1` and `max_pair_games=1` in every duel. Future candidates with `branch_only_with_regressions` or `singleton_regression_pressure` should be killed before attribution or runtime code.
- This iteration killed and discarded temporary recovery-rank/opening-utility scouts. Adding `SpiritImpact` to same-family head-utility risk comparison failed sampled Pro at `7-5` and made `inner_wedge_mana_rows` `0-2`. Demoting `DrainerSafetyRecovery` behind `SpiritImpact` and safe-progress families fixed inner-wedge and reached sampled Pro `9-3`; stacking the retained alternating-white component and the white-opening utility component only reached `10-2` (`0.8333`, confidence `0.9807`) with `forward_bridge_mana_rows` still split `2-2`. The focused decision records were singleton-heavy: two forward-bridge regressions and two flat losses split by color, turn, action/mana availability, attack/window context, and exact move pair. No runtime or test-only candidate source was retained.
- This iteration killed and discarded a temporary `inner_wedge_mana_rows` full-scored reply-guard scout. A minimal `pro-policy-cross-budget` smoke found `frontier_pro_v3_full_scored_reply_guard` was non-regressing on the first sampled inner-wedge opening for both colors, but widening that into a scoped sampled promotion-dashboard candidate failed vs shipping Pro at `7-5` (`0.5833`, confidence `0.6128`) and made the same `inner_wedge_mana_rows` row `0-2`. Single-opening cross-budget repairs do not generalize into variant-scoped full-scored routing.
- This iteration stayed diagnostic-only and did not produce a promotable candidate. A fresh retained `frontier_pro_v2_guarded` sampled promotion-seed decision-record refresh still showed singleton-heavy nonwins: Pro `5/12`, Normal `5/12`, Fast `6/12`. Every recorded nonwin went through `frontier_execute`, but the records split across variant, color, turn, resource state, family, accepted-head/advisor context, and exact move pair. Treat "all nonwins are frontier_execute" as too broad for runtime code unless a lower-level repeated mechanism appears.
- This iteration killed and discarded a temporary portfolio allowed-head continuation scout. The broad version, which gathered first moves from guarded, shipping-control, raw, no-selected-followup, full-scored reply guard, no-low-budget, alternating-white, and white-opening utility policies then let the turn engine choose among them as allowed heads, was too expensive for a Pro-only sampled dashboard and was terminated after several minutes. A narrowed early-conflict version finished but failed sampled Pro-only at `8-4` (`0.6667`, confidence `0.8062`, avg `311.91ms`) after `33` overrides, with `alternating_mana_rows` at `0-2`, `inner_wedge_mana_rows` at `1-1`, and `forward_bridge_mana_rows` at `3-1`. Allowed-head continuation over the existing policy portfolio is not the missing shared utility feature.
- This iteration killed and discarded a temporary sticky policy-continuation scout. Continuing sampled shipping/no-low-budget policy choices for four follow-up turns and falling back to retained guarded failed sampled Pro-only at `10-2` (`0.8333`, confidence `0.9807`), still missing `alternating_mana_rows` and `forward_bridge_mana_rows`. Stacking the retained alternating-white component restored sampled Pro to `11-1`, but the full sampled panel failed Normal `8-4` and Fast `6-6`; policy continuation plus known policy components is still a sampled-Pro false positive, not a cross-budget utility feature.
- This iteration killed and discarded a temporary policy-agreement scout over guarded, shipping-control, raw, no-selected-followup, full-scored reply guard, alternating-white edge mana, and white-opening utility mana. Both loose and strict agreement thresholds failed sampled Pro-only promotion dashboard at `5-7` (`0.4167`, confidence `0.0000`) with `inner_wedge_mana_rows` at `0-2`, and average move time was already high at `523.93ms` / `529.63ms`. Existing-policy consensus is not the missing cross-budget utility feature.
- This iteration stayed structural/test-only and did not produce a promotable candidate. A sampled promotion-dashboard-seed decision-record refresh for retained guarded showed singleton-heavy nonwins across all three duels: Pro `5/12` nonwins, Normal `5/12`, Fast `6/12`, all through `frontier_execute` but split by variant, color, turn, family, advisor status, and head acceptance. Two temporary head-layer scouts were discarded: no post-search head acceptance failed sampled `4-8 / 7-5 / 8-4`, and strict primary-axis-only head acceptance reproduced sampled Pro `4-8` after only `2` accepted heads and `20` vetoes. Broad head suppression is not the missing shared utility feature.
- This iteration stayed diagnostic/test-only and did not produce a promotable candidate. A retained canonical sampled decision-record refresh now has `0` sampled Pro nonwins, `1` sampled Normal nonwin, and `2` sampled Fast nonwins. Normal's only nonwin is a black `outer_edge_mana_rows` flat loss where shipping also loses; Fast still has one black `alternating_mana_rows` flat loss plus the known white `forward_bridge_mana_rows` accepted-head regression.
- This iteration killed and discarded two temporary scouts. A black turn-four weak-window shipping defer did not improve its local alternating slice and failed the sampled structural dashboard at Pro `7-5`. A late-black recovery defer targeted the sampled Fast flat-loss board after a forced-root oracle found `9/16` winning roots including rank-0 `DrainerSafetyRecovery`, but it failed the sampled structural dashboard at Pro `7-5`, Normal `7-5`, Fast `6-6` and did not reach an admissible defer branch there.
- This iteration stayed harness-first and did not produce a promotable runtime candidate. `pro-policy-cross-budget` / `smart_automove_pro_policy_cross_budget_probe` now replays the same opening side against Pro, Normal, and Fast shipping opponents and reports all-budget wins, non-regressing repairs, budget conflicts, and no-help classes. A narrow smoke over guarded, shipping-control, and raw found the sampled `inner_wedge_mana_rows` white side is a `budget_conflict` (`raw` fixes Pro but regresses Normal), while the black side is a clean shipping-control repair across all three budgets.
- This iteration killed and discarded a temporary cross-budget-clean repair composite. Active `outer_edge_mana_rows` white turn-three action+mana became a clean all-budget full-scored reply-guard repair on a one-opening smoke, and sampled `inner_wedge_mana_rows` black turn-four mana-only became a clean all-budget shipping-control repair. Shipping only the active outer-edge repair failed sampled `7-5 / 7-5 / 6-6`; adding the sampled inner-wedge black repair only moved sampled Pro to `8-4` while Normal stayed `7-5` and Fast stayed `6-6`. Clean one-opening cross-budget repairs are not enough as a static composite.
- This iteration killed and discarded two temporary quiet mana-utility scouts. The broad scout selected near-top quiet `ManaTempo` roots by `TurnEngineUtility` in quiet mana-available contexts through turn six. It was too broad: sampled Pro still failed at `9-3` (`0.7500`, confidence `0.9270`, avg `210.93ms`) after `79` quiet-mana overrides, with weak `center_spoke_mana_rows`, `alternating_mana_rows`, and `forward_bridge_mana_rows`; a focused decision-record attempt exposed a generic black turn-two `center_spoke_mana_rows` `candidate_execute` flat loss before the run was stopped for cost. Tightening the same feature to after-opening turns fixed the center row but worsened sampled Pro to `8-4` (`0.6667`, confidence `0.8062`) after `45` overrides, with weak `alternating`, `inner_wedge`, `split_flank`, and `forward_bridge` rows.
- This iteration also partially refreshed retained guarded sampled decision records before stopping the broad run for cost. The completed sampled Pro duel still showed singleton-heavy residue: `5` nonwins across `12` games, all through `frontier_execute`, split across `inner_wedge_mana_rows`, `forward_bridge_mana_rows`, and `alternating_mana_rows`; only `forward_bridge_mana_rows` repeated, and even that repeated across different color/turn/resource contexts.
- This iteration killed and discarded a temporary symmetric selected-followup utility projection. Broadening the existing black-only safe `ManaTempo` / `DrainerSafetyRecovery` followup projection to both colors failed sampled Pro immediately at `9-3` (`0.7500`, confidence `0.9270`, avg `153.02ms`) and collapsed `inner_wedge_mana_rows` to `0-2`, so the sampled pressure is not solved by simply making that utility path color-symmetric.
- This iteration killed a broad policy-output utility scout and refreshed the full-scored reply guard no-go. The temporary policy-output scout asked known policies for candidate moves and admitted only moves whose `TurnEngineUtility` cleared guarded override/eval gates, but it was too expensive to finish even the sampled Pro-only dashboard after several minutes and was discarded. The existing `frontier_pro_v3_full_scored_reply_guard` also failed sampled Pro directly at `1-11` (`0.0833`, avg `176ms`) with weak rows in `center_spoke_mana_rows`, `alternating_mana_rows`, `inner_wedge_mana_rows`, `split_flank_mana_rows`, and `forward_bridge_mana_rows`.
- This iteration killed and discarded a completed same-turn plan pressure/admission scout. The candidate completed candidate roots to opponent handoff, compared reply count and worst-reply score after the completed plan, and admitted roots when guarded had no completed pressure state; it still failed sampled Pro at `7-5`, logged `365` `completed_plan_guarded_pressure_missing` turns and `137` `completed_plan_no_candidate` turns, never reached an override/admission branch, and raised average Pro move time to about `216ms`.
- This iteration killed and discarded temporary opponent-mobility and plan-cache scouts. A root opponent-mobility pressure candidate was structurally mostly unavailable because most scored roots are intra-turn, not post-handoff: on sampled Pro it logged `428` `mobility_guarded_pressure_missing` turns, `74` `mobility_no_candidate`, no override branch, and reproduced the guarded `7-5` sampled-Pro miss. A persistent turn-engine plan-cache scout was a real behavior change and faster, but still failed sampled Pro at `8-4`; stacking the retained opening components improved only to `9-3`; adding raw ProV2 on `inner_wedge_mana_rows` improved only to `10-2`; widening raw to `forward_bridge_mana_rows` collapsed back to `8-4`.
- Do not retry root-level or completed-turn opponent mobility/pressure without a new utility feature; immediate roots are mostly pre-handoff, and completed-plan pressure still produced no admissible candidate under score/safety/utility gates. Do not promote persistent plan-cache preservation or variant raw routing as direct candidates; cache continuity is an interesting structural signal, but the current sampled Pro weak rows still rotate before Normal/Fast or active panels are worth running.
- This iteration stayed test-only and did not produce a promotable candidate. A retained sampled-Pro decision-record refresh for guarded logged `5` nonwins across `12` games: `3` regressions and `2` flat losses, all through `frontier_execute`, split across inner-wedge white flat, inner-wedge black window regression, forward-bridge white regression, forward-bridge black late regression, and alternating white flat. A capped sampled-Pro policy-winner refresh found only singleton policy wins before the cap (`raw` fixing an inner-wedge white loss from an earlier ply, and `shipping_pro_search_control` fixing the inner-wedge black window board), so it reinforced the timing/context conflict rather than giving a selector.
- This iteration killed and discarded two temporary scouts. A continuation-floor progress root tiebreak preserved guarded wrapper branches and overrode only `frontier_execute` roots with a much better worst-reply floor plus progress/setup gain; it still failed sampled Pro at `7-5`, fired only `2` turns, left `inner_wedge_mana_rows` at `0-2`, and raised average Pro move time to about `198ms`. A root-live black-window shipping-control plus early-white utility-veto scout failed sampled Pro at `8-4`; the black window branch fired once, the early-white veto never fired, and alternating, inner-wedge, and forward-bridge all split at `0.5000`.
- This iteration killed and discarded a temporary exact-opening/root-control scout. Guarded refreshed at sampled Pro `7-5`; adding white raw opening tail plus a root-live black-window shipping control reached `9-3` and fixed inner-wedge, but still left alternating `1-1` and forward-bridge `2-2`. Tightening further with exact inner-wedge raw tail, a forced forward-bridge opening sibling, and a late black pathological-score shipping control finally passed sampled Pro at `11-1` (`0.9167`, confidence `0.9968`, avg `145ms`), but failed sampled Normal `7-5`, sampled Fast `6-6`, and active Pro `3-3` with `outer_edge_mana_rows` at `0-2`. The useful signal is negative: sampled-Pro-only exact opening/root controls still do not touch the active blocker panel.
- An earlier diagnostic iteration stayed diagnostic-only and did not produce a promotable candidate. That retained decision-record refresh found no repeated mechanism to spend on: sampled nonwins were singleton-heavy (`vs_shipping_pro` had no nonwins, Normal had one flat `ManaTempo` post-search drift, Fast had one flat plus one `forward_bridge_mana_rows` same-family `SpiritImpact` accepted-head regression), and the active-blocker panel stayed split as Pro `3` nonwins / `3` mechanisms, Normal `1` accepted-head regression, Fast `4` nonwins / `4` mechanisms.
- A compact sampled policy matrix briefly made `frontier_pro_v2_no_low_budget_guard` look useful, but the canonical sampled dashboard killed it again: Pro `7-5` (`0.5833`), Normal `10-2` (`0.8333`) with `forward_bridge_mana_rows` at `0-2`, and Fast `9-3` (`0.7500`). Do not reopen no-low-budget from tiny matrix evidence.
- The expanded portfolio still has sampled oracle coverage, not selector viability. `pro-policy-winner` over guarded, alternating-white edge mana, white-opening utility mana, shipping-control, raw ProV2, no-selected-followup, full-scored reply guard, and no-low-budget produced sampled Pro `7` baseline wins / `5` policy wins / `0` no-policy wins, sampled Normal `7 / 5 / 0`, and sampled Fast `6 / 6 / 0`; this confirms the policy set contains winning lines but does not change the no-go on static policy selectors.
- A test-only ProV3 reply-interval floor feature was killed and discarded. Penalizing roots by the position after the opponent's best immediate reply failed sampled Pro at `7-5` with `43` overrides and `alternating_mana_rows` at `0-2`; a conservative non-dropping-search variant still failed sampled Pro at `9-3` with weak `alternating`, `inner_wedge`, and `forward_bridge` rows; restricting it to concrete score/pickup roots became effectively inert and still reproduced a `7-5` sampled-Pro failure shape. Do not retry opponent-interval safety as a root override/tie-breaker.
- This iteration killed and discarded a test-only exact score-race/root-admission line. A root override using exact own-vs-opponent score-path margin was effectively inert on sampled Pro (`2` overrides) and stayed at `7-5`; relaxing the margin from two steps to one step did not change branch count or result. Adding a first-turn early-white fallback utility admission touched `9` turns but still stayed `7-5`, which confirms the latest policy-winner roots need later profile behavior too, not just first-diff root replacement.
- Latest iteration stayed test-only and did not produce a promotable direct profile. A forced-root oracle on the sampled Pro white `alternating_mana_rows` no-policy board found that the retained guarded root set already had winning alternatives, but the selected root was losing. The resulting narrow sweep candidate `frontier_pro_v3_alternating_white_edge_mana` fixes that oracle class and is useful for policy-matrix coverage, but structural scout killed it as a direct challenger: sampled `8-4 / 8-4 / 6-6` and active blockers `3-3 / 6-0 / 2-4` across Pro/Normal/Fast.
- The important new signal is portfolio coverage, not a direct candidate. With `frontier_pro_v3_alternating_white_edge_mana`, `frontier_pro_v3_white_opening_utility_mana`, `shipping_pro_search_control`, `frontier_pro_v2_raw`, `frontier_pro_v2_no_selected_followup_projection`, `frontier_pro_v3_full_scored_reply_guard`, and `frontier_pro_v2_no_low_budget_guard`, the policy set has oracle coverage on the checked sampled and active panels.
- Latest iteration fixed a harness bug in `smart_automove_pro_forced_root_oracle_probe`: `SMART_PRO_FORCED_ROOT_ORACLE_FEN` must preserve case, because FEN payloads are case-sensitive. Use the raw env helper for FENs and other payload values; the canonicalizing helper lowercases profile/filter tokens only.
- The previous sampled Fast `corner_chain_mana_rows` white no-policy no-go was invalid. With case preserved, the forced-root oracle found `16` scored roots and `13` winning roots from that opening; the shipped guarded move `l10,3;l9,4` lost, while a high-utility quiet mana root `l10,5;l9,4` won.
- This iteration added the test-only policy component `frontier_pro_v3_white_opening_utility_mana`. It fixes that sampled Fast corner-chain white opening row and helps the policy portfolio reach oracle coverage on the checked sampled and active panels, but it is not a standalone challenger.
- A context selector over the expanded portfolio was tested and discarded. Its best local shape reached sampled `11-1 / 11-1 / 11-1`, but active Fast stayed `5-1`; a wider `outer_edge_mana_rows` delta run exposed the same white turn-three exact context as a Pro improvement and a Fast regression, plus another Pro regression. Do not retain `frontier_pro_v3_context_policy_portfolio` or replace it with guarded-selected-move/FEN gates.
- The latest active policy-winner refresh over the expanded portfolio still only proves oracle coverage, not selector viability. Active winners split Pro `3` baseline / `3` policy, Normal `5` / `1`, Fast `2` / `4` with `no_policy_wins=0`; Pro wants full-scored reply guard on the white `outer_edge_mana_rows` turn-three exact context, while Fast wants shipping-control on the same high-level context.
- This iteration corrected the forced-root oracle horizon for first-diff boards. Set `SMART_PRO_FORCED_ROOT_ORACLE_START_PLY=<first_diff_ply>` so a board copied from a full opening uses the remaining `96 - first_diff_ply` rollout horizon instead of granting an extra full 96 plies. On the active Pro/Fast white `outer_edge_mana_rows` conflict, the corrected counts are `8/16` Pro and `9/16` Fast, and the guarded-selected baseline roots now reproduce the full-opening losses.
- The dual-progress SpiritImpact no-go still stands. Even before the horizon correction, a dual-progress root preference failed active `3-3 / 5-1 / 3-3`, and using the same signal as a shipping fallback failed active `2-4 / 5-1 / 3-3`; source was discarded.
- Release readiness was refreshed on `2026-04-23`: public Pro wiring still ships `frontier_pro_v2_guarded`, experimentation remains test-only, and the full canonical confirm loop passed without selection-behavior changes.
- The current local package around `frontier_pro_v2_guarded` is still not promotable on the sampled Pro gate, even after fixing several retained late Normal/Fast boards.
- Remaining sampled blockers at the end of this wave are variant-level, not just the earlier retained boards:
  - Normal `outer_edge_mana_rows`
  - Fast `alternating_mana_rows`
  - Fast `forward_bridge_mana_rows`
- This iteration stayed structural and test-only. `smart_automove_pro_profile_sweep_probe` now compares experimental Pro candidates through the ignored harness and prints structured `PRO_PROFILE_SWEEP_RESULT` / `PRO_PROFILE_SWEEP_VARIANT` summaries without adding new retained profiles or changing public runtime behavior.
- This iteration added first-class experiment-runner wrappers for the test-only profile diagnostics. `./scripts/run-automove-experiment.sh pro-profile-sweep <candidate[,candidate...]>` now runs `smart_automove_pro_profile_sweep_probe`, and `pro-profile-attribution <left>` runs `smart_automove_pro_profile_attribution_probe` with `SMART_PRO_SWEEP_ATTRIBUTION_RIGHT` as the right candidate. Canonical promotion stages still require retained profiles only.
- The new runner path was smoke-validated on the active blockers with tiny samples only. On `outer_edge_mana_rows` vs shipping Pro, `frontier_pro_v2_raw` went `2-0` while `frontier_pro_v2_no_late_black_fallback` split `1-1`; attribution on the same tiny slice found one raw-better early-white fallback first divergence. This validates the structural path but is not promotion evidence because total games and confidence are intentionally far below gate shape.
- A tiny Fast-blocker sweep found directional evidence, not promotion evidence. On `alternating_mana_rows,forward_bridge_mana_rows` with `duel_filter=vs_shipping_fast`, `repeats=1`, and `games=2`, current `frontier_pro_v2_guarded` scored `0.2500` across `4` mirrored games, while `frontier_pro_v2_raw` scored `0.7500`; raw ProV2 was better on `forward_bridge_mana_rows` but still only even on `alternating_mana_rows`.
- This iteration expanded that sweep over `outer_edge_mana_rows,alternating_mana_rows,forward_bridge_mana_rows` with `repeats=1` and `games=3`. `frontier_pro_v2_raw` scored `0.8333` in each of Pro, Normal, and Fast duels, while current `frontier_pro_v2_guarded` scored `0.3333`, `0.3333`, and `0.6667`; raw ProV2 is directionally stronger but still not promotable.
- This iteration fixed an experiment-logger harness blocker exposed by a broad multi-candidate sweep: long comma-separated sweep candidate lists now shorten run-name and scope path components with a checksum suffix instead of failing with `File name too long`.
- This iteration widened the full active-blocker profile comparison across the current retained/test-only candidate set. The broad `games=2` pass was useful for elimination only and showed `frontier_pro_v2_no_spirit_family`, `frontier_pro_v2_no_mid_tactical_guard`, and `frontier_pro_v2_no_low_budget_guard` were not worth reopening; `games=2` only produced two per-variant rows, so the decisive follow-up used `games=3`.
- The decisive `games=3` follow-up on `outer_edge_mana_rows,alternating_mana_rows,forward_bridge_mana_rows` kept the work test-only and did not promote. Best candidates were still below gate shape: `frontier_pro_v2_raw` and `frontier_pro_v2_expansion_224` both scored Pro `5-1` (`0.8333`), Normal `5-1` (`0.8333`), Fast `5-1` (`0.8333`), all with confidence `0.8906`; `frontier_pro_v2_no_late_black_fallback` improved Fast to `6-0` (`1.0000`, confidence `0.9844`) but dropped Normal to `4-2` (`0.6667`); `frontier_pro_v2_head_rerank` scored `0.8333 / 0.6667 / 0.8333`.
- This iteration killed the raw-plus-expansion composite idea without editing runtime code. Attribution over the full active-blocker slice with `frontier_pro_v2_raw` on the left and `frontier_pro_v2_expansion_224` on the right produced identical outcomes in all buckets: Pro `same_outcome=6/6`, Normal `6/6`, Fast `6/6`, with `right_better=0` and `left_better=0`.
- This iteration killed `frontier_pro_v2_raw` as a direct sampled baseline. On the canonical sampled profile-sweep shape (`repeats=3`, `games=2`, default sampled variants), raw failed badly against shipping Pro at `5-7` (`0.4167`, confidence `0.0000`) while scoring `10-2` (`0.8333`, confidence `0.9807`) against both Normal and Fast.
- Guarded-vs-raw sampled Pro attribution showed the wrapper branches are mixed saves, not a branch-label no-go. Guarded beat raw in `4/12` games, raw beat guarded in `2/12`, and `6/12` were same-outcome; guarded-better cases split evenly across `early_white_fallback` and `late_black_shipping_fallback`, while raw-better cases also had one of each.
- This review added a structural strategy and scout harness instead of another seam patch. Future iterations should compare broad test-only candidates on both canonical sampled and active-blocker panels before touching runtime code; the preferred next implementation is a context-gated meta-selector candidate or a decision-record aggregation diagnostic.
- This iteration kept the structural work diagnostic-only. `smart_automove_pro_profile_attribution_probe` now emits first-divergence decision context (`active_color`, turn, `mons_moves`, action/mana availability, exact-opportunity context) and aggregates `PRO_PROFILE_SWEEP_ATTRIBUTION_CONTEXT` by variant so future context gates can be based on evidence below the branch label.
- This iteration killed the first context-gated meta-selector wave and discarded its temporary sweep candidates. The broad context gate improved sampled Pro-vs-Pro to `9-3` but collapsed Normal and Fast to `6-6`; static and worst-reply portfolio selectors failed Pro at `7-5` and `5-7`; the narrower exact context gate improved Pro to `10-2` but still missed `alternating_mana_rows` and `forward_bridge_mana_rows`, and a follow-up shipping fallback did not move the result. No candidate reached the structural scout promotion shape.
- This iteration killed a second attribution-informed context-gate probe and discarded the temporary sweep candidate. Canonical sampled guarded-vs-raw attribution still showed mixed saves/regressions, and two exact-context gates failed structural scout: the broader gate reached sampled Pro `9-3`, Normal `8-4`, Fast `6-6`, while a narrower gate fell to sampled Pro `8-4`, Normal `8-4`, Fast `6-6`; both active-blocker panels were still non-promotable at Pro `3-3`, Normal `5-1`, Fast `3-3`. The current active-blocker refresh also confirmed neither retained guarded (`3-3 / 5-1 / 2-4`) nor raw (`2-4 / 6-0 / 4-2`) is a hidden challenger on the same explicit variants.
- The sweep now prints guarded branch counts. On the active-blocker guarded rerun, `frontier_execute` dominated all buckets (`248`, `255`, and `254` turns), while `early_white_fallback` and `late_black_shipping_fallback` were secondary; aggregate branch frequency does not isolate a single fallback branch as the loss mechanism.
- This iteration added guarded-vs-raw outcome attribution. On the same active-blocker sweep, raw-better first divergences concentrated in `late_black_shipping_fallback`: Pro `3/3`, Normal `2/3`, and Fast `2/2`; Normal had one remaining raw-better `early_white_fallback` case, and Fast had one guarded-better `white_nonnegative_deny_search_only_fallback` case.
- This iteration also added and tested a no-runtime-impact sweep candidate, `frontier_pro_v2_no_late_black_fallback`. Disabling only the late-black shipping fallback improved the active-blocker slice over current guarded but did not promote: Pro `5-1` (`0.8333`), Normal `4-2` (`0.6667`), Fast `6-0` (`1.0000`, confidence `0.9844`). The candidate captures most of raw's branch-level gain and keeps a white fallback save, but Pro/Normal remain below gate shape.
- This iteration generalized the attribution probe so it can compare arbitrary sweep candidates via `SMART_PRO_SWEEP_ATTRIBUTION_LEFT` and `SMART_PRO_SWEEP_ATTRIBUTION_RIGHT`. Comparing `frontier_pro_v2_no_late_black_fallback` to raw showed Pro was flat, Normal had exactly one raw-better `early_white_fallback` case on `forward_bridge_mana_rows`, and Fast had one no-late-black `white_nonnegative_deny_search_only_fallback` save.
- This iteration killed the combined early-white plus late-black fallback removal. The first active-blocker slice looked stronger at Pro `5-1`, Normal `5-1`, Fast `6-0`, but a `repeats=2` active-blocker sweep collapsed Pro to `7-5` (`0.5833`), with `outer_edge_mana_rows` and `forward_bridge_mana_rows` both at `0.5000`; the temporary test-only candidate was discarded.
- No runtime candidate was promoted from these sweeps. The next efficient line is to explain the shared Pro loss that remains after removing the late-black fallback; it is not a late-black wrapper problem, and broad early-white removal is killed.
- This iteration killed a variant-scoped shipping fallback. Routing Pro back to retained `shipping_pro_search` on `outer_edge_mana_rows`, `alternating_mana_rows`, and `forward_bridge_mana_rows` flattened a targeted blocker trace to `0` regressions / `0` improvements / `12` flat in each of Pro, Normal, and Fast, but canonical sampled `pro-reliability` still failed because the gate requires frontier wins, not just non-regression: Pro `0.8333`, Normal `0.7500`, Fast `0.8333`; confidence `0.9807 / 0.9270 / 0.9807`; averages `107.46ms / 155.92ms / 125.52ms`. The runtime edit was discarded.
- This iteration stayed diagnostic-only. A bounded Fast replay on `SMART_AUTOMOVE_VARIANTS=forward_bridge_mana_rows` with `smart_automove_pro_reliability_nonwin_trace_probe`, `duel_filter=vs_shipping_fast`, `repeats=2`, and `games=3` logged `5` nonwins and did not produce a promotable head-accept spend. The misses stayed split across white post-search/head-selection drift (`l9,6;l8,6` vs shipping `l9,6;l7,4;l7,3`), white accepted safe-progress drift (`l9,6;l8,7` vs shipping `l9,6;l7,7;l8,8`), the known white fallback seam (`l9,7;l8,6` vs shipping `l9,7;l7,6;l7,7`), and the already-killed copied black seams (`l0,6;l1,6` vs shipping `l2,3;l3,4`; `l0,3;l1,3` vs shipping `l1,5;l3,4;l3,3`).
- This iteration killed a scoped black turn-four weak-window recovery override change. Restricting `pro_v2_root_advisor_black_turn_four_weak_window_recovery_override` to recovery roots already in the reply-risk shortlist preserved the focused retained Fast controls and improved the full isolated `alternating_mana_rows` Fast trace from `7` to `6` nonwins while removing the repeated `l2,7;l1,6` pair, but canonical sampled `pro-reliability` still failed: Pro `1.0000`, Normal `0.8333`, Fast `0.8333`; confidence `0.9998 / 0.9807 / 0.9807`; averages `143.32ms / 186.18ms / 165.40ms`. The runtime edit was discarded because it rotated Normal down to `center_spoke_mana_rows` plus `outer_edge_mana_rows` and left Fast blocked on `alternating_mana_rows` plus `forward_bridge_mana_rows`.
- This iteration stayed diagnostic-only. `smart_automove_pro_root_advisor_trace_probe` did not reveal a shared advisor-layer repair below the current sampled blockers. The blocker-adjacent boards split across `ApprovedReplyRiskGuard`, `ApprovedFamilyCompetition`, omitted-root reentry, preserved representatives, and rejected injected macro roots, so there is still no defensible shared advisor metadata or injection spend.
- This iteration stayed diagnostic-only. The canonical sampled `pro-reliability` gate was refreshed after a clean `runtime-preflight` pass, and `frontier_pro_v2_guarded` is still not promotable: Pro `1.0000`, Normal `0.9167`, Fast `0.8333`; confidence `0.9998 / 0.9968 / 0.9807`; frontier average move times `143.82ms / 179.83ms / 159.07ms`. Variant blockers matched the targeted refreshes: Normal `outer_edge_mana_rows` at `0.5000`, Fast `alternating_mana_rows` at `0.5000`, and Fast `forward_bridge_mana_rows` at `0.5000`.
- This iteration killed a white `forward_bridge_mana_rows` same-setup `SpiritImpact` head reject. Both a broad guard and a narrowed exact-shape guard removed the sampled Fast `l9,6;l7,4;l7,3` head-accept regression, reduced the focused Fast nonwin trace to the single pre-existing `alternating_mana_rows` flat loss, and passed sampled `pro-reliability` at Pro `1.0000`, Normal `0.9167`, Fast `0.9167`; both then failed all-variant `pro-reliability-confirm` with the same broad profile: Pro `0.6667`, Normal `0.7292`, Fast `0.6667`. The runtime and retained-test edits were discarded.
- This iteration killed the variant-scoped version of that same `forward_bridge_mana_rows` white same-setup `SpiritImpact` head reject. Scoping the reject to `GameVariant::ForwardBridgeManaRows` removed the sampled Fast win/loss blocker and passed sampled `pro-reliability` at Pro `1.0000`, Normal `0.9167`, Fast `0.9167`, but all-variant `pro-reliability-confirm` still failed with the same broad profile: Pro `0.6667`, Normal `0.7292`, Fast `0.6667`; per-variant floors failed across Pro, Normal, and Fast. The runtime edit was discarded.
- This iteration also killed an inert black weak-window search-only fallback. A runtime fallback from black turn-four quiet recovery to retained search-only windowed mana passed guardrails, variant-smoke, triage, and runtime-preflight, but sampled `pro-reliability` was unchanged at Pro `1.0000`, Normal `0.9167`, Fast `0.8333`; the edit did not touch the current sampled blockers and was discarded.
- This iteration killed five profile-level ProV2 ablations. Enabling ProV2 head rerank failed sampled `pro-reliability` at Pro `1.0000`, Normal `0.8333`, Fast `0.7500`; disabling ProV2 spirit-family expansion failed at `0.6667 / 0.8333 / 0.7500`; disabling the mid-turn tactical guard and raising only `turn_engine_expansion_cap` to `224` both left the live blocker profile unchanged at `1.0000 / 0.9167 / 0.8333`; disabling the low-budget guard failed at `0.7500 / 0.9167 / 0.8333`. All runtime edits were discarded.
- This iteration stayed diagnostic-only. A refreshed combined Fast replay on `alternating_mana_rows,forward_bridge_mana_rows` with `duel_filter=vs_shipping_fast`, `repeats=4`, and `games=3` logged `9` nonwins and did not collapse to a runtime spend. The only repeated pair was still the archived white `forward_bridge` accepted-head miss `l9,6;l7,4;l7,3` vs shipping `l9,6;l7,6;l7,7` (`2x`); every other miss stayed singleton and mostly matched copied-board or retained-extension no-go classes already killed in prior waves.
- This iteration killed a `ForwardBridgeManaRows`-only low-budget guard disable. It fixed the sampled Fast `forward_bridge_mana_rows` row and passed sampled `pro-reliability` at Pro `1.0000`, Normal `0.9167`, Fast `0.9167`, but all-variant `pro-reliability-confirm` failed broadly at Pro `0.6458`, Normal `0.7083`, Fast `0.6667`; per-variant floors failed across Pro, Normal, and Fast. The runtime and retained-test edits were discarded.
- This iteration stayed diagnostic-only. An alternate-seed all-blocker trace on `outer_edge_mana_rows,alternating_mana_rows,forward_bridge_mana_rows` with `seed_tag=pro_turn_planner_reliability_alt_v1`, `repeats=2`, and `games=3` found no shared recurrence: Pro `1` regression / `2` improvements / `9` flat, Normal `2` / `5` / `5`, Fast `4` / `3` / `5`, and every regression move pair was singleton.
- This iteration stayed diagnostic-only. A focused alternate-seed Fast-blocker trace on `alternating_mana_rows,forward_bridge_mana_rows` with `seed_tag=pro_turn_planner_reliability_alt_v2`, `repeats=2`, and `games=3` also found no reusable mechanism: Pro `3` regressions / `1` improvement / `8` flat, Normal `5` / `3` / `4`, Fast `1` / `5` / `6`, with every regression move pair singleton.
- This iteration stayed diagnostic-only. A Normal-only `outer_edge_mana_rows` alternate-seed trace with `seed_tag=pro_turn_planner_reliability_alt_v2`, `duel_filter=vs_shipping_normal`, `repeats=4`, and `games=3` logged `9` nonwins and still did not collapse: late black `l1,6;l1,5` vs `l2,6;l3,7` and early black `l1,5;l0,3;l1,3` vs `l1,6;l2,7` each repeated, but the same run also produced five singleton white/black seams across engine-disabled, post-search, and head-accepted surfaces.
- This iteration stayed diagnostic-only. A higher-signal alternate-seed all-blocker trace on `outer_edge_mana_rows,alternating_mana_rows,forward_bridge_mana_rows` with `seed_tag=pro_turn_planner_reliability_alt_v3`, `repeats=3`, and `games=3` still did not produce a promotable recurrence: Pro `5` regressions / `5` improvements / `8` flat, Normal `4` / `3` / `11`, Fast `2` / `4` / `12`. Pro and Fast regression pairs were all singleton; Normal had one repeated black pair, `l2,3;l3,3` vs `l2,3;l3,2` (`2x`), inside an otherwise mixed set.
- This iteration stayed diagnostic-only. A temporary hotspot probe on that alt-v3 Normal black pair confirmed it is not a root-pool availability seam: both profiles enumerated the same `10,941` selector children, while frontier diverged through `engine_post_search` to `l2,3;l3,3` and shipping stayed `engine_disabled` on `l2,3;l3,2`. The probe showed the familiar frontier-only extra exact/engine work (`payload_calls +163,990`, `pickup_calls +6,177`, `secure_mana_calls +1,784`, `tactical_spirit_calls +2,914`, engine accepted `6` vs `1`), so the temporary code was removed and only this no-go was kept.
- This iteration kept useful diagnostic code only. `smart_automove_pro_reliability_hotspot_probe` now accepts `SMART_PRO_RELIABILITY_HOTSPOT_FEN`, `SMART_PRO_RELIABILITY_HOTSPOT_LABEL`, and `SMART_PRO_RELIABILITY_HOTSPOT_MODE` so one-off blocker boards can be inspected without temporary source edits. Re-running the alt-v3 Normal black pair through the env hook reproduced the same no-go: identical selector child counts (`10,941`) and frontier-only `engine_post_search`/exact work.
- This iteration stayed diagnostic-only. A fresh alt-v4 all-blocker trace still did not produce a promotable recurrence: Pro `3` regressions / `3` improvements / `12` flat, Normal `5` / `2` / `11`, Fast `4` / `3` / `11`. Pro repeated only black `l2,7;l3,8` vs `l1,5;l0,3;l1,3` (`2x`), Normal repeated only the already-archived `l2,7;l1,6` vs `l2,7;l1,8` recovery jump (`2x`), and Fast was singleton-only. The env hotspot hook on the Pro repeat showed frontier-only extra expansion (`12,473` vs `11,612` selector children, engine accepted `23` vs `0`), so this remains mixed trace evidence rather than a runtime spend. `smart_automove_pro_reliability_duel_trace_probe` now also accepts `SMART_PRO_RELIABILITY_DUEL_FILTER` for focused future traces.
- This iteration stayed diagnostic-only. A filtered alt-v5 Fast-blocker trace on `alternating_mana_rows,forward_bridge_mana_rows` with `duel_filter=vs_shipping_fast`, `repeats=4`, and `games=3` produced `4` regressions / `6` improvements / `14` flat across `24` Fast games. Every regression move pair was singleton (`l10,3;l9,2` vs `l10,3;l9,3`, `l10,4;l9,4` vs `l9,3;l8,2`, `l2,5;l4,3;l3,2` vs `l2,5;l4,3;l3,3`, `l9,4;l8,4` vs `l9,4;l8,3`), so the focused Fast bucket still has no promotable recurrence.
- This iteration stayed diagnostic-only. A filtered alt-v5 Normal `outer_edge_mana_rows` trace with `duel_filter=vs_shipping_normal`, `repeats=4`, and `games=3` produced `3` regressions / `5` improvements / `16` flat across `24` Normal games. Every regression pair was singleton (`l1,4;l2,5` vs `l1,4;l1,6;l2,7`, `l1,5;l0,3;l1,3` vs `l1,6;l2,7`, `l9,4;l8,3` vs `l9,6;l9,8;l8,8`), and the printed seams split across white safe-progress/spirit setup plus black spirit/mana ordering.
- This iteration refreshed the retained sampled gate after a clean runtime-preflight pass. `frontier_pro_v2_guarded` is still not promotable: Pro `1.0000` / `0.9998` / `154.74ms`, Normal `0.9167` / `0.9968` / `192.00ms`, Fast `0.8333` / `0.9807` / `172.51ms`. The weak rows are unchanged: Normal `outer_edge_mana_rows` at `0.5000`, Fast `alternating_mana_rows` at `0.5000`, and Fast `forward_bridge_mana_rows` at `0.5000`.
- This iteration stayed diagnostic-only. A focused Fast nonwin trace on `alternating_mana_rows,forward_bridge_mana_rows` with default seed, `duel_filter=vs_shipping_fast`, `repeats=3`, and `games=2` logged `5` nonwins, all singleton and mixed: black `l0,10;l0,9` vs `l4,0;l5,0;mb`, white `l9,6;l8,7` vs `l9,6;l7,7;l8,8`, black `l0,6;l1,6` vs `l2,3;l3,4`, black `l2,5;l0,5;l1,6` vs `l2,5;l4,7;l3,8`, and white `l9,6;l7,4;l7,3` vs `l9,6;l7,6;l7,7`.
- This iteration stayed diagnostic-only. A focused Normal nonwin trace on `outer_edge_mana_rows` with default seed, `duel_filter=vs_shipping_normal`, `repeats=3`, and `games=2` logged `2` nonwins: late black `l1,6;l1,5` vs `l2,6;l3,7` and early white `l10,4;l9,5` vs `l9,4;l8,3`. Both frontier and shipping lost those games, and the first-diff surfaces are different, so there is still no Normal `outer_edge` standalone repair.
- This iteration stayed diagnostic-only. A refreshed Normal `outer_edge_mana_rows` replay with `duel_filter=vs_shipping_normal`, `repeats=6`, and `games=3` still logged `10` nonwins and did not collapse to one runtime hypothesis. The only repeated pair remained black `l2,7;l3,8` vs shipping `l1,5;l0,3;l1,3` (`3x`), while the rest stayed mixed across late black mana drift, early white post-search drift, early black copied-board recovery, and white `search_only_forced_prepass`. Source review showed that blocking the repeated accepted-head miss would be another special-case guard, not a shared promotable mechanism.
- This iteration stayed diagnostic-only. The copied Fast `forward_bridge` white setup singleton `l9,5;l9,6` vs shipping `l7,3;l6,2` did not survive a clean direct probe. Both frontier and shipping collapsed to shared engine-disabled `l7,3;l6,2`, so the board is not a stable local seam and there is nothing left to compare against the nearby retained white setup controls.
- This iteration stayed diagnostic-only. A widened Fast `forward_bridge_mana_rows` replay still logged `10` nonwins and recovered a black copied seam `l0,6;l1,6` vs shipping `l2,3;l3,4`, but the recovered board did not survive a clean direct probe. Both frontier and shipping collapsed to shared engine-disabled `l2,3;l3,4`, so the board is not a stable local seam and not a retained-extension candidate.
- This iteration stayed diagnostic-only. The current Fast `forward_bridge_mana_rows` replay recovered a black spirit/setup copied seam `l0,3;l1,3` vs shipping `l1,5;l3,4;l3,3`, but the copied board did not survive clean direct replay. Both frontier and shipping collapsed to shared engine-disabled `l0,3;l1,3`, so the board is not a stable local seam or retained-extension anchor.
- This iteration stayed diagnostic-only. A recovered Fast `alternating` late-fast recovery singleton still reproduced in the trace as `l2,5;l0,5;l1,6` vs shipping `l2,5;l4,7;l3,8`, but the copied board did not replay on a clean direct probe. Both frontier and shipping instead collapsed to shared engine-disabled `l2,5;l4,5;l3,4`, so the board is not a stable retained-extension or local runtime target.
- This iteration stayed diagnostic-only. A fresh Fast `alternating_mana_rows` trace recovered the two remaining unverified singleton pairs, but neither copied board survived clean direct replay. The white `l8,5;l10,5;l9,4` vs shipping `l8,5;l6,3;l7,2` board collapsed to shared engine-disabled `l8,5;l6,3;l7,3`, and the black `l2,4;l1,5` vs shipping `l2,4;l1,3` board collapsed to shared `search_only_forced_prepass` `l2,4;l1,5`.
- This iteration stayed diagnostic-only. A direct black early `outer_edge` recovery probe killed the remaining copied-board path for two singleton early black seams. On clean direct replay, the copied `l1,4;l2,4` vs `l0,5;l1,6` board collapsed to engine-disabled `l0,5;l1,6` for both frontier and shipping, and the copied `l1,4;l2,5` vs `l1,4;l1,6;l2,7` board did not replay the traced shipping final at all, collapsing instead to shared engine-disabled `l1,5;l2,7;l3,8`. That makes both copied boards unusable as stable local targets.
- This iteration stayed diagnostic-only. A direct retained white harvest structure probe killed the only plausible nearby reuse path for the `outer_edge_mana_rows` white `search_only_forced_prepass` seam. The traced board `l9,3;l8,3` vs shipping `l7,2;l6,1` does not replay that shipping final on a clean direct probe: both frontier and shipping collapse to `search_only_forced_prepass` with selected `l9,3;l8,3` and only `pre_accept=l7,2;l6,1`. Retained `primary_white_harvest_loss_c_ply24` is a different Pro `SafeSupermanaProgress` surface: `window=2/deny=2`, attack-enabled, shortlist-live, and frontier keeps `l7,2;l6,1` while rejecting non-progress head `l8,5;l7,4`.
- This iteration stayed diagnostic-only. A targeted replay killed the only plausible shared head-accept hypothesis across Normal `outer_edge_mana_rows` and Fast `forward_bridge_mana_rows`. `SMART_AUTOMOVE_VARIANTS=outer_edge_mana_rows` with `duel_filter=vs_shipping_normal`, `repeats=6`, and `games=3` widened back to `10` Normal nonwins across mixed rejected-head, accepted-head, and `search_only_forced_prepass` surfaces. `SMART_AUTOMOVE_VARIANTS=forward_bridge_mana_rows` with `duel_filter=vs_shipping_fast`, `repeats=4`, and `games=3` also logged `10` Fast nonwins, but it stayed a different mixed spirit/setup surface with only one repeated white accepted-head pair `l9,6;l7,4;l7,3` vs shipping `l9,6;l7,6;l7,7`.
- This iteration stayed diagnostic-only. A direct retained black recovery structure probe killed the last nearby retained reuse path for the singleton Fast `alternating_mana_rows` seam `l1,6;l1,7` vs shipping `l1,6;l0,5`. Both boards share the same coarse recovery lane and `DrainerSafetyRecovery` shipping move, but the live seam is different below that surface: frontier stays on approved `ManaTempo l1,6;l1,7` through `ApprovedReplyRiskGuard`, shipping only wins through `search_only_engine_allowed_head`, and there is no accepted recovery head. Retained `BLACK_RECOVERY_DUEL_FAST` instead approves `DrainerSafetyRecovery l1,6;l0,5` through `ApprovedReplyRiskGuard`, accepts that head, and then both frontier and shipping collapse to engine-disabled mana `l4,1;l5,0;mb`.
- This iteration stayed diagnostic-only. A direct retained black Fast-structure probe killed the last nearby retained reuse paths for the repeated `alternating_mana_rows` seam. The live board remains a turn-four black `window=1/deny=1` surface where frontier approves outside-shortlist `DrainerSafetyRecovery l2,7;l1,6` through `ApprovedFamilyCompetition` over two shortlisted `ManaTempo` siblings. The retained `BLACK_FAST_FLAT_NONWIN` board is different: `window=0/deny=0`, pure `SpiritImpact`, and frontier already matches shipping through `ApprovedReplyRiskGuard`. The retained `BLACK_SPIRIT_BRIDGE_DUEL_FAST` board is different again: it accepts a `SpiritImpact` head through `ApprovedLegacySelector` and still collapses to engine-disabled mana `l4,9;l5,10;mb`.
- This iteration stayed diagnostic-only. A direct retained `WHITE_FAST_PLY10` structure probe killed the last nearby retained reuse path for the singleton Fast `forward_bridge_mana_rows` fallback seam `l9,7;l8,6` vs shipping `l9,7;l7,6;l7,7`. Both boards share `turn=3`, `window=1/deny=1`, shipped root `SpiritImpact l9,7;l7,6;l7,7`, and the same rejected head `l8,5;l7,6`, but the live seam is different below that surface: frontier approves `SafeSupermanaProgress l9,7;l8,6` through `ApprovedReplyRiskGuard` with shipping spirit only shortlist-live, while retained `WHITE_FAST_PLY10` keeps shipping `SpiritImpact` through `ApprovedFamilyCompetition` and preserves safe progress only as a representative.
- This iteration stayed diagnostic-only. A direct retained white head/spirit structure probe killed the last nearby retained reuse path for the repeated Fast `forward_bridge_mana_rows` seam. On the live board, frontier keeps shipping `SpiritImpact l9,6;l7,6;l7,7` as pre-accept through `ApprovedFamilyCompetition` on `window=0/deny=0`, then loses only by accepting same-family head `l9,6;l7,4;l7,3`. The retained `WHITE_HEAD_FLAT_NONWIN` control is different: it is `window=1/deny=1`, also accepts a spirit head, but shipping already matches the accepted head and the advisor path is `ApprovedReplyRiskGuard`. The retained `WHITE_TURN_FIVE_SPIRIT_SETUP_PRE_ACCEPT_FAST` control is different again: it is `window=1/deny=1` and rejects a `ManaTempo` head over a shipped spirit setup root.
- This iteration stayed diagnostic-only. A widened isolated Normal replay on `SMART_AUTOMOVE_VARIANTS=outer_edge_mana_rows` with `duel_filter=vs_shipping_normal`, `repeats=6`, and `games=3` logged `10` nonwins, so the blocker is no longer a late-black-only story. The only repeated pair was black `l2,7;l3,8` vs shipping `l1,5;l0,3;l1,3` (`3x`), but a direct retained comparison killed the nearby reuse path: retained `BLACK_HEAD_DUEL_SAMPLED_NORMAL` keeps the same shipped `SpiritImpact` root and rejects head `l0,5;l1,6` through `ApprovedFamilyCompetition`, while the live repeated seam already approves shipping through `ApprovedReplyRiskGuard` and only loses later by accepting head `l2,7;l3,8`.
- This iteration stayed diagnostic-only. A direct retained black fast-recovery structure probe killed the last nearby retained reuse path for the repeated Fast `alternating_mana_rows` seam. The live board is still a turn-four black `window=1/deny=1` surface where frontier approves `DrainerSafetyRecovery l2,7;l1,6` through `ApprovedFamilyCompetition`, shipping stays on `ManaTempo l2,7;l1,8`, and the shortlist holds only the two `ManaTempo` siblings beneath rejected head `l2,7;l2,8`. The retained `BLACK_RECOVERY_DUEL_FAST` board is different: frontier pre-accept/head do hit `DrainerSafetyRecovery l1,6;l0,5`, but final selected and shipping both collapse to engine-disabled mana `l4,1;l5,0;mb` on `window=0/deny=0` with a singleton shortlist.
- This iteration stayed diagnostic-only. A direct retained black branch-structure probe killed the last nearby retained reuse path for the repeated Fast `alternating_mana_rows` seam. The live board is a turn-four black `window=1/deny=1` surface where frontier approves `DrainerSafetyRecovery l2,7;l1,6` through `ApprovedFamilyCompetition`, shipping stays on `ManaTempo l2,7;l1,8`, and the reply-risk shortlist contains only the two `ManaTempo` siblings beneath a rejected head `l2,7;l2,8`. The retained black fast/pro controls do not match that surface: `BLACK_BRANCH_DUEL_SAMPLED_FAST` is a wide-shortlist `SpiritImpact` own-setup board, `BLACK_SIBLING_DUEL_SAMPLED_PRO` is also `SpiritImpact` with one preserved `ManaTempo` representative, and `BLACK_FOLLOWUP_DUEL_SAMPLED_NORMAL` is a `window=0/deny=0` `ManaTempo` family-competition board that still keeps shipping.
- This iteration stayed diagnostic-only. A direct retained sampled black late-structure probe killed the last nearby retained reuse path for the clean late `outer_edge` black seam. The live board is a `window=1/deny=1` `ManaTempo` surface where shipping `l2,6;l3,7` sits inside a dense reply-risk shortlist with several spirit siblings, frontier still approves lower-ranked `l1,6;l1,5` through `ApprovedReplyRiskGuard`, and the head stays rejected. The retained sampled black late package does not match: `BLACK_LATE_MANA_DUEL_SAMPLED_NORMAL` keeps shipping `l0,7;l1,6` through `ApprovedFamilyCompetition` on a smaller shortlist mixed with `SafeOpponentManaProgress`, `BLACK_LATE_HEAD_ACCEPT_DUEL_SAMPLED_NORMAL` is a `SafeSupermanaProgress` preserved-root board, and `BLACK_HEAD_ACCEPT_SEARCH_ONLY_DUEL_SAMPLED_NORMAL` is a `DrainerSafetyRecovery` search-order board.
- This iteration stayed diagnostic-only. A direct retained sampled head-accept structure probe killed the last nearby retained reuse path for the repeated white `forward_bridge` seam. The live board remains a `window=0/deny=0` `SpiritImpact` own-setup surface where shipping `l9,6;l7,6;l7,7` is advisor-approved through `ApprovedFamilyCompetition`, the shortlist is still crowded with spirit own-setup siblings, and frontier only loses later by accepting head `l9,6;l7,4;l7,3`. The retained sampled white head-accept controls do not match that surface: Pro is a pure `ManaTempo` reply-risk board, Normal is `SpiritImpact` on `window=1/deny=1` but still keeps shipping and rejects the head, and Fast is a singleton-shortlist `ApprovedReplyRiskGuard` spirit board with only one preserved sibling representative.
- This iteration stayed diagnostic-only. A narrow Fast replay on `SMART_AUTOMOVE_VARIANTS=forward_bridge_mana_rows` with `smart_automove_pro_reliability_nonwin_trace_probe`, `duel_filter=vs_shipping_fast`, `repeats=4`, and `games=3` still reproduced the repeated white head-accept seam `l9,6;l7,4;l7,3` vs shipping `l9,6;l7,6;l7,7` three times inside `10` Fast nonwins on the current seeded corpus.
- This iteration stayed diagnostic-only. A direct retained-head structure probe killed the last plausible retained white reuse path for that repeated `forward_bridge` seam. The live board is a `window=0/deny=0` `SpiritImpact` surface where shipping `l9,6;l7,6;l7,7` is advisor-approved through `ApprovedFamilyCompetition`, the shortlist stays full of spirit own-setup siblings, and frontier only loses later by accepting head `l9,6;l7,4;l7,3`. The retained white turn-five head-reject controls are different: `window=1/deny=1`, pure `ManaTempo`, `ApprovedReplyRiskGuard`, frontier keeps shipping, and the head is rejected.
- This iteration stayed diagnostic-only. A widened Pro-only nonwin replay on `outer_edge_mana_rows,alternating_mana_rows,forward_bridge_mana_rows` still did not rescue a live spend: `smart_automove_pro_reliability_nonwin_trace_probe` with `duel_filter=vs_shipping_pro`, `repeats=6`, and `games=3` logged `11` Pro nonwins across `36` games. The new white `l6,7;l7,6` vs `l6,7;l7,7` seam stayed singleton, while black `l2,7;l1,6` vs `l2,7;l1,8` and black `l1,6;l2,7` vs shipping `l1,5;l2,3;l1,2` each repeated twice inside a still-mixed blocker set.
- This iteration stayed diagnostic-only. A direct Pro black recurrence structure probe killed the only plausible shared follow-up on those two repeated black seams. The repeated `alternating_mana_rows` black miss still approves outside-shortlist `DrainerSafetyRecovery l2,7;l1,6` over shortlist `ManaTempo l2,7;l1,8` through `ApprovedFamilyCompetition`, while the repeated Pro black `l1,6;l2,7` miss keeps frontier on shortlisted `SafeSupermanaProgress l1,6;l2,7` through `ApprovedReplyRiskGuard` and leaves shipping `SpiritImpact l1,5;l2,3;l1,2` only as a preserved family representative outside the candidate set. Both boards still share `window=1/deny=1` and frontier `engine_post_search` vs shipping `engine_disabled`, but the advisor/candidate shapes diverge below that.
- This iteration stayed diagnostic-only. A direct black safe-progress-vs-spirit structure probe killed the only plausible shared follow-up between the old black progress residue and the new Pro black setup-lane seam. Both boards still approve `SafeSupermanaProgress` through `ApprovedReplyRiskGuard`, but `black_progress_vs_setup_residue` keeps many `SpiritImpact` roots inside the candidate set, including a stronger full-pool own-setup challenger `l1,5;l3,7;l2,8`, while shipping `l1,5;l2,7;l1,8` is merely a preserved candidate outside the shortlist. The new Pro `l1,6;l2,7` seam is different: the shipped `SpiritImpact l1,5;l2,3;l1,2` is already a preserved rank-2 representative outside the candidate set, and the whole `SpiritImpact` family is filtered out before shortlist.
- This iteration stayed diagnostic-only. A direct white-vs-black safe-progress structure probe killed the only plausible cross-color follow-up between the white `forward_bridge` safe-progress seam and the Pro black setup-lane seam. Both boards approve `SafeSupermanaProgress` through `ApprovedReplyRiskGuard`, but the white `l9,6;l8,7` seam keeps the shipped `SpiritImpact l9,6;l7,7;l8,8` live inside both the candidate set and the reply-risk shortlist alongside several other spirit siblings, while the Pro black `l1,6;l2,7` seam filters the entire `SpiritImpact` family out before shortlist and preserves `l1,5;l2,3;l1,2` only as an outside-candidate representative.
- This iteration stayed diagnostic-only. A retained-extension probe killed the only plausible reuse path from the retained white safe-progress package into the `forward_bridge` safe-progress seam. The retained Pro board that already selects `l9,6;l8,7` is not a spirit/setup surface at all: it is a quiet `ManaTempo` approval on `window=0/deny=0` with no `SpiritImpact` roots live in the shortlist. The `forward_bridge` seam is different: `window=1/deny=1`, frontier approves `SafeSupermanaProgress l9,6;l8,7`, and shipping `SpiritImpact l9,6;l7,7;l8,8` stays candidate-live and shortlist-live with several spirit siblings.
- This iteration stayed diagnostic-only. A direct comparison against the retained white engine-disabled/search-only package killed the last plausible reuse path from that family into `forward_bridge`. The retained early-white boards still approve `ManaTempo` seam roots through `ApprovedReplyRiskGuard`: one keeps many `SpiritImpact` candidates live but outside shortlist (`l8,5;l7,6` vs shipping `l9,5;l8,3;l7,4`), and one has no spirit candidates at all (`l9,4;l8,3` vs shipping `l9,4;l8,5`). The `forward_bridge` safe-progress seam is different: frontier approves `SafeSupermanaProgress l9,6;l8,7`, while shipping `SpiritImpact l9,6;l7,7;l8,8` stays shortlist-live.
- This iteration stayed diagnostic-only. A retained-vs-new white late mana sibling surface probe killed the only plausible extension candidate for the new Pro white `l6,7;l7,6` vs `l6,7;l7,7` seam: the retained late white mana sibling board is a turn-eleven `SpiritImpact` / `SafeSupermanaProgress` `engine_post_search` surface with a live head plan and `window=2/deny=2`, while the new seam is a turn-three pure `ManaTempo` `engine_disabled` surface with no head plan and `window=0/deny=0`.
- This iteration stayed diagnostic-only. A same-family late mana residual probe killed the only plausible shared scoring angle left in the current `ManaTempo` drifts: `outer_edge_mana_rows` late black and the new Pro white `l9,7;l8,8` vs `l9,7;l8,7` seam both had zero residual delta, zero search-eval delta, and identical `TurnEngineUtility`, while the separate Pro white `l6,7;l7,6` vs `l6,7;l7,7` seam had a real residual split driven by `spirit_action_utility` and `mana_close_to_same_pool`.
- This iteration stayed diagnostic-only. A widened all-blocker recurrence trace on `outer_edge_mana_rows,alternating_mana_rows,forward_bridge_mana_rows` still did not collapse the residue: across `24` games per duel it logged Pro `3` regressions, Normal `3`, and Fast `7`, while every per-duel move pair stayed singleton. The earlier isolated repeated white and black pairs did not survive the broader blocker set as dominant residue.
- This iteration stayed diagnostic-only. A three-board blocker hotspot fingerprint probe killed the only plausible shared exact/selector angle left in the repeated seams: `outer_edge_mana_rows` late black, `alternating_mana_rows` black mana sibling, and the repeated white `forward_bridge_mana_rows` seam all kept the same full selector pool against `shipping_pro_search`, but `outer_edge` and `alternating` still drifted already at `pre_accept` while `forward_bridge` still drifted only at head acceptance, and their exact contexts still split between `window=1/deny=1` and `window=0/deny=0`.
- This iteration stayed diagnostic-only. A direct white `forward_bridge_mana_rows` seam-structure probe also killed the only plausible shared white runtime angle: the repeated white head-accept seam keeps shipping `SpiritImpact l9,6;l7,6;l7,7` as both legacy-selected and advisor-approved, then loses only at head acceptance to `l9,6;l7,4;l7,3`; the `l9,6;l8,7` seam instead approves `SafeSupermanaProgress` through `ApprovedReplyRiskGuard`; and the `l9,7;l8,6` seam additionally routes through `score_window_tactical_fallback` with a preserved safe-progress representative.
- This iteration stayed diagnostic-only. A direct black late-mana structure probe killed the only plausible cross-variant black hypothesis: `outer_edge_mana_rows` black still resolves as a legacy-selected, reply-risk-shortlisted `ManaTempo` shipping root that frontier drops for lower-ranked `ManaTempo` via `ApprovedReplyRiskGuard`, while the repeated `alternating_mana_rows` black seam keeps shipping `l2,7;l1,8` as the legacy-selected `ManaTempo` root but frontier instead approves outside-shortlist `DrainerSafetyRecovery l2,7;l1,6` via `ApprovedFamilyCompetition`.
- This iteration stayed diagnostic-only. Isolated Fast variant traces still did not collapse the residue: `forward_bridge_mana_rows` logged `8` regressions across `24` Fast games with one repeated pair `l9,6;l7,4;l7,3` vs `l9,6;l7,6;l7,7` (`3x`) plus five singleton seams, while `alternating_mana_rows` logged `7` Fast nonwins with one repeated pair `l2,7;l1,6` vs `l2,7;l1,8` (`2x`) plus five singleton seams.
- This iteration stayed diagnostic-only. A widened `smart_automove_pro_reliability_duel_trace_probe` on `alternating_mana_rows,forward_bridge_mana_rows` still did not collapse Fast to one mechanism: across `24` Fast games it logged `8` regressions, but only one repeated move pair appeared more than once, the archived white head-accept seam `l9,6;l7,4;l7,3` vs shipping `l9,6;l7,6;l7,7` (`2x`). Every other Fast pair stayed singleton.
- This iteration stayed diagnostic-only. The remaining `outer_edge_mana_rows` Normal miss is now one late black turn-eight advisor seam: shipping `l2,6;l3,7` is already the legacy-selected, reply-risk-shortlisted `ManaTempo`, while frontier still approves lower-ranked `l1,6;l1,5` through `ApprovedReplyRiskGuard`. A follow-up explicit Fast trace on `alternating_mana_rows,forward_bridge_mana_rows` still logged `5` nonwins across both colors and multiple surfaces, so there is still no single runtime hypothesis.
- This iteration killed a white turn-three mana-only legacy-progress override. The advisor-only candidate forced the clean `outer_edge_mana_rows` white seam onto shipping `l9,4;l8,3`, reduced the explicit `vs_shipping_normal` outer-edge nonwin trace from `2` boards to `1` board, then still failed sampled `pro-reliability` unchanged overall: Pro `1.0000`, Normal `0.9167`, Fast `0.8333`. The runtime code and temporary retained board were discarded.
- This iteration stayed diagnostic-only. Explicit-variant nonwin traces split the live sampled residue instead of collapsing it to one mechanism: Normal `outer_edge_mana_rows` reproduced both a late black `engine_post_search` vs shipping `engine_disabled` seam and an early white `engine_post_search` vs shipping `engine_disabled` seam; Fast explicit `forward_bridge_mana_rows` and `corner_chain_mana_rows` reproduced five nonwins across both colors, while isolated Fast `classic` did not reproduce on its own.
- This iteration killed a sampled-pass late-ply override line. Narrow white late spirit-setup head blocking plus black weak-window mana-lane overrides cleared sampled `pro-reliability`, then failed all-variant `pro-reliability-confirm` hard enough that the runtime code and temporary retained boards were discarded.
- This iteration also killed a late black shipping-fallback expansion. Extending the retained `late_black_shipping_fallback` variant path fixed the two traced sampled black boards, then still failed sampled `pro-reliability` by rotating Fast losses into `classic`, `corner_chain_mana_rows`, and `forward_bridge_mana_rows`. The runtime code and temporary retained boards were discarded.

## Latest Gate Snapshot

- Date: `2026-04-24`
- Shipping decision: public Pro remains on `frontier_pro_v2_guarded`.
- Release verification passed under the previous Classic-era gate shape: `cargo fmt --check`, host and wasm `cargo build --release --lib`, `git diff --check`, `cargo test --release --lib smart_automove_tactical_selected_profile -- --ignored --nocapture`, and `./scripts/run-automove-canonical-loop.sh --confirm frontier_pro_v2_guarded`.
- Previous confirm duel metrics: Pro `0.9688`, Normal `1.0000`, Fast `0.9688`, each with confidence `1.0000`; frontier average move times stayed below `200ms`.
- Release containment: public `Pro` dispatch still routes through `select_frontier_pro_v2_guarded_inputs`; `automove_experiments` remains under `#[cfg(test)]`, so diagnostics and experiment harness code are not production selection code.
- Latest sampled `pro-reliability` gate for the kept local package still failed promotion: Pro `1.0000`, Normal `0.9167`, Fast `0.8333`; confidence `0.9998 / 0.9968 / 0.9807`; frontier average move times `143.82ms / 179.83ms / 159.07ms`.
- This iteration widened the Pro-only replay instead of touching runtime code. `SMART_AUTOMOVE_VARIANTS=outer_edge_mana_rows,alternating_mana_rows,forward_bridge_mana_rows` with `smart_automove_pro_reliability_nonwin_trace_probe`, `duel_filter=vs_shipping_pro`, `repeats=6`, and `games=3` logged `11` Pro nonwins across `36` games.
- That replay did not promote the new white seams. The Pro white `l9,7;l8,8` vs `l9,7;l8,7` seam and the Pro white `l6,7;l7,6` vs `l6,7;l7,7` seam each appeared once, while the replay broadened into repeated black `l2,7;l1,6` vs `l2,7;l1,8` and black `l1,6;l2,7` vs shipping `l1,5;l2,3;l1,2` plus several other singleton Pro seams.
- That keeps the Pro-only blocker surface mixed. There is still no defensible white spend, and the black side is not yet clean enough to justify code without another direct structure probe.
- This iteration compared the only nearby retained white extension candidate against the new Pro white late-lane seam. The retained late white mana sibling board ships and frontier-aligns on `l7,7;l6,5;l6,6` through `engine_post_search` with a live head plan and `SpiritImpact` root under `window=2/deny=2`, while the new seam still ships `l6,7;l7,7` and frontier drifts to `l6,7;l7,6` through pure `engine_disabled` `ManaTempo` ordering under `window=0/deny=0`.
- That split kills the retained-extension hypothesis. The new Pro white `l6,7` seam is not a late white spirit/setup sibling that can reuse the retained late mana sibling path; it is a different early pure-ManaTempo engine-disabled surface.
- This iteration re-probed the only plausible shared same-family scoring angle left after the widened recurrence trace. The late black `outer_edge` `ManaTempo` drift and the new Pro white `l9,7;l8,8` vs `l9,7;l8,7` drift both came back as exact scoring ties: zero residual delta, zero search-eval delta, and identical `TurnEngineUtility`.
- The third same-family white seam did not match that shape. On `l6,7;l7,6` vs shipping `l6,7;l7,7`, the residual delta was real (`-112`) and localized to `spirit_action_utility` plus `mana_close_to_same_pool`, so it is not the same mechanism as the exact-tie drifts.
- That split kills the shared same-family late mana scoring hypothesis. Two seams are tie-order surfaces, one is a distinct residual-scoring surface, so there is still no defensible shared runtime spend across the current `ManaTempo` drifts.
- This iteration widened recurrence tracing across the full active blocker set instead of cutting runtime code. `SMART_AUTOMOVE_VARIANTS=outer_edge_mana_rows,alternating_mana_rows,forward_bridge_mana_rows` with `smart_automove_pro_reliability_duel_trace_probe`, `repeats=4`, and `games=3` produced `24` games in each duel bucket.
- The broader trace killed the remaining recurrence angle. Pro logged `3` regressions, Normal `3`, and Fast `7`, and every per-duel `repeated_move_pairs` entry stayed at count `1`. The isolated repeated white head-accept seam and isolated black mana sibling seam did not hold up as dominant residue once the full blocker set was traced together.
- The widened trace also broadened the surface instead of narrowing it. Alongside the known `alternating` and `forward_bridge` misses, it exposed extra singleton Pro/Normal seams such as `l9,7;l8,8` vs `l9,7;l8,7`, `l6,7;l7,6` vs `l6,7;l7,7`, and `l9,6;l9,4;l8,4` vs `l9,6;l7,8;l8,8`.
- This iteration reran bounded nonwin traces only to recover the current black blocker boards on clean logs. `SMART_AUTOMOVE_VARIANTS=outer_edge_mana_rows` with `duel_filter=vs_shipping_normal` still logged `2` nonwins, including the late black `l1,6;l1,5` vs shipping `l2,6;l3,7` seam. `SMART_AUTOMOVE_VARIANTS=alternating_mana_rows` with `duel_filter=vs_shipping_fast` logged `4` nonwins, including the black mana sibling `l2,7;l1,6` vs `l2,7;l1,8`.
- The follow-up three-board hotspot fingerprint probe still did not uncover a shared runtime mechanism. On late black `outer_edge`, black `alternating`, and repeated white `forward_bridge`, frontier and shipping enumerated the same full selector pool sizes and frontier simply paid the expected extra exact/pickup/tactical work on top of shipping's search-only baseline.
- That common cost shape is not enough to spend on. The actual move drift still splits across pre-accept same-family `ManaTempo` drift on `outer_edge`, pre-accept outside-family `DrainerSafetyRecovery` on `alternating`, and head-accepted `SpiritImpact` drift on `forward_bridge`; the exact contexts also still split between `window=1/deny=1` and `window=0/deny=0`.
- This iteration re-probed the only plausible shared white runtime angle and killed it. Inside `forward_bridge_mana_rows`, the repeated white head-accept seam is a head-accept problem on advisor-approved shipping `SpiritImpact l9,6;l7,6;l7,7`, the `l9,6;l8,7` seam is an `ApprovedReplyRiskGuard` safe-progress approval over shipping `SpiritImpact`, and the `l9,7;l8,6` seam additionally routes through `score_window_tactical_fallback` with a preserved safe-progress representative.
- That split kills the shared white `forward_bridge` hypothesis. The white seams only look related from the move list; they diverge across head acceptance, reply-risk approval, and runtime fallback layers, so there is still no defensible shared white runtime spend.
- This iteration re-probed the only plausible shared black runtime angle and killed it. On `outer_edge_mana_rows` black, shipping `l2,6;l3,7` is still the legacy-selected, reply-risk-shortlisted `ManaTempo` root and frontier drifts to lower-ranked `ManaTempo l1,6;l1,5` through `ApprovedReplyRiskGuard`. On the repeated `alternating_mana_rows` black seam, shipping `l2,7;l1,8` is also the legacy-selected `ManaTempo` root, but frontier instead approves outside-shortlist `DrainerSafetyRecovery l2,7;l1,6` through `ApprovedFamilyCompetition`.
- That split kills the shared black late-mana hypothesis. The unresolved Normal seam and the repeated Fast black seam only look similar from the move list; they diverge at the advisor/shortlist layer, so there is still no defensible shared runtime spend.
- This iteration isolated the two remaining Fast blockers instead of touching runtime code. `forward_bridge_mana_rows` on `smart_automove_pro_reliability_duel_trace_probe` still logged `8` regressions across `24` Fast games; the white head-accept seam `l9,6;l7,4;l7,3` vs `l9,6;l7,6;l7,7` repeated `3x`, but the variant still carried singleton black mana/progress seams plus extra white spirit/setup seams.
- `alternating_mana_rows` on `smart_automove_pro_reliability_nonwin_trace_probe` logged `7` Fast nonwins. The black mana sibling seam `l2,7;l1,6` vs `l2,7;l1,8` repeated `2x`, but the rest of the variant still split across singleton late mana/recovery and one white spirit/setup seam.
- This iteration widened Fast recurrence tracing instead of cutting runtime code. `SMART_AUTOMOVE_VARIANTS=alternating_mana_rows,forward_bridge_mana_rows` with `smart_automove_pro_reliability_duel_trace_probe`, `duel_filter=vs_shipping_fast`, `repeats=4`, and `games=3` produced `24` Fast games with `8` regressions, `7` improvements, and `9` flat results.
- That wider trace still did not support one shared Fast spend. The only repeated move pair was the already-archived white branch head-accept seam `l9,6;l7,4;l7,3` vs shipping `l9,6;l7,6;l7,7` (`2x`); every other Fast regression pair, including the `alternating_mana_rows` black mana/recovery misses and the other `forward_bridge_mana_rows` white/black misses, stayed singleton.
- This iteration kept runtime code untouched and re-probed the remaining late black outer-edge seam directly. On the late black turn-eight board `2 0 b 0 0 0 0 0 8 ...`, shipping `l2,6;l3,7` already appeared as the legacy-selected reply-risk-shortlist `ManaTempo` root (`root_rank=10`), while frontier still approved `l1,6;l1,5` as `ApprovedReplyRiskGuard` at `root_rank=14`.
- Fast residue also stayed mixed on explicit replay. `SMART_AUTOMOVE_VARIANTS=alternating_mana_rows,forward_bridge_mana_rows` with `duel_filter=vs_shipping_fast` produced `5` nonwins across both colors, including black `engine_post_search` bomb/setup divergences and white head-accepted mana-lane divergences, which is still too broad for a single late-black-only spend.
- This iteration also killed a white turn-three mana-only legacy-progress override. The local candidate added a narrow `ApprovedLegacySelector` carve-out for white turn-three mana-only, `window=0`, `deny=0`, positive-safety boards where legacy already preferred a vulnerable safe-progress root over approved non-vulnerable `ManaTempo`. It fixed the clean `outer_edge_mana_rows` white board and reduced the explicit outer-edge `vs_shipping_normal` trace from `2` nonwins to `1`, but sampled `pro-reliability` still failed at Pro `1.0000`, Normal `0.9167`, Fast `0.8333`; confidence `0.9998 / 0.9968 / 0.9807`; frontier average move times `146.56ms / 187.62ms / 166.47ms`. The runtime code was discarded.
- The sampled residue on that killed line narrowed in one place and stayed broad elsewhere. Normal `outer_edge_mana_rows` still failed at `0.5000` from the late black seam alone, while Fast `alternating_mana_rows` and `forward_bridge_mana_rows` failed at `0.5000`; `classic` and `corner_chain_mana_rows` were clean on that sample.
- This iteration ran explicit-variant `smart_automove_pro_reliability_nonwin_trace_probe` instead of cutting runtime code. `SMART_AUTOMOVE_VARIANTS=outer_edge_mana_rows` with `duel_filter=vs_shipping_normal` produced `2` nonwins: one late black turn-eight `engine_post_search` miss (`l1,6;l1,5` vs shipping `l2,6;l3,7`) and one early white turn-three action+mana `engine_post_search` miss (`l10,4;l9,5` vs shipping `l9,4;l8,3`).
- Fast explicit blocker tracing also stayed split. `SMART_AUTOMOVE_VARIANTS=classic,forward_bridge_mana_rows,corner_chain_mana_rows` with `duel_filter=vs_shipping_fast` produced `5` nonwins, all on `forward_bridge_mana_rows` and `corner_chain_mana_rows`; `forward_bridge` stayed mainly `engine_post_search`, while `corner_chain` also exposed an `engine_disabled` ordering miss. Standalone Fast `classic` replay on the same bounded probe produced `0` nonwins.
- This iteration also killed a sampled-pass late-ply override package. The local candidate stacked a white late spirit-setup branch-head reject with black weak-window mana-lane advisor exceptions, passed sampled `pro-reliability` at Pro `1.0000`, Normal `0.9167`, Fast `0.9167`, then failed all-variant `pro-reliability-confirm` at Pro `0.6667`, Normal `0.7292`, Fast `0.6667`; frontier average move times stayed below `200ms`, so the failure was strength, not cost. The runtime code was discarded.
- This iteration also killed a late black shipping-fallback expansion. The local candidate extended `select_late_black_search_fallback_inputs` into late weak-window black turn-start and mana-only states, fixed the traced sampled black Normal/Fast boards, then still failed sampled `pro-reliability`: Pro `1.0000`, Normal `0.9167`, Fast `0.7500`; confidence `0.9998 / 0.9968 / 0.9270`; frontier average move times `153.01ms / 194.05ms / 174.84ms`. The runtime code was discarded.
- This iteration killed the `black_recovery_branch` static-exact spend. A reply-floor-only exact cut passed `guardrails`, `variant-smoke`, `pro-triage`, and `runtime-preflight`, then failed sampled `pro-reliability` at Pro `0.5000`, Normal `0.5000`, Fast `0.9167`; average move times stayed under `200ms`. The runtime code was discarded.
- This iteration also killed black progress material/cooldown-only scoring. Zeroing `fainted_mon` and `fainted_cooldown_step` only on the scoped black turn-six window/deny state shrank the target residual delta from `843/778` to `83/18` and preserved the turn-ten setup-control board, but final selection still stayed on `l7,1;l9,3`.
- Follow-up selector-layer probe for black progress retained only diagnostic code. Under the material-dampened replay, frontier still selected `l7,1;l9,3` through `frontier_execute` / `engine_post_search`, with advisor approval `l7,1;l9,3:SafeSupermanaProgress:ApprovedReplyRiskGuard:rank0`; shipping setup `l1,5;l2,7;l1,8` was already present in the reply-risk shortlist at rank `10`.
- This iteration killed the combined black progress material-plus-rank line. The runtime cut aligned the local residue to shipping setup `l1,5;l2,7;l1,8`, passed `guardrails`, `variant-smoke`, `pro-triage`, and `runtime-preflight`, then failed sampled `pro-reliability`: Pro `0.5000`, Normal `0.5000`, Fast `0.8333`; average move times stayed below `200ms`. The runtime code was discarded.
- The kept runtime edits from this wave repaired several stable retained late boards, including black search/head-accept Normal seams and two Fast seams, but the sampled gate still rotated to deeper late-ply losses.
- One traced Fast nonwin from this wave was not safe to retain. The copied board snapshot replayed to frontier's own move instead of the live shipping-selected root, so only the reproducibility lesson is kept.
- This iteration added the decision-record aggregation diagnostic requested by the structural reset plan. It records first divergences with baseline-root liveness (`selected`, `pre_accept`, `head`, legacy-selected, candidate-live/omitted, advisor-approved/ordered/preserved/injected), runtime branch, selector stage, pre/head families, head acceptance, and exact context.
- The decision-record aggregate did not produce a promotable runtime mechanism. Active-blocker nonwins split across alternating black recovery-vs-mana flat loss, forward-bridge white SpiritImpact-vs-ManaTempo regression, outer-edge white SpiritImpact-vs-DrainerSafetyRecovery regression, and forward-bridge white safe-progress-vs-spirit regression. Canonical sampled nonwins added outer-edge black same-family ManaTempo flat loss plus the archived forward-bridge white accepted-head spirit regression.
- This iteration also killed and discarded a test-only live-root shipping meta-selector. Routing to shipping when the shipping root was candidate-live at rank `0` failed the active Pro panel at `1-5`; broader top-2 and advisor-live variants also failed at `2-4`, with `outer_edge_mana_rows` and `forward_bridge_mana_rows` both `0-2`.
- This iteration killed and discarded a test-only ProV3 utility selector/switch wave. Pure utility root selection failed active blockers (`candidate-set 3-3 / 3-3 / 4-2`, full-pool `3-3 / 4-2 / 4-2` across Pro/Normal/Fast). The best shallow switch, candidate-set nonstrict utility over guarded with score window `96`, reached active blockers `5-1 / 5-1 / 5-1`, but canonical sampled Pro immediately failed at `5-7`, with `alternating_mana_rows` and `inner_wedge_mana_rows` both `0-2`. Tightening score tolerance to `64`, widening to `128`, adding utility eval tolerance `64`, and an early-white quiet-turn guard all worsened Pro.
- This iteration kept a harness improvement instead of cutting another runtime seam. `smart_automove_pro_promotion_dashboard_probe` and `./scripts/run-automove-experiment.sh pro-promotion-dashboard <candidate>` now summarize sampled plus active-blocker panels, weakest variant rows, candidate-vs-shipping strength, candidate-vs-guarded pressure, and whether a candidate is active-blocker-only, sampled-only, or broadly promising. Smoke runs passed on `frontier_pro_v2_raw`; the tiny filtered run was validation of the harness, not promotion evidence.
- This iteration used the promotion dashboard to kill a dormant ProV2 policy-toggle portfolio without retaining code. Active-blocker panels failed for eligibility gating (`3-3 / 5-1 / 5-1`), selected-followup projection off (`3-3 / 6-0 / 4-2` with Fast `forward_bridge_mana_rows` at `0-2`), secondary analysis off (`3-3 / 5-1 / 4-2`), late-safe-mana preference off (`2-4 / 6-0 / 4-2`), late-black setup rescue (`2-4 / 6-0 / 4-2`), and bounded root injection (`2-4 / 6-0 / 4-2`).
- This iteration also killed a generic ProV3 head/root utility-floor line. Test-only head-accept policies requiring the head or root to support the selected root within eval tolerances `96` and `192` both failed the active-blocker dashboard: each scored Pro `2-4`, Normal at best `6-0`, and Fast `4-2`, with Pro `outer_edge_mana_rows` at `0-2`. The source experiments were discarded; only this no-go remains.
- This iteration killed a test-only ProV3 shipping-rollout meta-selector. A 2-ply fast continuation comparison first failed sampled Pro at `8-4`, Normal at `10-2`, and Fast at `8-4`; after reducing static eval to a tie-breaker it worsened sampled Pro to `7-5`, including `inner_wedge_mana_rows` at `0-2`. The source experiment was discarded.
- This iteration also killed a deeper-budget ProV3 scout. Widening the guarded depth-4 Pro search to `2x` nodes plus larger root/node coverage failed sampled Pro at `9-3`; pushing to depth `5` with `2x` nodes also failed sampled Pro at `9-3` while raising average move time to roughly `335ms`. Extra search budget alone is not a promotable path.
- This iteration killed a test-only ProV3 stability-utility root override before runtime promotion work. Preserving guarded wrapper branches and allowing only `frontier_execute` roots with concrete stability gain plus stronger `TurnEngineUtility` still failed sampled Pro at `8-4`; only `11` overrides were enough to introduce losses on `alternating_mana_rows`, `inner_wedge_mana_rows`, and `forward_bridge_mana_rows`. The source experiment was discarded.
- This iteration killed a test-only ProV3 search-policy sweep. Raw guarded-config search with killer/history/TT replacement failed sampled Pro at `5-7`, PVS+ordering and the full search-policy combo only reached `6-6`, quiescence stayed `5-7`, and preserving guarded wrapper branches while enabling PVS still failed sampled Pro at `5-7`; the remaining guarded search-policy variants were stopped. The source experiments were discarded.
- This iteration killed a test-only ProV3 phase-weight portfolio. Replacing current Pro attacker-proximity weights with walk-threat-medium failed sampled Pro at `5-7` with three `0-2` rows, drainer-shield only reached `6-6` with `alternating_mana_rows` at `0-2`, stronger attacker-proximity failed `5-7`, and combo proximity/attack failed `5-7`. Broad weight-family swaps rotate weak rows instead of improving the sampled Pro panel. The source experiments were discarded.
- This iteration killed and discarded a test-only guarded iterative-deepening wave. Plain iterative deepening passed sampled Pro at `11-1` but failed sampled Normal at `9-3`; offset-1 plus `1.25x` nodes passed sampled Normal at `11-1` but failed sampled Pro at `8-4`; simple variant gates passed sampled Pro/Normal but still failed sampled Fast.
- The best sampled-only composite was `frontier_pro_v3_guarded_iterdeep_row_composite`: guarded iterative deepening by default, raw ProV2 only for `offset_arc_mana_rows`, windowed iterative deepening plus `1.25x` nodes only for `inner_wedge_mana_rows`, and offset-1 plus `1.25x` nodes for `outer_edge_mana_rows`, `forward_bridge_mana_rows`, and `corner_chain_mana_rows`. It passed sampled dashboard Pro/Normal/Fast at `11-1 / 11-1 / 11-1` with max average `183.71ms`.
- That composite was still not promotable. The active-blocker panel failed at Pro `3-3`, Normal `3-3`, Fast `5-1`; active Normal `outer_edge_mana_rows` was `0-2`, active Pro split all three rows, and active Fast still split `outer_edge_mana_rows`.
- The active follow-up confirmed a mode/context conflict, not a simple row gate. Raw ProV2 and no-late-black fallback can fix active Normal `outer_edge_mana_rows`, but both lose active Pro `outer_edge_mana_rows` at `0-2`, and raw/no-late also lose active Fast `outer_edge_mana_rows` at `0-2`. Do not promote sampled-only row composites or variant-only raw fallbacks from this evidence.
- This iteration killed an attribution-informed context-shipping row gate without retaining source code. Guarded-vs-raw and shipping-control-vs-raw attribution over the active blockers did not expose a one-sided below-variant `outer_edge_mana_rows` feature. A row composite using raw outer-edge search fixed active Normal but lost active Pro/Fast; adding exact-context shipping fallbacks repaired active Fast to `6-0` but active Pro stayed `3-3` and active Normal stayed `5-1`. Broadening the white turn-three `forward_bridge_mana_rows` shipping context still left active Pro at `3-3`.
- This iteration kept a harness improvement and did not promote a candidate. `smart_automove_pro_sweep_decision_record_probe` and `./scripts/run-automove-experiment.sh pro-sweep-decision-record <candidate>` now aggregate nonwins/deltas for arbitrary sweep candidates against same-seed `shipping_pro_search_control`. The active guarded run was mixed (`3` Pro nonwins, `1` Normal nonwin, `4` Fast nonwins across the active-blocker panel), and raw's sampled Pro failure stayed singleton-heavy: `7/12` nonwins across five variants, with the two regressions split between `forward_bridge_mana_rows` and `inner_wedge_mana_rows`.
- This iteration killed a stricter score-window ProV3 utility switch before retaining source. Preserving guarded wrapper branches and only overriding `frontier_execute` roots with candidate-set nonstrict utility inside score window `96` still failed the active dashboard: vs shipping Fast `4-2` with `outer_edge_mana_rows` at `0-2`, and vs guarded `2-4` with `alternating_mana_rows` and `forward_bridge_mana_rows` both `0-2`. Tighter guard variants were not worth running because they cannot repair the active `outer_edge` vs shipping failure while preserving the rows that already lost to guarded.
- This iteration refreshed the no-late-black fallback active dashboard and killed it as a current structural scout candidate. `frontier_pro_v2_no_late_black_fallback` failed active vs shipping at Pro `3-3`, Normal `5-1`, Fast `3-3`; `outer_edge_mana_rows` was `0-2` in both Pro and Fast, Normal split `alternating_mana_rows`, and the guarded delta was only `4-2` with `outer_edge` and `forward_bridge` both split. No source change was retained.
- This iteration recreated the sampled-pass guarded iterative-deepening row composite only as a diagnostic scaffold and killed it on the active dashboard again: shipping Pro `3-3`, Normal `3-3`, Fast `5-1`; Normal `outer_edge_mana_rows` stayed `0-2`. A narrow outer-edge context-shipping repair improved Normal to `4-2` but left Pro `3-3` and Fast `5-1`; active Pro decision records split across `outer_edge` flat, `alternating` flat, and a `forward_bridge` early-white-fallback regression, so no source was retained.
- This iteration killed a test-only exact-evidence and portfolio scout wave without retaining source. Full exact static evaluation and root exact tactics were stopped as cost failures; broad exact-lite reached active Pro only `4-2` with `outer_edge_mana_rows` and `forward_bridge_mana_rows` split. Guarded/raw/shipping portfolio majority failed active Pro `3-3` with `outer_edge_mana_rows` at `0-2`, and raw-on-divergence failed active Pro `2-4` with `outer_edge_mana_rows` at `0-2`.
- This iteration killed two test-only ProV3 reply-risk selector scouts without retaining source. Reply-floor utility failed active Pro at `3-3`, with `outer_edge_mana_rows` still `0-2`. Ranked reply-guard deference improved active Pro to `4-2` and removed the sharp Pro `outer_edge` regression, but the full active panel still failed at Pro `4-2`, Normal `5-1`, Fast `2-4`, with Fast `outer_edge_mana_rows` `0-2`.
- This iteration killed two more test-only ProV3 reply-risk policy scouts without retaining source. A pressure-blend scorer over advisor ordered/preserved roots failed active Pro at `3-3`, with `outer_edge_mana_rows` still `0-2`; disabling `prefer_clean_reply_risk_roots` worsened active Pro to `2-4`, also with `outer_edge_mana_rows` `0-2`.
- This iteration did not find a promotable challenger. The retained active-blocker decision aggregate stayed mixed: Pro had one flat nonwin, Normal had one regression, and Fast had two regressions plus one flat nonwin, all split across different variants/families/stages. The active policy matrix showed no strict replacement: `no_selected_followup_projection` helped Normal/Fast but left Pro flat, while full-scored reply guard was balanced on Pro and regressed Normal/Fast. A temporary no-selected plus safe-white full-scored gate was killed on canonical sampled before active validation (`Pro 4-8`, `Normal 8-4`) and its source was discarded.
- The policy matrix now emits `PRO_POLICY_MATRIX_PORTFOLIO` so future iterations can measure whether the compared policy set has enough oracle coverage before building a selector. Use `candidate_only_wins` as possible selector upside, `baseline_only_wins` as regression risk, and `no_policy_wins` as evidence that a new policy feature is needed rather than another gate over existing candidates.
- This iteration added `PRO_POLICY_MATRIX_PORTFOLIO_CLASS` and used it to check whether adding `frontier_pro_v2_no_low_budget_guard` to the known-policy portfolio could make sampled Pro selector work viable. It did not: sampled Pro oracle coverage stayed `9/12`, with `baseline_only_wins=1`, `candidate_only_wins=2`, and `no_policy_wins=3`. The no-policy sampled Pro buckets were white `alternating_mana_rows`, white `forward_bridge_mana_rows`, and black `inner_wedge_mana_rows`, so the known policy set still lacks enough winning lines before selector design.
- This iteration refreshed `pro-policy-winner` after the latest portfolio additions and killed a broad utility-admission idea without retaining source. The portfolio now has oracle coverage on the checked panels: sampled `vs_shipping_pro` was `baseline_wins=7`, `policy_wins=5`, `no_policy_wins=0`; active blockers were Pro `3/3/0`, Normal `5/1/0`, Fast `2/4/0` for baseline/policy/no-policy. Coverage is not promotion evidence: winners are singleton-heavy and split across `shipping_pro_search_control`, raw ProV2, full-scored reply guard, and the alternating-white policy.
- The attempted ProV3 utility-admission reply-risk candidate admitted projected-utility roots into the reply-risk guard before final selection. It failed immediately on sampled Pro at `7-5` (`0.5833`, confidence `0.6128`) with split weak rows in `center_spoke_mana_rows`, `alternating_mana_rows`, `inner_wedge_mana_rows`, and `forward_bridge_mana_rows`; source was discarded.
- The refreshed winner records explain why another move-table selector is a dead end. Active `outer_edge_mana_rows` white turn-three `mons_moves=1`, action+mana, `window=0/deny=0` needs full-scored reply guard in Pro but shipping-control routing in Fast, distinguished only by evolved board/guarded-selected move. Encoding those first-diff moves would be a board-local selector, not a cross-budget automove improvement.
- This iteration killed a cross-budget score-floor root-evaluation candidate without retaining source. The candidate preserved guarded wrapper branches, then overrode only `frontier_execute` roots whose Pro/Normal/Fast guarded score floor beat the guarded root. It still failed sampled Pro at `7-5` (`0.5833`, confidence `0.6128`) after `24` overrides, with split weak rows in `center_spoke_mana_rows`, `alternating_mana_rows`, `inner_wedge_mana_rows`, `split_flank_mana_rows`, and `forward_bridge_mana_rows`; average move time rose to `234.48ms`.
- This iteration killed two more test-only scouts without retaining source. A matrix-positive selected-followup-off context gate failed the sampled dashboard before active validation: Pro `7-5`, Normal `8-4`, Fast `6-6`, with hard weak rows on Pro `inner_wedge_mana_rows`, Normal `forward_bridge_mana_rows`, and Fast `corner_chain_mana_rows`. A utility-ordered full reply-risk shortlist was worse and too expensive, failing sampled Pro `3-9` with `forward_bridge_mana_rows` `0-4`, `split_flank_mana_rows` `0-2`, and `284.52ms` average candidate turns.

## Next Hypothesis

- Do not spend the next candidate on sampled Pro repairs alone. The latest retained canonical sampled decision-record refresh has no Pro nonwins; the current sampled residue is Normal/Fast plus active-panel stability, so a candidate that only moves Pro or only fixes one canonical Fast board is expected to be another false positive.
- Do not build the next experiment as another static context selector over the current policy portfolio. `pro-policy-winner` and the promotion dashboard showed that first winning policy context is not enough: active `outer_edge_mana_rows` needs different policy-entry timing across Pro/Fast with the same exact-opportunity context, and stacking turn/color/`mons_moves` gates rotates Normal or Fast losses.
- Before implementing another selector from policy-winner or policy-matrix labels, run `pro-policy-cross-budget` on the smallest relevant policy set. If a board family is classified as `budget_conflict`, the selector is expected to rotate losses between Pro/Normal/Fast; spend only on a new utility/root-evaluation feature that can make the same policy non-regressing across budgets.
- When a policy-winner slice shows a tempting repeated policy or mechanism class, run seed-aligned `pro-policy-cross-budget` with the same source duel before writing a selector. The active Fast no-selected-followup slice now has a concrete no-go: one side is a clean repair and the other is a budget conflict on the exact Fast seed.
- Do not stack one-opening `pro-policy-cross-budget` clean repairs into a static composite. The active outer-edge full-scored repair plus sampled inner-wedge black shipping repair still failed sampled Normal/Fast and left broad weak rows; cross-budget clean classes are candidate-design evidence, not promotion evidence.
- Do not retry broad or after-opening quiet mana-utility selection as the new ProV3 feature. A near-top quiet `ManaTempo` root chooser improved some sampled Pro residue but still failed sampled Pro at `9-3`, fired often enough to create generic early-opening losses, and carried extra CPU; the after-opening refinement fell to `8-4`. A future mana feature needs a repeated decision-record mechanism below variant/color/turn/resource context, not a wider quiet-mana gate.
- Do not retry color-symmetric safe-mana selected-followup projection as the next utility feature. It is narrower than disabling selected-followup projection, but it still fails sampled Pro and creates a hard `inner_wedge_mana_rows` row loss.
- Do not build a selector keyed by guarded selected-move FEN or first-diff board fingerprints. The current portfolio can cover the sampled and active panels as an oracle, but the winning labels are singleton-heavy and conflict across opponent budgets; a move table would only memorize dashboard openings and is expected to fail broader confirmation.
- Do not treat the recovered `corner_chain_mana_rows` white opening root as a selector unlock. It fixes one false `no_policy_win`, but the expanded context selector still failed active Fast and a wider outer-edge delta run showed same-context cross-budget conflict.
- The next useful spend is a genuinely new ProV3 utility or root-evaluation feature, evaluated first on `pro-promotion-dashboard` and then through `pro-sweep-decision-record` if it has one explainable panel miss. The candidate must explain why a policy wins when entered earlier or later than the first divergence, not just memorize the first winning policy label.
- Do not retry opponent reply-interval floor/safety as that new feature. It was too broad when active, still weak when score-non-dropping, and inert when constrained to concrete score/pickup roots.
- Do not retry exact score-race margin or first-diff fallback utility admission as that new feature. The sampled Pro policy-winner records showed winning alternatives in the portfolio, but replacing only the first divergent root did not move outcomes; the missing behavior is profile-continuation quality after the first diff.
- Do not retry an internal Pro/Normal/Fast score-floor gate as the next stability signal. It changed enough decisions to be dangerous but still produced the same sampled-Pro failure shape as the broad utility-admission line.
- Do not retry continuation-floor plus progress/setup root tiebreaking as the next ProV3 utility feature. It was too inert to repair the sampled rows and still carried extra reply-floor cost.
- Do not retry root-live black-window shipping-control plus an early-white utility veto as the next selector. Even with root-live evidence and a narrow exact window, the sampled Pro row only improved to `8-4`, and the early-white utility guard was inert.
- Do not retry selected-followup-off context gates from policy-matrix positives, or a full utility-ordered reply-risk shortlist. The former rotated sampled weak rows across Pro/Normal/Fast before active validation; the latter collapsed sampled Pro and raised CPU without needing Normal/Fast validation.
- Do not retry broad no-post-search-head selection or strict primary-axis-only post-search head acceptance as the next challenger. Both collapse sampled Pro to `4-8`; the strict version mostly behaves like the no-head ablation because primary-axis-improving heads are rare on the sampled Pro dashboard.
- Do not retry policy-output majority/consensus over the current portfolio as the next challenger. Even requiring multiple existing policies to agree still collapsed sampled Pro to `5-7` and pushed candidate move time above `520ms`.
- Do not retry shallow ProV4 unified root-value comparators that just sort scored roots by existing tactical, utility, safety, progress, family, score, and rank fields. The best sampled Pro quick gate reached only `9-3` and the remaining records were `singleton_regression_pressure`, not a spendable repeated mechanism.
- Do not retry early-white or late-black guarded-fallback relaxation just because raw ProV2 fixes a narrow active Fast policy-matrix slice. The mechanism-class matrix made that slice visible, but the direct fallback-relaxation candidate failed sampled Pro at `7-5`; use matrix classes as focused-probe routing evidence, not fallback-removal permission.
- Do not retry post-root turn-engine continuation-stability selection as the next challenger. A wrapper-preserving test-only scout made enough overrides to add cost, but the loose version stopped at sampled Pro `8-4` and the strict primary-axis version stopped at `7-5`; this is another noisy utility gate unless a new discriminator explains which continuation projections are safe across Pro/Normal/Fast.
- Do not retry selector-time cross-budget `TurnEngineUtility` admission as the next challenger. The Pro/Normal/Fast primary-axis version was too expensive before strength could be measured, and the capped corpus behind it still showed only singleton Normal/Fast mechanisms.
- Do not reopen `frontier_pro_v2_no_low_budget_guard` because it looks clean on a tiny matrix slice. The full sampled dashboard still failed all three duels, and the Normal miss exposed a hard `forward_bridge_mana_rows` `0-2` row.
- Do not retry narrow black pressure shipping-control routing from sampled Fast policy-winner rows. Even with turns capped to `2..=6` and exact pressure required, the sampled Pro dashboard fell to `8-4` after `22` overrides.
- Keep using `pro-policy-matrix` and `pro-policy-winner` as diagnostics for new policy components, but only to prove oracle coverage and identify decision timing. They do not justify another selector unless the winning contexts separate cleanly across sampled and active Pro/Normal/Fast and the resulting dashboard passes both panels.
- Do not promote the narrow alternating-white edge-mana or white-opening utility-mana candidates directly. They are policy components and root-oracle regression targets only.
- Do not retry advisor ordered/preserved-root pressure blends or dirty reply-risk root toggles as the next challenger. The pressure feature did not move the hard active Pro row, and removing clean-root preference made the active Pro panel strictly worse.
- Do not retry reply-floor-only root switching or generic ranked `ApprovedReplyRiskGuard` deference as the next challenger. Rank deference can repair one active Pro regression, but it does not create wins from flat Pro losses and does not solve active Fast `outer_edge_mana_rows`.
- The latest active-blocker context check did not produce a one-sided `outer_edge_mana_rows` split. Do not keep stacking turn/color/mons-count/exact-context shipping gates from first divergences; first-move alignment is not enough when the dashboard outcome stays stuck.
- The arbitrary-candidate decision/nonwin harness now exists. Use it on any new sweep candidate before attribution or runtime work; if the candidate's sampled Pro or active Pro miss is singleton-heavy across variants, kill it immediately.
- The next useful spend is a genuinely new ProV3 utility feature, evaluated first on the promotion dashboard and then through `pro-sweep-decision-record` if it has one explainable panel miss. Do not retry score-window utility switches, surface-gain tie breakers, or guarded-wrapper-preserved utility overrides; the candidate must explain why utility helps active `outer_edge_mana_rows` vs guarded while still losing that row to shipping and harming active `alternating_mana_rows` / `forward_bridge_mana_rows` vs guarded.
- Do not revisit the sampled-pass iterative-deepening row composite or the outer-edge context-shipping repair as the next challenger. The refreshed active checks confirmed the sampled pass is a false positive while active Pro/Normal/Fast keep rotating `outer_edge_mana_rows` and related rows.
- Do not spend the next iteration on full exact static evaluation, root exact tactics, broad exact-lite, or guarded/raw/shipping agreement portfolios. The exact line is either too expensive or still weak on active Pro, and the portfolio line reopens the killed context/meta-selector family while losing `outer_edge_mana_rows`.
- Structural reset remains the next path, but do not recreate the killed broad/raw/exact context gates, live-root shipping meta-selectors, portfolio selectors, or shallow ProV3 utility root switches. The next useful spend is a deeper ProV3 utility change with new features that explain sampled Pro and active blockers together without rotating Normal/Fast down.
- Do not retry online policy rollout over the current policy portfolio as the next challenger unless it first has a cheap precomputed feature. The direct selector is cost-blocked before strength validation, even under an early-turn sampled Pro-only quick check.
- Do not reopen mid-turn progress guard or late-black setup-progress rescue as dormant toggles. Wrapper-preserving sampled Pro checks reproduced retained guarded `7-5` with the same hard `inner_wedge_mana_rows` miss.
- The promotion-dashboard harness now exists. Use it before the next runtime spend; the next automove hypothesis should be a deeper ProV3 policy change with new utility features, not a preserved-priority replay, because preserved and injected advisor roots are already fed back into focused root candidates before scoring.
- Do not retry dormant ProV2 config toggles or generic head/root utility-floor head-accept gates as the next challenger. The active-blocker dashboard killed both lines before sampled validation; the next ProV3 attempt needs a new feature that explains why `outer_edge_mana_rows` Pro is harmed by simple head/root floors while sampled Pro is harmed by shallow utility switching.
- Do not retry a shipping-root rollout meta-selector as the next challenger. Bounded fast continuation was too noisy on sampled Pro even after calibration, so future ProV3 work needs learned/shared utility features inside the selector rather than a late shipping-vs-frontier wrapper.
- Do not spend the next iteration on raw depth/node increases. The filtered sampled-Pro check killed both wider depth-4 and depth-5 scouts before Normal/Fast or active-blocker panels were worth running.
- Do not retry a generic stability-gain plus `TurnEngineUtility` root override as the next challenger. Even with guarded wrapper preservation and only a few overrides, sampled Pro dropped to `8-4`, so the next feature must explain when a stability/utility override is one-sided rather than merely stronger by local axes.
- Do not retry dormant search mechanics as the next challenger, either raw or behind guarded wrappers. Killer/history ordering, TT depth replacement, PVS, and quiescence did not clear sampled Pro and mostly added CPU while rotating weak variants.
- Do not retry broad phase-adaptive scoring-weight family swaps as the next challenger. Walk-threat, drainer-shield, stronger attacker-proximity, and combo proximity/attack weights all failed sampled Pro; the next valuation change needs a targeted new feature, not a wholesale existing weight family.
- Do not retry guarded preaccept utility-floor head vetoes as the next challenger. A legacy/full-pool-supported preaccept fallback killed on the active dashboard before sampled validation: Pro `3-3`, Normal `5-1`, Fast `2-4`, with `outer_edge_mana_rows` still `0-2` in Pro/Fast and average move time near `300ms`.
- Do not build another active policy-winner selector over the current expanded portfolio. The focused active refresh confirmed the same mode conflict: Pro's white `outer_edge_mana_rows` exact context needs full-scored reply guard, Fast's matching white `outer_edge_mana_rows` exact context needs shipping-control, and Fast also needs raw only on a separate black `alternating_mana_rows` late fallback.
- Do not retry dual-progress SpiritImpact root preference or dual-progress-triggered shipping fallback. The feature is not shared enough: it improves active Normal `outer_edge_mana_rows`, but active Pro `outer_edge_mana_rows` still fails and active Fast stays split across all three blocker variants.
- Use the decision-record aggregate before writing another candidate. A shipping root being candidate-live, rank-0/top-2, or advisor-live is not sufficient; the aggregate must show one repeated mechanism below advisor/head/final-selection status across both canonical sampled and active-blocker panels.
- The latest retained sampled and active-blocker decision records did not show that mechanism. Future work needs a new utility/root-evaluation feature first; do not infer a shared advisor, head-acceptance, reply-risk, or shipping-control gate from the current singleton-heavy records.
- Use the enriched attribution context output before writing another candidate. A proposed gate must be one-sided by variant and exact-opportunity context across Pro, Normal, and Fast; branch label plus turn/color is not enough.
- Do not spend another candidate on opening or late-black context toggles unless a decision-record aggregate shows the baseline root is candidate-live/shortlist-live with a repeated advisor/head/final-selection mechanism. The latest exact-context gates changed isolated rows but still failed both scout panels, and the refreshed no-late-black dashboard still failed active Pro/Fast on `outer_edge_mana_rows`.
- Do not use `frontier_pro_v2_no_late_black_fallback` as the next structural baseline. The refreshed active dashboard failed Pro and Fast at `3-3` with `outer_edge_mana_rows` at `0-2`, so it is not a near-promotable ablation.
- Do not promote `frontier_pro_v2_raw` or remove the guarded wrapper chain wholesale. Raw can improve the active blocker slice, but the canonical sampled sweep exposed broad Pro losses, and sampled Pro attribution showed both `early_white_fallback` and `late_black_shipping_fallback` are sometimes saves and sometimes regressions.
- Do not reopen the current profile-level ablations as standalone candidates. Raw and `frontier_pro_v2_expansion_224` are the best structural diagnostics, but both still stop at `0.8333 / 0.8333 / 0.8333` on the active-blocker sweep; the next spend needs a new mechanism for the remaining split variants, not another wrapper toggle.
- Do not build a raw-plus-expansion composite candidate. Direct attribution found no complementary outcome changes between `frontier_pro_v2_raw` and `frontier_pro_v2_expansion_224` across the full active-blocker slice.
- There is no promotable challenger after the latest sampled no-go. Default to no broad runtime change until a focused probe produces a new shared mechanism below the current late-ply search/head layer.
- Do not retry the black turn-four weak-window recovery reply-risk-shortlist scoping line as a standalone patch. It removed the repeated `alternating_mana_rows` recovery jump locally, but sampled reliability rotated Normal down and still left Fast blocked.
- Do not retry the variant-scoped shipping fallback on the current sampled blockers. Matching retained shipping on blocker variants can erase trace regressions, but it also erases frontier wins and failed sampled `pro-reliability`.
- There is still no live runtime hypothesis after the widened Pro-only nonwin replay. Do not reopen the new white Pro seams as standalone spends; they remained singleton while the Pro blocker surface broadened into mixed black recurrence.
- There is still no live runtime hypothesis after the retained-vs-new white late mana sibling surface probe. Do not reopen the retained late white mana sibling path as an extension candidate for `l6,7;l7,6` vs `l6,7;l7,7`; the retained board is a late `SpiritImpact`/head-plan surface and the new seam is an early pure `ManaTempo` engine-disabled ordering surface.
- There is still no live runtime hypothesis after the same-family late mana residual probe. Do not reopen a shared same-family `ManaTempo` scoring spend across late black `outer_edge` and the newly exposed white Pro mana siblings; two seams are exact ties and the third is a different residual split.
- There is still no live runtime hypothesis after the widened all-blocker recurrence trace. Do not reopen a shared spend across `outer_edge_mana_rows`, `alternating_mana_rows`, and `forward_bridge_mana_rows`; once the full blocker set was traced together, Pro/Normal/Fast regressions all stayed singleton-heavy and the earlier isolated repeated pairs stopped dominating.
- There is still no live runtime hypothesis after the blocker hotspot fingerprint probe. Do not reopen a shared exact/selector hotspot spend across `outer_edge_mana_rows`, `alternating_mana_rows`, and the repeated white `forward_bridge_mana_rows` seam; the common exact-cost inflation is only frontier running against the shipping search-only baseline, while the actual move drift still splits across two `pre_accept` seams and one head-accept seam.
- There is still no live runtime hypothesis after the alternate-seed all-blocker trace. Do not spend on the new alternate-seed singleton pairs; the current blocker set stayed mixed across Pro, Normal, and Fast, and no move pair repeated.
- There is still no live runtime hypothesis after the focused alternate-seed Fast-blocker trace. Do not reopen `alternating_mana_rows` or `forward_bridge_mana_rows` from the alt-v2 singleton pairs; Fast had only one regression and five improvements on that seed.
- There is still no live runtime hypothesis after the Normal-only `outer_edge_mana_rows` alt-v2 trace. Do not reopen either repeated black pair as a standalone spend while the same trace still includes five singleton seams across multiple stages and colors.
- There is still no live runtime hypothesis after the alt-v3 all-blocker trace. Do not reopen the Normal black `l2,3;l3,3` vs `l2,3;l3,2` pair as a standalone spend while Pro/Fast stayed singleton-only and the same run showed nearly balanced regressions and improvements.
- There is still no live runtime hypothesis after the alt-v3 Normal hotspot probe. Do not spend on root-pool, shortlist-width, or exact-cost controls for `l2,3;l3,3` vs `l2,3;l3,2`; the pair has the same child pool on both profiles and only reproduces the known frontier-extra-work pattern.
- Use the env-driven hotspot hook for the next one-off board inspection instead of adding temporary probe cases to source.
- There is still no live runtime hypothesis after the alt-v4 all-blocker trace. Do not reopen the repeated Pro black `l2,7;l3,8` pair or the repeated Normal `l2,7;l1,6` recovery pair as standalone spends while the same run remains balanced/mixed and Fast has no repeated move pair.
- Use `SMART_PRO_RELIABILITY_DUEL_FILTER` on `smart_automove_pro_reliability_duel_trace_probe` when only one duel bucket needs recurrence tracing.
- There is still no live runtime hypothesis after the filtered alt-v5 Fast trace. Do not reopen its singleton white safe-progress/mana-ordering pairs or black spirit/setup pair as standalone spends; the bucket had more improvements than regressions and no repeated move pair.
- There is still no live runtime hypothesis after the filtered alt-v5 Normal trace. Do not reopen its singleton early black or white `outer_edge_mana_rows` pairs as standalone spends; the bucket had more improvements than regressions and no repeated move pair.
- The refreshed sampled gate confirms the same blocker rows after the focused alt-v5 no-gos. Do not spend runtime code until a new focused probe produces a repeated mechanism below Normal `outer_edge_mana_rows` or Fast `alternating_mana_rows` / `forward_bridge_mana_rows`.
- There is still no live runtime hypothesis after the focused Fast nonwin trace. Do not reopen a Fast nonwin line from this seed unless a later trace repeats one of the same move pairs; this run split across five already-known singleton surfaces.
- There is still no live runtime hypothesis after the focused Normal nonwin trace. Do not reopen either singleton `outer_edge_mana_rows` nonwin as a standalone spend; the two first-diff surfaces are different and both games were losses for shipping as well.
- There is still no live runtime hypothesis after the direct white seam-structure probe. Do not reopen a shared white `forward_bridge_mana_rows` spend; the repeated head-accept seam, the `l9,6;l8,7` safe-progress seam, and the `l9,7;l8,6` fallback seam diverge below the move list.
- There is still no live runtime hypothesis after the direct black seam-structure probe. Do not reopen a shared black late-mana spend across `outer_edge_mana_rows` and `alternating_mana_rows`; `outer_edge` is an `ApprovedReplyRiskGuard` same-family `ManaTempo` drift, while `alternating` is an `ApprovedFamilyCompetition` jump from shortlisted `ManaTempo` into outside-shortlist `DrainerSafetyRecovery`.
- There is still no live runtime hypothesis after isolated Fast variant tracing. Do not reopen an isolated `forward_bridge_mana_rows` white head blocker or an isolated `alternating_mana_rows` black mana-sibling blocker yet; each variant has one repeated pair, but both still keep five singleton seams.
- There is still no live runtime hypothesis after the widened Fast recurrence trace. Do not reopen a standalone white late head-accept blocker just because `l9,6;l7,4;l7,3` vs `l9,6;l7,6;l7,7` repeated twice; the rest of Fast `alternating_mana_rows` / `forward_bridge_mana_rows` residue is still singleton and mixed.
- There is still no live runtime hypothesis after the latest explicit-variant trace wave. Reopen only after a clean probe collapses the current `outer_edge_mana_rows` and Fast `forward_bridge_mana_rows` / `corner_chain_mana_rows` residue to one shared mechanism; the current split spans early white, late black, `engine_post_search`, and `engine_disabled` surfaces, and isolated Fast `classic` did not reproduce.
- There is still no live runtime hypothesis after the latest late-black probe wave. Do not spend a direct late-black same-family legacy-alignment override yet; the remaining Normal seam is clean, but the sampled Fast residue is still mixed across black/white and across `engine_post_search` plus head-accepted surfaces.
- Do not reopen the direct white turn-three mana-only legacy-progress override on the positive-safety `window=0/deny=0` surface; it fixed the clean outer-edge white board, reduced the explicit outer-edge Normal trace from `2` nonwins to `1`, and still failed the sampled gate unchanged overall.
- For `black_recovery_branch`, do not retry broad static exact or reply-floor-only static exact. Reopen only with a new mechanism that improves direct multi-variant duel strength, not just the local board.
- For black progress-vs-setup residue, do not retry material/cooldown-only scoring, final-selector-only patches, or the scoped material-plus-higher-rank advisor exception. Reopen only with a mechanism that improves sampled multi-variant duel strength, not just the local residue.
- For white search-order residue, do not retry wrapper config mirroring, broad `ProV1` reroutes, root-rank gates, or simple shortlist widening. Reopen only with a mechanism that separates unresolved siblings from the retained vulnerable guard below the current shortlist/reply-risk surface.
- For the current late-ply sampled residue, do not promote copied `first_diff_ply` boards into retained until the clean retained replay reproduces the same final shipping-selected root.
- Do not reopen the sampled-pass late white head-block plus black weak-window mana-lane override package; it overfit sampled late-ply seams and collapsed in all-variant confirm.
- Do not reopen a broad late black shipping-fallback expansion. The direct weak-window extension fixed the traced black sampled boards and still rotated Fast losses instead of promoting the sampled gate.
- Do not spend runtime code on the mixed `outer_edge_mana_rows` plus Fast `forward_bridge_mana_rows` / `corner_chain_mana_rows` residue until a later clean probe shows one shared mechanism. The latest explicit-variant trace split it across multiple stages and turn families, and standalone Fast `classic` did not reproduce.
- Do not reopen a standalone white late head-accept blocker for `l9,6;l7,4;l7,3` vs `l9,6;l7,6;l7,7`; both broad and exact-shape head rejects can pass sampled but fail all-variant confirm at Pro `0.6667`, Normal `0.7292`, Fast `0.6667`.
- Do not reopen the variant-scoped `ForwardBridgeManaRows` version of that white same-setup head reject. Scoping it to the blocker variant still passed sampled only and failed all-variant confirm at Pro `0.6667`, Normal `0.7292`, Fast `0.6667`.
- Do not reopen a black turn-four weak-window search-only fallback from quiet recovery to windowed mana unless a new trace shows it touches the sampled blockers. The direct runtime fallback was inert on the current sampled gate.
- Do not reopen broad ProV2 profile toggles without a new trace tying the toggle to live blockers. Head rerank, spirit-family disable, mid-turn tactical guard disable, expansion-cap-only widening, and low-budget guard disable all passed early gates and failed sampled reliability.
- Do not reopen the `ForwardBridgeManaRows`-only low-budget guard disable. It can repair the sampled Fast forward-bridge row but still collapses in all-variant confirm with broad Pro/Normal/Fast strength loss.
- Do not reopen an isolated `alternating_mana_rows` black mana-sibling blocker for `l2,7;l1,6` vs `l2,7;l1,8` yet; it repeats locally, but the rest of `alternating_mana_rows` still stays singleton and mixed.
- Do not extend the retained black bridge or engine-disabled Fast controls into the singleton `alternating_mana_rows` seam `l0,10;l0,9` vs shipping `l4,0;l5,0;mb`; shared `window=0/deny=0` and singleton `ManaTempo` shortlist shape are not enough when `BLACK_BRIDGE_NONWIN_DUEL_FAST` only matches after collapsing an accepted head to engine-disabled mana and `BLACK_ENGINE_DISABLED_DUEL_FAST` is a dense-shortlist `SpiritImpact` preserved-representative board.
- Do not reopen a shared head-accept patch across Normal `outer_edge_mana_rows` and Fast `forward_bridge_mana_rows`; the widened replays split `outer_edge` across rejected-head, accepted-head, and `search_only_forced_prepass` surfaces, while `forward_bridge` stays mixed across one repeated white accepted-head pair plus separate safe-progress and black setup drifts.
- Do not extend retained `primary_white_harvest_loss_c_ply24` into the `outer_edge_mana_rows` white forced-prepass seam; the exact move overlap only survives at `pre_accept`, and the copied live board does not replay the traced shipping final on a clean direct probe.
- Do not spend on the singleton early black `outer_edge` seams `l1,4;l2,4` vs `l0,5;l1,6` or `l1,4;l2,5` vs `l1,4;l1,6;l2,7` until they are recovered on a clean direct probe. The copied board states collapse to shared shipping behavior or to a different shared final, so they are not stable local seams.
- Do not spend on the recovered Fast `alternating` late-fast recovery singleton `l2,5;l0,5;l1,6` vs `l2,5;l4,7;l3,8` until it reproduces on a clean direct probe. The copied board collapses to shared engine-disabled `l2,5;l4,5;l3,4`, so it is not a stable local seam.
- Do not spend on the copied Fast `alternating` singleton pair `l8,5;l10,5;l9,4` vs `l8,5;l6,3;l7,2` or `l2,4;l1,5` vs `l2,4;l1,3` until they reproduce on a clean direct probe. The copied boards collapse to shared `l8,5;l6,3;l7,3` and shared `l2,4;l1,5`, respectively.
- Do not spend on the copied Fast `forward_bridge` white setup singleton `l9,5;l9,6` vs shipping `l7,3;l6,2` until it reproduces on a clean direct probe. The copied board collapses to shared engine-disabled `l7,3;l6,2`, so it is not a stable local seam or a retained-extension anchor.
- Do not spend on the copied Fast `forward_bridge` black post-search seam `l0,6;l1,6` vs shipping `l2,3;l3,4` until it reproduces on a clean direct probe. The copied board collapses to shared engine-disabled `l2,3;l3,4`, so it is not a stable local seam or a retained-extension anchor.
- Do not spend on the copied Fast `forward_bridge` black spirit/setup seam `l0,3;l1,3` vs shipping `l1,5;l3,4;l3,3` until it reproduces on a clean direct probe. The copied board collapses to shared engine-disabled `l0,3;l1,3`, so it is not a stable local seam or a retained-extension anchor.
- Do not reopen a shared black late-mana patch for `outer_edge` plus `alternating`; the former is a shortlisted same-family `ManaTempo` drift and the latter is an outside-shortlist `DrainerSafetyRecovery` family-competition approval.
- Do not reopen a shared Pro-only black spend across `l2,7;l1,6` vs `l2,7;l1,8` and `l1,6;l2,7` vs `l1,5;l2,3;l1,2`; they only match on stage/context. The first seam is outside-shortlist `DrainerSafetyRecovery` via `ApprovedFamilyCompetition`, and the second is shortlisted `SafeSupermanaProgress` vs a preserved `SpiritImpact` representative under `ApprovedReplyRiskGuard`.
- Do not reopen a shared black safe-progress-vs-spirit spend across `l7,1;l9,3` vs `l1,5;l2,7;l1,8` and Pro `l1,6;l2,7` vs `l1,5;l2,3;l1,2`; they only match at the approved family. The old residue keeps multiple `SpiritImpact` roots inside the candidate set and even has a stronger full-pool own-setup challenger than shipping, while the Pro setup-lane seam filters the entire `SpiritImpact` family out before shortlist and only preserves the shipped rank-2 root.
- Do not reopen a shared cross-color safe-progress-vs-spirit spend across white `l9,6;l8,7` vs `l9,6;l7,7;l8,8` and Pro black `l1,6;l2,7` vs `l1,5;l2,3;l1,2`; they only match at the approved family. The white seam keeps shipping spirit roots candidate-live and shortlist-live, while the black seam filters the entire `SpiritImpact` family out before shortlist and only preserves the shipped root outside the candidate set.
- Do not extend the retained white `l9,6;l8,7` post-search duel board into the `forward_bridge` safe-progress seam. The retained board is a quiet `ManaTempo` `window=0/deny=0` surface with no shortlist-live spirit competition, while `forward_bridge` is a `SafeSupermanaProgress` vs shortlist-live `SpiritImpact` seam on `window=1/deny=1`.
- Do not extend the retained white early engine-disabled/search-only package into `forward_bridge`. Those retained boards are still `ManaTempo` `ApprovedReplyRiskGuard` seams, with shipping either outside shortlist or outside the spirit family entirely, while `forward_bridge` is a `SafeSupermanaProgress` vs shortlist-live `SpiritImpact` seam.
- Do not reopen a shared white `forward_bridge` patch; the repeated head-accept seam, the safe-progress approval seam, and the score-window fallback seam are different mechanisms.
- Do not reopen the direct white turn-three mana-only legacy-progress override; it only removed the clean white half of `outer_edge_mana_rows` and left the sampled gate blocked by late black `outer_edge` plus Fast `alternating_mana_rows` and `forward_bridge_mana_rows`.
- Do not spend a direct late-black same-family legacy-alignment override on `l2,6;l3,7` vs `l1,6;l1,5` yet; the probe shows a real local advisor seam, but it is still only one remaining Normal board while Fast `alternating_mana_rows` and `forward_bridge_mana_rows` stay mixed.
- This iteration killed two temporary active-scout candidates. A black turn-two action+mana shipping fallback repaired the known active Pro `outer_edge` regression enough to reach Pro `4-2`, but the remaining Pro losses were flat losses where shipping also lost. Adding scoped rejected-head recovery on the two white flat-loss shapes improved active Pro and Normal to `5-1` each, but collapsed active Fast to `1-5`; the source was discarded.
- The useful signal is narrow: the black turn-two repair and scoped white head recovery can move active Pro, but neither creates the winning policy needed for active Fast. Do not spend runtime code on these context gates unless a later candidate also explains Fast `outer_edge_mana_rows` and `alternating_mana_rows` without rotating Pro/Normal.
- This iteration also killed narrower variant-scoped versions of that family. Black turn-two alone now measures active `4-2 / 5-1 / 2-4`; adding only an outer-edge white head or only an alternating white head was behaviorally identical on the active dashboard. Source was discarded.
- This iteration refreshed active-blocker decision records before another candidate. Guarded nonwins stayed mixed: active Pro logged singleton flat/regression surfaces across `outer_edge_mana_rows`, `alternating_mana_rows`, and `forward_bridge_mana_rows`; active Normal logged four one-off white regressions; active Fast concentrated in `frontier_execute` but split across `ManaTempo`, `SpiritImpact`, and `ImmediateScore` heads plus advisor-approved, legacy, and preserved baseline statuses.
- This iteration killed two test-only structural scouts and discarded source. A root-level shipping/utility consensus that only overrode `frontier_execute` when the shipping root was candidate-live and reply-safe failed active Fast at `2-4`, with `alternating_mana_rows` `0-2`. A broad no-post-search-head selector preserved guarded fallbacks but returned the pre-accept root on `frontier_execute`; it stayed non-promotable at active Fast `3-3` with every active variant split.
- This iteration killed two more head/representative utility scouts and discarded source. A selective reply-risk head-accept guard that rejected only reply-risk-dominated non-concrete heads still failed active Fast at `3-3`; its candidate decision record left three different nonwins: early-white `outer_edge` preserved-spirit miss, late-white `forward_bridge` same-family head drift, and black `alternating` flat loss. A combined spirit-stability scout that also tried to promote safe quiet `SpiritImpact` representatives over non-concrete `ManaTempo` roots stayed active Fast `3-3`, even after loosening rank/score/reply tolerances.
- This iteration killed score-path-pressure utility variants and discarded source. Adding own-path/opponent-path pressure as a primary turn-engine axis improved raw sampled Pro directionally but failed guarded active blockers (`2-4` as a primary axis, `3-3 / 4-2` when reduced to a tiebreaker), so the residue is not solved by another shallow path-pressure utility reorder.
- This iteration killed regular-mana-window ProV3 scoring scouts and discarded source. A guarded-preserving regular-mana-window scorer reached active blockers `5-1 / 4-2 / 4-2`; adding the no-late-black ablation lifted active Normal to `6-0` but collapsed active Fast to `2-4`. Attribution showed the no-late piece helps and hurts the same high-level black turn-two action+mana shape depending on the evolved board, so it is not a promotable context gate.
- This iteration killed worst-reply portfolio scouts and discarded source. A safe-regular/no-late/guarded portfolio cleared active Pro at `6-0` but rotated to active Normal `5-1` and Fast `3-3` with Fast `forward_bridge_mana_rows` over the cost ceiling. Adding retained shipping dropped active Pro to `4-2`; adding raw ProV2 still failed active Normal at `5-1` and exceeded the `700ms` average ceiling. Do not retry portfolio routing unless the selector evidence is one-sided before the portfolio layer.
- This iteration killed early-black-advisor scouts and discarded source. The first scout accidentally used the raw frontier path and was invalid as guarded evidence; the corrected guarded early-black-advisor candidate measured active blockers at `2-4 / 5-1 / 2-4`, with Pro `outer_edge_mana_rows` still `0-2` and Fast still `2-4`. Retained guarded on the same active panel was `3-3 / 5-1 / 2-4`, confirming the live active blocker is not solved by simply letting the root advisor participate on early black action+mana.
- This iteration killed selected-followup, budget, and SpiritImpact-anchor structural scouts and discarded source. Disabling selected follow-up projection under the guarded wrapper was the best active scout at `3-3 / 5-1 / 4-2`: it fixed Fast `outer_edge_mana_rows` to `2-0` and improved Pro `outer_edge_mana_rows` to `1-1`, but left every Pro variant at `1-1` and left Fast `alternating_mana_rows` plus `forward_bridge_mana_rows` at `1-1`. Expansion `224` measured `2-4 / 6-0 / 4-2`; no-low-budget measured `3-3 / 5-1 / 2-4`; expansion+no-selected and no-low-budget+no-selected both collapsed to the same `3-3 / 5-1 / 4-2` shape as no-selected alone. A gated SpiritImpact-anchor guard moved Pro `outer_edge` to `1-1` and kept Pro `forward_bridge` at `2-0`, but broke Pro `alternating` to `0-2` and left Fast `2-4`.
- The useful signal is now specific: selected-followup projection is implicated in the active Fast `outer_edge` loss, and retained decision records show live or preserved `SpiritImpact` roots being displaced by `ManaTempo`/safe-progress approval or by safe-progress head acceptance. However, broad projection disable and direct SpiritImpact anchoring both rotate losses. Do not spend on another direct flag toggle, rank/risk margin, or family anchor; the next useful spend must integrate head acceptance, reply-risk approval, and preserved-root utility into one policy that can improve `outer_edge_mana_rows` without losing Pro `alternating_mana_rows` or `forward_bridge_mana_rows`.
- This iteration killed the first unified ProV3 root-policy wave and discarded source. Broad direct unified selection failed active blockers at `1-5 / 3-3 / 2-4`; a conservative post-advisor override failed at `2-4 / 5-1 / 2-4`; full-selection reply-risk approval failed at `3-3 / 4-2 / 3-3`; no-selected followup refreshed at `3-3 / 6-0 / 4-2`; full-selection plus no-selected failed at `3-3 / 5-1 / 2-4`.
- This iteration also killed local portfolio rerankers. A reply-floor portfolio over guarded, no-selected, and full-selection reached active `5-1 / 5-1 / 4-2` under the `700ms` ceiling, but it collapsed Fast `alternating_mana_rows` to `0-2`. A context-gated portfolio reached `5-1 / 5-1 / 5-1` but still left split weak rows and exceeded the CPU ceiling (`~992ms` Normal average), so it is not promotable.
- The useful signal is that selected-followup-off and full-selection reply-risk approval are complementary in isolated rows, but attribution is mixed inside the same variant and high-level context. Do not retry direct unified root selection, conservative utility overrides, full-selection reply-risk approval, no-selected composites, or active-context portfolio gates without new cross-panel evidence.
- This iteration added the policy-matrix harness requested by the structural reset path. `smart_automove_pro_policy_matrix_probe` and `./scripts/run-automove-experiment.sh pro-policy-matrix <baseline,candidate...>` now compare multiple registered sweep policies on identical sampled and active-blocker openings, with candidate-vs-baseline outcomes plus first-divergence branch, context, and move-pair aggregates.
- The policy matrix killed the obvious selected-followup/full-selection composite direction as a promotion source. A small active Fast run confirmed both selected-followup-off and full-scored reply guard can beat guarded on the first active `outer_edge_mana_rows` openings (`2/2` each), but the sampled Pro matrix immediately reversed the signal: selected-followup-off was `1` better / `4` worse and full-scored reply guard was `1` better / `7` worse over the first `12` same-seed sampled Pro games. This is active-row complementarity with sampled-Pro regression pressure, not a promotable mechanism.
- This iteration enriched the policy matrix with optional decision probes. `SMART_PRO_POLICY_MATRIX_INCLUDE_DECISION_PROBE=true` now emits guarded root rank/family/score, selected/advisor status, and full-vs-no-selected-followup `TurnEngineUtility` for printed divergences. Keep it to narrow runs because it reruns root scoring for each printed record.
- The decision-probed matrix killed the plausible primary-axis selected-followup guard before retaining runtime code. On the inspected active Fast `outer_edge_mana_rows` win and sampled Pro `center_spoke_mana_rows` losses, full and no-followup utilities were identical for both divergent roots, with `full_vs_no_followup_primary=Equal`; the no-selected policy was changing fallback/root-selection behavior, not exposing a selected-followup utility-axis feature.
- This iteration killed action+mana no-selected hybrids and discarded source. A broad existing-candidate policy matrix reconfirmed no live challenger: no-selected was strong on active Fast (`3` better / `0` worse over the first four active Fast games) but still lost sampled Pro (`0` better / `2` worse), while raw, no-late, and full-scored reply guard stayed mixed. A temporary action+mana no-selected scout avoided the early sampled Pro regressions and improved active Pro/Fast, but was mixed on sampled Fast; a black turn-4 zero-move family/window hybrid removed the sampled Fast regression and kept active Fast positive, then rotated sampled Normal `corner_chain_mana_rows` into `baseline_better` (`0` better / `1` worse, candidate nonwins `3` vs guarded `2`). Do not retain these gates.
- This iteration killed safe-utility and no-action projection scouts and discarded source. A loose vulnerable-root safety override that accepted utility tolerance or temporary-risk recovery immediately regressed sampled Pro (`0` better / `1` worse, nonwins `3` vs guarded `2`). Tightening to `utility >= guarded` and scoping to no-action mana turns produced a clean policy-matrix scout (`sampled Pro 1/0, sampled Normal/Fast 0/0, active Pro 0/0, active Normal 1/0, active Fast 0/0` versus guarded), but it was too small to promote: the structural scout still failed sampled Pro at `9-3`, with `alternating_mana_rows` and `inner_wedge_mana_rows` split rows. A no-action-only selected-followup projection gate also failed sampled Fast (`0` better / `1` worse) on `split_flank_mana_rows` white `l8,6;l7,5` vs `l8,5;l8,4`.
- This iteration killed and discarded a ProV3 risk/safe/opening composite after it produced the first sampled-Pro pass in this wave. The best context composite combined a black weak-window utility gate, the prior no-action safe-utility gate, an exact early-white raw opening gate, and a `ForwardBridgeManaRows`-only white utility rerank; it reached sampled Pro `11-1` (`0.9167`, confidence `0.9968`, avg `513.51ms`) but collapsed sampled Normal to `7-5` (`0.5833`, confidence `0.6128`, avg `654.59ms`) with `forward_bridge_mana_rows` at `0-2` and per-turn cost near the ceiling. Source was removed.
- The useful signal is a hard cross-budget conflict: Pro-only early-white/raw and `forward_bridge` utility routing can clear sampled Pro, but the same first moves are not opponent-mode aware and rotate sampled Normal badly. Do not spend runtime code on context gates that only prove Pro-vs-Pro strength; the next candidate must show sampled Normal/Fast stability in policy-matrix or dashboard form before structural scout.
- This iteration refreshed the no-low-budget direction in the policy matrix and discarded the temporary scoped candidate. Broad no-low-budget was cross-budget useful but not promotable versus guarded on sampled openings: Pro `2` better / `2` worse, Normal `3` / `0`, Fast `4` / `1`. A broader context screen immediately rotated Pro on black mana-only `center_spoke_mana_rows` and `split_flank_mana_rows`; a positive-list version was only a tiny sampled win and stayed inert on active Pro/Fast (`0/0`, all Pro games still nonwins; Fast `0/0`). Do not reopen no-low-budget by variant/context gating without a deeper decision-record mechanism.
- This iteration killed three test-only structural probes and discarded source. A post-guard `NormalRootSafetySnapshot` reply-gain override was behaviorally inert on sampled Pro (`7-5`, same as current guarded dashboard, no override branch recorded). A portfolio rollout selector was non-promotable on cost: even early-only, 24-ply rollouts from `frontier_execute` decisions did not complete the sampled Pro quick check after several minutes. A sampled policy-winner context selector lifted sampled Pro to `11-1` (`0.9167`, confidence `0.9968`, avg `144.31ms`) but failed sampled Normal at `8-4` and sampled Fast at `6-6`.
- The useful signal is now negative: sampled Pro policy-winner coverage is not enough. The sampled Normal/Fast winners require different contexts and policy-entry timing than sampled Pro, and the recovered `corner_chain_mana_rows` white opening root did not make the expanded selector pass active Fast. Do not spend on another Pro-only policy-winner selector, full rollout selector, post-guard normal-safety root switch, or existing-policy portfolio selector.
- There is still no live challenger. The next useful spend is a cross-budget-stable mechanism for the remaining split rows, especially active/sampled `outer_edge_mana_rows` timing conflict and Fast blocker stability, not another Pro-only opening/raw gate, selected-followup projection guard, selector wrapper, portfolio, or vulnerable-root safety override. A candidate must move sampled Pro while staying at least neutral on sampled Normal/Fast before broader promotion gates.
- This iteration killed and discarded a temporary `frontier_pro_v3_early_safe_spirit_opening` scout. The line combined the retained alternating-white edge component, a safe-looking early `SpiritImpact` opening admission, and a same-vulnerability weak-window path-alignment override. After fixing a reachability bug in the weak-window branch, the scout improved sampled Pro from `8-4` to `10-2`; after adding the alternating component it passed sampled Pro at `11-1` (`0.9167`, confidence `0.9968`, avg `~207ms`) but failed sampled Normal at `7-5` and sampled Fast at `6-6`.
- The useful signal is cross-budget negative, not promotion-ready. Matrix against guarded showed sampled Normal was only a one-for-one swap: the scout fixed one white `alternating_mana_rows` opening but regressed one white `outer_edge_mana_rows` early-`SpiritImpact` opening; sampled Fast was unchanged at `6-6`. Forced-root oracles showed winning roots exist on the sampled Pro inner-wedge white opener (`11/16`), inner-wedge black turn-four board (`13/16`), and forward-bridge white opener (`6/16`), so the blocker is not root availability. It is selecting those roots without rotating Normal/Fast.
- Do not retry the Pro-only early safe `SpiritImpact` opening composite, the same-vulnerability weak-window path-alignment override, or the alternating-plus-opening package as a direct challenger. Reopen only if a new feature separates the Pro wins from the Normal `outer_edge_mana_rows` regression and Fast no-policy rows without opponent-profile or board-fingerprint routing.
- This iteration stayed structural and killed a temporary action+mana recovery/tempo floor. The refreshed retained decision-record aggregate was still split: sampled Pro had no nonwins; sampled Normal had one flat black `outer_edge_mana_rows` `ManaTempo` post-search loss where shipping also lost; sampled Fast split between one black `alternating_mana_rows` flat loss and the known white `forward_bridge_mana_rows` head-accepted `SpiritImpact` regression. Active blockers also split across Pro flat `DrainerSafetyRecovery`, Normal white `forward_bridge` `SpiritImpact` regression, and Fast `outer_edge` / `alternating` / `forward_bridge` rows with different families and branches. A test-only floor that preferred live `ManaTempo` / `DrainerSafetyRecovery` / fallback `SpiritImpact` roots on action+mana post-search turns was too broad and failed sampled Pro immediately at `7-5`, with `22` overrides and `inner_wedge_mana_rows` at `0-2`; source was discarded.
- Do not retry a generic live non-spirit recovery/tempo floor as the next challenger. The remaining decision records need a feature that explains one-sided policy-entry timing across panels; rank/utility support for a live `ManaTempo` or `DrainerSafetyRecovery` alternative is not enough and actively damages sampled Pro.
- This iteration refreshed sampled Normal/Fast `pro-policy-winner` evidence and killed a temporary late action+mana shipping selector. The winner refresh was mixed: sampled Normal needed shipping-control on later `inner_wedge`, `corner_chain`, and black `forward_bridge` action+mana boards, full-scored reply guard on one early white `forward_bridge`, and the alternating-white component on one early `alternating` board; sampled Fast needed shipping-control on several later black/white action+mana boards, white-opening utility on one corner-chain opener, and raw ProV2 on one late-black fallback. A simple test-only selector that routed later action+mana `frontier_execute` / score-window turns to shipping fired `183` sampled-Pro turns and failed immediately at `8-4`, with `inner_wedge_mana_rows` at `0-2`; source was discarded.
- Do not retry broad late action+mana shipping-control routing as the next selector. The winning labels remain singleton-heavy and cross-policy; shipping is a useful oracle on several sampled Normal/Fast losses, but the direct route erases sampled Pro strength before Normal/Fast can matter.
- This iteration killed a narrower black pressure shipping-control scout and discarded source. The candidate kept the existing white opening policy components, then routed only black turns `2..=6` with mana live, `window/deny <= 1`, no safe mana-progress context, and negative drainer safety or drainer-attack pressure to shipping-control. It still failed sampled Pro immediately at `8-4`, fired `22` black pressure-shipping turns, and split losses across `center_spoke_mana_rows`, `inner_wedge_mana_rows`, and `forward_bridge_mana_rows`.
- The policy-winner harness now has `SMART_PRO_POLICY_WINNER_CANDIDATE_TRACE_LIMIT`, and summaries report `candidate_trace_limit_hit`. Use that with panel/duel filters for broad portfolio refreshes; a full sampled expanded-portfolio run was stopped after roughly eleven minutes while still only producing fragmented singleton policy wins.
- Do not retry narrow black pressure shipping-control routing from the sampled Fast policy-winner records. The narrower gate was still too broad for sampled Pro, so the missing feature is not simply black pressure/action+mana shipping admission.
- This iteration killed a standalone same-vulnerability path-alignment scout and discarded source. The narrow black turn-four mana-only `window=1/deny=1/drainer_safety<0` path-step override fired once and only moved sampled Pro to `8-4`; sampled Normal stayed `7-5` and sampled Fast stayed `6-6`. A follow-up calm white turn-five route-score shipping admission was inert and only increased average candidate time.
- Do not retry same-vulnerability path alignment, or a white turn-five quiet shipping-root score gate, as standalone candidates. The path-step signal is real for one inner-wedge Pro loss, but it is too small and too Pro-only to promote; the next useful spend still needs a cross-budget-stable feature that moves sampled Pro without leaving Normal/Fast at the same weak rows.
- This iteration refreshed sampled retained decision records and killed a late-black recovery-bait scout without retaining source. Sampled Pro had no nonwins; sampled Normal had one black `outer_edge_mana_rows` flat loss where both frontier and shipping lost; sampled Fast had one black `alternating_mana_rows` flat loss plus the known white `forward_bridge_mana_rows` accepted-head regression. Forced-root oracle showed the Normal flat board has `0/16` winning roots at the first-diff horizon, while the Fast alternating board has `9/16` winning roots including rank-0 `DrainerSafetyRecovery`. A narrow late-black no-action recovery-over-huge-score `ManaTempo` scout did not fire on the sampled dashboard and failed at Pro `7-5`, Normal `7-5`, Fast `6-6`.
- Do not retry late black no-action recovery-bait selection as a standalone candidate. The local forced-root signal is real on one sampled Fast board, but the refreshed defer scout still did not reproduce as a dashboard-level selector and failed the sampled structural dashboard at `7-5 / 7-5 / 6-6`.
- This iteration killed and discarded a sampled shipping/no-low-budget repair scout. The current clean sampled Pro baseline is still guarded `7-5`, with nonwins on inner-wedge white quiet mana, inner-wedge black weak-window mana, two forward-bridge white/black shipping-control seams, and alternating white opening. A temporary selector that kept the alternating-white component and routed only the sampled shipping-control seams improved sampled Pro to `10-2` with `5` repaired turns but still missed inner-wedge white and one forward-bridge continuation. Broadening the forward-bridge quiet-white route fired more and added cost without improving `10-2`; adding a narrow no-low-budget early-white inner-wedge route fired once but also stayed `10-2`.
- The useful signal is policy-entry timing, not first-diff move identity. Full-policy `shipping_pro_search_control` wins the sampled forward-bridge Pro games, and full-policy `frontier_pro_v2_no_low_budget_guard` wins the inner-wedge white Pro game, but one-shot routing at the printed first divergence did not reproduce those wins. The raw inner-wedge white full-policy win is a confirmed budget conflict from the partial cross-budget run: it fixed Pro but regressed sampled Normal. Do not retry exact sampled shipping/no-low-budget gates, broadened forward-bridge quiet-mana routing, or raw early-white inner-wedge routing as standalone candidates.

## Active No-Go Notes

- Do not reopen archived profiles, archived seams, or archived stages.
- Do not treat retained `primary_pro` churn by itself as promotion evidence.
- Do not treat Classic retained fixtures as broad variant evidence.
- Do not spend canonical gates on a challenger that stays behaviorally inert at `target_changed=0 off_target_changed=0`.
- Do not treat “all live walls aligned” as enough if duel strength or CPU cost still fails.
- Do not add board-local shipping mirrors for residuals that lose under frontier reply-risk, selector, or residual-score metrics.
- Do not reopen the direct black engine-disabled shipping fallback on `l7,1;l9,3` vs `l1,5;l2,7;l1,8`; it fixed the local board and still failed confirm.
- Do not reopen the black shortlist-local, guarded-legacy, or direct `black_recovery_branch` fallback variants; they align the board locally and still fail retained reliability.
- Do not reopen `black_recovery_branch` with broad scoped static exact or reply-floor-only static exact; the former selected `l1,5;l2,6`, and the latter still failed sampled retained reliability.
- Do not reopen black progress-vs-setup with material/cooldown-only scoring; it reduces the local residual gap but leaves the selected root unchanged.
- Do not reopen black progress-vs-setup with final-selector-only changes; the material-dampened replay still approves safe progress at the advisor `ApprovedReplyRiskGuard` layer.
- Do not reopen black progress-vs-setup with the combined scoped material dampening plus higher-scoring setup-rank exception; it fixes the board locally and still fails sampled retained reliability.
- Do not broaden white turn-three no-action recovery from turn-start to `mons_moves_count == 1`; that line already failed retained reliability.
- Do not broaden white search-order fallback into raw `search-only + ProV1`, shipping own caps, root-rank checks, or wrapper config toggles; each has a documented retained-control failure or inert wrapper path.
- Do not retry black turn-two action+mana shipping fallback by itself. It removes the sharp active Pro `outer_edge` regression but stops at `4-2` and leaves only flat losses.
- Do not retry broad scoped white rejected-head acceptance on `window=1/deny=1` turn-five or `window=0/deny=0` turn-three white shapes. It can lift active Pro/Normal to `5-1`, but it rotates active Fast down to `1-5`.
- Do not retry variant-scoped outer-edge or alternating-only white head variants on top of the black turn-two fallback. They were inert relative to black turn-two alone and still failed active Fast at `2-4`.
- Do not retry root-level shipping/utility consensus or broad no-post-search-head selection as the next challenger. The former worsened active Fast to `2-4`; the latter was only equal to guarded's active Fast `3-3` and did not create a promotable variant row.
- Do not retry selective reply-risk head rejection or quiet spirit-representative promotion as the next challenger. Both stayed at active Fast `3-3` and left the same split variant rows rather than producing a shared utility repair.
- Do not retry score-path-pressure utility axes, guarded regular-mana-window scoring packages, no-late-plus-regular composites, or worst-reply portfolios as the next challenger. Each moved one active leg while rotating another, and the portfolio variants also pushed cost toward or above the promotion ceiling.
- Do not retry guarded preaccept utility-floor head vetoes, even when the fallback is legacy-selected or legacy-full-pool-selected and non-vulnerable. The active dashboard measured only `3-3 / 5-1 / 2-4`, left `outer_edge_mana_rows` at `0-2` in Pro/Fast, and added cost.
- Do not retry early-black-advisor presearch, broad selected-followup projection disable, expansion/no-low-budget combinations, or direct SpiritImpact-anchor guards as the next challenger. They each repaired one active row while rotating another; the best shape was still only `3-3 / 5-1 / 4-2` active blockers.
- Do not retry unified root-policy scouts, full-selection reply-risk approval, no-selected/full-selection composites, reply-floor portfolios, or active-context portfolio gates as the next challenger. The best active shape was `5-1 / 5-1 / 5-1`, still non-promotable and over the CPU ceiling.
- Do not compose selected-followup-off with full-scored reply-risk approval just because both help the first active Fast `outer_edge_mana_rows` policy-matrix openings. The same policies both lose badly to guarded on sampled Pro, so any future use needs a new utility feature that explains and filters out the sampled regressions first.
- Do not spend runtime code on a primary-axis guard for selected-followup projection. The decision-probed divergences showed no utility difference between full and no-followup evaluation, so that guard is inert on the current selected-followup-off evidence.
- Do not retry action+mana selected-followup gates or black turn-4 zero-move no-selected/no-late hybrids. They can make sampled Pro or active Fast look promising, but the same surface rotates sampled Fast or sampled Normal depending on whether the black zero-move case routes through no-selected or no-late.
- Do not retry loose, strict, or no-action-only vulnerable-root safe-utility overrides. The no-action version was one-sided in small policy-matrix panels but still failed the sampled shipping-strength scout at Pro `9-3`, so it is not a promotion mechanism by itself.
- Do not retry no-action-only selected-followup projection gates. Even scoped to no-action mana turns, the projection gate rotated sampled Fast `split_flank_mana_rows` into `baseline_better`.
- Do not retry Pro-only early-white raw opening gates or `ForwardBridgeManaRows`-only utility reranks as promotion candidates. Their composite can pass sampled Pro at `11-1`, but sampled Normal falls to `7-5` with `forward_bridge_mana_rows` at `0-2`; the runtime has no opponent-profile signal to make that route safe only against Pro.
- Do not retry exact sampled-Pro opening gates plus root-live black shipping controls as the next challenger. The latest tight version reached sampled Pro `11-1`, but sampled Normal/Fast stayed `7-5 / 6-6` and active Pro stayed `3-3` because the new branches did not fire on `outer_edge_mana_rows`.
