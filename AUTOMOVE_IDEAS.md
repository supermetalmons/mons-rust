# Automove Ideas

This is the live decision board for automove work. Keep it short and decision-oriented. Use `HOW_TO_ITERATE_ON_AUTOMOVE.md` for workflow, `docs/automove-major-reset-plan.md` for the reset handoff, `docs/automove-knowledge.md` for durable rules, and `docs/automove-archive.md` for retired wave detail.

## Current State

- Public Pro routes through `frontier_pro_v2_guarded`.
- `shipping_pro_search` remains the retained search-only baseline.
- Retained profiles are only `shipping_pro_search` and `frontier_pro_v2_guarded`.
- The live experiment surface is Pro-only and multi-variant.
- The current mode is `structural-reset`.
- There is no live runtime hypothesis and no promotable challenger.
- Latest sampled/active Fast scouts found no source permission: sampled Fast was `no_candidate_route`; active Fast had repeated candidate axes, but every route remained singleton or fragmented by policy, branch, or first move, and ProV4 root-pool / guarded-delta rows were also fragmented.
- Latest ProV4 cross-budget static-eval consensus scout was not promotable: it preserved guarded fallbacks after an unsafe loose smoke, then fast-failed the sampled dashboard at Pro `6-6` and never appeared as a policy-corpus winner.
- Latest ProV4 turn-completion stability scout was not promotable: it preserved guarded fallbacks and used a same-turn continuation rollout, but failed sampled Pro at `7-5`; a looser cut improved sampled Fast to `11-1` while still missing Pro `8-4` and Normal `10-2`, with no policy-corpus winner evidence.
- Latest ProV4 two-turn resilience scout was not promotable: a self-opponent-self continuation probe failed the sampled dashboard at Pro `8-4`, Normal `8-4`, Fast `7-5`, with max average `211.65ms` and no reason to spend active panels.
- Latest ProV4 move-efficiency delta scout was not promotable: it preserved guarded fallbacks and used cached move-efficiency delta against guarded, but failed sampled Pro/Normal/Fast at `7-5 / 10-2 / 6-6`.
- Latest ProV4 scoring-context robustness scout was not promotable: it preserved guarded fallbacks and compared roots across exact-on/off plus local/no-local static scoring contexts, but sampled dashboard failed at Pro/Normal/Fast `6-6 / 8-4 / 8-4` with max average `198.50ms`.
- Latest ProV4 objective-latency scout was not promotable: it preserved guarded fallbacks and preferred roots that reached scoring, pickup, drainer pressure, or safe progress earlier, but sampled dashboard failed at Pro/Normal/Fast `6-6 / 8-4 / 8-4` with max average `196.21ms`.
- Latest ProV4 reply-floor/progress scout was not promotable: it preserved guarded fallbacks and used bounded worst-reply floor plus progress/setup value, but sampled dashboard failed at Pro/Normal/Fast `7-5 / 9-3 / 6-6` with max average `189.53ms`.
- Latest ProV4 policy-root-pool scout was not promotable: it ranked live guarded/raw/no-followup/full-scored/shipping policy outputs as roots, but the one-game sampled Fast smoke failed `0-2` and cost `597.78ms`, so it was killed before dashboard spend.
- Latest ProV4 bounded-followup/reply scout was not promotable: it kept selected-followup projection but narrowed reply-risk/opponent/reply breadth, then failed sampled Pro/Normal/Fast at `7-5 / 7-5 / 4-8` with max average `234.13ms`.
- Latest ProV4 root-breadth rebalance scout was not promotable: it preserved guarded fallbacks and widened root focus plus turn-engine seed breadth, but sampled dashboard failed before Fast/active spend at Pro `9-3` and Normal `8-4`, with Normal forward-bridge `0-2`.
- Latest ProV4 targeted exact-narrowing scout was not promotable: it preserved guarded fallbacks and enabled targeted exact-turn memo plus score-window candidate narrowing, but sampled dashboard failed before Fast/active spend at Pro `7-5` and Normal `7-5`, with Pro inner-wedge `0-2`.
- Latest ProV4 child-ordering shortlist scout was not promotable: it preserved guarded fallbacks and enabled child eval bundles plus two-stage child ordering, but sampled dashboard failed before Fast/active spend at Pro `8-4` and Normal `8-4`, with Normal alternating `0-2`.
- Latest ProV4 PVS/search-ordering scout was not promotable: it preserved guarded fallbacks and enabled PVS, killer ordering, history heuristic, and depth-preferred TT replacement, but sampled dashboard failed before Fast/active spend at Pro `6-6` and Normal `9-3`, with Pro inner-wedge `0-2`.
- Latest ProV4 depth-consensus scout was not promotable: it preserved guarded fallbacks and only overrode `frontier_execute` roots when a strategic root was stable across full and shallow depth checks, but sampled Pro failed immediately at `6-6`, with inner-wedge `0-2` and forward-bridge `1-3`.
- Latest ProV4 drainer-pressure scout was not promotable: score-aware forced drainer attack and minimax selection within forced attack roots both cleared tiny Fast smoke, but each fast-failed sampled Pro at `7-5`, with inner-wedge `0-2` and forward-bridge split `2-2`.
- Latest ProV4 root-aspiration scout was not promotable: it preserved guarded fallbacks and restored root aspiration windows for root search; the tiny Fast smoke did not meet the sweep pass threshold and cost `217.54ms`, then the sampled dashboard fast-failed Pro at `5-7` with `not_promising`, inner-wedge `0-2`, and `169.68ms` average.
- Latest ProV4 walk-threat safety scout was not promotable: it preserved guarded fallbacks and treated drainer walk threats as unsafe roots; the tiny Fast smoke did not meet the sweep pass threshold and cost `209.81ms`, then the sampled dashboard fast-failed Pro at `7-5` with `not_promising`, inner-wedge `0-2`, and `143.94ms` average.
- Latest ProV4 no-quiescence scout was not promotable: it preserved guarded fallbacks and disabled tactical quiescence leaf extension; the tiny Fast smoke did not meet the sweep pass threshold, then the sampled dashboard fast-failed Pro at `8-4` with `not_promising`, inner-wedge `0-2`, and `154.89ms` average.
- Latest ProV4 tactical-prepass scout was not promotable: it preserved guarded fallbacks and restored forced tactical prepass; the tiny Fast smoke did not meet the sweep pass threshold, then the sampled dashboard fast-failed Pro at `6-6` with `not_promising`, alternating `0-2`, and `121.98ms` average.
- Latest ProV4 no-futility scout was not promotable: it preserved guarded fallbacks and disabled futility pruning; the tiny Fast smoke did not meet the sweep pass threshold and cost `233.58ms`, then the sampled dashboard fast-failed Pro at `8-4` with `not_promising`, inner-wedge `0-2`, and `171.62ms` average.
- Latest ProV4 no-quiet-reductions scout was not promotable: it preserved guarded fallbacks and disabled quiet move reductions; the tiny Fast smoke failed at `1-1`, `duel_passes=false`, and `330.17ms`, then the sampled dashboard fast-failed Pro at `7-5` with `not_promising` and `340.12ms` average.
- Latest ProV4 allocation/tiebreak scout batch was not promotable: disabling selective extensions failed the tiny Fast smoke at `0-2`; disabling two-pass root allocation and deterministic interview tiebreak each reached `2-0` in the tiny Fast smoke but then fast-failed sampled Pro at `7-5` with inner-wedge `0-2`.
- Latest ProV4 no-event-ordering scout was not promotable: it preserved guarded fallbacks and disabled the event-ordering bonus; the tiny Fast smoke reached `2-0` but did not meet the sweep pass threshold, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2` and `139.02ms` average.
- Latest ProV4 no-node-uplift scout was not promotable: it preserved guarded fallbacks but reversed the final Pro node-budget uplift; sampled Pro improved only to `8-4` and still fast-failed with inner-wedge `0-2`, `not_promising`, and `145.31ms` average.
- Latest ProV4 no-attacker-proximity-scoring scout was not promotable: it preserved guarded fallbacks but reverted the final Pro attacker-proximity scoring weights to regular runtime scoring; the tiny sampled Fast smoke failed at `1-1`, `duel_passes=false`, and `203.43ms`, so no dashboard spend was earned.
- Latest ProV4 no-branch-uplift scout was not promotable: it preserved guarded fallbacks but reversed the final Pro root/node branch-limit uplift; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `6-6` with inner-wedge `0-2`, `not_promising`, and `125.08ms` average.
- Latest ProV4 no-opponent-mana-bonus-uplift scout was not promotable: it preserved guarded fallbacks but reverted the final Pro opponent-mana soft-priority bonus bump; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `140.38ms` average.
- Latest ProV4 no-reply-risk-guard scout was not promotable: it preserved guarded fallbacks but disabled the Pro root reply-risk guard; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `8-4` with split rows, `not_promising`, and `146.58ms` average.
- Latest ProV4 no-normal-root-safety scout was not promotable: it preserved guarded fallbacks but disabled normal-root-safety rerank and deep floor; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `139.63ms` average.
- Latest ProV4 no-root-mana-handoff-guard scout was not promotable: it preserved guarded fallbacks but disabled the root-level mana-handoff guard; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `140.72ms` average.
- Latest ProV4 no-strict-anti-help scout was not promotable: it preserved guarded fallbacks but disabled the strict root anti-help filter; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `141.16ms` average.
- Latest ProV4 no-hard-spirit-deploy scout was not promotable: it preserved guarded fallbacks but disabled the hard spirit-deploy interview preference; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `141.06ms` average.
- Latest ProV4 no-move-class-coverage scout was not promotable: it preserved guarded fallbacks but disabled root/child move-class coverage and strict tactical class coverage; the tiny sampled Fast smoke failed at `1-1`, `duel_passes=false`, `177.00ms`, so no dashboard spend was earned.
- Latest ProV4 no-potion-mix scout was not promotable: it preserved guarded fallbacks but disabled potion-action root mixing and potion progress compensation; the tiny sampled Fast smoke failed at `1-1`, `duel_passes=false`, `223.21ms`, so no dashboard spend was earned.
- Latest ProV4 no-interview-soft-priors scout was not promotable: it preserved guarded fallbacks but disabled all interview soft root priors; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `147.81ms` average.
- Latest ProV4 no-root-spirit-development-pref scout was not promotable: it preserved guarded fallbacks but disabled the root spirit-development preference; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `140.84ms` average.
- Latest ProV4 no-enhanced-drainer-vulnerability scout was not promotable: it preserved guarded fallbacks but disabled enhanced drainer vulnerability handling; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `137.20ms` average.
- Latest ProV4 no-backtrack-penalty scout was not promotable: it preserved guarded fallbacks but disabled the root-efficiency backtrack penalty; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `138.59ms` average.
- Latest ProV4 child-vulnerability-plausibility scout was not promotable: it preserved guarded fallbacks but enabled the child vulnerability plausibility screen; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `142.92ms` average.
- Latest ProV4 no-two-pass-volatility-focus scout was not promotable: it preserved guarded fallbacks but disabled two-pass root volatility focus; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `6-6` with inner-wedge `0-2`, `not_promising`, and `148.48ms` average.
- Latest ProV4 root-injection scouts were not promotable: broad and emergency-only turn-engine root injection each reached only weak sampled Fast `2-0`, then sampled Pro fast-failed at `7-5`; broad injection regressed forward-bridge `1-3`, emergency-only regressed inner-wedge `0-2`, and both stopped as `not_promising`.
- Latest ProV4 exact-lite-progress scout was not promotable: it preserved guarded fallbacks but enabled budgeted exact-lite progress and spirit-window root analysis; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `142.88ms` average.
- Latest ProV4 eligibility-guard scout was not promotable: it preserved guarded fallbacks but enabled the turn-engine eligibility guard; the tiny sampled Fast smoke failed immediately at `1-1`, `duel_passes=false`, with ordinary guarded branch coverage, so no dashboard spend was earned.
- Latest ProV4 pre-exact-policy scout was not promotable: it preserved guarded fallbacks but disabled root/child/static exact tactics and exact-lite budgets; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `143.20ms` average.
- Latest ProV4 no-drainer-safety-prefilter scout was not promotable: it preserved guarded fallbacks but disabled root drainer-safety prefiltering; the tiny sampled Fast smoke failed immediately at `1-1`, `duel_passes=false`, and `220.63ms`, so no dashboard spend was earned.
- Latest ProV4 no-root-efficiency scout was not promotable: it preserved guarded fallbacks but disabled root efficiency snapshots/deltas; the tiny sampled Fast smoke failed immediately at `1-1`, `duel_passes=false`, and `150.82ms`, so no dashboard spend was earned.
- Latest ProV4 drainer-attack-priority-enum scout was not promotable: it preserved guarded fallbacks and prioritized drainer-attack roots with a small enum boost; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `136.13ms` average.
- Latest ProV4 targeted-drainer-full-pool scout was not promotable: it preserved guarded fallbacks and added targeted drainer-attack fallback roots without forcing attack selection; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `8-4` with inner-wedge/split-flank `1-1`, forward-bridge `2-2`, `not_promising`, and `143.25ms` average.
- Latest ProV4 scoring-context-narrowing scout was not promotable: it preserved guarded fallbacks and enabled local scoring context, attack-reach target narrowing, child vulnerability context reuse, and tactical score-window narrowing; the tiny sampled Fast smoke reached only weak `2-0`, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `146.16ms` average.
- Latest ProV4 safe-mana-prepass scout was not promotable: it preserved guarded fallbacks but allowed safe immediate mana scoring/pickup roots to preempt from the guarded scored pool; the tiny sampled Fast smoke reached only weak `2-0` with one safe-mana branch and high cost, then sampled Pro fast-failed at `7-5` with inner-wedge `0-2`, `not_promising`, and `260.09ms` average.
- Latest root-pool step-threat feature scout produced no promotable Pro mode: the retained harness now emits one-step latent action-pressure fields, but the sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes and root-pool/guarded-delta discriminators both `no_source`.
- Latest root-pool carrier-route feature scout produced no promotable Pro mode: the retained harness now emits carrier payload route fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; every carrier-route rollup was blocker-contaminated and both root-pool/guarded-delta discriminators stayed `no_source`.
- Latest root-pool carrier-score-profile feature scout produced no promotable Pro mode: the retained harness now emits carried-mana score-distance and match-value fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; carrier-score-profile root-pool and guarded-delta rollups were blocker-contaminated, so both discriminators stayed `no_source`.
- Latest root-pool mana-contest feature scout produced no promotable Pro mode: the retained harness now emits free-mana proximity contest fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; root-pool mana-contest rollups were blocker-contaminated, guarded-delta mana-contest was singleton-only, and both discriminators stayed `no_source`.
- Latest root-pool pool-access feature scout produced no promotable Pro mode: the retained harness now emits scoring-pool staging and denial fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; every pool-access rollup was blocker-contaminated and both root-pool/guarded-delta discriminators stayed `no_source`.
- Latest root-pool root-sequence feature scout produced no promotable Pro mode: the retained harness now emits ordered input-token sequence fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; root-sequence rollups were contaminated or singleton-and-contaminated, and both root-pool/guarded-delta discriminators stayed `no_source`.
- Latest root-pool follow-up-shape feature scout produced no promotable Pro mode: the retained harness now emits same-turn legal follow-up shape/effect fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; follow-up rollups were contaminated or singleton-and-contaminated, and both root-pool/guarded-delta discriminators stayed `no_source`.
- Latest root-pool mana-base feature scout produced no promotable Pro mode: the retained harness now emits regular/Supermana base access fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; mana-base root-pool rollups were blocker-contaminated, guarded-delta exact mana-base rows were singleton-only, and both discriminators stayed `no_source`.
- Latest root-pool pickup-access feature scout produced no promotable Pro mode: the retained harness now emits role-specific free-mana pickup-access fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; pickup-access root-pool and guarded-delta rollups were blocker-contaminated and both discriminators stayed `no_source`.
- Latest root-pool cooldown-tempo feature scout produced no promotable Pro mode: the retained harness now emits role-specific cooldown-1 versus later-fainted tempo fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; cooldown-tempo root-pool and guarded-delta rollups were blocker-contaminated and both discriminators stayed `no_source`.
- Latest root-pool drainer-geometry feature scout produced no promotable Pro mode: the retained harness now emits drainer status, spacing, forwardness, zone, and payload fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; drainer-geometry root-pool and guarded-delta rollups were singleton-and-contaminated, and both discriminators stayed `no_source`.
- Latest root-pool role-coordination feature scout produced no promotable Pro mode: the retained harness now emits drainer/angel/spirit and carrier/angel spacing fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; role-coordination root-pool and guarded-delta rollups were singleton-and-contaminated, and both discriminators stayed `no_source`.
- Latest root-pool formation-balance feature scout produced no promotable Pro mode: the retained harness now emits compact-vs-stretched formation fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; formation-balance root-pool and guarded-delta rollups were blocker-contaminated, and both discriminators stayed `no_source`.
- Latest root-pool role-deployment feature scout produced no promotable Pro mode: the retained harness now emits side-relative forward deployment per live role, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; role-deployment root-pool and guarded-delta rollups were singleton-and-contaminated, and both discriminators stayed `no_source`.
- Latest root-pool role-pressure feature scout produced no promotable Pro mode: the retained harness now emits nearest-enemy pressure per live role, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; role-pressure root-pool and guarded-delta rollups were singleton-and-contaminated, and both discriminators stayed `no_source`.
- Latest root-pool role-mobility feature scout produced no promotable Pro mode: the retained harness now emits legal step, forward-step, pool-step, and pickup-step counts per live role, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; role-mobility root-pool and guarded-delta rollups were singleton-and-contaminated, and both discriminators stayed `no_source`.
- Latest root-pool action-reach feature scout produced no promotable Pro mode: the retained harness now emits action-role reach occupancy and demon blockers, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-reach root-pool and guarded-delta rollups were singleton-and-contaminated, and both discriminators stayed `no_source`.
- Latest root-pool role-contact feature scout produced no promotable Pro mode: the retained harness now emits adjacent opposing role-pair topology, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; role-contact root-pool and guarded-delta rollups were blocker-contaminated, and both discriminators stayed `no_source`.
- Latest root-pool cohesion feature scout produced no promotable Pro mode: the retained harness now emits friendly adjacency components and isolated-role topology, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; cohesion root-pool rollups were blocker-contaminated, guarded-delta cohesion was singleton-and-contaminated, and both discriminators stayed `no_source`.
- Latest root-pool objective-screen feature scout produced no promotable Pro mode: the retained harness now emits protective adjacency around carried and free mana objectives, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; objective-screen root-pool rollups were blocker-contaminated and guarded-delta objective-screen was singleton-and-contaminated, so both discriminators stayed `no_source`.
- Latest root-pool consumable-base feature scout produced no promotable Pro mode: the retained harness now emits consumable-base occupancy/control/access fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; consumable-base root-pool and guarded-delta rollups were blocker-contaminated and both discriminators stayed `no_source`.
- Latest root-pool carrier-contact feature scout produced no promotable Pro mode: the retained harness now emits mana-carrier contact/guard/contest fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; carrier-contact root-pool and guarded-delta rollups were blocker-contaminated and both discriminators stayed `no_source`.
- Latest root-pool carrier-escape feature scout produced no promotable Pro mode: the retained harness now emits mana-carrier escape-route fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; carrier-escape root-pool and guarded-delta rollups were blocker-contaminated and both discriminators stayed `no_source`.
- Latest root-pool role-escape feature scout produced no promotable Pro mode: the retained harness now emits contact-conditioned role escape-route fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; role-escape root-pool and guarded-delta rollups were blocker-contaminated and both discriminators stayed `no_source`.
- Latest root-pool action-target-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate action target role/payload fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-target-profile root-pool and guarded-delta rollups were singleton-and-contaminated and both discriminators stayed `no_source`.
- Latest root-pool spirit-item-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate Spirit-action free item value/type fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; spirit-item-profile root-pool and guarded-delta rollups were singleton-and-contaminated and both discriminators stayed `no_source`.
- Latest root-pool action-role-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate Mystic/Demon/Spirit target-role fields by ally/enemy role, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-role-profile root-pool rollups were contaminated and guarded-delta rollups were singleton-and-contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-guard-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate Mystic/Demon/Spirit enemy targets split by angel-guarded versus unguarded payload, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-guard-profile root-pool and guarded-delta rollups were contaminated and both discriminators stayed `no_source`.
- Latest root-pool action-actor-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate Mystic/Demon/Spirit actor readiness by guarded enemy, unguarded enemy, and Spirit free-item access, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-actor-profile root-pool and guarded-delta rollups were singleton-and-contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-zone-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate enemy/item action targets by actor-relative home/mid/deep/special-square zone, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-zone-profile root-pool and guarded-delta rollups were singleton-and-contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-payload-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate Mystic/Demon/Spirit target payload fields for enemy targets and Spirit free items, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-payload-profile root-pool and guarded-delta rollups were singleton-and-contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-escape-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate action-target escape-route fields for enemy mons under Mystic/Demon/Spirit pressure, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-escape-profile root-pool and guarded-delta rollups were contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-actor-safety-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate action-ready actor guard/contact/escape/payload fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-actor-safety-profile root-pool and guarded-delta rollups were singleton-and-contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-counter-profile feature scout produced no promotable Pro mode: the retained harness now emits reciprocal immediate action-counter pressure fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-counter-profile root-pool and guarded-delta rollups were contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-target-safety-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate action-target contact/support/trap fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-target-safety-profile root-pool and guarded-delta rollups were contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-score-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate action pressure on scoring carriers and match-step targets, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-score-profile root-pool and guarded-delta rollups were contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-denial-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate action targets that are themselves threatening Drainers, carriers, and scoring carriers, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-denial-profile root-pool and guarded-delta rollups were contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-pickup-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate action targets that could next pick up high-value mana, regular mana, Supermana, or consumables, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-pickup-profile root-pool and guarded-delta rollups were contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-square-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate enemy and Spirit-item action target square occupancy by regular/special base and mana-pool type, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-square-profile root-pool and guarded-delta rollups were singleton-and-contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-vector-profile feature scout produced no promotable Pro mode: the retained harness now emits kind-specific Mystic/Demon/Spirit action-reach occupancy for enemy, ally, item, empty, and blocked Demon lanes, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-vector-profile root-pool and guarded-delta rollups were singleton-and-contaminated, so both discriminators stayed `no_source`.
- Latest root-pool action-fork-profile feature scout produced no promotable Pro mode: the retained harness now emits Mystic/Demon/Spirit actor fork posture for guarded-filtered enemy multi-targets and Spirit item/mixed forks, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; action-fork-profile root-pool and guarded-delta rollups were singleton-and-contaminated, so both discriminators stayed `no_source`.
- Latest root-pool spirit-handoff-profile feature scout produced no promotable Pro mode: the retained harness now emits Spirit-action handoff access from reachable mana/carriers into own Drainer pickups or scoring-pool deliveries, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; spirit-handoff-profile root-pool and guarded-delta rollups were contaminated, so both discriminators stayed `no_source`.
- Latest root-pool carrier-action-profile feature scout produced no promotable Pro mode: the retained harness now emits immediate Mystic/Demon/Spirit action reach into mana carriers split by high-value/regular payload and Angel guard, but the capped sampled+active Fast outcome slice stayed `baseline_save_risk` with `route_permission=no_source`, zero source-candidate axes, and contaminated carrier-action rollups, so both discriminators stayed `no_source`.
- Latest root-pool potion-stock feature scout produced no promotable Pro mode: the retained harness now emits potion-stock and potion-action-extension fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; potion-stock root-pool and guarded-delta rollups were blocker-contaminated and both discriminators stayed `no_source`.
- Latest root-pool bomb-threat-profile feature scout produced no promotable Pro mode: the retained harness now emits bomb-capable carrier target posture fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; bomb-threat-profile root-pool and guarded-delta rollups were blocker-contaminated and both discriminators stayed `no_source`.
- Latest root-pool exact-score-profile feature scout produced no promotable Pro mode: the retained harness now emits exact-state score-path, immediate-window, drainer-pickup, carrier-step, and Spirit exact-score/denial posture fields, but the same sampled+active cross-budget outcome slice stayed `coverage_gap`, with zero source-candidate axes; exact-score-profile root-pool and guarded-delta rollups were singleton-and-contaminated and both discriminators stayed `no_source`.
- Latest root-pool control-map feature scout produced no promotable Pro mode: the retained harness now emits unique post-root step/action control, contested control, controlled mons, and free high-value mana coverage fields, but the capped sampled+active Fast outcome slice stayed `singleton_no_source` with `route_permission=no_source`, zero source-candidate rollups, root-pool `singleton_root_pool_signal`, and guarded-delta `singleton_root_pool_guarded_delta`; control-map rollups were singleton-and-contaminated or contaminated, so both discriminators stayed `no_source`.
- Latest root-pool demon-line-blocker feature scout produced no promotable Pro mode: the retained harness now emits post-root Demon lane obstruction identity, but the capped sampled+active Fast outcome slice stayed `singleton_no_source` with `route_permission=no_source`, zero source-candidate axes, root-pool `singleton_root_pool_signal`, and guarded-delta `singleton_root_pool_guarded_delta`; every demon-line-blocker exact/compound rollup was contaminated, so both discriminators stayed `no_source`.
- Latest root-pool mana-identity-profile feature scout produced no promotable Pro mode: the retained harness now splits post-root mana custody into Supermana, own regular, and opponent regular payloads plus Drainer/Spirit carrier roles, but the capped sampled+active Fast outcome slice stayed `singleton_no_source` with `route_permission=no_source`, zero source-candidate axes, root-pool `singleton_root_pool_signal`, and guarded-delta `singleton_root_pool_guarded_delta`; every mana-identity exact/compound rollup was contaminated, so both discriminators stayed `no_source`.
- Latest root-pool edge-anchor-profile feature scout produced no promotable Pro mode: the retained harness now emits post-root edge/corner/near-edge anchoring plus edge payload counts, but the capped sampled+active Fast outcome slice stayed `singleton_no_source` with `route_permission=no_source`, zero source-candidate axes, root-pool `singleton_root_pool_signal`, and guarded-delta `singleton_root_pool_guarded_delta`; edge-anchor rows appeared only as singleton/guarded-contaminated evidence, so both discriminators stayed `no_source`.
- Latest root-pool item-zone-profile feature scout produced no promotable Pro mode: the retained harness now emits post-root item/payload location buckets across active-side home/mid/deep bands, but the capped sampled+active Fast outcome slice stayed `singleton_no_source` with `route_permission=no_source`, zero source-candidate axes, root-pool `singleton_root_pool_signal`, and guarded-delta `singleton_root_pool_guarded_delta`; item-zone evidence only produced singleton/no-source counts, so both discriminators stayed `no_source`.
- Latest root-pool objective-proximity feature scout produced no promotable Pro mode: the retained harness now emits post-root non-carrier proximity/support around Supermana, regular mana, and consumables, but the capped sampled+active Fast outcome slice stayed `baseline_save_risk` with `route_permission=no_source`, zero source-candidate axes, root-pool `singleton_root_pool_signal`, and guarded-delta `singleton_root_pool_guarded_delta`; objective-proximity rollups were singleton and contaminated, so both discriminators stayed `no_source`.
- Latest root-pool objective-control feature scout produced no promotable Pro mode: the retained harness now emits post-root objective-square step/action/either control around Supermana, regular mana, and consumables, but the capped sampled+active Fast outcome slice stayed `baseline_save_risk` with `route_permission=no_source`, zero source-candidate axes, root-pool `singleton_root_pool_signal`, and guarded-delta `singleton_root_pool_guarded_delta`; objective-control rollups were singleton and contaminated, so both discriminators stayed `no_source`.
- Latest root-pool objective-square-profile feature scout produced no promotable Pro mode: the retained harness now emits post-root objective square-type buckets for Supermana, own regular mana, opponent regular mana, and consumables, but the capped sampled+active Fast outcome slice stayed `baseline_save_risk` with `route_permission=no_source`, zero source-candidate axes, and contaminated objective-square rollups, so both discriminators stayed `no_source`.
- Latest root-pool score-term-profile feature scout produced no promotable Pro mode: the retained harness now emits post-root residual scoring-term leaders and deltas, but the capped sampled+active Fast outcome slice stayed `baseline_save_risk` with `route_permission=no_source`, zero source-candidate axes, singleton root-pool/guarded-delta source checks, and score-term evidence was singleton or contaminated, so both discriminators stayed `no_source`.
- Latest root-pool follow-up-role-profile feature scout produced no promotable Pro mode: the retained harness now emits same-turn legal follow-up actor/action/pickup/faint role profiles and deltas, but the capped sampled+active Fast outcome slice stayed `baseline_save_risk` with `route_permission=no_source`, zero source-candidate axes, and follow-up-role evidence was singleton or contaminated, so both root-pool discriminators stayed `no_source`.
- Runtime source stays untouched unless a new corpus/root feature separates candidate wins from baseline saves across sampled and active evidence with low fragmentation.

## Reset Portfolio

Use this retained portfolio for policy-corpus and outcome-corpus reset work:

```text
frontier_pro_v2_guarded,
frontier_pro_v3_alternating_white_edge_mana,
frontier_pro_v3_white_opening_utility_mana,
shipping_pro_search_control,
frontier_pro_v2_raw,
frontier_pro_v2_no_selected_followup_projection,
frontier_pro_v3_full_scored_reply_guard,
frontier_pro_v2_no_low_budget_guard
```

These stale test-only sweep candidates are pruned from the active runner surface and must not be reopened as direct targets:

```text
frontier_pro_v2_no_late_black_fallback,
frontier_pro_v2_head_rerank,
frontier_pro_v2_no_spirit_family,
frontier_pro_v2_no_mid_tactical_guard,
frontier_pro_v2_expansion_224
```

## Retired Source Evidence

Do not write runtime selectors from:

- existing policy labels, branch labels, exact contexts, first moves, variants, or singleton-heavy corpus rows;
- broad zero-window exact-pressure classes or current exact-pressure deltas;
- current active Fast lower-live safe-step / ManaTempo pressure, ProV4 root-pool, policy-root-pool, guarded-delta, root-ordering profile, root input sequence/order, follow-up transition shape, follow-up role profile, score-term-profile, mana-base access/control, free-mana pickup access, cooldown tempo, drainer geometry, role coordination, formation balance, role deployment, role pressure, role-contact, friendly cohesion, role mobility, carrier-score-profile, carrier-action-profile, bomb-threat-profile, exact-score-profile, mana-identity-profile, edge-anchor-profile, item-zone-profile, objective-proximity-profile, objective-control-profile, objective-square-profile, control-map, demon-line-blocker, potion-stock, action-reach, action-target-profile, spirit-item-profile, spirit-handoff-profile, action-role-profile, action-guard-profile, action-actor-profile, action-actor-safety-profile, action-zone-profile, action-payload-profile, action-escape-profile, action-counter-profile, action-target-safety-profile, action-score-profile, action-denial-profile, action-pickup-profile, action-square-profile, action-vector-profile, action-fork-profile, root-efficiency toggles, root-aspiration windowing, drainer walk-threat safety filtering, root drainer-safety prefilter toggles, enhanced drainer vulnerability toggles, child-vulnerability plausibility screens, backtrack-penalty toggles, two-pass volatility-focus toggles, turn-engine root-injection toggles, turn-engine eligibility guard toggles, exact-lite progress/spirit-window toggles, pre-exact/exact-tactics disabling toggles, tactical quiescence toggles, safe-mana tactical prepass selectors, forced tactical prepass, futility-pruning toggles, quiet-reduction toggles, selective-extension toggles, root-allocation toggles, deterministic interview-tiebreak toggles, event-ordering toggles, node-budget uplift toggles, branch-limit uplift toggles, attacker-proximity scoring toggles, opponent-mana soft-priority toggles, reply-risk guard toggles, normal-root-safety toggles, root mana-handoff guard toggles, strict anti-help filter toggles, hard spirit-deploy toggles, move-class coverage toggles, potion-mix toggles, interview soft-prior toggles, root spirit-development toggles, PVS/search-ordering toggles, depth-consensus root stability, forced drainer-attack pressure, drainer-attack priority enumeration toggles, targeted drainer full-pool fallback toggles, scoring context / attack-reach narrowing toggles, root-breadth config rebalancing, targeted exact-turn / score-window narrowing, child-ordering shortlist tuning, move-efficiency delta, scoring-context robustness, objective latency, bounded followup/reply breadth, root-preservation, reply-floor, reply-floor/progress, root-safety, utility/rank, cross-budget static-eval consensus, turn-completion stability, two-turn resilience, forced-root feature-axis, root-pool provenance, forced-root pool JSONL, root trajectory, race geometry, root-pool contrast, outcome contrast, family-overlap, state-discriminator, broad token, vulnerable-baseline token, or utility/rank token-pair evidence;
- current post-root feature families: exact pressure, exact-score-profile, score-term-profile, board-resource custody/material, mana-identity-profile, edge-anchor-profile, item-zone-profile, objective-proximity-profile, objective-control-profile, objective-square-profile, cooldown tempo, drainer geometry, role coordination, formation balance, role deployment, role pressure, role-contact, role escape routing, friendly cohesion, role mobility, scoreboard/turn-budget, legal-transition fanout, same-turn follow-up transition shape, same-turn follow-up role profile, attack-exposure, support-guard, objective-screen, territory, control-map / unique step-action coverage, mana-path, free-mana proximity contest, free-mana pickup access, regular/Supermana base access, mana-pool access, carrier payload routing, carrier score-distance/match profile, carrier contact/vulnerability, carrier action vulnerability, carrier escape routing, consumable-base access/control, consumable, bomb-threat-profile, potion-stock/action-extension, engagement, mobility, action-threat, action-reach, action-target-profile, spirit-item-profile, spirit-handoff-profile, action-role-profile, action-guard-profile, action-actor-profile, action-actor-safety-profile, action-zone-profile, action-payload-profile, action-escape-profile, action-counter-profile, action-target-safety-profile, action-score-profile, action-denial-profile, action-pickup-profile, action-square-profile, action-vector-profile, demon-line-blocker identity, action-fork-profile, one-step step-threat projection, role-state/loadout, base-recovery, lane-shape, root-transition/event footprint, worst-reply event footprint, and immediate reply-spectrum shape.

Those paths produced no source permission because the evidence stayed `coverage_gap`, `baseline_save_risk`, `no_candidate_route`, singleton-only, policy/branch/pair fragmented, shared with blockers, or contaminated by guarded baseline saves. Their detailed run notes are archived in `docs/automove-archive.md`; durable rules live in `docs/automove-knowledge.md`.

## Next Command Sequence

First validate local hygiene:

```sh
python3 -m py_compile \
  scripts/summarize-automove-policy-matrix-log.py \
  scripts/summarize-automove-outcome-jsonl.py \
  scripts/summarize-automove-forced-root-oracle-log.py \
  scripts/summarize-automove-forced-root-pool-jsonl.py

./scripts/cleanup-automove-iteration-artifacts.sh --dry-run
```

Do not rerun the current sampled/active Fast portfolio, step-threat root-pool, score-term-profile root-pool, carrier-route root-pool, carrier-score-profile root-pool, carrier-contact root-pool, carrier-action-profile root-pool, carrier-escape root-pool, mana-contest root-pool, pool-access root-pool, root-sequence root-pool, follow-up-shape root-pool, follow-up-role-profile root-pool, mana-base root-pool, pickup-access root-pool, consumable-base root-pool, bomb-threat-profile root-pool, exact-score-profile root-pool, mana-identity-profile root-pool, edge-anchor-profile root-pool, item-zone-profile root-pool, objective-proximity-profile root-pool, objective-control-profile root-pool, objective-square-profile root-pool, control-map root-pool, demon-line-blocker root-pool, potion-stock root-pool, cooldown-tempo root-pool, drainer-geometry root-pool, role-coordination root-pool, formation-balance root-pool, role-deployment root-pool, role-pressure root-pool, role-contact root-pool, role-escape root-pool, cohesion root-pool, objective-screen root-pool, role-mobility root-pool, action-reach root-pool, action-target-profile root-pool, spirit-item-profile root-pool, spirit-handoff-profile root-pool, action-role-profile root-pool, action-guard-profile root-pool, action-actor-profile root-pool, action-actor-safety-profile root-pool, action-zone-profile root-pool, action-payload-profile root-pool, action-escape-profile root-pool, action-counter-profile root-pool, action-target-safety-profile root-pool, action-score-profile root-pool, action-denial-profile root-pool, action-pickup-profile root-pool, action-square-profile root-pool, action-vector-profile root-pool, or action-fork-profile root-pool slices as source work. They are now archived as no-source. If continuing reset-mode diagnostics without a new candidate, first add or expose a genuinely new measured root feature that is not in the retired evidence list, then run the smallest outcome-corpus scout that emits that feature.

For a new test-only ProV4/root-policy candidate, register it as a sweep candidate with metadata first, then use:

```sh
./scripts/run-automove-structural-scout.sh --corpus <candidate>
```

## Session End

1. Leave this file with one current state and one next command sequence.
2. Move durable lessons to `docs/automove-knowledge.md`.
3. Move probe diaries and failed wave detail to `docs/automove-archive.md`.
4. Clean scratch artifacts with `./scripts/cleanup-automove-iteration-artifacts.sh --dry-run`.
5. Clean target logs/stamps separately with `./scripts/clean-experiment-artifacts.sh --dry-run` only when disposable evidence is no longer needed.
