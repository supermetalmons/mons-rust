# Automove Ideas

This is the live decision board for automove work.

Use `HOW_TO_ITERATE_ON_AUTOMOVE.md` as the runbook. Keep this file short. Move durable lessons to `docs/automove-knowledge.md` and retired branch history to `docs/automove-archive.md`.

## Current State (2026-03-27)

- Shipping Pro stays `runtime_current`.
- `runtime_current` is the planner-plus-quiescence Pro runtime.
- `runtime_pro_turn_engine_v30` is the only live Pro challenger.
- `runtime_pro_turn_engine_v30` is a guarded `ProV2` engine path with deliberate opening and early-white fallbacks.
- The latest shared exact cuts improved `runtime_pro_turn_engine_v30`, but it is still not promotable because `pro-reliability` keeps stalling past a practical window.
- The latest local after-window cache-axis-sharing cut only shaved the hotspot slightly; it did not materially reduce tactical projection call volume or clear the promotion gate.
- The latest tactical cache-sharing cuts did move the live wall again: `human_win_pro_a` dropped from about `1425ms` to about `1305ms`, but `pro-reliability` still stalled past `2:17`.
- The latest bounded carrier-window cut is worth keeping: it collapsed `human_win_pro_a` payload work from about `32.2m` to about `6.3m` and materially sped up the probe, but the real duel gate still stalled past `2:17`.
- The latest retained exact cut is also worth keeping: low-budget drainer pickup fast paths removed most uncached pickup BFS from the hotspot, score-floor after-window filtering trimmed some off-target tactical queries, and incremental board-hash reuse across spirit previews moved `human_win_pro_a` from about `570ms` to about `520ms`, but `pro-reliability` was still live past `2:20` on 2026-03-27.
- The latest retained exact cut is worth keeping too: a zero-move after-window tactical fast path updates touched mana-pool counts directly instead of re-querying the immediate-window helper, which moved `human_win_pro_a` again from about `520ms` to about `433ms` and dropped immediate-window / after-window query volume from about `428999` / `427069` to about `168068` / `166138`, but `pro-reliability` still had to be killed at `3:37` on 2026-03-27.
- The latest retained exact cut is also worth keeping: a budget-1 after-window tactical fast path updates touched-neighborhood one-move contributions instead of re-running the immediate-window helper, which dropped `human_win_pro_a` immediate-window / after-window query volume again from about `168068` / `166138` to about `41740` / `39810` and moved the hotspot from about `433ms` to about `413ms`, but `pro-reliability` still had to be killed at `2:35` on 2026-03-27.
- The latest local budget-2 after-window summary experiment was not worth keeping: it collapsed `human_win_pro_a` immediate-window / after-window query volume again from about `41740` / `39810` to about `7590` / `5660`, but the hotspot itself regressed from about `420ms` to about `452ms` and `pro-reliability` still had to be killed at `2:35` on 2026-03-27.
- The latest shared actor-move-memo experiment was not worth keeping either: sharing payload transition memos across immediate-window and budget-1 summary searches trimmed `human_win_pro_a` payload calls only slightly and cut pickup-window calls from about `2910` to about `2218`, but the hotspot regressed from about `420ms` to about `433ms` and `pro-reliability` still had to be killed at `2:34` on 2026-03-27.
- The latest carrier-specific move-memo plus small-budget carrier fast-path experiment was not worth keeping either: replacing the generic actor memo with a carrier-only memo and a local `0..=3` frontier path left `human_win_pro_a` effectively flat at about `411.32ms` versus the retained `411.56ms`, with `payload_calls` still about `5083818`, so carrier memo/init overhead is not the remaining wall.
- The latest retained exact cut is worth keeping too: a bounded reverse carrier-distance map, built only after a board/budget pair proves it has multiple distinct carrier queries, moved `human_win_pro_a` from the retained `411.56ms` down to about `404.74ms` and then `398.73ms`, with `payload_calls` down to about `4922742`, but `pro-reliability` still had to be killed at about `2:45` on 2026-03-27.
- The latest retained exact cut is worth keeping too: far spirit previews now reuse the base after-action board when their touched locations lie outside every relevant actor's bounded neighborhood, which moved `human_win_pro_a` down again to about `383.97ms` and then `389.66ms`, with `payload_calls` down to about `4506607` and immediate-window / after-window queries down to about `27900` / `25970`, but `pro-reliability` still had to be killed at about `2:47` on 2026-03-27.
- The latest local `>1` after-window actor-summary experiment was not worth keeping: replacing those queries with touched-actor local summaries, even when gated to previews that only affected one allied actor, collapsed `human_win_pro_a` immediate-window / after-window counts to about `3551` / `1621` but raised `payload_calls` to about `4588693` and regressed the hotspot to about `403.34ms`, so helper-count collapse alone still does not clear the live wall.
- The latest retained exact cut is worth keeping too: bounded carrier and drainer lower-bound rejects now skip payload BFS when an empty-board geometric bound already exceeds the caller's budget, which moved `human_win_pro_a` down again to about `351.67ms` and `354.94ms`, with `payload_calls` down to about `3562056` and pickup calls down to about `2684`, but `pro-reliability` still had to be killed at about `2:34` on 2026-03-27.
- The latest retained exact cut is worth keeping too: bounded carrier BFS and drainer pickup search now also prune individual states whose empty-board remaining-distance lower bound already exceeds the remaining budget, which moved `human_win_pro_a` down again to about `344.52ms` and `347.07ms`, with `payload_calls` down to about `3397716` and pickup calls still about `2684`, but `pro-reliability` still had to be killed at about `2:28` on 2026-03-27.
- The latest filtered pickup-path reuse experiment was not worth keeping: routing `exact_best_drainer_pickup_path_filtered_with_hash` through the pickup-window surface reduced `payload_calls` on several secondary fixtures, but `human_win_pro_a` stayed effectively flat at about `348.72ms` / `349.09ms` with `payload_calls` still about `3327205`, so duplicate filtered pickup search is not the live wall by itself.
- The latest exact empty-board carrier/drainer lower-bound table experiment was also not worth keeping: replacing the geometric bounds with static exact empty-board distances left the retained hotspot effectively flat at about `350.08ms` with `payload_calls` back at about `3397716`, so stronger static bounds do not materially beat the cheaper geometric bounds on the live wall.
- The latest exact one-actor after-window reuse experiment was not worth keeping either: reusing base action-board per-actor immediate tactical contributions collapsed `human_win_pro_a` immediate-window queries from about `27900` to about `1930`, but fresh stable reruns sat at about `347.56ms` and `347.95ms` with `payload_calls` still about `3398229`, so the branch regressed slightly versus the retained deeper-pruning baseline and was killed before another duel loop.
- The latest low-budget drainer frontier-dedupe experiment was not worth keeping either: deduping `0..=3` pickup fast-path frontier states left hotspot counters effectively flat because the actor-move memo already reuses repeated `(payload, destination)` transition checks, so the extra seen bookkeeping was pure overhead and was killed before another duel loop.
- The latest lazy one-actor after-window reuse follow-up was not worth keeping either: delaying base actor-summary construction and adding an early-exit one-actor gate still collapsed `human_win_pro_a` immediate-window queries from about `27900` to about `1930`, but fresh stable reruns sat at about `354.33ms` and `350.92ms` with `payload_calls` still about `3398229`, so eager summary setup was not the real regression source and the line still lost to the retained deeper-pruning baseline.
- The latest bounded reverse drainer-pickup map experiment was not worth keeping either: lazily building a board/color/budget reverse pickup map after repeated bounded drainer queries preserved exact parity, but the first hotspot sample regressed `human_win_pro_a` to about `362.08ms` with `payload_calls` still about `3397716` and also raised payload work on secondary fixtures, so the map build overhead lost to the retained small-budget pickup path.
- The latest retained exact cut is worth keeping too: drainer-only pickup-window and filtered-path callers now use a smaller drainer-specific payload-transition memo instead of the generic actor memo, which left `human_win_pro_a` `payload_calls` flat at about `3397716` but improved stable hotspot reruns from the retained `349.88ms` down to `340.61ms` / `340.60ms`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `2:40` on 2026-03-27 and had to be killed.
- The latest retained exact cut is worth keeping too: single-target attack reach now skips attacker BFS when a specific attacker has no exact-safe geometric path to ever threaten the target, and attack BFS never enters mana states that cannot contribute to attack success. That collapsed `human_win_pro_a` `payload_calls` from about `3397716` to about `1567910` and improved stable hotspot reruns to about `326.49ms` / `312.30ms`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed again, but `pro-reliability` was still live at about `3:00` on 2026-03-27 and had to be killed.
- The latest retained exact cut is worth keeping too: single-target attack reach now also prunes individual states whose exact-safe remaining attack lower bound already exceeds the remaining budget. That moved `human_win_pro_a` down again to about `303.51ms` / `299.94ms` and cut `payload_calls` from about `1567910` to about `1202108`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed again, but `pro-reliability` was still live at about `3:00` on 2026-03-27 and had to be killed.
- The latest attack-only move-memo and two-state seen-set experiment was not worth keeping: shrinking attack BFS state shape to `None`/`Bomb` only preserved parity, but the hotspot regressed to about `303.79ms` with `human_win_pro_a` `payload_calls` still about `1202108`, so generic memo width was not the remaining attack wall.
- The latest lazy attack min-steps cache experiment was not worth keeping either: promoting repeated board/target attack queries onto a shared min-step cache, first at full-budget and then only after a second distinct budget, improved some secondary fixtures but still regressed the live hotspot to about `322.29ms` and then `320.19ms`. The selective version left `human_win_pro_a` `payload_calls` flat at about `1202108`, so budget-axis attack-query reuse is not the next wall either.
- The latest query-aware after-window reach-mask experiment was not worth keeping either: excluding low-score carrier neighborhoods from spirit after-window reuse did cut `human_win_pro_a` immediate-window / after-window counts from about `27900` / `25970` to about `26962` / `25032`, but stable hotspot reruns stayed flat to worse at about `305.52ms` and `303.63ms`, and a lower-overhead fixed-slot follow-up still produced a `316.84ms` rerun. That means local query-aware after-window reach filtering is not enough to clear the remaining tactical wall by itself.
- The latest query-aware actor-gated after-window mask follow-up was not worth keeping either: excluding entire low-score carrier and drainer neighborhoods on score/denial-only spirit paths, while still keeping drainers relevant under optimistic one-step drainer/mana spirit shifts, left the live hotspot effectively flat at about `301.77ms` with `payload_calls` still about `1202108`. That means broader score-floor actor gating on tactical spirit reuse is still not enough to move the remaining wall.
- The latest local attack target lower-bound context follow-up was not worth keeping either: precomputing per-target action and bomb lower-bound tables so plausibility and BFS could share them regressed the live hotspot badly to about `415.03ms`, and `payload_calls` even rose slightly to about `1208336`. That means the remaining attack wall is not repeated target-lower-bound arithmetic; the local table build cost lost to the retained direct checks.
- The latest scoring attack-summary activation follow-up was not worth keeping either: enabling local scoring attack summaries and target narrowing on `runtime_pro_turn_engine_v30` exploded `human_win_pro_a` `attack_summary_builds` to about `6322` while leaving `payload_calls` flat at about `1202108`, and stable hotspot reruns regressed to about `310.09ms` / `311.28ms`. That means the remaining wall is not repeated scoring-side attack/threat point queries in a form that full-board summaries can amortize cheaply.
- The latest lightweight attack-cache follow-ups were also not worth keeping: collapsing the duplicated uncached attack prescan was effectively flat at about `306.15ms` / `304.40ms`; an exact drainer immediate-threat point cache collapsed `human_win_pro_a` `threat_calls` from about `82082` to about `17066` but left wall-clock flat at about `305.31ms` / `304.11ms`; and a monotonic attack budget-bounds cache nudged `attack_hits` up and `payload_calls` down only slightly to about `1201799`, but stable reruns regressed to about `310.15ms` / `310.96ms`. That means neither fixed threat-scan cost nor lightweight budget-axis attack-query reuse is the next wall.
- The latest lazy scoring attack-summary warmup follow-up was not worth keeping either: deferring full scoring attack-summary construction until the second distinct target preserved parity, but the live hotspot still showed `attack_summary_builds=0` and regressed to about `315.71ms` after a `302.61ms` noise sample. That means the remaining wall is not dormant scoring attack/threat query repetition waiting for a cheaper warmup trigger.
- The latest board-scoped shared attack move-memo follow-up was not worth keeping either: reusing actor payload transitions across same-board uncached attack queries preserved parity in a focused test, but the live hotspot kept `payload_calls` flat at about `1202108` and regressed to about `315.99ms` / `315.26ms`. That means the remaining wall is not repeated same-board attack transition lookup in a form that a shared move memo can amortize cheaply.
- The latest attack enqueue-time short-circuit follow-up was not worth keeping either: returning immediately on newly reached attack states and pruning doomed next states cut `human_win_pro_a` `payload_calls` from about `1202108` to about `1110231`, but stable hotspot reruns still regressed to about `330.56ms` and `317.77ms`. That means reducing attack queue churn alone is not enough when the extra per-neighbor target checks cost more than the saved payload expansion.
- The latest broad drainer-threat short-circuit follow-up was not worth keeping either: adding immediate-threat prechecks ahead of exact drainer danger / vulnerability paths cut `human_win_pro_a` `attack_calls` to about `56923`, `payload_calls` to about `1193838`, and, after re-adding an exact threat cache, dropped `threat_calls` to about `24769`, but the stable hotspot still regressed to about `323.66ms`. That means broader exact drainer-threat prechecks are not the next wall.
- The latest local exact drainer-safety early-exit follow-up was also not worth keeping: skipping exact attack reach inside `exact_own_drainer_safety_score` when immediate or walk threat already proved the drainer unsafe initially looked faster, but same-session A/B reruns put the candidate at about `320.20ms` while the reverted retained baseline landed at about `302.18ms`, so the apparent win was probe noise rather than a durable cut.
- The latest attack start-state fast-path follow-up was not worth keeping either: bypassing queue and memo setup when an attacker already threatened the target from its start square left `human_win_pro_a` counters unchanged and regressed the hotspot to about `324.15ms`, so immediate-start attack BFS setup is not the live wall.
- `runtime_pro_turn_engine_v1` stays reference-only regression history.
- Archive profiles and retired planner/quiescence lines are docs-only context.

## Live Objective

Promote `runtime_pro_turn_engine_v30` against `runtime_current` in Pro mode with fewer, cheaper iterations.

Success means:
- `guardrails` stays clean.
- `SMART_TRIAGE_SURFACE=primary_pro` passes `pro-triage`.
- `runtime-preflight` stays clean.
- `pro-reliability` produces real promotable evidence against `runtime_current`.

Default loop:
- `./scripts/run-automove-experiment.sh guardrails runtime_pro_turn_engine_v30`
- `SMART_TRIAGE_SURFACE=primary_pro ./scripts/run-automove-experiment.sh pro-triage runtime_pro_turn_engine_v30 runtime_current`
- `./scripts/run-automove-experiment.sh runtime-preflight runtime_pro_turn_engine_v30`
- `./scripts/run-automove-experiment.sh pro-reliability runtime_pro_turn_engine_v30 runtime_current`

## Primary Split Family

Shared exact/search cuts on the current live wall.

Use this family when:
- a fresh `pro-reliability` sample points at the next wall clearly
- the wall is still inside exact tactical, projection, or search-side scoring work
- the split can plausibly improve `primary_pro` or reduce direct duel friction

Reject this family for a branch when:
- the split is only another wrapper retune
- the hotspot moved but the planned change still attacks the old wall
- the split has no direct story for `pro-triage` or `pro-reliability`

Current live wall:
- inside tactical projection itself, especially `exact_tactical_spirit_summary` after-window work and the remaining `exact_best_immediate_tactical_window_on_board_with_hash` / pickup-window cost
- cross-flag cache reuse at the tactical spirit, immediate-window, pickup-window, and projection layers is worth keeping, but it still does not clear the duel gate by itself
- bounded carrier reach checks are worth keeping on budgeted immediate-window callers, but they still leave the live wall inside after-window tactical queries and drainer pickup work
- low-budget pickup fast paths and incremental exact board-hash reuse are worth keeping, but the main `human_win_pro_a` wall still emits about `428999` immediate-window queries and about `427069` after-window calls
- zero-move after-window tactical queries were a real wall and are now worth bypassing with exact touched-item updates, but the live duel wall still sits in the remaining budgeted after-window fanout
- budget-1 after-window tactical queries were also worth bypassing locally, but once they drop out the wall-clock gain is modest and the remaining wall looks deeper than simple one-move query volume
- budget-2 after-window tactical query volume is no longer the right target by itself; collapsing it with a local summary moved counters but not wall-clock
- board-scoped payload-transition memo reuse is also not enough by itself once the obvious after-window fanout is gone
- carrier-specific memo shrink plus small-budget carrier search is also not enough by itself; leaving `payload_calls` flat means the remaining wall is deeper than generic carrier BFS setup cost
- board-scoped bounded reverse carrier maps can help, but only when built lazily after repeated queries on the same board/budget; building them unconditionally on the first bounded query is too broad and regresses wall-clock
- far spirit previews that do not touch any location within the remaining-budget neighborhood of an allied carrier or drainer should reuse the base after-action board; querying each such preview separately is wasted exact spend
- local actor-summary replacement for the remaining `>1` after-window queries is also not enough by itself; even one-actor-gated summaries can remove most helper calls while raising payload work and regressing wall-clock
- exact-safe empty-board geometry lower bounds can still be worth keeping in the payload core; bounded carrier and drainer callers should reject impossible pool/pickup lines before entering payload BFS
- exact-safe lower bounds are still worth pushing one layer deeper into bounded payload search itself; even after bounded carrier/drainer wrappers reject impossible lines, per-state remaining-budget pruning is still a real win inside the surviving payload BFS
- filtered pickup-path reuse through the pickup-window surface is not enough by itself either; even when it trims payload work on secondary fixtures, the main `human_win_pro_a` wall stays flat
- stronger static empty-board distance tables are also not the next wall by themselves; once the retained deeper-pruning line is in place, replacing the cheap geometric bounds with exact empty-board tables does not materially improve the live hotspot
- low-budget drainer frontier dedupe is not a real wall either; actor-move memo reuse already removes duplicate `(payload, destination)` transition evaluation, so extra seen bookkeeping only adds overhead
- bounded reverse drainer-pickup maps are not the next wall either; even with lazy warmup, map build overhead can outweigh the saved repeated bounded drainer queries while leaving the main `human_win_pro_a` payload wall flat
- drainer-only payload-transition memo specialization can still be worth keeping on truly drainer-only pickup surfaces; even with flat `payload_calls`, shrinking the memo shape trims enough fixed transition overhead to move the live hotspot
- attack reach still contained exact-safe payload waste too; single-target attack queries should skip attackers with no exact-safe geometric threat path, and attack BFS should never enter mana states that can never lead to an attack
- attack reach also benefits from per-state lower bounds once the impossible-attacker and mana-state waste is gone; if a state's cheapest exact-safe path to any attack outcome already exceeds the remaining budget, prune it before paying more transition work
- future cuts need to reduce the number of after-window tactical queries or spirit-preview fanout itself, not just make hashing or pickup subroutines cheaper
- per-actor after-window reuse is not enough by itself either; even when it preserves the retained exact search surface and removes most full immediate-window rebuilds, flat payload work can leave wall-clock slightly worse than the retained baseline
- lazy base-summary construction does not rescue one-actor after-window reuse either; if `payload_calls` stay flat and wall-clock still regresses, eager setup was not the main cost
- the next live target is still the remaining payload reachability algorithm itself, not more memo reuse, wrapper-only guards, or budget-count collapse alone
- query-aware actor gating on spirit after-window masks is also not enough by itself; even when low-score carrier and drainer neighborhoods drop out on score/denial-only paths under optimistic one-step preview relevance checks, the live hotspot can stay effectively flat with the same payload work
- local per-target attack lower-bound table builds are also not enough by themselves; replacing repeated action/bomb lower-bound arithmetic with precomputed target-context arrays can regress badly even before the canonical Pro loop
- lazy scoring attack-summary warmup is not enough either; if the live hotspot never builds the summary at all, repeated scoring-side attack/threat point queries are not the real wall
- board-scoped shared attack transition memos are not enough either; if `payload_calls` stay flat on the live hotspot, the remaining attack wall is not same-board transition recomputation in a form that a shared memo can exploit
- enqueue-time attack short-circuits are not enough either; even when they cut `payload_calls`, extra per-neighbor target checks can still lose to the retained simpler BFS
- broader exact drainer-threat prechecks are also not enough by themselves; even when they reduce `attack_calls`, `payload_calls`, or `threat_calls`, the added precheck path can still regress the live hotspot
- tiny exact drainer-safety or attack start-state setup skips must survive same-session A/B reruns against the reverted retained baseline before they are worth keeping; one promising hotspot sample is not enough
- not in planner/oracle summary construction anymore

## Secondary Split Family

Minimal new `primary_pro` fixtures or stuck-state / bounded-progress fixtures.

Use this family only when:
- a small fixture addition can reject bad branches before duel spend
- the current wall is already understood and the missing piece is earlier failure detection
- the fixture is directly tied to a repeated live failure mode

Do not use this family to avoid live Pro evidence.

## Parked

Do not spend time here unless the live objective changes:
- Fast work
- archive profiles
- retired planner/quiescence revivals
- wrapper-only knob sweeps
- hotspot-first iteration loops
- broad reporting cleanup that does not change promote/kill speed

## Hard Kill Condition

Stop the line and record the lesson when a focused split does not do at least one of these:
- improve `pro-triage`
- reduce `pro-reliability` friction
- move the live wall to a new code surface

If the direct duel wall is unclear, take a fresh live `pro-reliability` sample before starting another micro-optimization pass.

- The latest same-state exact opportunity-context cache experiment was not worth keeping: it moved `human_win_pro_a` only to about `300.33ms` / `300.66ms` from the retained `302.27ms`, but it regressed `primary_spirit_setup` to about `224ms` and raised secondary tactical-spirit / secure-mana / pickup work, so repeated same-state opportunity assembly is not the live wall.
- The latest retained exact cut is worth keeping too: board/color exact drainer safety now caches the full safety score and the turn-engine / planner wrappers route through it, which moved stable `human_win_pro_a` to about `300.03ms` / `298.27ms` with a retained rerun at `299.12ms`, while cutting `attack_calls` to about `48411` and `threat_calls` to about `72268` from the prior retained `58225` / `82082`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `3:30` on 2026-03-27 and had to be killed.
- The latest exact drainer-threat snapshot / scoring memo follow-up was not worth keeping either: board/location threat snapshots collapsed `human_win_pro_a` `attack_calls` to about `22092` and `payload_calls` to about `1130540`, but stable hotspot reruns regressed to about `304.45ms` and `320.43ms`, so broader point-snapshot caching just shifted work around instead of improving the live wall.
- The latest safe-drainer-mana wrapper follow-up was not worth keeping either: routing `own_drainer_carries_safe_mana` through the retained safety cache left `human_win_pro_a` effectively flat at about `299.13ms`, with `attack_calls` only down from about `48411` to `48331`, so that wrapper was too small to matter.
- The latest scoring exact danger-point memo follow-up was not worth keeping either: adding a board-local `(color, location)` exact danger memo inside `ScoringEvalContext` preserved parity, but the hotspot only moved to about `296.45ms` on the first rerun before regressing to about `303.81ms` on the confirming rerun, with exact-side counters flat at about `attack_calls=48411` and `payload_calls=1199361`. That means the apparent win was not a stable live-wall move and did not justify another canonical Pro loop.
- The latest shared attack target setup follow-up was not worth keeping either: sharing `target_guarded`, bomb-pickup locations, and the plausible-attacker subset between the attack plausibility screen and attack BFS preserved parity, but stable hotspot reruns regressed to about `298.30ms` and `300.74ms` with exact-side counters flat at about `attack_calls=48411` and `payload_calls=1199361`. That means duplicate pre-BFS board scans and bomb-list setup are not the live wall either.
- The latest planner-side attack hoist follow-up was not worth keeping either: hoisting repeated root-board drainer-attack checks in the planner and routing planner attack probes through the hash-aware exact helper did reduce a focused zero-setup planner test to one exact attack query, but stable hotspot reruns still regressed to about `298.40ms` and `297.05ms` with exact-side counters flat at about `attack_calls=48411` and `payload_calls=1199361`. That means planner-side attack cache-hit and board-hash overhead is not the live wall either.
- The latest retained secure-mana cut is worth keeping too: exact secure-mana search now rejects impossible wanted-mana lines with a cheap wanted-mana distance lower bound before entering recursive secure-walk mutation, which was the top retained duel stack on a fresh `pro-reliability` sample. Stable hotspot reruns moved `human_win_pro_a` down to about `269.59ms` and then `257.42ms`, with `attack_calls` down to about `36272`, `payload_calls` down to about `1194927`, and `secure_mana_calls` down from about `10304` to about `5126`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `3:37` on 2026-03-27 and had to be killed.
- The latest retained filtered-pickup cut is worth keeping too: exact filtered drainer-pickup search now rejects bounded impossible lines up front and prunes BFS once the current best pickup metric can no longer be beaten. Stable hotspot reruns moved `human_win_pro_a` down to about `258.31ms`, `245.64ms`, and finally `242.41ms`, with `payload_calls` down from about `1194927` to about `850393`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `3:14` on 2026-03-27 and had to be killed. A fresh retained duel sample showed the wall had moved again into `drainer_immediate_threats_with_hash` and `can_attack_target_on_board_with_hash`.
- The latest global immediate-threat cache follow-up was not worth keeping: caching exact immediate drainer threats by `(board_hash, color, location)` preserved focused invariants, but the retained hotspot stayed effectively unchanged at about `245.21ms` on `human_win_pro_a`, with counters flat. That means the new wall is not repeated exact immediate-threat lookup in a form that a simple global cache can amortize cheaply.
- The latest retained exact cut is worth keeping too: exact immediate drainer-threat counting now probes only the fixed local mystic / demon / bomb source squares around the target instead of scanning every occupied enemy mon. Stable hotspot reruns moved `human_win_pro_a` to about `240.01ms` and `241.65ms`, and the retained final rerun after reverting a failed follow-up was about `243.03ms`, with exact counters essentially unchanged. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `3:31` on 2026-03-27 and had to be killed. The fresh retained duel sample showed `drainer_immediate_threats_with_hash` falling out of the top stack; the wall is now mostly `can_attack_target_on_board_with_hash` under budget-one tactical spirit work.
- The latest budget-0/1 attack fast-path follow-up was not worth keeping: replacing the generic attack BFS with a direct zero/one-move evaluator preserved focused parity, but the live hotspot regressed to about `245.34ms` on `human_win_pro_a` and raised `payload_calls` to about `852594`. That means small-budget attack queries still benefit from the existing plausibility screen plus generic BFS more than from a special-case path.
- The latest shared budget-one secure-mana summary follow-up was not worth keeping either: solving the `remaining_moves == 1` supermana and opponent-mana secure queries together on the same board did cut `secure_mana_calls` on `human_win_pro_a` from about `5126` to about `4720`, but it raised `attack_calls` from about `36272` to about `36847` and `payload_calls` from about `850393` to about `866232`. The confirming hotspot rerun drifted back to about `242.96ms`, so the saved duplicate secure-query wrapper work did not beat the retained attack/payload mix.
