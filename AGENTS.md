# Agent Instructions

- Use `./scripts/run-rules-tests.sh` for the canonical compressed rules regression corpus; do not unpack or regenerate it during unrelated work.
- Treat `test-data/complete-games/v1/` as an immutable source corpus. Never delete, regenerate, normalize, deduplicate, reorder, replace, or otherwise rewrite it during cleanup or unrelated work. Put new source revisions in a new version directory and keep derived data under `target/` or `/tmp`.
- Preserve the payload bytes in `test-data/rules-regressions.jsonl.gz`, `test-data/automove-decisions/v1/`, `test-data/automove-decisions/v4/`, `test-data/automove-decisions/v5/`, `test-data/automove-decisions/v6/`, `test-data/automove-decisions/v7/`, `test-data/automove-decisions/v8/`, `test-data/automove-decisions/v9/`, `test-data/automove-decisions/v10/`, `test-data/automove-decisions/v11/`, `test-data/automove-decisions/v12/`, `test-data/automove-decisions/v13/`, `test-data/automove-decisions/v14/`, `test-data/automove-decisions/v15/`, `test-data/automove-decisions/v16/`, and `test-data/compatibility-edge-cases/v1/`. Put revised payloads in a new version directory instead of rewriting existing data.
- Avoid code comments except for TODOs or essential context.
