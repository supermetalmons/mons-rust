# Automove Knowledge

This document keeps only durable lessons that should outlast the current session.

Use `HOW_TO_ITERATE_ON_AUTOMOVE.md` for the operator loop and `AUTOMOVE_IDEAS.md` for the next live split.

## Stable Runtime Truths

- Shipping Pro is `runtime_current`, not the retained challenger profiles.
- `runtime_pro_turn_engine_v30` is the only live Pro challenger. Treat it as a guarded `ProV2` path with deliberate opening and early-white fallbacks, not a raw always-on engine.
- `runtime_pro_turn_engine_v1` is reference-only regression history.
- Full-turn planning can beat per-input expansion, but only when it survives the real ranked-root surface and direct Pro-vs-Pro duel evidence.
- Opportunity-context extraction is worth keeping separate from raw input search. It is useful shared structure for planner seeds and selector guards.
- Continuation and no-plan reuse matter, but cache keys must include a config fingerprint.
- Drainer safety, root reply-risk guards, and efficiency tie-breaks still earn their keep because they kill fake-good roots cheaply.
- Opening-specific latency still matters. A candidate that stalls on the first real black reply is not promotable.
- Hybrid fallbacks must respect retained opening and eligibility guards before they call expensive plan probes; otherwise the fallback machinery itself becomes the regression.
- Wrapper-only tuning saturates quickly. Once the obvious knobs are already in place, the next gain usually has to come from shared engine/search code.
- Config knob space is effectively exhausted. Future gains need new code, not more `SmartSearchConfig` permutations.
- Production wasm still needs single-shot, predictable search. Deferred or post-return work is not release-safe.

## Durable Workflow Rules

- Keep the active frontier small: one live idea, one candidate, one canonical path.
- For Pro work, compare directly against `runtime_current`, not the script's compatibility default baseline.
- The canonical Pro path is `guardrails -> pro-triage(primary_pro) -> runtime-preflight -> pro-reliability`.
- Pro promotion now requires both direct matchups: candidate Pro versus current Pro, and candidate Pro versus current Normal, each at the same `0.90 / 0.99 / 700ms` floor.
- `opening_reply` is a narrow fallback-order and opening-regression surface, not the default Pro surface.
- `pro-triage` only counts as a pass when the target surface changes and off-target churn stays at `<= 1`.
- `runtime-preflight` is the required stamp before duel stages unless the run is intentionally diagnostic.
- `pro-reliability` is the first real promotion gate, and it now checks both current-Pro and current-Normal duels. `pro-fast-screen`, `pro-progressive`, and `pro-ladder` come later.
- If a surface is already calibrated, do not move the CPU gate back in front of triage.
- `audit-screen` and `pro-audit-screen` are reject diagnostics, not promotion evidence.
- Prefer a fresh live `pro-reliability` sample over the hotspot probe when the wall is unclear or has moved.
- Use the hotspot probe only after a real duel stall, and only to narrow the next code surface.
- Compress the lesson immediately when a run matters. Logs, stamps, and process samples are disposable evidence, not memory.
- Keep ignored harness test names unique; `cargo test` substring filters can run the wrong stage.

## Engine And Search Lessons Worth Keeping

- If a caller only consumes spirit-assisted exact fields plus safe-progress fields, do not request the exact tactical `SCORE_WINDOW` payload there.
- If a caller only needs the active-turn score window, route it through a tactical score-window projection instead of a full turn summary.
- Cheap lower-bound drainer-attack screens are worth keeping when they can reject impossible child vulnerability probes before exact reach work.
- On one-shot spirit-preview paths, local work can beat repeated filtered global cache queries.
- Mutate/undo preview application is often better than clone-per-preview on exact tactical spirit paths.
- After a cut moves the wall, follow the moved wall immediately instead of iterating again on the old surface.
- When a new helper removes one hotspot, verify that the helper itself did not become the new wall before going deeper.
- If a retained optimization shifts cost from summary construction to direct point queries on the same caller surface, follow the caller that still emits those queries.
- Low-budget exact fast paths are worth keeping when they can prove a drainer pickup or immediate window is impossible without entering the full BFS / pickup-window path.
- Low-budget drainer pickup windows are also worth direct fast paths. They can remove most uncached pickup BFS work from the live hotspot, even if that does not guarantee a duel-stage wall-clock win by itself.
- Sharing local after-window cache entries across score/denial flag variants is not enough by itself when the underlying after-window projection still runs at nearly the same frequency.
- When a tactical result can be masked exactly, reuse cached superset flag results for smaller tactical spirit, immediate-window, pickup-window, and projection queries instead of rebuilding each flag subset.
- Budgeted carrier-to-pool queries should not compute full shortest paths. When the caller only needs reachability within `N`, use a bounded search and do not hash the whole board just to initialize an actor move memo that ignores that hash.
- Once pickup BFS cost drops, full-board exact hash rebuilds on spirit preview surfaces become a real wall. Keep exact board hashing incrementally maintainable so preview code can update touched squares instead of rescanning the board.
- Zero-move after-window tactical spirit queries do not need the full immediate-window helper. When a spirit preview leaves no remaining mon moves, derive the exact tactical window from touched mana-pool count deltas instead of re-entering the cached search path.
- One-move after-window tactical spirit queries also admit a local exact path. If the spirit preview leaves one remaining mon move, update the touched-neighborhood one-move tactical contributions instead of rescanning the whole immediate-window helper surface.
- Do not keep a local budget-2 after-window tactical summary just because it collapses helper-call counts. If building that summary raises hotspot wall-clock and the duel still stalls, the real wall has moved into the remaining payload reachability work, not the raw query counter.
- Do not assume board-scoped actor-move memo reuse will clear the remaining wall. Sharing payload transition memos across immediate-window and budget-1 summary searches can reduce some repeated pickup and carrier transition work without moving the duel if the underlying payload reachability algorithm is still the bottleneck.
- Do not assume carrier-only memo shrink or a local small-budget carrier frontier will clear the remaining wall either. If `payload_calls` stay flat and hotspot wall-clock barely moves, the bottleneck is deeper than generic carrier memo initialization or queue setup.
- If you try board-scoped reverse carrier distance maps, keep them bounded and lazy. Building a reverse map on the first bounded carrier query is too broad and can regress wall-clock even while `payload_calls` drop; only build it after the board/budget pair proves it has multiple distinct carrier queries.
- Spirit previews also admit exact far-touch reuse. If the preview only touches locations outside the remaining-budget neighborhood of every allied carrier and drainer, reuse the base after-action board for after-window and secure-mana work instead of querying the preview board separately.
- Do not assume replacing the remaining `>1` after-window helper calls with local actor summaries will help either. Even when the preview only affects one allied actor, collapsing immediate-window / after-window query counts can still raise underlying payload reachability work and regress wall-clock.
- Exact-safe geometry lower bounds are worth using inside bounded payload helpers. If even the empty-board distance to a pool, or to a relevant mana-plus-pool line, exceeds the caller's budget, reject before entering payload BFS.
- Exact-safe geometry lower bounds are also worth using inside bounded payload BFS itself. If `steps + empty-board remaining-distance lower bound > max_steps`, skip expanding that state instead of paying transition work that cannot reach a legal bounded finish.
- Do not assume filtered pickup-path reuse via the pickup-window surface will move the real wall. It can trim payload work on some secondary fixtures without materially improving the live `human_win_pro_a` hotspot.
- Do not assume stronger static exact empty-board carrier/drainer distance tables will beat the cheap geometric bounds either. If the retained deeper-pruning line already has wrapper and per-state lower bounds, swapping in exact empty-board tables can stay flat on the live hotspot.
- Do not assume exact one-actor after-window reuse will help either. Even when it preserves the retained exact search surface and collapses full immediate-window rebuilds, flat payload work can leave wall-clock slightly worse than the retained deeper-pruning baseline.
- Do not assume low-budget drainer frontier dedupe will help either. The actor-move memo already caches repeated `(payload, destination)` transition checks, so adding a separate seen set to the `0..=3` pickup fast path can be pure overhead.
- Do not assume lazy actor-summary setup rescues one-actor after-window reuse. If delaying the base summary build and adding an early-exit affected-actor gate still leaves `payload_calls` flat, eager setup was not the real wall.
- Do not assume a bounded reverse drainer-pickup map will help either. Even with lazy warmup keyed by board, color, and budget, the map build can cost more than the repeated bounded drainer queries it replaces while leaving the main hotspot's payload work flat.
- Do not assume all memo-shape work is exhausted after the rejected shared and carrier-only memo cuts. A truly drainer-only payload-transition memo on pickup-window and filtered-path callers can still buy a real wall-clock win by shrinking fixed per-query transition overhead, even when `payload_calls` stay flat.
- Attack reach also admits exact-safe payload pruning. Single-target attack queries should skip individual attackers that have no exact-safe geometric path to threaten the target at all, and attack BFS should never enter mana states because carrying mana cannot contribute to an attack outcome on that surface.
- Attack reach also admits per-state lower-bound pruning after the impossible-attacker and mana-state cuts. If a state's cheapest exact-safe path to either an action source or a bomb attack still exceeds the remaining budget, skip expanding it.
- Do not assume broader query-aware actor gating on spirit after-window masks will help either. Even when low-score carrier and drainer neighborhoods drop out on score/denial-only paths under optimistic one-step preview relevance checks, the live hotspot can stay flat with unchanged payload work.
- Do not assume local precomputed target-lower-bound tables will help either. Replacing repeated attack action/bomb lower-bound arithmetic with per-target arrays can regress badly if the table build cost exceeds the saved repeated checks.
- Do not assume enabling full scoring-side attack summaries will help either. On the live `runtime_pro_turn_engine_v30` wall, turning on local scoring attack summaries and target narrowing can explode summary builds while leaving `payload_calls` flat and regressing wall-clock, so repeated scoring attack/threat point queries are not the right surface for full-board summary construction.
- Do not assume a cheaper scoring-side attack-summary warmup will help either. If a deferred "build after the second distinct target" trigger never fires on the live hotspot and `attack_summary_builds` stays at `0`, then dormant scoring attack/threat query repetition is not the remaining wall.
- Do not assume a board-scoped shared attack move memo will help either. If same-board uncached attack queries preserve parity in a focused test but the live hotspot leaves `payload_calls` flat, then repeated same-board attack transition lookup is not the wall in a form that a shared memo can amortize cheaply.
- Do not assume enqueue-time attack short-circuits will help either. Returning immediately on newly reached attack states and pruning doomed next states can reduce `payload_calls`, yet still lose wall-clock if the added per-neighbor target checks cost more than the saved queue churn.
- Do not assume lighter attack-side cache reuse is automatically worth keeping either. Removing a duplicated uncached attack prescan, caching exact drainer immediate-threat point queries, and reusing monotonic attack results across nearby budgets can all reduce counters (`threat_calls`, `attack_hits`, or a few `payload_calls`) while leaving the live hotspot flat to worse; if `human_win_pro_a` does not move materially, those fixed-cost attack/threat helpers are not the next wall.
- Do not assume broader exact drainer-threat prechecks will help either. Routing drainer danger or vulnerability callers through extra immediate-threat checks can cut `attack_calls`, `payload_calls`, and even `threat_calls`, yet still regress the live hotspot if the new precheck path adds more fixed work than it saves.
- Do not retain tiny exact drainer-safety or attack start-state setup skips on a single promising hotspot sample. Same-session A/B reruns against the reverted retained baseline can show those micro-cuts are pure noise even when the first rerun looked faster.

## Mistakes Not To Repeat

- Do not keep historical profiles live after their lesson is absorbed.
- Do not treat `target/experiment-runs` or `target/experiment-stamps` as durable memory.
- Do not reopen a branch just because it looked good in a bounded screen; reopen only with a new hypothesis.
- Do not spend ladder budget on branches that are still flat in the first real duel stage.
- Do not trust pooled or sampled losses as selector evidence when direct reliability tracing shows `disagreements=0`.
- Do not let Pro-specific selector flow bypass opening-book fallback ordering.
- Do not inject planner roots globally. Crisis-only gating is safer, and even then it must prove direct value against `runtime_current`.
- Do not keep sprawling wrapper split families alive once one retained frontier is clearly dominant.
- Do not use fixture additions or hotspot work as an excuse to avoid live Pro evidence.

## Current Durable Direction

- The current problem is not proving that Pro full-turn planning can ever be stronger. The retained code already shows that it can be.
- The current problem is finishing the earned path cleanly with one retained challenger under strict gates.
- The latest shared exact cuts moved the wall from planner/oracle summary construction into tactical projection itself.
- `runtime_pro_turn_engine_v30` is still not promotable after the current cuts because `pro-reliability` continues to stall in a practical window.
- The latest `exact_tactical_spirit_summary` cache-axis-sharing tweak only shaved the hotspot slightly; it did not materially reduce tactical projection call volume.
- The follow-up superset-cache cuts were worth keeping and moved `human_win_pro_a` again, from about `1425ms` down to about `1305ms`, but `pro-reliability` still stalled past `2:17`.
- The follow-up bounded carrier-window cut was also worth keeping: it collapsed `human_win_pro_a` payload work from about `32.2m` to about `6.3m`, but `pro-reliability` still stalled past `2:17`.
- The follow-up low-budget pickup-window, score-floor after-window, and incremental board-hash cut was also worth keeping: it removed most uncached pickup BFS from the hotspot and moved `human_win_pro_a` from about `570ms` to about `520ms`, but `pro-reliability` still stalled past `2:20` on 2026-03-27.
- The follow-up zero-move after-window tactical cut was also worth keeping: exact touched-item mana-pool updates moved `human_win_pro_a` from about `520ms` to about `433ms` and dropped immediate-window / after-window query volume from about `428999` / `427069` to about `168068` / `166138`, but `pro-reliability` still had to be killed at `3:37` on 2026-03-27.
- The follow-up budget-1 after-window tactical cut was also worth keeping: touched-neighborhood one-move updates dropped `human_win_pro_a` immediate-window / after-window query volume from about `168068` / `166138` to about `41740` / `39810` and moved the hotspot from about `433ms` to about `413ms`, but `pro-reliability` still had to be killed at `2:35` on 2026-03-27.
- A follow-up local budget-2 after-window summary was not worth keeping: it dropped `human_win_pro_a` immediate-window / after-window query volume from about `41740` / `39810` to about `7590` / `5660`, but the hotspot regressed from about `420ms` to about `452ms` and `pro-reliability` still had to be killed at `2:35` on 2026-03-27.
- A follow-up shared actor-move-memo cut was not worth keeping either: it only trimmed `human_win_pro_a` payload calls slightly and reduced pickup-window calls from about `2910` to about `2218`, but the hotspot regressed from about `420ms` to about `433ms` and `pro-reliability` still had to be killed at `2:34` on 2026-03-27.
- A follow-up carrier-specific move-memo plus small-budget carrier fast path was not worth keeping either: it left `human_win_pro_a` effectively flat at about `411.32ms` versus the retained `411.56ms` with `payload_calls` still about `5083818`, so shrinking generic carrier BFS setup cost did not move the real wall.
- A follow-up lazy bounded reverse carrier-distance map was worth keeping: once it only built after repeated bounded carrier queries on the same board/budget, `human_win_pro_a` moved from the retained `411.56ms` down to about `404.74ms` and then `398.73ms`, with `payload_calls` down to about `4922742`, but `pro-reliability` still had to be killed at about `2:45` on 2026-03-27.
- A follow-up far-preview base-board reuse cut was also worth keeping: reusing the action board when a spirit preview only touched locations outside every relevant actor's bounded neighborhood moved `human_win_pro_a` down again to about `383.97ms` and then `389.66ms`, with `payload_calls` down to about `4506607` and immediate-window / after-window queries down to about `27900` / `25970`, but `pro-reliability` still had to be killed at about `2:47` on 2026-03-27.
- A follow-up local actor-summary replacement for the remaining `>1` after-window queries was not worth keeping. The broad version collapsed `human_win_pro_a` immediate-window / after-window counts to about `1930` / `0`, and the one-actor-gated version still dropped them to about `3551` / `1621`, but both raised `payload_calls` above the retained `4506607` and regressed wall-clock (about `390.28ms` and `403.34ms` respectively), so the live wall is deeper than helper-call count alone.
- A follow-up bounded carrier/drainer lower-bound cut was worth keeping: rejecting impossible bounded pool and pickup lines with empty-board geometry moved `human_win_pro_a` down again to about `351.67ms` and `354.94ms`, with `payload_calls` down to about `3562056` and pickup calls down to about `2684`, but `pro-reliability` still had to be killed at about `2:34` on 2026-03-27.
- A follow-up deeper bounded payload-pruning cut was also worth keeping: bounded carrier BFS and drainer pickup search now prune individual states whose empty-board remaining-distance lower bound already exceeds the remaining budget, which moved `human_win_pro_a` down again to about `344.52ms` and `347.07ms`, with `payload_calls` down to about `3397716`, but `pro-reliability` still had to be killed at about `2:28` on 2026-03-27.
- A follow-up filtered pickup-path reuse cut was not worth keeping: deriving `exact_best_drainer_pickup_path_filtered_with_hash` from the pickup-window surface reduced `payload_calls` on several secondary fixtures, but `human_win_pro_a` stayed effectively flat at about `348.72ms` / `349.09ms` with `payload_calls` still about `3327205`, so duplicate filtered pickup search is not the live wall by itself.
- A follow-up static exact empty-board carrier/drainer distance-table cut was also not worth keeping: replacing the cheap geometric lower bounds with exact empty-board tables left the retained hotspot effectively flat at about `350.08ms` with `payload_calls` back at about `3397716`, so stronger static bounds did not materially improve the live wall on 2026-03-27.
- A follow-up exact one-actor after-window reuse cut was not worth keeping either: it collapsed `human_win_pro_a` immediate-window queries from about `27900` to about `1930`, but fresh stable reruns sat at about `347.56ms` and `347.95ms` with `payload_calls` still about `3398229`, so wall-clock regressed slightly versus the retained deeper-pruning baseline and the branch was killed before another duel loop on 2026-03-27.
- A follow-up low-budget drainer frontier-dedupe cut was not worth keeping either: deduping `0..=3` pickup fast-path frontier states left hotspot counters effectively flat because the actor-move memo already reused repeated `(payload, destination)` transition checks, so the extra seen bookkeeping was pure overhead and the branch was killed at the hotspot stage on 2026-03-27.
- A follow-up lazy one-actor after-window reuse cut was not worth keeping either: delaying base action-board actor-summary construction and adding an early-exit one-actor gate still collapsed `human_win_pro_a` immediate-window queries from about `27900` to about `1930`, but fresh stable reruns sat at about `354.33ms` and `350.92ms` with `payload_calls` still about `3398229`, so eager summary setup was not the real regression source and the line still lost to the retained deeper-pruning baseline on 2026-03-27.
- A follow-up bounded reverse drainer-pickup map cut was not worth keeping either: lazily building a board/color/budget reverse pickup map after repeated bounded drainer queries preserved exact parity, but the first hotspot sample regressed `human_win_pro_a` to about `362.08ms` with `payload_calls` still about `3397716` and also raised payload work on secondary fixtures, so the map build overhead lost to the retained small-budget pickup path on 2026-03-27.
- A follow-up drainer-only move-memo specialization was worth keeping: replacing the generic actor move memo with a smaller drainer-only memo on pickup-window and filtered-path callers left `human_win_pro_a` `payload_calls` flat at about `3397716`, but stable hotspot reruns improved wall-clock from the retained `349.88ms` down to `340.61ms` / `340.60ms`, which means fixed drainer transition overhead was still worth trimming. The cut still did not promote, because `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed while `pro-reliability` was still live at about `2:40` on 2026-03-27 and had to be killed.
- A follow-up attack-path pruning cut was also worth keeping: single-target attack reach now skips per-attacker BFS when that attacker has no exact-safe geometric path to ever threaten the target, and attack BFS no longer enters mana states that cannot contribute to an attack. That collapsed `human_win_pro_a` `payload_calls` from about `3397716` to about `1567910` and improved stable hotspot reruns to about `326.49ms` / `312.30ms`. The cut still did not promote, because `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed while `pro-reliability` was still live at about `3:00` on 2026-03-27 and had to be killed.
- A follow-up attack-path lower-bound cut was also worth keeping: single-target attack reach now prunes individual states whose exact-safe remaining attack lower bound already exceeds the remaining budget. That moved `human_win_pro_a` down again to about `303.51ms` / `299.94ms` and cut `payload_calls` from about `1567910` to about `1202108`. The cut still did not promote, because `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed while `pro-reliability` was still live at about `3:00` on 2026-03-27 and had to be killed.
- A follow-up attack-only move-memo and two-state seen-set cut was not worth keeping either: shrinking the attack BFS state shape to `None`/`Bomb` only preserved exact parity, but the live `human_win_pro_a` hotspot regressed to about `303.79ms` with `payload_calls` still about `1202108`, so generic memo width was not the remaining attack wall on 2026-03-27.
- A follow-up lazy attack min-steps cache cut was not worth keeping either: promoting repeated board/target attack queries onto a shared min-step cache, first at full-budget and then only after a second distinct budget, improved some secondary fixtures but still regressed the live hotspot to about `322.29ms` and then `320.19ms`. The selective version left `human_win_pro_a` `payload_calls` flat at about `1202108`, so budget-axis attack-query reuse was not the next wall on 2026-03-27.
- A follow-up query-aware after-window reach-mask cut was not worth keeping either: excluding low-score carrier neighborhoods from spirit after-window reuse did reduce `human_win_pro_a` immediate-window / after-window counts from about `27900` / `25970` to about `26962` / `25032`, but stable hotspot reruns stayed flat to worse at about `305.52ms` and `303.63ms`, and a lower-overhead fixed-slot follow-up still produced a `316.84ms` rerun. That means local query-aware after-window reach filtering is not enough to clear the remaining tactical wall on 2026-03-27.
- A follow-up query-aware actor-gated after-window mask cut was not worth keeping either: excluding entire low-score carrier and drainer neighborhoods on score/denial-only spirit paths, while still keeping drainers relevant under optimistic one-step drainer/mana spirit shifts, left the live hotspot effectively flat at about `301.77ms` with `payload_calls` still about `1202108`. That means broader score-floor actor gating on tactical spirit reuse is still not enough to move the remaining wall on 2026-03-27.
- A follow-up local attack target lower-bound context cut was not worth keeping either: precomputing per-target action and bomb lower-bound tables so plausibility and BFS could share them regressed the live hotspot badly to about `415.03ms`, and `payload_calls` even rose slightly to about `1208336`. That means the remaining attack wall is not repeated target-lower-bound arithmetic; the local table build cost lost to the retained direct checks on 2026-03-27.
- A follow-up lazy scoring attack-summary warmup cut was also not worth keeping on 2026-03-27: deferring full scoring attack-summary construction until the second distinct target preserved parity, but the live hotspot still showed `attack_summary_builds=0` and regressed to about `315.71ms` after a `302.61ms` noise sample. That means the remaining wall is not latent scoring attack/threat query repetition waiting for a cheaper summary trigger.
- A follow-up board-scoped shared attack move-memo cut was also not worth keeping on 2026-03-27: reusing actor payload transitions across same-board uncached attack queries preserved parity in a focused test, but the live hotspot left `payload_calls` flat at about `1202108` and regressed to about `315.99ms` / `315.26ms`. That means repeated same-board attack transition lookup is not the remaining wall in a form that a shared memo can amortize cheaply.
- A follow-up attack enqueue-time short-circuit cut was also not worth keeping on 2026-03-27: returning immediately on newly reached attack states and pruning doomed next states cut `human_win_pro_a` `payload_calls` from about `1202108` to about `1110231`, but stable hotspot reruns still regressed to about `330.56ms` and `317.77ms`. That means reducing attack queue churn alone is not enough if the extra per-neighbor target checks cost more than the saved payload expansion.
- A local uncached after-window path that bypassed the retained exact caches was not worth keeping; it increased pickup work and did not beat the retained bounded-cache line.
- A score-floor threshold on after-window tactical queries can trim some off-target immediate-window work, but the live `human_win_pro_a` wall still emits roughly the same after-window query volume.
- Once the zero-move, budget-1, and far-preview branches are removed, the remaining live wall is the payload reachability work that survives under the remaining tactical paths; further budget-count collapse alone is not enough.
- Next code should target the number of remaining after-window tactical queries or the spirit-preview fanout that creates them, not planner-oracle wrapper code or more per-query micro-optimizations alone.
- A broad drainer-threat precheck follow-up was not worth keeping on 2026-03-27: immediate-threat short-circuits ahead of exact drainer danger / vulnerability paths reduced `human_win_pro_a` `attack_calls` to about `56923`, `payload_calls` to about `1193838`, and, with a local exact threat cache, `threat_calls` to about `24769`, but the stable hotspot still regressed to about `323.66ms`.
- A local exact drainer-safety early-exit follow-up was also not worth keeping on 2026-03-27: skipping exact attack reach inside `exact_own_drainer_safety_score` when immediate or walk threat already proved the drainer unsafe initially looked faster, but same-session A/B reruns put the candidate at about `320.20ms` while the reverted retained baseline landed at about `302.18ms`, so the apparent win was probe noise.
- A local attack start-state fast-path follow-up was also not worth keeping on 2026-03-27: bypassing queue / memo setup when an attacker already threatened the target from its start square left `human_win_pro_a` counters unchanged and regressed the hotspot to about `324.15ms`, so fixed immediate-start attack setup is not the remaining wall.
- A same-state exact opportunity-context cache follow-up was not worth keeping on 2026-03-27: it moved `human_win_pro_a` only to about `300.33ms` / `300.66ms` from the retained `302.27ms`, but it regressed `primary_spirit_setup` to about `224ms` and raised secondary tactical-spirit / secure-mana / pickup work, so repeated same-state opportunity assembly is not the live wall.
- A board/color exact drainer-safety cache follow-up was worth keeping on 2026-03-27: caching the full safety score and routing turn-engine / planner safety wrappers through it moved stable `human_win_pro_a` to about `300.03ms` / `298.27ms` with a retained rerun at `299.12ms`, while cutting `attack_calls` to about `48411` and `threat_calls` to about `72268` from the prior retained `58225` / `82082`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `3:30` and had to be killed.
- A broader exact drainer-threat snapshot / scoring memo follow-up was not worth keeping on 2026-03-27: board/location threat snapshots collapsed `human_win_pro_a` `attack_calls` to about `22092` and `payload_calls` to about `1130540`, but stable hotspot reruns regressed to about `304.45ms` and `320.43ms`, so broader point-snapshot caching just shifted work around instead of improving the live wall.
- A safe-drainer-mana wrapper follow-up was also not worth keeping on 2026-03-27: routing `own_drainer_carries_safe_mana` through the retained safety cache left `human_win_pro_a` effectively flat at about `299.13ms`, with `attack_calls` only down from about `48411` to `48331`, so that wrapper was too small to matter.
- A scoring exact danger-point memo follow-up was also not worth keeping on 2026-03-27: adding a board-local `(color, location)` exact danger memo inside `ScoringEvalContext` preserved parity, but the hotspot only moved to about `296.45ms` on the first rerun before regressing to about `303.81ms` on the confirming rerun, with exact-side counters flat at about `attack_calls=48411` and `payload_calls=1199361`. That means the apparent win was not a stable live-wall move and did not justify another canonical Pro loop.
- A shared attack target setup follow-up was also not worth keeping on 2026-03-27: sharing `target_guarded`, bomb-pickup locations, and the plausible-attacker subset between the attack plausibility screen and attack BFS preserved parity, but stable hotspot reruns regressed to about `298.30ms` and `300.74ms` with exact-side counters flat at about `attack_calls=48411` and `payload_calls=1199361`. That means duplicate pre-BFS board scans and bomb-list setup are not the live wall either.
- A planner-side attack-hoist follow-up was also not worth keeping on 2026-03-27: hoisting repeated root-board drainer-attack checks in the planner and routing planner attack probes through the hash-aware exact helper did reduce a focused zero-setup planner test to one exact attack query, but stable hotspot reruns still regressed to about `298.40ms` and `297.05ms` with exact-side counters flat at about `attack_calls=48411` and `payload_calls=1199361`. That means planner-side attack cache-hit and board-hash overhead is not the live wall either.
- An exact secure-mana lower-bound reject cut was worth keeping on 2026-03-27: after a fresh live `pro-reliability` sample put `exact_apply_secure_drainer_walk_in_place` at the top of the retained duel stack, exact secure-mana search learned to reject impossible wanted-mana lines with a cheap wanted-mana distance lower bound before recursive secure-walk mutation. Stable hotspot reruns moved `human_win_pro_a` to about `269.59ms` and then `257.42ms`, with `attack_calls` down to about `36272`, `payload_calls` down to about `1194927`, and `secure_mana_calls` down from about `10304` to about `5126`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `3:37` and had to be killed.
- An exact filtered-pickup pruning cut was also worth keeping on 2026-03-27: exact filtered drainer-pickup search now rejects bounded impossible lines up front and prunes BFS once the current best pickup metric can no longer be beaten. Stable hotspot reruns moved `human_win_pro_a` to about `258.31ms`, `245.64ms`, and then `242.41ms`, with `payload_calls` down from about `1194927` to about `850393`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `3:14` and had to be killed. A fresh retained duel sample showed the wall had moved again into `drainer_immediate_threats_with_hash` and `can_attack_target_on_board_with_hash`.
- A global exact immediate-threat cache follow-up was not worth keeping on 2026-03-27: caching exact immediate drainer threats by `(board_hash, color, location)` preserved focused invariants, but the retained hotspot stayed effectively unchanged at about `245.21ms` on `human_win_pro_a`, with counters flat. That means the new wall is not repeated exact immediate-threat lookup in a form that a simple global cache can amortize cheaply.
- An exact local immediate-threat geometry cut was worth keeping on 2026-03-27: `drainer_immediate_threats_with_hash` now probes only the fixed local mystic / demon / bomb source squares around the target instead of scanning every occupied enemy mon. Stable hotspot reruns moved `human_win_pro_a` to about `240.01ms` and `241.65ms`, and the retained final rerun after reverting a failed follow-up was about `243.03ms`, with exact counters essentially unchanged. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `3:31` and had to be killed. A fresh retained duel sample showed `drainer_immediate_threats_with_hash` dropping out of the top stack; the remaining wall is mostly `can_attack_target_on_board_with_hash` under budget-one tactical spirit work.
- A budget-0/1 exact attack fast-path follow-up was not worth keeping on 2026-03-27: replacing the generic attack BFS with a direct zero/one-move evaluator preserved focused parity, but the live hotspot regressed to about `245.34ms` on `human_win_pro_a` and raised `payload_calls` to about `852594`. That means small-budget attack queries still benefit from the existing plausibility screen plus generic BFS more than from a special-case path.
- A shared budget-one secure-mana summary follow-up was also not worth keeping on 2026-03-27: solving the `remaining_moves == 1` supermana and opponent-mana secure queries together on the same board did reduce `human_win_pro_a` `secure_mana_calls` from about `5126` to about `4720`, but it raised `attack_calls` from about `36272` to about `36847` and `payload_calls` from about `850393` to about `866232`. The confirming hotspot rerun drifted back to about `242.96ms`, so removing duplicate secure-query wrapper work did not beat the retained attack/payload mix.
- A direct secure-mana cache-hit cut was worth keeping on 2026-03-27: `exact_secure_specific_mana_steps_on_board` now checks the existing secure-mana cache before building a synthetic `MonsGame`, so repeated same-board secure queries can return on cache hits without paying `board.clone()` and `clone_for_simulation()`. Stable hotspot reruns left exact counters flat but moved `primary_spirit_setup` from about `151.51ms` down to `139.84ms` / `139.39ms` and `human_win_pro_a` from about `233.53ms` down to `218.81ms` / `218.87ms`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `2:59` and had to be killed, so removing secure-query wrapper overhead was real but still not enough to clear the duel gate.
- An upfront secure-state-hash follow-up was not worth keeping on 2026-03-27: moving the direct secure-mana cache lookup ahead of `find_awake_drainer(...)` and the wanted-mana lower-bound reject forced every secure query to pay `exact_secure_board_state(...)` first, and the hotspot regressed. On the rejected branch, `human_win_pro_a` moved back to about `229.80ms` and `235.68ms` from the retained `218.81ms` / `218.87ms`, while `primary_spirit_setup` failed to beat the retained line at about `140.41ms` and then jumped to about `164.36ms`. The counters also worsened on the live wall (`human_win_pro_a` `secure_mana_calls` rose from about `5126` to `6599`, and `primary_spirit_setup` from about `108825` to `111516`), so upfront secure-state hashing costs more than the cheap drainer / wanted-mana gating it replaces.
- A drainer-safety exact-safe short-circuit follow-up was not worth keeping on 2026-03-27: skipping the exact next-turn attack query once immediate or walk threat had already forced the drainer-safety score to `-2` preserved parity, but the live hotspot barely changed and regressed in wall-clock. `human_win_pro_a` moved from the retained about `221.74ms` to about `224.33ms`, while `attack_calls` only fell from about `36272` to `36244`, so wrapper-side exact-safe short-circuits are not the remaining attack wall.
- An attack-source availability cut was worth keeping on 2026-03-27: attack plausibility and per-state attack lower bounds now ignore mystic/demon action source squares that are statically blocked on the current board, including demon lines whose middle square is occupied. Stable hotspot reruns moved `human_win_pro_a` to about `218.28ms` / `218.15ms` from the retained `218.81ms` / `218.87ms`, while `payload_calls` dropped from about `850393` to about `735490`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `3:49` and had to be killed, so the cut trims impossible attack paths without clearing the duel gate.
- A board-aware bomb-center attack follow-up was not worth keeping on 2026-03-27: replacing the geometric bomb lower bound with a scan of occupiable blast-center squares around the target did cut `human_win_pro_a` `payload_calls` further from about `735490` to about `720943`, but wall-clock regressed badly to about `264.03ms` and also pushed `primary_black_loss_opening_a_ply19` up to about `74.96ms`. The extra per-query bomb-center scan costs more than the payload it saves, so board-aware bomb blast geometry is not the next live wall.
- An attack BFS queue rewrite was not worth keeping on 2026-03-27: replacing the hot attack-path `VecDeque` pop-front loops with flat `Vec` plus head-index iteration produced a small stable hotspot win, moving `human_win_pro_a` from about `218.04ms` to about `217.05ms` / `216.82ms`, but exact counters stayed flat and the canonical gate still stalled. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live past about `4:49`, so pure queue bookkeeping reduction is not enough to reduce real duel friction.
- A fresh retained `pro-reliability` sample still pointed at budget-one tactical spirit work on 2026-03-27: the live stack put `exact_tactical_spirit_summary` and `exact_budget_one_tactical_counts_after_touched_locations` essentially tied at the top of the retained duel run, with `can_attack_target_on_board_with_hash` still present but smaller. That means the remaining attack wall is still mostly a child cost under budget-one tactical spirit fanout, not a standalone planner or scoring surface.
- A budget-one touched-location allocation follow-up was not worth keeping on 2026-03-27: removing the temporary dedup `Vec<Location>` from `exact_budget_one_tactical_counts_after_touched_locations` preserved parity and briefly moved `human_win_pro_a` into about `215.03ms` / `217.62ms` from the retained about `218.09ms`, but exact counters stayed flat and `primary_spirit_setup` regressed into about `149-150ms` from about `139.73ms`. The live sample was right that the function is hot, but eliminating its local dedup/allocation churn alone is not enough to reduce the real duel wall.
- A larger attack-cache follow-up was not worth keeping on 2026-03-27: doubling the exact attack reach cache to `16384` and pre-sizing it increased `human_win_pro_a` `attack_hits` from about `20715` to about `23733` and cut `payload_calls` from about `735490` to about `641875`, but stable hotspot reruns regressed badly to about `230.42ms` and `228.93ms`. More attack-cache residency just traded payload BFS for slower hash-table work.
- A larger tactical-spirit cache follow-up was also not worth keeping on 2026-03-27: doubling the exact spirit/tactical spirit cache cap to `4096` and pre-sizing those tables increased `human_win_pro_a` `tactical_spirit_hits` from about `895` to about `1080` and cut immediate-window / tactical-window queries from about `27900 / 25970` to about `27353 / 25565`, but stable hotspot reruns still regressed to about `230.66ms` and `232.08ms`. More tactical cache residency is not enough; larger hash tables cost more than the saved spirit-summary misses.
- A tactical-spirit base-score reuse follow-up was not worth keeping on 2026-03-27: reusing the pre-spirit score window from `exact_tactical_spirit_summary(...)` instead of the standalone `exact_best_immediate_score_on_board(...)` pass preserved parity and improved some secondary fixtures, but stable hotspot reruns regressed the main wall to about `232.02ms` and `234.01ms`. The tactical-spirit base score is not a worthwhile live substitute for the direct immediate-score scan.
- A lazy budget-one summary warmup follow-up was also not worth keeping on 2026-03-27: delaying `exact_budget_one_tactical_summary(...)` until a preview actually needed budget-one score/denial after-window work preserved parity, but stable hotspot reruns still regressed to about `232.78ms` and `231.47ms`. Eager budget-one summary construction was not the retained wall.
- A release-build verification cut was worth keeping on 2026-03-27: `cargo check --release --lib` exposed stale `#[cfg(any(target_arch = "wasm32", test))]` gates across exact tactical, pickup, secure-mana, spirit-preview, and selector-diagnostics helpers that the native runtime path now references. Ungating those live-path definitions fixed the release-library build without materially changing the retained hotspot (`primary_spirit_setup` stayed about `143.74ms`, `human_win_pro_a` about `225.96ms`), so this is a correctness/build fix rather than a promotable speed cut.
- A direct budget-one drainer tactical cut was worth keeping on 2026-03-27: the budget-one drainer branch inside `exact_budget_one_tactical_counts_for_location(...)` now evaluates the one-step drainer case directly instead of routing through the generic pickup-window helper. Focused parity checks passed, stable hotspot reruns moved `human_win_pro_a` to about `220.40ms` / `220.18ms` from the retained about `225.96ms`, and the canonical loop still passed `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight`.
- That direct budget-one drainer cut still was not promotable on 2026-03-27: `pro-reliability` was still live past about `3:17`, and a live sample from the duel still spent heavily in deeper search/scoring work instead of revealing a new clean exact-only wall. Removing the generic one-step pickup helper overhead helps the retained hotspot, but it still does not reduce real duel friction enough by itself.
- A scoring board-summary capacity pre-pass was worth keeping on 2026-03-27: `ScoringBoardSummary::from_board(...)` now does a fixed-cost count pass over `board.items` and pre-sizes its vectors before filling them. Focused parity checks passed, stable hotspot reruns moved `human_win_pro_a` to about `215.62ms` / `215.79ms` from the retained `220.40ms` / `220.18ms`, while `primary_spirit_setup` stayed about `140.80ms` / `142.78ms`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live at about `3:29` and had to be killed.
- That scoring capacity cut also clarified the remaining scoring wall on 2026-03-27: a live duel sample still showed repeated `ScoringBoardSummary::from_board(...)` construction and `ScoringEvalContext` / summary drop work under root scoring, alongside the existing tactical-spirit and attack child costs. The useful lesson is that vector growth was real enough to trim, but the next scoring-side hypothesis would need summary/context lifetime reuse rather than more per-`Vec` capacity tuning.
- A profile-level child-bundle scoring-context reuse follow-up was not worth keeping on 2026-03-27: enabling `runtime_pro_turn_engine_v30` to use `build_child_eval_bundle(...)` together with local scoring-context reuse and child vulnerability reuse preserved focused parity, and it trimmed some exact child costs on `human_win_pro_a` (`attack_calls` about `36272 -> 32411`, `payload_calls` about `895231 -> 890496`), but the hotspot stayed effectively flat at about `216.58ms` and `215.53ms`. The selector-side `search_preferability_builds` diagnostics also ballooned, so flipping the existing bundle/reuse path on at the profile level is not a clean live-wall win.
- An inline common-case scoring-summary cut was worth keeping on 2026-03-27: `ScoringBoardSummary` and `ManaPathSnapshot` now keep their common-case entries in fixed-capacity inline lists and spill to heap only on rare oversized boards. Stable hotspot reruns moved `human_win_pro_a` from about `215.62ms` / `215.79ms` down to about `208.38ms`, `208.16ms`, and `208.90ms`, while `guardrails`, `pro-triage`, and `runtime-preflight` all passed. The branch still did not promote, because `pro-reliability` was still live past about `3:54`.
- That inline summary cut also moved the sampled duel wall on 2026-03-27: after the scoring-summary heap work fell out, the retained `pro-reliability` sample spent its time deeper in `collect_legal_transitions(...)`, `process_input_internal(...)`, `process_second_input(...)`, and `apply_and_add_resulting_events(...)`, with visible `HashMap` insert growth and transition-generation churn under move generation.
- A reserve-heavy follow-up on that surfaced search wall was not worth keeping on 2026-03-27: pre-sizing `ProcessInputCache` hash maps to their cache caps and reserving root `collect_legal_transitions(...)` vectors to the full move budget caused an immediate catastrophe, with `primary_spirit_setup` jumping to about `977ms` and `human_win_pro_a` to about `380ms` while exact counters stayed flat. Broad up-front reservation is not the right way to attack the new live wall.
- A local fast-hasher follow-up on that same move-generation wall was also not worth keeping on 2026-03-27: switching the `process_input_*` caches in `mons_game` to a local non-cryptographic hasher preserved the runtime profile tests, but the hotspot regressed to about `220.43ms` on `human_win_pro_a` and about `144.96ms` on `primary_spirit_setup`. The remaining search churn is not dominated by default `HashMap` hasher overhead in that cache layer.
- A same-board scoring summary/snapshot reuse follow-up was also not worth keeping on 2026-03-27: a one-entry thread-local cache keyed by scoring board hash did reuse `ScoringBoardSummary` and `ManaPathSnapshot` across short-lived `ScoringEvalContext`s in focused tests, but the live hotspot still regressed to about `213.11ms` on `human_win_pro_a` and about `141.98ms` on `primary_spirit_setup`. Repeated root scoring cost is not dominated by summary rebuilds in a form where thread-local clone-and-reuse beats its own cache overhead.
- A direct scoring board scan cut was worth keeping on 2026-03-27: `ScoringBoardSummary::from_board(...)` now scans `board.items` directly instead of going through `board.occupied()`, which removes some iterator/setup overhead without adding new cache state. Stable hotspot reruns moved `human_win_pro_a` from about `209.66ms` to about `208.41ms` / `208.35ms`, `primary_spirit_setup` from about `143.26ms` to about `140.35ms` / `139.59ms`, and `primary_black_loss_opening_a_ply19` from about `51.63ms` to about `50.04ms` / `50.72ms`. `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` passed, but `pro-reliability` was still live past about `3:02` and had to be killed, so this is a retained micro-cut rather than a promotable wall shift.
- The live duel sample on that retained direct-scan cut still mixed scoring and deeper search work: `evaluate_preferability_with_context`, `HashMap::insert`, `search_state_hash`, `ScoringBoardSummary::from_board(...)`, `drainer_immediate_threats_uncached`, `can_attack_target_on_board_with_hash`, and move-processing helpers all stayed visible together. The useful lesson is that direct board scanning trims scoring-summary wrapper cost a bit, but the remaining duel friction is still split between repeated scoring-summary builds and search/hash churn, not a fresh isolated target.
- A legacy score-window summary-reuse cut was worth keeping on 2026-03-27: legacy score/immediate scoring windows now reuse `ScoringBoardSummary` carrier and drainer lists through context-aware helpers instead of rescanning `board.occupied()` on every call, and they only build `ManaPathSnapshot` when a pickup branch actually needs it. Focused parity checks passed, and same-session hotspot reruns moved `human_win_pro_a` from about `207.99ms` to about `203.42ms` / `203.55ms`, with one noisy `209.47ms` backslide; `guardrails`, `pro-triage` (`target_changed=14`, `off_target_changed=0`), and `runtime-preflight` all passed, but `pro-reliability` was still live at about `2:26` and had to be killed.
- That legacy-window cut also clarified the next surface on 2026-03-27: the sampled duel no longer showed `score_path_window_to_any_pool_with_snapshot(...)` near the top. The remaining spend clustered around `evaluate_preferability_with_context`, `can_attack_target_on_board_with_hash`, `search_state_hash`, `process_input_internal`, `ScoringBoardSummary::from_board(...)`, `drainer_immediate_threats_uncached`, `classify_move_classes`, and `collect_legal_transitions`. Do not reopen legacy score-window rescans; the wall is back in mixed scoring-plus-search churn.
- A direct board-scan move-generation follow-up was not worth keeping on 2026-03-27: replacing `board.occupied()` with full `board.items` scans inside `evaluate_preferability_with_context`, `has_awake_spirit_off_base`, and `carrier_progress_snapshot` preserved focused parity, but a same-session retained baseline was already about `223.40ms` on `human_win_pro_a`, and the candidate slipped to about `224.35ms`. On the current sparse duel boards, scanning every cell costs more than the iterator/setup work that `board.occupied()` avoids.
- A child-hash reuse follow-up was also not worth keeping on 2026-03-27: feeding the cheap-pass `search_state_hash(&transition.game)` into `build_child_eval_bundle(...)` preserved focused invariants, but the same-session hotspot only moved from about `223.40ms` to about `223.08ms` on `human_win_pro_a`, with counters flat. Duplicate child hashing inside the two-stage child-ordering path is too small to matter against the retained mixed search/scoring wall.
- A legal-transition sort-elision follow-up was also not worth keeping on 2026-03-27: `collect_legal_transitions(...)` already emitted lexicographically sorted inputs on representative boards, but removing the explicit `enumerate_legal_transitions(...)` sort still pushed the same-session retained `human_win_pro_a` from about `223.40ms` to about `242.18ms`, with other move-generation fixtures worse too. Even when correctness does not need it, that final sort still helps the downstream search order and cache behavior enough that removing it is a regression.
- Keep Fast work parked until there is a genuinely new code path. Minor search-order retunes and scoring-only tweaks are already saturated.
- A quality-first selector-clamp follow-up was not worth keeping on 2026-03-27: directly hardening the traced `SafeSupermanaProgress` loss families with a white spirit-setup suffix pick plus early-black non-concrete progress-head rejects did fix the exact replay fixtures and kept the curated dominance probe green, but the focused Pro gate regressed back to `win_rate=0.6667`, `confidence=0.8062`, and `candidate_avg_ms=90.36`. The durable lesson is that replay-targeted selector clamps are too brittle here; they move the traced disagreements around and create new duel losses elsewhere instead of producing a stable direct-vs-`runtime_current` gain.
- A separately compared injected-progress follow-up was also not worth keeping on 2026-03-27: comparing an injected ProV2 `SafeSupermanaProgress` head against the untouched regular root winner did fix the traced black injected-progress loss and stayed green on curated dominance, but the focused duel gate stayed flat at `win_rate=0.7500`, `confidence=0.9270`, and `candidate_avg_ms=91.58`. A follow-up spirit-phase clamp still did not move the traced white loss. The durable lesson is that separately injected non-concrete progress heads are not the main remaining quality wall; fixing that isolated black case just shifts the direct-vs-`runtime_current` losses elsewhere.
- A broader white turn-three full-resource fallback follow-up was also not worth keeping on 2026-03-27: routing all white turn-three mid-turn states with both action and mana back to the shared current Pro line did fix the traced engine-disabled immediate-score disagreement and kept curated dominance green, but the focused Pro gate stayed flat at `win_rate=0.7500`, `confidence=0.9270`, and `candidate_avg_ms=91.57`. The durable lesson is that the broad white turn-three fast-fallback split is not the main remaining quality wall; fixing that traced state alone just trades one disagreement for another without improving the direct duel result.
- A plain non-tactical `SpiritImpact` acceptance clamp and late-turn current-runtime fallback bundle was not worth keeping on 2026-03-27. The selector-side clamp did fix the traced plain-spirit followup overrides and cut the disagreement harvest from `9` logged disagreements to `6`, but the focused Pro gate still stayed flat at `win_rate=0.7500`, `confidence=0.9270`, and `candidate_avg_ms=88.02`. A follow-up pair of wrapper-level fallbacks for late turn-start action+mana states and late White mana-only mid-turn states reduced the harvest further to `5` logged disagreements, yet the focused Pro gate regressed to `win_rate=0.6667`, `confidence=0.8062`, and `candidate_avg_ms=87.26`. The durable lesson is that disagreement-count shrink and profile-level current-runtime fallbacks are still not reliable proxies for direct duel quality; they keep moving the remaining losses around instead of clearing the real selector/search wall.
- A broad search-only Pro spend follow-up was not worth keeping on 2026-03-27: increasing `runtime_pro_turn_engine_v30` root/node budgets, focus budget, and reply-risk coverage while leaving the turn-engine plan caps unchanged kept the retained regression suite and curated dominance probe green, but the focused duel gate stayed exactly flat on quality at `win_rate=0.7500` and `confidence=0.9270` while slowing the candidate from about `85.66ms` to `104.69ms`. The durable lesson is that raw search/reply-budget spend alone is not the missing wall; if more compute is spent again, it needs a narrower selector/root-choice hypothesis instead of a blanket budget hike.
- A black accepted-head clamp follow-up was also not worth keeping on 2026-03-27: trying to reject traced black non-concrete setup heads when a safe non-spirit search root won locally did not line up cleanly with the live loss families. One traced “progress” state surfaced as `SpiritImpact` instead of a stable `SafeSupermanaProgress` family, and the plain-spirit late-black state only differs because the engine still carries a real ProV2 utility lead over the safe search root. The durable lesson is that the remaining black accepted-head losses are not a single generic non-concrete family that can be clamped away safely; the next useful split has to reason about why the utility model prefers those heads, not just their nominal family names.
- A narrower reply-shortlist resurrection follow-up was also not worth keeping on 2026-03-27: rejecting plain `SpiritImpact` heads only because they were omitted from the reply-risk shortlist did not solve the remaining selector wall. On the traced white loss it merely pushed `runtime_pro_turn_engine_v30` from the accepted plain-spirit head to the raw root-search non-spirit winner instead of the shipping `runtime_current` move, and it left the traced black plain-spirit loss unchanged. The durable lesson is that the live problem is not just “plain spirit accepted too easily”; it is a deeper omitted-root interaction between concrete spirit-setup/progress candidates, safe non-spirit shortlist winners, and accepted spirit heads. The next useful split should target that omitted-root competition directly instead of adding another plain-spirit clamp.
- A traced white setup-progress selector follow-up was also not worth keeping on 2026-03-27: adding a ProV2-specific reply-order rescue for a concrete white `spirit_own_mana_setup_now + opponent_mana_progress` root versus a close safe non-spirit root, then tightening acceptance so a plain spirit head would not override that selected setup-progress root, did fix the exact traced replay and kept curated dominance green. But the focused direct duel still stayed flat at `win_rate=0.7500`, `confidence=0.9270`, and slowed to `candidate_avg_ms=96.36`. The durable lesson is that even selector-plus-acceptance clamps on a real traced loss can still just redistribute the remaining duel losses; traced replay repair is not enough unless the direct-vs-`runtime_current` gate actually improves.
- A projected plain-spirit followup acceptance clamp was also not worth keeping on 2026-03-27: blocking `projected_completed_plan_override` for the traced black multi-chunk plain-spirit followup that beat a safe non-progress search root did fix that exact replay and kept the curated dominance probe green, but the focused direct duel still stayed flat at `win_rate=0.7500`, `confidence=0.9270`, and slowed to `candidate_avg_ms=92.79`. The durable lesson is that even the projected-completion rescue path is not the remaining wall by itself; repairing that traced black acceptance override in isolation still just redistributes the losses elsewhere.
- A positive attack/safety-edge plain-spirit acceptance clamp was also not worth keeping on 2026-03-27: rejecting a plain non-tactical `SpiritImpact` followup whenever the selected search root already carried positive attack or drainer-safety utility did fix both traced white/black replay losses locally, but it immediately failed the cheap quality screen by flipping `human_win_pro_a` from `CandidateWin` to `OpponentWin` and dropping curated dominance to `0.667`. The durable lesson is that positive attack/safety utility on the selected root is still too blunt as a plain-spirit veto; useful SpiritImpact wins also pass through that shape, so this branch just trades live losses for live wins.
- A paired late-white setup-progress comparator plus black safe-followup acceptance cut was also not worth keeping on 2026-03-27: adding a ProV2 reply-order rescue for late white `spirit_own_mana_setup_now + opponent_mana_progress` roots versus non-spirit roots did move the outer selector probe, and a matching black safe-non-spirit followup acceptance reject did fix the traced black replay locally. But together they still failed the cheap quality screen: curated dominance stayed at `0.667`, with `primary_spirit_setup` and `primary_black_loss_opening_a_ply19` both `CandidateWin` while `human_win_pro_a` flipped to `OpponentWin`. The durable lesson is that this local pair-wise selector/acceptance family is still too narrow. Moving a traced black acceptance loss and nudging a traced white selector probe is not enough unless the deeper searched-root competition on the live human fixture also changes.
- A combined late-white setup-progress rescue plus black projected plain-spirit override clamp was also not worth keeping on 2026-03-27: pairing those two individually plausible traced replay repairs did fix the traced white and black fixtures and clear curated dominance, but the focused Pro gate still stayed flat at `win_rate=0.7500`, `confidence=0.9270`, and slowed to `candidate_avg_ms=90.50`. The durable lesson is that combining green traced-replay fixes is still not enough; if the direct duel does not improve, kill the branch even when curated dominance turns green.
- A narrow early-black plain-spirit followup-floor acceptance cut was also not worth keeping on 2026-03-27: reusing `pro_v2_spirit_followup_floor_score(...)` inside final ProV2 acceptance did fix the traced black followup replay family and kept curated dominance green, but the focused Pro gate still stayed flat at `win_rate=0.7500`, `confidence=0.9270`, and slowed to `candidate_avg_ms=91.01`. The durable lesson is that followup-floor comparisons are already useful in selector ordering, but copying that signal into post-search acceptance is still not enough by itself to improve the direct duel against `runtime_current`.
- A broader losing-state projected plain-spirit suppressor was also not worth keeping on 2026-03-27: blocking `projected_completed_plan_override` for plain `SpiritImpact` heads whenever they tried to override a safe non-progress search root with a large lead in already-losing states did fix the repeated black followup replay family and kept curated dominance green, but the focused Pro gate still stayed exactly flat at `win_rate=0.7500`, `confidence=0.9270`, and slowed to `candidate_avg_ms=93.72`. The durable lesson is that even the broader “losing safe-search root versus projected plain spirit” family is not enough by itself; the remaining direct-vs-`runtime_current` wall is deeper than the projected-completion rescue path alone.
- A broader late-white setup-progress reply-order plus matching plain-spirit acceptance follow-up was also not worth keeping on 2026-03-27: I tried to generalize the traced white rescue away from a single replay by adding a late-white action+mana turn-start comparator for `spirit_own_mana_setup_now + opponent_mana_progress` roots versus flat non-spirit roots, and a matching `SpiritImpact` acceptance block when a searched concrete setup-progress root should survive. The branch died immediately at the focused fixture stage: even after relaxing the late-white comparator to ignore the reply-floor gate, `runtime_pro_turn_engine_v30` still kept `reply_guard_selected_move_fen=l10,3;l9,2` and the final move `l4,9;l2,7;l2,8` on the live white loss. The durable lesson is that the remaining white wall is not captured by a simple late-turn setup-progress versus non-spirit pair rule; the decisive competition is deeper than another turn-shape-specific comparator or paired acceptance clamp.
- A deeper late-white deterministic-tiebreak follow-up was also not worth keeping on 2026-03-27: restoring a broader late-white action+mana setup-progress tie-break inside reply-risk ordering did finally move the traced white loss the right way, and a matching late-white acceptance guard also fixed the exact replay, with curated dominance still green. But the focused duel gate still stayed flat at `win_rate=0.7500`, `confidence=0.9270`, and `candidate_avg_ms=91.86`. The durable lesson is that even when the live white traced loss is repaired through a more generic root-choice mechanism instead of a one-off pair helper, the direct-vs-`runtime_current` result still just redistributes losses elsewhere. The remaining wall is broader than this late-white root-choice/acceptance family too.
- A direct black plain-spirit score-lead acceptance experiment was also not worth keeping on 2026-03-27: I tried to reject the repeated black followup loss family by forcing `accept_turn_engine_head_after_search(...)` to deny non-tactical, non-progress `SpiritImpact` heads whenever the searched safe non-spirit root already held a large search-score lead. I also added focused regression tests on the traced black followup FEN. The branch was killed and reverted because the acceptance probe still came back `accepted=true` on that exact state even after the guard was broadened to the probe-visible booleans (`selected_spirit_phase=false`, `candidate_progress_surface=false`, `large_safe_search_lead=true`). The durable lesson is that this repeated black loss is not actually controlled by the post-search `SpiritImpact` acceptance branch the way the probe fields make it look; the deciding path lies earlier or in a different selector/search interaction, so do not keep spending turns on simple acceptance-side vetoes for that family.
- A black-only safe-root projection selector cut was worth keeping on 2026-03-27: letting `turn_engine_selected_override_utility(...)` project followup plans for otherwise plain black `ManaTempo` / `DrainerSafetyRecovery` roots, and pairing that with a black-only projected plain-spirit override block, materially improved the direct duel instead of just fixing a replay. The focused Pro gate moved from `win_rate=0.7500`, `confidence=0.9270`, and `candidate_avg_ms=85.66` to `win_rate=0.8333`, `confidence=0.9807`, and `candidate_avg_ms=96.34`, while `guardrails`, `pro-triage`, `runtime-preflight`, and curated dominance all stayed green. The durable lesson is that the remaining black followup losses can improve when the searched safe root gets access to the same projected continuation surface that the accepted spirit head already enjoys; this selector/search interaction is real, unlike the earlier wrapper-only fallback families.
- A follow-up near-tie black spirit-development clamp was not worth keeping on 2026-03-27: I added black-only blockers for traced opening-B plain `SpiritImpact` bundles that only beat a safe non-progress root by a one-point search-score edge, and the focused replay tests flipped the exact FEN the right way. But the real focused gate stayed flat at `win_rate=0.8333`, `confidence=0.9807`, with `candidate_avg_ms` only drifting from `96.34` to `93.64`. The durable lesson is that once the retained black safe-root projection cut is in place, additional near-tie spirit-development clamps can still just redistribute the remaining duel losses without buying the extra win needed for promotion.
- Exact profile fallbacks for the live late-white spirit-acceptance FEN and the live black opening-B plain-spirit FEN were also not worth keeping on 2026-03-27. Each routed the traced fixture back to `runtime_current` exactly as intended, and the combined branch stayed green on curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, but the focused duel gate still came back unchanged at `win_rate=0.8333`, `confidence=0.9807`, and only moved move time from about `96.34ms` to `98.88ms`. The durable lesson is that the remaining direct-vs-`runtime_current` losses are not fixed by patching the first traced disagreement state in those games; the live wall sits deeper in later search/selector branches than those initial seam FENs.
- A black-only projected non-concrete progress override clamp was also not worth keeping on 2026-03-27. I blocked a later opening-B `projected_completed_plan_override` seam where an unsafe non-concrete `SafeSupermanaProgress` head (`l0,5;l1,6`) overrode the searched safe pickup root (`l5,1;l5,0;mp`) even though the projected utility was not better, and I added focused regression coverage around that later FEN. The branch really did flip that local seam, kept curated dominance green, and passed `guardrails`, `pro-triage`, and `runtime-preflight`, but the focused duel gate still stayed flat at `win_rate=0.8333`, `confidence=0.9807`, with `candidate_avg_ms` only drifting to `98.07`. The durable lesson is that once the first later black projected-progress disagreement is repaired, the missing win is still not unlocked; single opening-B continuation clamps in the projected non-concrete progress/immediate-score family are still too local to clear promotion.
- The focused Pro disagreement harvest needs to use the same seed tag as the real gate. On 2026-03-27 I confirmed that `smart_automove_pro_reliability_disagreement_harvest` defaults to `pro_turn_engine_direct_activity_v1`, while the focused gate uses `pro_turn_planner_reliability_v1`. Harvests run without overriding the seed tag can therefore miss or mis-rank the real remaining loss families. For direct duel debugging, always align the harvest with `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v1` before trusting the traced openings.
- A combined cached-resume and white turn-three mana-only reroute was not worth keeping on 2026-03-27. Disabling blind `engine_cached_resume` for `TurnEngineMode::ProV2` did fix the traced white opening followup seam where stale cached continuation `l9,3;l8,2` overrode the fresh better move `l9,3;l9,2`, and broadening the white turn-three mana-only wrapper reroute also fixed the traced `engine_disabled` loss where `runtime_pro_turn_engine_v30` picked `l7,7;l8,8` instead of the current/runtime-safe `l7,4;l8,4`. But the combined branch regressed the focused gate all the way back to `win_rate=0.7500`, `confidence=0.9270`, and `candidate_avg_ms=103.77`, despite curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight` all staying green. The durable lesson is that fixing traced opening-0/ab seams in isolation can still make the direct duel worse; do not retain cached-resume or broad white mana-only reroutes unless they improve the real focused gate, not just replay probes.
- A narrow white projected-progress override clamp was also not worth keeping on 2026-03-27. I blocked a `projected_completed_plan_override` seam where a white `SafeSupermanaProgress` head (`l9,3;l9,4`) overrode the searched non-progress carrier root (`l9,3;l8,3`) even though the searched root had the better search score, better same-turn score window, and shorter score path. The branch fixed the exact replay, passed the retained `runtime_pro_turn_engine_v30_` suite, kept curated dominance green, and cleared `guardrails`, `pro-triage`, and `runtime-preflight`, but the focused gate still stayed flat at `win_rate=0.8333`, `confidence=0.9807`, and `candidate_avg_ms=95.35`. The durable lesson is that even a clean white projected-progress acceptance seam in the real opening-0/ab lane can still be too local; if the direct duel does not move, revert it even when the traced replay and cheap screens are green.
- A narrow white turn-five profile fallback bundle was also not worth keeping on 2026-03-27. I added two wrapper-only guards in `runtime_pro_turn_engine_v30_guarded_inputs(...)` to route white turn-five turn-start action+mana states back to the shared current Pro line and white turn-five mana-only states to the `runtime_release_safe_pre_exact` line, and I added focused regression tests around the two traced opening-0/ab FENs (`l9,6;l7,4;l7,3` and `l9,3;l9,2`). The branch fixed both traced fixtures exactly, but it failed immediately on the cheap quality screen: `runtime_pro_turn_engine_v30_curated_dominance_probe` flipped `human_win_pro_a` to `OpponentWin`, dropping curated dominance to `0.667`. The durable lesson is that another wrapper-level white current-line reroute is still too local; even when the traced turn-five seams are correct, the real selector/search competition on the human fixture regresses, so keep white turn-five fallback widening parked unless it improves direct duel quality instead of just replay probes.
- A black exact-utility `ImmediateScore` tie block was also not worth keeping on 2026-03-27. I tried to stop a projected multi-chunk `ImmediateScore` engine head (`l2,7;l2,8`) from overriding the searched black root (`l2,7;l1,8`) when the two lines had the same search score, the same final `TurnEngineUtility`, and the searched root had the better efficiency. Even an explicit early-return tie-break and a secondary projected-override block failed to flip the focused fixture: `turn_engine_acceptance_probe_for_test(...)` still came back `accepted=true`, and the final decision stayed on the engine head. The durable lesson is that this equal-utility black seam is not actually controlled by the immediate-score acceptance path in a way that simple tie-break guards can intercept; the real wall is earlier or in another selector/search interaction, so do not keep spending turns on this exact-tie acceptance family.
- A combined stale cached-resume plus black projected-tempo follow-up was also not worth keeping on 2026-03-27. Disabling the blind `engine_cached_resume` return for ProV2 did fix the traced white stale-cache seam (`l9,3;l8,2` versus `l9,3;l9,2`), and a second black-only block that rejected projected `ManaTempo` heads over searched progress roots did fix the traced opening-1/ab black replay (`l1,5;l2,5` versus `l0,5;l1,4`). The branch stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, but the real focused gate still regressed all the way back to `win_rate=0.7500`, `confidence=0.9270`, and `candidate_avg_ms=97.52`. The durable lesson is that even a real black `selected progress root vs projected ManaTempo head` seam, like the white stale-cache seam before it, is still too local on its own; if a replay repair does not improve the direct duel, revert it even when every cheap screen stays green.
- A broad black turn-four mana-only wrapper fallback was not worth keeping on 2026-03-27. Routing the whole `runtime_pro_turn_engine_v30` family of black turn-four mana-only states back to the shared current Pro line did pass focused regression tests, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, but the real focused gate regressed from the retained `0.8333 / 0.9807 / 96.34ms` back to `win_rate=0.7500`, `confidence=0.9270`, and `candidate_avg_ms=95.24`. The durable lesson is that even when the traced remaining losses cluster around one turn shape, broad wrapper-level family fallbacks are still too blunt; they repair some local seams and reopen older losses elsewhere.
- Exact first-divergence black opening-B fallbacks were also not worth keeping on 2026-03-27. I traced the real focused-gate seed (`SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v1`), identified the two first black divergence FENs in opening `1/ab`, and added exact profile fallbacks so `runtime_pro_turn_engine_v30` would take the current-Pro roots (`l2,7;l1,8` and `l0,5;l1,4`) on those exact states. The branch cleared the focused replay tests, the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, but the real focused gate still regressed to `win_rate=0.7500`, `confidence=0.9270`, and `candidate_avg_ms=100.54`. The durable lesson is that even exact first-divergence FEN reroutes in the real lane can be replay-fix traps: fixing the first traced disagreement is not enough if the direct duel against `runtime_current` gets worse.
- A black opening-B projected progress-vs-tempo acceptance cut was also not worth keeping on 2026-03-27. I traced the live `opening 1 / ab` black seam to a projected multi-chunk head (`l1,5;l2,5`) overriding the searched progress root (`l0,5;l1,4`), then added a narrow black-only guard so the focused acceptance probe and final move both flipped to the searched root on that exact FEN. The branch stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, but the real focused gate still regressed all the way back to `win_rate=0.7500`, `confidence=0.9270`, and `candidate_avg_ms=101.48`. The durable lesson is that even a real selector-side `selected progress root vs projected ManaTempo head` repair can still be too local; if the direct duel gets worse, revert it even when the exact live seam is genuinely fixed.
- That same opening-B projected-tempo seam also clarified one more selector detail on 2026-03-27: on the live acceptance probe, the losing projected head was still being accepted with `safe_non_progress_fallback=false` and `engine_progress_override=false`, so the remaining path was narrower than the fallback booleans suggested. In other words, probe-visible acceptance flags can still underdescribe the real winning path for these projected multi-chunk heads; do not assume that flipping the obvious fallback booleans will intercept the live seam unless the exact probe actually changes.
- Two forced-input order follow-ups were also not worth keeping on 2026-03-27. On the retained black `opening 0 / ba` seam, `runtime_pro_turn_engine_v30` picked `l1,8;l1,9` even though both `root_selection_probe_for_test(...)` and `root_search_probe_for_test(...)` preferred the searched root `l1,8;l0,9`; the real reason was that `inject_turn_engine_root_candidate(...)` handed `forced_engine_inputs` into the focus pass, which reordered an already-existing tied engine root to the front, and the live search path then kept that front-of-tie choice. I tried both a narrow veto for already-existing flat tied engine roots and a broader structural tie-break change that replaced evaluation-order fallback with stable `root_rank` fallback after search. Both branches fixed the exact black seam, stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, and shaved candidate move time to about `93.92ms` and `92.45ms`, but the focused gate still stayed flat at `win_rate=0.8333` and `confidence=0.9807`. The durable lesson is that forced-input order bias is a real local mechanism, but fixing that black tied-root seam by itself is still too local; do not retain forced-priority or search-tie reordering cuts unless they move the direct duel result, not just the traced replay and move time.
- A combined black opening-A plus opening-B replay-repair branch was also not worth keeping on 2026-03-27. I paired the previously traced black `opening 0 / ba` forced-tie veto (`l1,8;l1,9` versus `l1,8;l0,9`) with a new black `opening 1 / ab` `ImmediateScore` projected-completion guard that rejected equal-utility multi-chunk heads (`l2,7;l2,8` versus `l2,7;l1,8`) when the searched root already had at least the same search score and no tactical edge was being added. The combined branch fixed both focused black replay seams, stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, and still kept move time comfortable at about `95.62ms`. But the focused gate remained exactly flat at `win_rate=0.8333` and `confidence=0.9807`. The durable lesson is stronger than the individual ones: even repairing both currently traced black loss families together can still just redistribute the remaining direct-duel losses elsewhere. At this point, paired replay fixes in the black opening lanes are not enough by themselves; the missing win is still hiding in a broader selector/search interaction that the traced seam repairs do not capture.
- A broader white mid-turn progress fallback was also not worth keeping on 2026-03-27. I enabled the dormant `enable_turn_engine_mid_turn_progress_guard` path for `runtime_pro_turn_engine_v30`, which disables ProV2 on white mid-turn states when exact opportunity context shows only safe mana progress with no immediate tactical surface. The branch stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, but the focused gate stayed exactly flat at `win_rate=0.8333`, `confidence=0.9807`, and slowed from the retained `96.34ms` to `candidate_avg_ms=100.42`. The durable lesson is that a broader exact-context white progress fallback is still too local: even when it plausibly suppresses some non-promotable engine states, it does not buy the missing direct-duel win against `runtime_current`.
- A lazy turn-engine score-window projection follow-up was also not worth keeping on 2026-03-27. I enabled the dormant `enable_turn_engine_lazy_oracle_score_window_projection` path for `runtime_pro_turn_engine_v30`, and the dedicated turn-engine invariants plus the retained `runtime_pro_turn_engine_v30_` suite all stayed green. Curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight` also passed, and the focused gate even ran a bit faster at `candidate_avg_ms=88.07`. But the only metric that matters stayed exactly flat at `win_rate=0.8333` and `confidence=0.9807`. The durable lesson is that more accurate or cheaper turn-engine score-window projection alone is not the missing quality lever; if a broader projection-quality change does not move the direct duel, do not retain it just because it preserves fixtures and improves move time.
- A late-black setup-progress rescue follow-up was also not worth keeping on 2026-03-27. I enabled the dormant `enable_turn_engine_late_black_setup_progress_rescue` path for `runtime_pro_turn_engine_v30`, which lets omitted late-black `spirit_own_mana_setup_now + progress` roots re-enter reply-risk selection against plain-spirit winners at action+mana turn starts. The branch stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, but the focused gate still stayed exactly flat at `win_rate=0.8333`, `confidence=0.9807`, and `candidate_avg_ms=98.61`. The durable lesson is that this dormant late-black selector rescue is not the missing win either; enabling one more omitted-root competition path inside ProV2 still just redistributes quality locally unless the direct duel actually moves.
- A broader black unsafe-progress search-lead guard was also not worth keeping on 2026-03-27. I traced the live `opening 1 / ab` black seam to a searched unsafe progress root (`l0,5;l1,4`) losing to a safe non-progress multi-chunk engine head (`l1,5;l2,5`), then blocked both the local `safe_non_progress_fallback` path and the matching `projected_completed_plan_override` path when a black unsafe progress root already had a large search lead without a pickup or override-axis advantage. The branch really did flip the exact replay, stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, but the focused gate still regressed to `win_rate=0.7500`, `confidence=0.9270`, and `candidate_avg_ms=102.67`. The durable lesson is that even a broader unsafe-progress-versus-safe-head structural guard in the live opening-B family is still too local; once the direct duel gets worse, revert it even if the traced seam and all cheap screens are green.
- A tighter `supports_family_fallback(...)` tolerance retune was also not worth keeping on 2026-03-27. I lowered `TurnEngineUtility::supports_family_fallback(...)` from an eval-drop allowance of `192` to `128` so weak family-fallback heads would have a harder time overriding searched roots, then reran the live black `opening 1 / ab` selector probe plus the full focused Pro loop. The branch stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, but it did not flip the traced opening-B seam and the focused gate stayed exactly flat on quality at `win_rate=0.8333` and `confidence=0.9807` while slowing from the retained `96.34ms` to `candidate_avg_ms=101.50`. The durable lesson is that generic family-fallback tolerance retunes are still too blunt here: if they do not move the live seam and only preserve the same direct-duel result more slowly, revert them.
- A broader white turn-three mid-turn ProV2 access cut was also not worth keeping on 2026-03-27. I narrowed the profile-level `white_turn_three_mid_turn` fallback so action+mana states would stay on the ProV2 path, and I relaxed `should_disable_pro_v2_mid_turn_tactical_engine(...)` when exact context still showed a positive same-turn score window. The branch fixed the targeted white turn-three action+mana seam, stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, and even cleared the small focused gate at `win_rate=0.9167`, `confidence=0.9968`, and `candidate_avg_ms=103.25`. But the real confirmation batch killed it: `pro-reliability-confirm` fell back to `win_rate=0.7500`, `confidence=0.9965`, and `candidate_avg_ms=111.13`, and the seed-aligned disagreement harvest showed that widening white turn-three mid-turn ProV2 access opened a much broader `opening 0 / ab` white regression surface. The durable lesson is that a focused-gate pass is not enough here; broader white mid-turn engine access is still too unstable unless the confirm batch stays above the promotion floor too.
- A narrower stale-cache blind-resume guard was also not worth keeping on 2026-03-27. I blocked the early `engine_cached_resume` return only when a fresh ProV2 head existed, differed from the cached step, and had `head_family = ImmediateScore`. That branch fixed the seeded stale white cached-followup seam (`l9,3;l8,2` -> `l9,3;l9,2`) while preserving the legitimate cached spirit-setup continuation path, and it stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`. But the focused gate still stayed exactly flat on quality at `win_rate=0.8333` and `confidence=0.9807`, and it slowed from the retained `96.34ms` to `candidate_avg_ms=102.78`. The durable lesson is that even a clean stale-cache replay repair at the blind cached-resume seam is still too local; if it does not lift the direct duel against `runtime_current`, revert it rather than retaining another ProV2 cached-step special case.
- A broader black equal-utility `ImmediateScore` projected-override clamp was also not worth keeping on 2026-03-27. I blocked `projected_completed_plan_override` for black multi-chunk `ImmediateScore` heads when the searched root already matched the plan on all primary utility axes, added no tactical edge, and differed only by the projected bundle (`l2,7;l2,8` versus `l2,7;l1,8`). The branch really did flip that live opening-B seam, passed the retained `runtime_pro_turn_engine_v30_` suite, stayed green on curated dominance, and cleared `guardrails`, `pro-triage`, and `runtime-preflight`. But the focused gate still stayed exactly flat at `win_rate=0.8333` and `confidence=0.9807`, with `candidate_avg_ms=96.55`. The durable lesson is that even the correct `projected_completed_plan_override` seam for this black equal-utility `ImmediateScore` loss is still too local; projected-completion micro-clamps in the black opening lanes are not enough by themselves to clear promotion.
- A narrow white turn-three mid-turn mana-only reroute was also not worth keeping on 2026-03-27. I traced a real focused-gate loss to `runtime_pro_turn_engine_v30_guarded_inputs(...)`, where white turn-three mana-only states with `mons_moves_count >= 5` still fell through the broad fast pre-exact wrapper even though both `runtime_current` and the full Pro/current selector already preferred the safer searched root (`l7,4;l8,4` over `l7,7;l8,8`). Routing just that exact turn-shape family back to the shared Pro/current selector did fix the traced replay, passed the retained `runtime_pro_turn_engine_v30_` suite, kept curated dominance green, and cleared `guardrails`, `pro-triage`, and `runtime-preflight`. But the real focused gate regressed badly to `win_rate=0.5833`, `confidence=0.6128`, and `candidate_avg_ms=108.02`. The durable lesson is that even a surgically narrow white wrapper repair for a confirmed losing seam can still reopen more damaging white opening regressions elsewhere; do not retain white turn-three mana-only wrapper escapes unless the direct duel result improves, not just the traced replay.
- A narrow white turn-five reply-risk projection-scope cut was also not worth keeping on 2026-03-27. I tried stopping `root_reply_risk_snapshot_with_projection(...)` from projecting `SafeSupermanaProgress` and `SafeOpponentManaProgress` heads when the searched root was already a concrete `spirit_own_mana_setup_now + progress` continuation, because the live white `opening 0 / ab` turn-five seam looked like ProV2 was over-penalizing that current-like root through projected reply-risk snapshots. The branch kept curated dominance green, but it failed the focused fixture immediately: the exact traced FEN still stayed on `l9,6;l7,4;l8,4` instead of the current/runtime-safe `l9,6;l7,4;l7,3`. The durable lesson is that this late-white shortlist disagreement is not caused by projection scope alone; do not keep spending time on reply-risk projection toggles for this seam unless a branch can first flip the focused fixture itself.
- A narrow white turn-three score-window access cut was worth keeping on 2026-03-27. I let white turn-three mid-turn action+mana states with `mons_moves_count in 1..=2` stay on the ProV2 path only when `exact_opportunity_context(...)` already showed `same_turn_score_window_value > 0`, and I relaxed `should_disable_pro_v2_mid_turn_tactical_engine(...)` to respect that same exact score-window signal. The branch stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`, and it materially improved the focused duel gate from the retained `win_rate=0.8333`, `confidence=0.9807`, `candidate_avg_ms=96.34` to `win_rate=0.9167`, `confidence=0.9968`, and `candidate_avg_ms=99.64`. The larger confirmation batch still failed promotion at `win_rate=0.7812`, `confidence=0.9989`, and `candidate_avg_ms=100.11`, so this is not promotable yet. The durable lesson is that the narrow score-window-gated version is meaningfully stronger than the old retained baseline and worth keeping, but the live wall has now moved to the broader white opening-0/ab regression surface that still appears once those turn-three ProV2 entries are opened up; the next split should attack that larger-corpus white selector/search surface instead of reopening the broader wrapper-access branch.
- A narrow white turn-three mana-only score-window reroute was also not worth keeping on 2026-03-27. On the retained branch, the confirm-aligned `opening 0 / ab` harvest exposed an early white `turn=3`, `mons_moves=1`, mana-only seam where the profile wrapper still routed the state to `runtime_release_safe_pre_exact` and chose `l10,4;l9,4`, even though the full v30 selector and `runtime_current` both preferred the positive-score-window root `l8,4;l7,3`. Routing just that exact mana-only shape back through the full v30 selector did fix the focused replay and stayed green on the retained `runtime_pro_turn_engine_v30_` suite, curated dominance, `guardrails`, `pro-triage`, and `runtime-preflight`. It also kept the focused gate at `win_rate=0.9167`, `confidence=0.9968` while lowering `candidate_avg_ms` from `99.64` to `95.31`. But the larger confirmation batch stayed exactly flat on quality at `win_rate=0.7812`, `confidence=0.9989`, only lowering `candidate_avg_ms` from `100.11` to `96.44`. The durable lesson is that even a real confirm-aligned white wrapper seam can still be only a speed cleanup once the broader duel result does not move; if confirmation win rate stays unchanged, revert the reroute and keep looking deeper than that single fallback boundary.
- A narrow white mid-turn setup-progress injection guard was also not worth keeping on 2026-03-27. I targeted the first confirm-aligned white `opening 0 / ab` loss on the retained branch, where at `turn=3`, `mons_moves=1`, action+mana, `runtime_pro_turn_engine_v30` kept a multi-chunk `spirit_own_mana_setup_now + progress` continuation (`l9,6;l7,4;l7,3`) over the current/runtime-safe progress root (`l8,7;l7,8`). I tried blocking the narrow `inject_turn_engine_root_candidate(...)` path that lets non-tactical white `SpiritImpact` setup-progress heads enter focus when the live root surface is otherwise only clean non-spirit progress. The branch was not worth keeping because it failed the focused fixture immediately: even with that guard, the exact state still stayed on `l9,6;l7,4;l7,3`. The durable lesson is that this early white confirm-aligned seam is not controlled by that injected-root allow-path alone; the winning continuation is surviving through another selector/search path, so do not keep spending turns on narrow `inject_turn_engine_root_candidate(...)` guards for this family unless a branch can first flip the exact fixture.
- A narrow white turn-three setup-progress acceptance clamp was also not worth keeping on 2026-03-27. On the retained `0.9167 / 0.9968 / 99.64ms` branch, the first confirm-aligned white `opening 0 / ab` loss turned out to be a post-search acceptance seam: plain search still preferred the current/runtime-safe progress root (`l8,7;l7,8`), but `accept_turn_engine_head_after_search(...)` accepted a multi-chunk `SpiritImpact` setup-progress head (`l9,6;l7,6;l7,7`) over it through the projected-completed-plan path. I added a narrow early-white guard for that exact family and verified that it flipped the focused replay, kept the retained `runtime_pro_turn_engine_v30_` suite green, and passed curated dominance plus the full canonical `guardrails` / `pro-triage` / `runtime-preflight` / `pro-reliability` loop. But it still was not worth keeping: the focused gate stayed exactly flat on quality at `win_rate=0.9167` and `confidence=0.9968` while slowing from `candidate_avg_ms=99.64` to `101.61`, and the confirmation batch stayed exactly flat on quality at `win_rate=0.7812` and `confidence=0.9989` while slowing from `candidate_avg_ms=100.11` to `104.51`. The durable lesson is that even a real confirm-aligned white `SpiritImpact` acceptance seam can still be only a focused-gate mirage; do not retain single early-white setup-progress acceptance clamps unless the larger confirmation batch actually improves.
- A narrow white turn-three mana-only followup reroute was also not worth keeping on 2026-03-27. On the retained `0.9167 / 0.9968 / 99.64ms` branch, the confirm-aligned `opening 0 / ab` harvest exposed a later `turn=3`, `mons_moves=3`, mana-only white seam where the profile wrapper still returned the fallback move `l9,6;l8,5` even though the full v30 selector and `runtime_current` both preferred the safer root `l10,2;l9,1`. Routing only that exact shape back through the full v30 selector did fix the focused replay, kept the retained `runtime_pro_turn_engine_v30_` suite green, and passed curated dominance plus the full canonical `guardrails` / `pro-triage` / `runtime-preflight` / `pro-reliability` loop. But it still was not worth keeping: the focused gate stayed exactly flat on quality at `win_rate=0.9167` and `confidence=0.9968` while slowing from `candidate_avg_ms=99.64` to `102.08`, and the larger confirmation batch also stayed exactly flat on quality at `win_rate=0.7812` and `confidence=0.9989` while slowing from `candidate_avg_ms=100.11` to `105.11`. The durable lesson is that this later white mana-only followup reroute is another confirm-flat wrapper seam, not a promotion path; do not reopen more narrow white mana-only wrapper slices unless a branch changes confirmation win rate, not just the traced replay.
- A narrow white turn-three action+mana wrapper compare to the shared current line was also not worth keeping on 2026-03-28. On the retained `0.9167 / 0.9968 / 99.64ms` branch, I traced the first confirm-aligned `opening 0 / ab` loss back to `runtime_pro_turn_engine_v30_guarded_inputs(...)`: at `turn=3`, `mons_moves=1`, action+mana, the retained score-window access path still let ProV2 choose `l9,6;l7,4;l7,3` while `runtime_current` preferred the safer root `l8,7;l7,8`. I tried a very narrow direct compare on that exact family, letting the v30 profile evaluate both the shared current line and the v30 line and keep the current move when they disagreed. It fixed the isolated replay, stayed green on the retained `runtime_pro_turn_engine_v30_` suite and curated dominance, and passed `guardrails`, `pro-triage`, and `runtime-preflight`. But it was not worth keeping: the focused gate dropped straight back to the older retained band at `win_rate=0.8333`, `confidence=0.9807`, and `candidate_avg_ms=97.92`. The durable lesson is that direct current-line wrapper compares on this early white action+mana family just throw away the retained focused-gate gain; do not reopen more white turn-three action+mana current-line wrapper comparisons unless a branch improves the duel itself, not just the isolated replay.
- A `pro-vs-normal` dominance-gap diagnostic pass was useful but not worth turning into a primary iteration lane on 2026-03-29. I ran matched `16`-game mirrored `smart_automove_pro_fast_screen_loss_probe_vs_normal` batches with `SMART_PRO_FAST_SCREEN_SEED_TAG=pro_vs_normal_leverage_v1`: `runtime_pro_turn_engine_v30` versus current Normal finished only `8-8`, while shipping Pro (`runtime_current`) versus current Normal finished `11-5`. The candidate-only loss set had four openings: `repeat=0 opening=0 mirror=ab`, `repeat=1 opening=0 mirror=ba`, `repeat=1 opening=1 mirror=ab`, and `repeat=2 opening=0 mirror=ba`. Replaying those openings showed only one likely overlap with the live blocked confirm surface: the `repeat=0 opening=0 mirror=ab` white loss diverged early at `ply=7`, where the candidate's root search chose `l7,8;l6,9` over the Normal/current-safe line while shipping Pro still matched Normal until later. The other three candidate-only losses were black-side and clustered around later multi-chunk acceptance/projection seams, where shipping Pro also diverged from Normal early but still converted the game. The durable lesson is that the `pro-vs-normal` gap does reveal one more early white root-search miss, but as a whole this lane is mostly black Normal-only acceptance noise and does not overlap enough with the retained branch's broader white confirm-aligned `pro-vs-pro` wall to justify a dedicated code branch. Keep it only as a secondary ranking signal if a future branch already targets the same family.
- A narrow early-white setup-progress force/focus cut was also not worth keeping on 2026-04-03. I blocked both `inject_turn_engine_root_candidate(...)` and `focused_root_candidates_with_forced_inputs(...)` from force-selecting non-tactical early-white turn-three `spirit_own_mana_setup_now + progress` heads when a safe non-spirit progress root already matched the same progress/path surface, which finally flipped the first confirm-aligned `opening 0 / ab` seam from `l9,6;l7,4;l7,3` to the current/runtime-safe root `l8,7;l7,8`. The branch stayed green on `guardrails`, `pro-triage` (`target_changed=10`, `off_target_changed=0`), and `runtime-preflight`, and it still cleared the focused duel gate at `win_rate=0.9167`, `confidence=0.9968`, and `candidate_avg_ms=94.50`. But the larger confirmation batch stayed exactly flat on quality at `win_rate=0.7812` and `confidence=0.9989` while slowing from the retained `candidate_avg_ms=100.11` to `103.92`, so this is still not promotable. The durable lesson is that even cutting the shared force/focus path for the first early-white setup-progress head is another confirm-flat replay repair, not the promotion path; do not retain more early-white setup-progress force/focus guards unless `pro-reliability-confirm` actually improves. After this repair, the next live `opening 0 / ab` seam moved later and out of the engine path: at `ply=18`, plain search still diverged with `l7,7;l8,8` versus the current/runtime-safe `l7,4;l8,4`.
