# Automove Knowledge

This document keeps only durable lessons that should outlast the current session.

Use `HOW_TO_ITERATE_ON_AUTOMOVE.md` for the operator loop and `AUTOMOVE_IDEAS.md` for the next live split.

## Stable Runtime Truths

- Shipping Pro is `runtime_current`.
- `runtime_pro_turn_engine_v30` is the only live Pro challenger.
- `runtime_pro_turn_engine_v1` is reference-only regression history.
- `runtime_pro_turn_engine_v30` is a guarded `ProV2` path with deliberate opening-book and early-white fallbacks, not a raw always-on engine.
- Full-turn planning can beat per-input expansion, but only if it survives the real ranked-root surface and direct duel evidence.
- Opportunity-context extraction is worth keeping separate from raw input search. It remains useful shared structure for planner seeds and selector guards.
- Continuation reuse and no-plan reuse matter, but cache keys must include a config fingerprint.
- Drainer safety, root reply-risk guards, and efficiency tie-breaks still earn their keep because they cheaply reject fake-good roots.
- Opening-specific latency still matters. A candidate that stalls on the first real black reply is not promotable.
- Hybrid fallbacks must respect retained opening and eligibility guards before they call expensive plan probes.
- Wrapper-only tuning saturates quickly. Once the obvious knobs are in place, the next gain usually has to come from shared engine or search code.
- Global `SmartSearchConfig` knob space is effectively exhausted. Future gains need new code or a sharper selector/search hypothesis, not another broad retune.
- Weak deferred progress heads are a repeat failure mode. Keep blocking them when the real selected root already offers a safer immediate pickup, a concrete setup window, or an unsafe non-progress root that the head does not concretely improve.
- Production wasm still needs single-shot, predictable search. Deferred or post-return work is not release-safe.
- The retained churn probe is worth keeping. For live ProV2 misses, distinguish `pre_accept` search choice from final `engine_post_search` output before changing shared heuristics.
- The runtime-faithful retained churn probe is worth keeping too. It must inject forced engine inputs before `focused_root_candidates_with_forced_inputs(...)`, or it can misclassify injected-root seams as selector churn.
- The compare-oriented hotspot probe is worth keeping. On Apr 5, 2026 it showed the bounded reliability hotspot corpus was move-identical to `runtime_current` on every real duel hotspot, with only a synthetic `quiet_positional` difference.
- The duel-replay probe is worth keeping too. On Apr 8, 2026 `smart_automove_pro_reliability_duel_trace_probe` replayed the exact `pro-reliability` seeds, exposed a repeated real fast-duel `engine_post_search` `SafeSupermanaProgress` override, and showed that the hotspot corpus was no longer sufficient by itself to explain the live wall.
- Matching a forced head is weaker than matching the full runtime-faithful surface. On Apr 8, 2026 the fresh `v15` Normal white board `l10,5;l9,4` vs current `l4,9;l4,7;l5,7` looked close to retained `primary_harvest_white_score_route_win_b` because both involved forced `l10,5;l9,4`, but the traced board was already `engine_post_search` with `selected=pre_accept=head=l10,5;l9,4` and `goal_family=DrainerSafetyRecovery`, while retained `win_b` stayed `engine_disabled` with `selected=baseline=l10,7;l9,8`, `pre_accept=l10,6;l9,5`, and `goal_family=ImmediateScore`.
- Re-running the hotspot compare after the retained `primary_pvs_sensitive_search` repair did not expose a new real duel seam. On Apr 8, 2026 every real hotspot was still move-identical to `runtime_current`, so `human_win_pro_c` remained a triage-only selector drift rather than promotion evidence.
- The current baseline `l4,1;l5,0;mb` now supports two distinct retained black `engine_post_search` reranks, not one. On Apr 8, 2026 `primary_black_mana_bridge_ply20` retained the accepted `SafeSupermanaProgress -> SpiritImpact` mana rerank `l0,5;l1,4`, and fresh seed `v16` retained `primary_black_spirit_bridge_ply19` for the repeated accepted `SpiritImpact -> SpiritImpact` rerank `l1,5;l1,7;l0,7`. Both end with `selected=pre_accept=head` and `accepted=true`, but they are different black surfaces, so shared current-bias repairs around `l4,1;l5,0;mb` are still too broad.
- Those two retained `l4,1;l5,0;mb` black seams also share the same injection geometry. On Apr 8, 2026 widening `smart_automove_pro_black_forced_root_probe` showed both `primary_black_mana_bridge_ply20` and `primary_black_spirit_bridge_ply19` keep current `l4,1;l5,0;mb` at raw rank `0`, inject the forced bridge root only at rank `1`, and then promote it to focused rank `0`. The retained late-head seam `primary_black_late_accepted_head_ply4` stays different: it injects `l1,5;l1,7;l0,7` straight to rank `0` and remains the rejected `ImmediateScore` head story.
- The current baseline `l2,3;l3,4` now supports at least one distinct later-black accepted runtime seam too. On Apr 9, 2026 widening `smart_automove_pro_black_forced_runtime_probe` with the traced Fast `v69` board showed `l1,6;l2,7` vs current `l2,3;l3,4` is not the retained `primary_black_turn_four_action_mana_ply15` surface even though it reuses the same forced root and `SafeSupermanaProgress -> ImmediateScore` story: runtime-faithful v30 still selects and heads on forced `l1,6;l2,7`, but `pre_accept` and baseline both stay on current `l2,3;l3,4` instead of already collapsing onto the forced root. Do not reopen a black production split from that one cross-bucket replay alone; the same forced root now appears on another current baseline with different pre-accept geometry.
- Even a narrow current-Pro fallback for that black bridge-injection family is still too local. On Apr 9, 2026 routing only black `turn=4`, `mons_moves=2`, `action+mana` boards where current Pro already selected `l4,1;l5,0;mb` back to current cleanly collapsed both retained bridge seams, passed `guardrails`, moved `pro-triage(primary_pro)` to `3/60` with `off_target_changed=0`, and passed `runtime-preflight`, but `pro-reliability` still failed at `0.8333` vs current Pro, `0.5000` vs current Normal, and `0.7500` vs current Fast.
- Even a narrower inject-time block on the retained later-black action+mana seam is still too local. On Apr 9, 2026 rejecting absent forced `ManaTempo` same-turn-window roots under `Safe*Progress -> ImmediateScore` collapsed `primary_black_turn_four_action_mana_ply15`, passed `guardrails`, moved `pro-triage(primary_pro)` to `4/60` with `off_target_changed=0`, and passed `runtime-preflight`, but `pro-reliability` got worse at `0.7500` vs current Pro, `0.5000` vs current Normal, and `0.5833` vs current Fast.
- The current retained seam map is stable enough to plan around: `primary_pvs_sensitive_search` now has a retained late `engine_post_search` fix, `human_win_pro_c` remains a retained `pre_accept` safe-progress bias, `primary_black_turn_four_action_mana_ply15` is now the retained later-black forced-engine seam, and the previously live `primary_white_harvest_loss_c_ply24`, `primary_spirit_setup`, and `primary_black_reliability_opening_3_ply4` seams still hold their retained fixes.
- `primary_spirit_setup` was a two-step bug, not one seam: the engine was force-pinning an existing low-ranked plain `SpiritImpact` head into the focused shortlist, then a completed-plan override was still allowed to replace an equivalent stronger selected plain spirit sibling. Both checks have to stay aligned.
- The black turn-two low-budget clamp was too broad. On `turn=2`, `mons_moves=1`, `action+mana` states it could suppress a stronger spirit-own-setup root; the clamp should only fire on truly resource-constrained turn-two black states.
- White `turn=3`, mana-only mid-turn wrapper regressions are real, but still not enough by themselves. On Apr 8, 2026 routing those traced boards back to the current Pro surface fixed the replayed duel choices, yet `pro-triage(primary_pro)` stayed flat at `1/52`, so the guard repair was still too local to earn more spend.
- Even a broader duel-backed wrapper bundle can still be too local. On Apr 8, 2026 routing all traced white `turn=3` mid-turn boards plus black `turn=2`/`turn=4` one-move `action+mana` boards back to the current Pro surface fixed the targeted replay boards and cleared `guardrails`, but `pro-triage(primary_pro)` still stayed flat at `1/52`.
- Simple speculative immediate-score non-regression clamps and setup-gain-only spirit-setup promotion are not enough. They can reshuffle `primary_pro` fixtures and regress direct Pro-vs-Pro without moving the `vs current Normal` wall.
- Broad same-lane `spirit_own_mana_setup_now` overrides are too blunt as well. On Apr 8, 2026 a shared ProV2 override collapsed `human_win_pro_c` and fixed one traced normal-duel board, but it reopened `primary_black_reliability_opening_3_ply4` and regressed `pro-reliability` to `0.7500` vs current Pro and `0.4167` vs current Normal/Fast.
- Late-white full-resource current-Pro guards are not a useful `human_win_pro_c` lever either. On Apr 8, 2026 a late white turn-start safe-supermana wrapper did not change the selected move on the live `human_win_pro_c` fixture.
- A late-white omitted-root reply-risk rescue is too local as well. On Apr 8, 2026 it cleared `human_win_pro_c` without reopening black reliability, but that only collapsed `pro-triage(primary_pro)` to `0/52`, which still fails because the cheap target surface must move.
- Soft followup-tolerance for `spirit_own_mana_setup_now` roots and close quiet-root normal-safety blocks are also not enough by themselves. If the runtime-faithful seam stays unchanged, kill the split before spending the canonical loop.
- Eval-only progress-head wins are too soft for unsafe late overrides. When both the selected root and the `Safe*Progress` head stay unsafe, do not let a lower-scored progress head replace the selected non-progress root unless it brings a non-eval strategic gain or a forced `score_delta` jump.
- Even a real duel-traced acceptance seam can still be too local. On Apr 8, 2026 a bounded fast-duel `Safe*Progress` override clamp matched the traced seed, but it left `pro-triage(primary_pro)` unchanged at `1/52`, so the production split was killed before `runtime-preflight` and `pro-reliability`.
- Once the wrapper-local misses are gone, the live wall can shift cleanly into later `engine_post_search` drift. On Apr 8, 2026 the wrapper bundle plus late-white omitted-root rescue left `vs current Pro` at `0` regressions / `2` improvements, but `vs current Normal` still sat at `2` regressions / `1` improvement and `vs current Fast` at `3` regressions / `3` improvements, so the next spend would have to attack post-search head acceptance rather than more wrapper or human-only fixes.
- When debugging `engine_post_search` head acceptance, print both `turn_engine_selected_override_utility(...)` and the raw selected-root `turn_engine_root_plan_utility(...)`. On Apr 8, 2026 a retained fast white turn-seven seam looked like a weak raw selected root, but the real gate was comparing against a much stronger projected `selected_override_utility`.
- A multi-chunk `ImmediateScore` near-tie `ManaTempo` clamp can still be too local. On Apr 8, 2026 it cleared `human_win_pro_c`, fixed the bounded duel-accept seams, and passed `guardrails`, `pro-triage(primary_pro)=1/52`, and `runtime-preflight`, but `pro-reliability` still failed at `0.7500` vs current Pro, `0.6667` vs current Normal, and `0.5000` vs current Fast. A sampled duel trace still diverged on a later black turn-four `engine_post_search` choice (`l1,6;l2,7` vs current `l2,3;l3,2`), so future spend has to target the broader later duel wall rather than another near-tie accept clamp.
- The sampled fast-duel black turn-four seam is wrapper-shaped but still too local on its own. On Apr 8, 2026 an isolated `turn=4`, `mons_moves=1`, `action+mana` current-Pro guard did route the live board back from `l1,6;l2,7` to current `l2,3;l3,2`, but `pro-triage(primary_pro)` still stayed unchanged at the same `human_win_pro_c`-only `1/52`.
- The remaining human drift and the sampled fast black turn-four duel seam do not currently share one projection-family fix. On Apr 8, 2026 the shared probe showed `human_win_pro_c` winning through a higher followup floor on a root whose post-root projection became `SpiritImpact -> ImmediateScore`, while the fast duel board stayed an injected vulnerable `ManaTempo` root whose post-root projection became `SafeSupermanaProgress -> ImmediateScore`.
- A repeated later-black duel seam can justify its own retained cheap-surface foothold too. On Apr 8, 2026 the sampled `l1,6;l2,7` vs current `l2,3;l3,2` family became `primary_black_turn_four_action_mana_ply15` plus a widened probe, and the cheap gate moved to `2/57` with only that new seam plus `human_win_pro_c` changed.
- That later-black action+mana seam is not a final accept-after-search clamp. On Apr 8, 2026 the widened retained probe showed raw ranked roots and current still prefer `l2,3;l3,2`, but runtime-faithful v30 injects `l1,6;l2,7` and already collapses selected, `pre_accept`, and head onto the same vulnerable `ManaTempo` root under a `SafeSupermanaProgress -> ImmediateScore` plan.
- Compare retained black seams at injection time before assuming they share one production rule. On Apr 8, 2026 the black forced-root comparison probe showed both retained black families inject a root that was absent from raw roots and jump it straight to rank `0`, but the action+mana seam injects a vulnerable non-progress `ManaTempo` root while the later black family injects a genuine `SpiritImpact` progress root.
- A fresh traced black replay can still sit between those two retained families without justifying one shared injected-root rule. On Apr 8, 2026 the widened black forced-root probe added a traced fast `v7` board `l0,5;l1,4` vs current `l4,1;l5,0;mb`; it also injected an absent raw root, but only to injected rank `1` before focused rank `0`, with `head_family=SafeSupermanaProgress` and `goal_family=SpiritImpact`.
- Do not keep a shared black injected non-progress progress-head block just because it closes the retained action+mana seam and nearby traced fast replay boards. On Apr 8, 2026 that shared guard passed `guardrails`, returned `pro-triage(primary_pro)` to `1/57` with only `human_win_pro_c`, and passed `runtime-preflight`, but `pro-reliability` still failed at `0.5833` vs current Pro, `0.4167` vs current Normal, and `0.6667` vs current Fast.
- Even with the retained foothold, the isolated black wrapper repair is still not promotable. On Apr 8, 2026 a narrow current-Pro reroute closed `primary_black_turn_four_action_mana_ply15`, passed `guardrails`, returned `pro-triage(primary_pro)` to `1/57` with only `human_win_pro_c`, and passed `runtime-preflight`, but `pro-reliability` still failed at `0.7500` vs current Pro, `0.5000` vs current Normal, and `0.5833` vs current Fast.
- A fresh replay can stay non-actionable even after a new retained black foothold lands. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v7` still produced only count-`1` move pairs across Pro, Normal, and Fast; the later-black accepted-head seam resurfaced only once in Normal, while the other regressions were all new white or black one-offs, so there was still no repeated duel family worth code.
- A fresh replay can also surface attractive new top-ranked engine-post-search drifts and still be a kill. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v8` exposed a Pro black `SafeSupermanaProgress` root `l1,4;l2,5` over current `l2,3;l3,4`, a Normal white spirit-own-setup rerank `l3,8;l1,7;l2,8` over `l3,8;l3,6;l4,7`, and two Fast black spirit families, but every move pair still stayed at count `1` across Pro, Normal, and Fast.
- A cleaner duel seed is still not enough if the remaining wall fragments only in Fast. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v9` produced `0` regressions vs current Pro and only `1` regression vs current Normal, but Fast still broke into six unrelated count-`1` misses across black spirit families, black `ManaTempo` sibling reranks, and white spirit/forced-prepass reranks. That is still a diagnostics-only kill, not a code-ready family.
- The inverse is also true: Fast going clean does not make a duel replay actionable by itself. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v12` finished with `0` regressions vs current Fast, but direct Pro and Normal still each contained only count-`1` one-offs, including black spirit reranks `l0,4;l1,3` vs `l0,4;l1,5` and `l2,5;l0,5;l1,6` vs `l3,2;l4,1`, a black `ManaTempo` rerank `l1,5;l2,5` vs `l1,6;l0,6`, and a white safe-progress rerank `l9,5;l8,5` vs `l10,7;l9,8`. Kill that replay at diagnostics too.
- The recurring black `l0,5;l1,4` vs `l4,1;l5,0;mb` family now has a retained foothold. On Apr 8, 2026 `primary_black_mana_bridge_ply20` matched the old traced fast `v7` board exactly: the forced root was absent from raw roots, injected to rank `1`, then promoted to focused rank `0`, with `head_family=SafeSupermanaProgress` and `goal_family=SpiritImpact`.
- Compare retained black seams at runtime-faithful selection time too, not only at injection time. On Apr 8, 2026 `primary_black_turn_four_action_mana_ply15` and `primary_black_mana_bridge_ply20` both ended as `engine_post_search` accepted `ManaTempo` roots with `selected=pre_accept=head=forced` under a `SafeSupermanaProgress` head, while current stayed on a nearby `ManaTempo` baseline. That means the remaining difference is a narrow utility and risk preference, not a clean shared current-forcing repair.
- The same current black baseline can still branch into multiple unrelated live stories. On Apr 8, 2026 widening `smart_automove_pro_black_forced_runtime_probe` with the `v12` direct-Pro board `l2,5;l0,5;l1,6` vs current `l3,2;l4,1` showed a third distinct shape on that baseline: `selected=pre_accept=head=forced`, `accepted=true`, `head_family=SpiritImpact`, `goal_family=ImmediateScore`, and a vulnerable spirit root. That is neither the retained late-black rejected-head surface nor the retained non-vulnerable mana-bridge seam, so shared current `l3,2;l4,1` is still not enough to justify one black production rule.
- The same early-black baseline move can split into different runtime stories too. On Apr 8, 2026 `smart_automove_pro_black_spirit_sibling_probe` compared the fresh `v12` direct-Pro board `l0,4;l1,3` vs current `l0,4;l1,5` against several existing opening fixtures that already target `l0,4;l1,5`. The traced board was still separate: `forced_inputs=None`, `stage=engine_post_search`, `accepted=false`, and `pre_accept` already preferred `l0,4;l1,3`, while the opening fixtures either stayed fully unforced on current or carried forced `SafeSupermanaProgress` heads such as `l0,5;l1,6` or `l0,5;l1,5`.
- Accepted non-vulnerable black `ManaTempo` reranks still split into multiple distinct surfaces. On Apr 8, 2026 widening `smart_automove_pro_black_forced_runtime_probe` with the `v12` Normal board `l1,5;l2,5` vs current `l1,6;l0,6` showed another branch: `pre_accept` stayed on current, then `runtime_pro_turn_engine_v30` accepted `l1,5;l2,5` under `head_family=ManaTempo`, `goal_family=DrainerSafetyRecovery`, and `stage=engine_cached_resume`. That is different from the retained non-vulnerable mana-bridge seam and also different from the traced fast `v10` rerank, which used current `l3,2;l4,1` and `goal_family=SpiritImpact`.
- The same current black baseline can still split into yet another accepted family. On Apr 8, 2026 widening `smart_automove_pro_black_forced_runtime_probe` with the `v13` Normal board `l1,6;l1,5` vs current `l3,2;l4,1` showed a new branch on that same baseline: `selected=pre_accept=head=l1,6;l1,5`, `accepted=true`, `stage=engine_post_search`, and both `head_family` and `goal_family` equal `DrainerSafetyRecovery` on a non-vulnerable root. That is different from the retained late-black rejected-head surface, the retained mana-bridge seam, the traced fast `v10` accepted `ManaTempo` rerank, and the traced `v12` accepted spirit-head board.
- The same white move string can also hide different safe-progress surfaces. On Apr 8, 2026 `smart_automove_pro_white_safe_progress_probe` showed the fresh Normal board `l9,5;l8,5` vs current `l10,7;l9,8` was not the retained `primary_white_safe_progress_rerank_ply27` seam and not the retained `primary_white_fast_screen_opening_0_ply9` seam: the traced board already had `selected=pre_accept=head=l9,5;l8,5`, `accepted=true`, `head_family=SafeSupermanaProgress`, and `goal_family=DrainerSafetyRecovery`, while the retained safe-progress seam is an accepted vulnerable `ManaTempo` rerank and the retained fast-screen surface leaves the same `l9,5;l8,5` head rejected under spirit-progress pre-accept/current roots.
- Family resemblance across duel buckets is still weaker than exact pair repetition. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v10` resurfaced the retained early-black `negative_deny` seam in direct Pro, the later-black accepted-head seam in Normal and Fast, and the known white forced-prepass family in Normal and Fast, but every exact move pair still stayed count `1`. Treat that as another diagnostics-only kill, not as permission to merge those families into one production branch.
- A repeated move pair inside `vs current Normal` still is not a retained Pro foothold by itself. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v11` repeated white `l9,5;l7,4;l8,3` vs current `l9,5;l7,6;l7,7` twice in Normal, but probing that exact board in Pro mode showed `selected=pre_accept=baseline=l10,4;l9,4`, `forced_inputs=None`, `accepted=false`, and both repeated Normal targets absent from the Pro shortlist. Kill that branch at diagnostics rather than retaining a fake `primary_pro` seam.
- Shared current baselines are still weaker than runtime-faithful stage matching. On Apr 8, 2026 a fresh Fast black drift `l1,5;l1,4` vs current `l3,2;l4,1` looked adjacent to the retained later-black accepted-head seam because both boards share current `l3,2;l4,1`, but the widened black forced-runtime probe showed a different story: `pre_accept` stayed on current, then `runtime_pro_turn_engine_v30` accepted a non-vulnerable `ManaTempo` head `l1,5;l1,4` under `goal_family=SpiritImpact`. That is a separate live rerank, not the retained late-black rejected-head surface.
- Not every white accepted-head board with `goal_family=ImmediateScore` belongs to the retained harvest score-route family. On Apr 8, 2026 `primary_white_safe_progress_rerank_ply27` retained a fresh Normal board where `runtime_pro_turn_engine_v30` already had `selected=pre_accept=head=l9,4;l8,3`, `accepted=true`, `drainer_vulnerable=false`, and a vulnerable `ManaTempo` root over a non-vulnerable `SafeSupermanaProgress` current baseline `l5,2;l4,1`. That is a separate accepted white rerank, not the retained harvest surface with `accepted=false` and vulnerable spirit siblings.
- The live duel wall can shift into a broader white turn-three family without touching the retained cheap surface. On Apr 8, 2026 a fresh duel trace showed three white `turn=3`, `mons_moves>0`, `action=false`, `mana=true` boards across Pro/Normal/Fast were all broad fallback drifts, and one repeated white `turn=3`, `mons_moves=1`, `action=true`, `mana=true` board was still a separate accepted-head seam.
- Even a broader white turn-three mana-only reroute can still be too local. On Apr 8, 2026 routing all white `turn=3`, `mons_moves>0`, `action=false`, `mana=true` states back to the current Pro surface fixed three live duel boards, but `pro-triage(primary_pro)` still stayed unchanged at the same `human_win_pro_c`-only `1/52`.
- A repeated live accepted-head seam still is not enough without a retained target-surface foothold. On Apr 8, 2026 the white `turn=3`, `mons_moves=1`, `action+mana` fast-duel seam (`l9,4;l8,4` vs `l8,7;l7,8`) repeated twice, but the runtime-faithful retained churn probe still showed `accepted=false` on every retained `primary_pro` fixture.
- A fresh early black spirit-own-setup drift can still be the wrong merge. On Apr 8, 2026 the new normal-duel board `l0,5;l1,6` vs current `l1,5;l3,6;l2,7` looked superficially like the retained `human_win_pro_c` setup-vs-progress bias, but the focused selector probe showed `negative_deny_competes=true` and `followup_progress_competes=false`, so it was a separate negative-deny spirit-preference seam rather than the retained human followup-progress story.
- A lone `vs current Pro` white regression can still be only wrapper churn. On Apr 8, 2026 the board `l9,2;l8,3` vs current `l10,7;l9,7` looked like a new Pro-only selector miss, but the focused probe showed the configured v30 path still pre-accepted the current-style root and the outer `runtime_pro_turn_engine_v30_guarded_inputs(...)` wrapper was the piece rerouting the board through the broad white `turn=3`, `action=false`, `mana=true` fast fallback.
- A later black `ManaTempo` drift can still be only a one-off forced shortlist seam. On Apr 8, 2026 the normal-duel board `l2,4;l3,3` vs current `l3,6;l2,7` injected a terrible `ManaTempo` head `l3,6;l3,7`, left it unaccepted, and still pre-accepted `l2,4;l3,3`; the retained runtime-faithful churn surface still had no matching forced-input seam outside the already-closed `primary_pvs_sensitive_search` case.
- A white fast-duel one-off can still be only wrapper churn too. On Apr 8, 2026 the board `l9,5;l8,5` vs current `l7,5;l6,4` looked like a new opening-family selector drift, but the focused probe showed configured v30 still pre-accepted current while the live path returned a move that was not even present in the configured root shortlist, so it was another outer white `turn=3`, `action=false`, `mana=true` wrapper miss.
- A white normal-duel one-off can still be only wrapper churn too. On Apr 8, 2026 the board `l10,4;l9,4` vs current `l8,6;l7,7` looked like a new opening-family selector drift, but the focused probe showed configured v30 still pre-accepted current on another white `turn=3`, `action=false`, `mana=true` board, so it was again only wrapper-level drift rather than a retained selector seam.
- A retained white mana-sibling seam can still reduce to wrapper churn too. On Apr 8, 2026 `primary_white_mana_sibling_ply9` looked like a new selector foothold until the focused wrapper probe showed the board was really `turn=3`, `mons_moves=3`, `action+mana`: guarded `runtime_pro_turn_engine_v30` fell through to `runtime_release_safe_pre_exact` and picked `l5,0;l5,1`, while current Pro still chose `l5,0;l4,1`. Lowering the existing white turn-three full-resource current-Pro fallback from `mons_moves>=5` to `mons_moves>=3` safely closed the retained seam and passed `guardrails`, `pro-triage`, and `runtime-preflight`, but the default `pro-reliability` wall stayed unchanged one-off churn.
- A fresh duel replay with only count-`1` move pairs is still a kill signal. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v2` produced no repeated move pair across direct Pro, Normal, or Fast duels; even the apparent later black `SpiritImpact` takeover and the resurfaced early black `negative_deny` seam each appeared only once per duel, so the right move was to stop at diagnostics and wait for a replay that also repeats on a retained `primary_pro` surface.
- Even a seed that resurfaces several retained black seams across buckets is still only diagnostics if each exact pair stays count-`1`. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v13` replayed the retained direct-Pro mana-bridge seam `l0,5;l1,4` vs current `l4,1;l5,0;mb`, the retained direct-Pro early-black `negative_deny` seam `l0,5;l1,6` vs current `l1,5;l3,6;l2,7`, and the retained Fast black turn-four action+mana seam `l1,6;l2,7` vs current `l3,2;l4,1`, but each appeared only once and Normal added only another one-off accepted black drainer-safety rerank `l1,6;l1,5` vs `l3,2;l4,1`. That is still fragmented black churn, not one bounded shared production rule.
- A cleaner replay is still only diagnostics if Normal and Fast remain count-`1`. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v14` reduced direct Pro to a single black spirit-sibling rerank `l0,4;l1,4` vs current `l0,4;l1,5`, but Normal still had two one-off black regressions and Fast still had only one-off white forced-prepass plus retained black mana-bridge drift. Do not promote or cut code from that shape just because the direct-Pro bucket looks cleaner.
- A replay that mixes retained seams with nearby sibling ties is still only diagnostics if every exact pair stays count-`1`. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v15` replayed the retained black mana-bridge seam in direct Pro, later-black accepted-head in Normal, and several nearby black and white sibling reranks, but Pro/Normal/Fast all stayed count-`1`. That is still not a code-ready family; keep only the note and wait for one exact surface to repeat.
- Even a fresh direct-Pro white `l8,4;*` split can still miss both the retained fast-screen surface and the old forced-prepass shell. On Apr 8, 2026 widening `smart_automove_pro_white_fast_forced_prepass_probe` with the `v15` board `l8,4;l7,3` vs current `l8,4;l9,3` showed `selected=l8,4;l7,3`, `pre_accept=baseline=head=l8,4;l9,3`, `forced_inputs=Some("l8,4;l9,3")`, `stage=engine_disabled`, and all nearby `l8,4;*` roots stayed the same vulnerable `ManaTempo` family. The retained `primary_white_fast_screen_opening_0_ply9` fixture still stayed `drainer_vulnerable=false` on spirit-progress roots with no `l8,4;*` siblings at all, while the older traced forced-prepass shell had `stage=search_only_forced_prepass` and a safe `DrainerSafetyRecovery` selected root. Shared move neighborhood is still weaker than runtime-faithful stage shape.
- Even a fresh direct-Pro black sibling rerank can still miss the retained opening surface right next to it. On Apr 8, 2026 widening `smart_automove_pro_black_spirit_sibling_probe` with the `v14` board `l0,4;l1,4` vs current `l0,4;l1,5` showed `selected=pre_accept=l0,4;l1,4`, `forced_inputs=None`, `stage=engine_post_search`, and a rejected head `l0,5;l1,6` under `goal_family=DrainerSafetyRecovery`, while the retained `primary_black_loss_opening_b_black_turn` fixture still stayed `engine_disabled` on current `l0,4;l1,5` with no head at all. That means even the nearby opening-`b` fixture is a different surface, so sharing a baseline cluster or sibling move string is still not enough for a shared early-black opening rule.
- A repeated live candidate can still be only a `search_only_forced_prepass` shell with no retained foothold. On Apr 8, 2026 the fresh `pro_turn_planner_reliability_v3` replay surfaced white fast `l8,4;l8,5` twice in `vs current Fast`, but the focused probe showed current and `pre_accept` both already prefer `l8,4;l7,3`; only the early `search_only_forced_prepass` shell overrides that to `l8,4;l8,5`, and the nearby `primary_white_fast_screen_opening_0_ply9` fixture did not share the same targets or stage.
- A later fresh replay can still collapse back into pure count-`1` churn immediately after a promising diagnostic family. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v4` produced only one-off accepted-head, spirit-setup, and mana-tempo reranks across direct Pro, Normal, and Fast, so none of those new-looking seams justified a retained fixture or production split.
- A direct-Pro black spirit-vs-spirit drift is not automatically a retained black seam. On Apr 8, 2026 the one-off `l1,5;l2,3;l3,3` vs current `l1,5;l2,7;l1,8` board looked adjacent to the retained black families, but the focused probe showed all spirit-pref competition flags stayed `false`, the forced head `l1,5;l1,7;l0,7` was still rejected, and the whole candidate shortlist stayed a vulnerable plain-spirit cluster. That means it is not the retained early-black `negative_deny` story and not the retained later black accepted-head story either.
- A direct-Pro white spirit-own-setup drift is not automatically `human_win_pro_c` in reverse. On Apr 8, 2026 the one-off `l4,9;l4,7;l5,7` vs current `l9,4;l8,3` board looked superficially like the retained human spirit-vs-progress surface, but the focused probe showed `followup_progress_competes=false`, `progress_competes=false`, and `risky_score_competes=true` while both roots remained vulnerable. That makes it a separate risky-score spirit cluster, not the retained human followup-floor seam.
- When the retained cheap surface is down to `human_win_pro_c` alone, that is not permission to reopen a human-only production split. On Apr 8, 2026 the refreshed `human_win_pro_c` selector probe still showed the same safe-progress / followup-floor story, while the fresh direct-Pro white one-off stayed a separate risky-score spirit cluster. With the live duel wall still off-surface count-`1` churn, another human-only fix would still be a dead line.
- A repeated live family can justify a retained fixture addition even when the old retained surface misses it. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v3` repeated the early black `l0,5;l1,6` vs current `l1,5;l3,6;l2,7` seam `3x` in direct Pro, `2x` in Normal, and `1x` in Fast, so the right durable move was to add `primary_black_negative_deny_ply4` plus a focused selector probe rather than keep treating it as replay-only.
- The repeated black early seam is an over-broad plain-spirit `negative_deny` story, not a followup-progress story. On Apr 8, 2026 the new retained selector probe showed `negative_deny_competes=true` while `progress_competes`, `followup_progress_competes`, `risky_score_competes`, `score_competes`, `projection_competes`, and `risky_recovery_competes` all stayed `false`; the losing plain-spirit projections were the piece blocking stronger spirit-own-setup roots.
- Even that shared selector cut can still be too local. On Apr 8, 2026 a narrow override that ignored the plain-spirit `negative_deny` block when a spirit-setup root already beat every non-spirit challenger fixed `primary_black_negative_deny_ply4` and passed `guardrails`, but `pro-triage(primary_pro)` still fell back to the stale `human_win_pro_c`-only `1/53`, so the production change was killed before `runtime-preflight`.
- Even a retained seam that survives the full cheap loop can still be the wrong branch. On Apr 8, 2026 the same early-black `negative_deny` override was pushed through `guardrails`, `pro-triage(primary_pro)=1/53`, and `runtime-preflight`, but `pro-reliability` still failed at `0.8333` vs current Pro, `0.4167` vs current Normal, and `0.5833` vs current Fast; a fresh `v3` duel trace showed the live wall had shifted to a later black accepted-head seam `l1,5;l1,7;l0,7` vs current `l3,2;l4,1` repeated `2x` in Pro and `3x` in Normal, while the old early-black family still resurfaced once in Pro.
- The later black accepted-head family now has a retained foothold too. On Apr 8, 2026 `primary_black_late_accepted_head_ply4` plus `smart_automove_pro_black_late_accepted_head_probe` showed current and `pre_accept` both prefer `l3,2;l4,1`, while a forced plain `SpiritImpact` head `l1,5;l1,7;l0,7` can still be accepted on projected-plan utility even though the head root is lower-scored and brings only non-concrete spirit progress.
- Closing both retained black seams together is still not enough. On Apr 8, 2026 the combined early-black `negative_deny` override plus late-black plain-spirit-progress block passed `guardrails`, moved `pro-triage(primary_pro)` to `1/54` with only `human_win_pro_c`, and passed `runtime-preflight`, but `pro-reliability` still failed at `0.8333` vs current Pro, `0.5000` vs current Normal, and `0.5833` vs current Fast. The default duel replay then fell back to one white wrapper miss in direct Pro, three one-off Normal misses, and the old repeated white fast accepted-head seam, so future spend should not keep black-only production cuts without a broader white live-wall story.
- The repeated white fast accepted-head family now has a retained foothold too. On Apr 8, 2026 `primary_white_fast_accepted_head_ply13` plus `smart_automove_pro_white_fast_accepted_head_probe` showed current and `pre_accept` both prefer `l8,7;l7,8`, while a forced `SafeSupermanaProgress -> DrainerSafetyRecovery` head `l9,4;l8,4` could still be accepted even though the head root stayed unsafe and brought no concrete immediate recovery.
- Closing the retained white fast accepted-head seam is useful, but still not promotable. On Apr 8, 2026 the combined retained branch that fixed early-black `negative_deny`, later-black accepted-head drift, and the new white deferred-recovery accepted head passed `guardrails`, moved `pro-triage(primary_pro)` to `1/55` with only `human_win_pro_c`, and passed `runtime-preflight`, but `pro-reliability` still failed at `0.8333` vs current Pro, `0.5000` vs current Normal, and `0.7500` vs current Fast. The default duel replay showed the repeated white fast seam was gone and Fast regressions were down to three one-offs, yet Pro still had one wrapper one-off and Normal still had the same three one-offs.

## Durable Workflow Rules

- Keep the active frontier small: one live idea, one retained candidate, one canonical path.
- For Pro work, compare directly against `runtime_current`.
- The canonical Pro path is `guardrails -> pro-triage(primary_pro) -> runtime-preflight -> pro-reliability`.
- `opening_reply` is a narrow fallback-order and opening-regression surface, not the default Pro surface.
- `pro-triage` only passes when the target surface changes and off-target churn stays at `<= 1`.
- `runtime-preflight` is the required stamp before duel stages unless the run is intentionally diagnostic.
- `pro-reliability` is the first real promotion gate. It must clear current `Pro`, current `Normal`, and current `Fast` at `win_rate >= 0.90`, `confidence >= 0.99`, and `candidate_avg_move_ms <= 700`.
- `pro-reliability-confirm` is the final promotion proof. Do not promote on smaller-corpus evidence.
- Prefer a fresh live `pro-reliability` sample over diagnostic probes when the wall is unclear.
- Use `triage-calibrate` only when a retained triage surface is new or no longer calibrated.
- Use `pro-opening-speed-probe` only for opening-specific regressions.
- Use the hotspot probe only after a real duel stall, and only to narrow the next code surface.
- When the hotspot corpus stays flat but the direct wall is still unclear, use the duel-replay probe before changing shared code.
- If the compare hotspot probe shows decision parity on the real hotspot cases, kill the line immediately. Selector/exact counter deltas without candidate-vs-current move differences are not promotion evidence.
- Logs, stamps, and process samples are disposable evidence, not durable memory.
- Keep ignored harness test names unique; `cargo test` substring filters can hit the wrong stage.

## Mistakes Not To Repeat

- Do not reopen archived profile IDs or retired branch families without a brand-new hypothesis.
- Do not spend another loop on wrapper-only reroutes, current-Normal fallbacks, or replay-specific acceptance clamps just because they fix a traced exact.
- Do not retain a traced white turn-three mana-only wrapper reroute unless it also moves `primary_pro`; fixing replay boards alone was not enough.
- Do not retain a broader white-turn-three plus black one-move wrapper bundle if it still leaves `pro-triage(primary_pro)` unchanged at `1/52`, even when multiple traced duel boards now match current.
- Do not reopen cache-size, memo-shape, reserve-heavy, or hasher experiments without a direct quality story tied to the live duel wall.
- Do not reopen broad search-budget, reply-budget, or generic search-knob clamps without evidence that the real wall lives on that surface.
- Do not keep branches just because disagreement counts shrink or hotspot counters move. If direct duel quality stays flat or regresses, kill the line.
- Do not retain a duel-traced acceptance clamp just because it fixes one repeated seed. If `pro-triage(primary_pro)` does not move, kill the production split and keep only the probe or lesson.
- Do not assume `human_win_pro_c` and the sampled fast black turn-four divergence can be fixed by one shared projection clamp. If the probe says the human drift is a followup-floor `SpiritImpact` story and the duel board is a vulnerable `ManaTempo` progress-head story, kill the shared idea before code edits.
- Do not attack `primary_black_turn_four_action_mana_ply15` with another acceptance-only clamp. If the retained probe shows raw ranked roots still prefer current while forced engine injection already makes `l1,6;l2,7` the selected/pre-accept/head root, the live lever is earlier than the final accept gate.
- Do not merge the retained black action+mana seam and the retained later-black spirit-head seam into one shared injection fix just because both forced roots jump from absent raw roots to injected rank `0`. If one injected root is still a vulnerable non-progress `ManaTempo` root and the other is a genuine spirit-progress root, keep the probe and kill the shared production idea.
- Do not reopen from a fresh duel replay like `pro_turn_planner_reliability_v7` just because it shows both the retained later-black accepted-head move and a new batch of white reranks. If every move pair still stays count-`1` inside its duel bucket, keep only the lesson and wait for a replay that repeats on a retained surface.
- Do not retain a broader white `turn=3`, `mons_moves>0`, `action=false`, `mana=true` reroute just because it fixes several live duel boards. If `pro-triage(primary_pro)` does not move off the stale `human_win_pro_c`-only `1/52`, kill it before `runtime-preflight`.
- Do not retain a repeated white `turn=3`, `mons_moves=1`, `action+mana` accepted-head clamp just because it repeats in fast duels. If the retained churn probe still shows `accepted=false` on every retained `primary_pro` fixture, kill the idea before code edits.
- Do not merge an early black `negative_deny_competes` spirit-setup drift with `human_win_pro_c` just because both are spirit-vs-non-spirit selector stories. If the black board shows `followup_progress_competes=false`, treat it as a separate one-off seam and kill the shared idea before code edits.
- Do not treat a lone `vs current Pro` white mana-only regression as a new selector seam until the probe proves it is not just another `runtime_pro_turn_engine_v30_guarded_inputs(...)` fast-fallback reroute. If configured v30 already pre-accepts the current root, kill the idea before code edits.
- Do not reopen a later black `ManaTempo` shortlist repair unless the retained runtime-faithful churn probe shows a matching forced-input seam on `primary_pro`. A one-off injected head with `accepted=false` is still not enough by itself.
- Do not treat a white fast-duel one-off as a new selector seam if the live move is missing from the configured root shortlist. That is another wrapper-level drift, not a retained in-path surface.
- Do not treat a white normal-duel one-off as a new selector seam if configured v30 already pre-accepts current on another `turn=3`, `action=false`, `mana=true` board. That is still wrapper churn, not a retained in-path surface.
- Do not stretch a fresh duel seed with only count-`1` move pairs into a production split. If Pro, Normal, and Fast all come back as one-off seams, keep only the lesson and wait for a replay that repeats on a retained surface.
- When the canonical cheap gate explicitly reconfirms `opening_reply=0/3`, `primary_pro=1/55`, `off_target_changed=0`, and the only changed fixture is still `human_win_pro_c`, treat the retained frontier as exhausted for now. Do not reopen a human-only selector split from that state while fresh duel seams remain off-surface count-`1` churn.
- Do not reopen from a fresh duel replay just because previously known black families resurface. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v5` brought back the early-black `negative_deny` seam, the later-black accepted-head seam, and a new white forced-prepass drift, but every move pair still stayed count-`1` inside its duel bucket, so the replay was still non-actionable churn.
- Do not reopen a white forced-prepass production split just because a new duel replay lands on another `DrainerSafetyRecovery` root. On Apr 8, 2026 the widened white forced-prepass probe showed the new Normal `l9,4;l8,5` vs `l9,4;l8,3` seam is the same live-only `search_only_forced_prepass` family as the old Fast board: the live boards are `drainer_vulnerable=true` and override vulnerable `ManaTempo` pre-accept/current roots, while the retained comparison fixture still stays `drainer_vulnerable=false` and `engine_disabled`.
- Do not reopen a white score-route production split just because `l7,4;l8,3` reappears in a fresh direct-Pro replay. On Apr 8, 2026 the dedicated white score-route probe showed the traced `l7,4;l8,3` vs `l9,7;l7,6;l8,7` seam is an accepted `SafeSupermanaProgress -> ImmediateScore` override on a vulnerable board, while the retained harvest fixture `primary_harvest_white_score_route_win_a` stays a different `engine_post_search` surface where current already matches and the plain `l7,4;l8,3` mana route is only a lower-ranked sibling.
- Do not reopen the retained late-black accepted-head fix just because `l1,5;l1,7;l0,7` reappears in a fresh duel replay. On Apr 8, 2026 the widened black late accepted-head probe showed the new Normal `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb` seam is a different root-selection case: `pre_accept` already chooses the spirit head, `accepted=true`, and `goal_family=SpiritImpact`, while the retained fixture remains a rejected `SpiritImpact -> ImmediateScore` head over a `ManaTempo` root.
- Do not reopen from a white `l8,3;l8,2` vs `l8,3;l9,2` replay without a retained surface. On Apr 8, 2026 the traced board showed an `engine_disabled` live-only `ManaTempo` sibling tie: forced head/current already matched `l8,3;l9,2`, candidate differed only by picking the equal-utility sibling `l8,3;l8,2`, and both roots stayed identically vulnerable.
- Do not cut production code from a fresh replay like `pro_turn_planner_reliability_v6` when every duel bucket still stays count-`1`. Even if the seed reintroduces multiple already-classified shapes at once, such as white forced-prepass drainer-safety, white `ManaTempo` sibling reranks, the non-retained late-black spirit-head, and a one-off black mana rerank, that is still non-actionable churn until one family repeats on a retained surface.
- Do not reopen from a cleaner replay like `pro_turn_planner_reliability_v17` just because Normal is nearly clean. On Apr 9, 2026 direct Pro still finished `2/6/4`, Fast `3/3/6`, and every exact move pair stayed count-`1`, leaving only live-only white forced-prepass drift `l8,4;l8,5` vs `l8,4;l8,3`, white `ManaTempo` sibling drift `l8,3;l8,2` vs `l8,3;l9,2`, and a late black spirit-head rerank `l1,5;l1,7;l0,7` vs `l1,6;l2,7`.
- Do not reopen from `pro_turn_planner_reliability_v18` just because direct Pro and Fast go clean while Normal repeats one white pair. On Apr 9, 2026 the only repeated exact pair was Normal white `l9,6;l8,5` vs `l9,6;l8,7` twice, and the widened white forced-prepass probe showed it is still the old live-only `search_only_forced_prepass` drainer-safety family: `drainer_vulnerable=true`, selected `DrainerSafetyRecovery`, and `pre_accept`/baseline/head all on vulnerable `ManaTempo`, while the retained `primary_white_fast_screen_opening_0_ply9` fixture remains `drainer_vulnerable=false`, `engine_disabled`, and lives on spirit-progress lines.
- Do not reopen from `pro_turn_planner_reliability_v19` just because several retained black seams reappear in one replay. On Apr 9, 2026 direct Pro, Normal, and Fast together resurfaced the accepted non-vulnerable black `ManaTempo` rerank `l1,5;l2,5` vs `l1,6;l0,6`, the mana-bridge seam `l0,5;l1,4` vs `l4,1;l5,0;mb`, the action+mana seam `l1,6;l2,7` vs `l3,2;l4,1`, and the late spirit-head seam `l1,5;l1,7;l0,7` vs `l1,6;l2,7`, but every exact move pair still stayed count-`1`. That is still replay churn, not a code-ready shared black family.
- Do not reopen a spirit-only fallback on the `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb` seam just because `pro_turn_planner_reliability_v20` repeats it. On Apr 9, 2026 the widened black forced-runtime probe showed the traced Normal `v20` board is effectively the same runtime-faithful surface as retained `primary_black_spirit_bridge_ply19`, and a narrow wrapper branch that routed only that exact pair back to current still passed `guardrails`, `pro-triage(primary_pro)=4/60`, and `runtime-preflight` but failed `pro-reliability` at the exact same `0.8333` vs current Pro, `0.5000` vs current Normal, and `0.7500` vs current Fast as the older broad black bridge fallback.
- Do not spend from `pro_turn_planner_reliability_v21` just because it still contains several familiar seams. On Apr 9, 2026 direct Pro, Normal, and Fast only remixed one-off black spirit-bridge, early-black `negative_deny`, black mana rerank, white mana-sibling, and white spirit-own-setup drifts, and every exact move pair still stayed count-`1`. That is immediate replay churn again, not a continuation of the `v20` bridge story.
- Do not spend from `pro_turn_planner_reliability_v22` just because the same familiar black and white seams resurface again. On Apr 9, 2026 direct Pro, Normal, and Fast still had only count-`1` exact pairs, remixing black spirit-bridge, black `ManaTempo` ties, early-black `negative_deny`, black mana-bridge, white mana-sibling, and white spirit-own-setup drift. That remains replay churn, not a code-ready continuation.
- Do not spend from `pro_turn_planner_reliability_v23` just because it resurfaces the same black bridge and early-black families while adding another direct-Pro black tie. On Apr 9, 2026 direct Pro finished `3` regressions / `1` improvement / `8` flat, Normal `3` / `2` / `7`, and Fast `1` / `4` / `7`, with every exact move pair still at count `1`; the seed only remixed black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb`, direct-Pro black `ManaTempo` tie `l1,2;l1,1` vs `l1,2;l0,1`, early-black `negative_deny` `l0,5;l1,6` vs `l1,5;l3,6;l2,7`, black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`, black mana rerank `l1,5;l2,5` vs `l1,6;l0,6`, white mana-sibling `l8,3;l8,2` vs `l8,3;l9,2`, and white spirit-own-setup `l9,5;l7,4;l7,3` vs `l9,5;l7,6;l7,7`. That is still replay churn, not a code-ready continuation.
- Do not reopen the `l4,1;l5,0;mb` bridge family with another current-Pro fallback just because `pro_turn_planner_reliability_v24` repeats it in Fast. On Apr 9, 2026 direct Pro finished `1` regression / `4` improvements / `7` flat, Normal `2` / `1` / `9`, and Fast `4` / `2` / `6`, with Fast repeating `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb` three times and adding one `l0,5;l1,4` vs `l4,1;l5,0;mb`. That does confirm the retained bridge baseline is still live, but the older broad bridge fallback and the later spirit-only fallback already failed full loops, so another fallback-only reopen is still too local.
- Do not reopen the `v24` Normal white `l9,5;l8,6` vs current `l9,6;l8,6` seam just because `l9,5;l8,6` already appears inside the closed `primary_spirit_setup` probe surface. On Apr 9, 2026 `smart_automove_pro_white_engine_disabled_runtime_probe` showed the traced board is a different runtime shape: `selected=l9,5;l8,6`, `pre_accept=baseline=l9,6;l8,6`, `stage=engine_disabled`, `accepted=false`, and the nearby head is `l8,5;l7,4` under `SafeSupermanaProgress -> ImmediateScore`, while `primary_spirit_setup` stays `engine_post_search` on spirit roots `l9,7;...`, and the nearby engine-disabled opening board keeps `selected=baseline=l9,6;l8,6`, `pre_accept=l10,6;l9,5`, and head `l9,6;l8,5` with `l9,5;l8,6` absent from the shortlist. Shared neighborhood is still weaker than runtime-faithful stage shape.
- Do not reopen the `v24` Normal black `l1,5;l2,5` vs current `l3,2;l4,1` seam just because it shares the same current baseline as several older black probes. On Apr 9, 2026 widening `smart_automove_pro_black_forced_runtime_probe` showed another distinct branch on that baseline: runtime-faithful v30 already has `selected=pre_accept=head=l1,5;l2,5`, `forced_inputs=Some("l1,5;l2,5")`, `stage=engine_post_search`, `accepted=true`, `head_family=ManaTempo`, and `goal_family=SpiritImpact`, while current stays on non-vulnerable `ManaTempo` `l3,2;l4,1`. That is different from the retained late-black accepted-head seam, different from the traced fast `v10` rerank where `pre_accept` stayed on current, and different from the traced `v13` drainer-safety family. Shared current baseline `l3,2;l4,1` is still weaker than runtime-faithful stage shape.
- Do not spend from `pro_turn_planner_reliability_v25` just because it introduces a fresh Fast black `DrainerSafetyRecovery` rerank on the `l4,1;l5,0;mb` baseline. On Apr 9, 2026 direct Pro finished `4` regressions / `3` improvements / `5` flat, Normal `3` / `3` / `6`, and Fast `1` / `3` / `8`, with every exact move pair still at count `1`; the new Fast seam `l1,4;l0,5` vs current `l4,1;l5,0;mb` only joined one-off direct-Pro black safe-progress `l0,6;l1,6` vs `l1,5;l2,7;l1,8`, several white engine-disabled/forced-prepass drifts, and a lone Normal black spirit-bridge replay. That is still mixed replay churn, not a code-ready family.
- Do not spend from `pro_turn_planner_reliability_v26` just because Normal repeats white `l9,5;l8,5` vs current `l9,5;l7,6;l7,7`. On Apr 9, 2026 direct Pro finished `2` regressions / `4` improvements / `6` flat, Normal `3` / `2` / `7`, and Fast `2` / `5` / `5`; although that Normal white pair repeated twice, widening `smart_automove_pro_white_safe_progress_probe` showed the traced board is a different runtime shape from both retained white surfaces. Runtime-faithful v30 already has `selected=pre_accept=l9,5;l8,5`, `forced_inputs=None`, `stage=engine_post_search`, `accepted=false`, and a `SafeOpponentManaProgress -> DrainerSafetyRecovery` head shell, while `primary_white_safe_progress_rerank_ply27` stays an accepted vulnerable `ManaTempo` rerank and `primary_white_fast_screen_opening_0_ply9` keeps `l9,5;l8,5` only as a rejected head over spirit-progress roots. Repetition alone is still weaker than a retained-surface match.
- Do not spend from `pro_turn_planner_reliability_v27` just because it looks cleaner than `v26` or because Fast improves more often than it regresses. On Apr 9, 2026 direct Pro finished `1` regression / `3` improvements / `8` flat, Normal `2` / `2` / `8`, and Fast `2` / `7` / `3`, but every exact move pair still stayed at count `1`. The seed only produced one-off direct-Pro white engine-disabled `ManaTempo` sibling drift `l7,4;l8,3` vs current `l6,7;l6,6`, one-off Normal black spirit reranks `l1,5;l2,7;l3,7` vs `l1,5;l2,7;l1,8` and `l2,5;l4,3;l5,3` vs `l2,5;l2,7;l1,6`, and one-off Fast white safe-progress / mana reranks `l8,5;l8,6` vs `l7,0;l6,1` and `l10,5;l9,6` vs `l9,7;l8,6`. Cleaner counts are still weaker than one repeated exact seam with a retained cheap-surface match.
- Do not spend from `pro_turn_planner_reliability_v28` just because direct Pro repeats the retained early-black `negative_deny` seam. On Apr 9, 2026 direct Pro finished `3` regressions / `6` improvements / `3` flat and repeated `l0,5;l1,6` vs current `l1,5;l3,6;l2,7` twice, but Normal simultaneously repeated white `l8,4;l8,5` vs `l8,4;l8,3` twice and Fast still remixed the old black bridge plus one-off white drift. Widening `smart_automove_pro_white_fast_forced_prepass_probe` showed the repeated Normal white board is still the old live-only forced-prepass family: runtime-faithful v30 has `selected=l8,4;l8,5`, `pre_accept=baseline=l8,4;l8,3`, `forced_inputs=Some("l8,4;l8,3")`, `stage=search_only_forced_prepass`, `accepted=true`, and `drainer_vulnerable=true`, while the retained fast-screen fixture remains `drainer_vulnerable=false`, `engine_disabled`, and on spirit-progress roots. A replay split across previously known black and white families is still weaker than one code-ready seam.
- Do not spend from `pro_turn_planner_reliability_v29` just because it looks cleaner than `v28`. On Apr 9, 2026 direct Pro finished `2` regressions / `4` improvements / `6` flat, Normal `2` / `3` / `7`, and Fast `3` / `2` / `7`, but every exact move pair still stayed at count `1`. The seed only remixed one-off direct-Pro early-black `negative_deny` `l0,5;l1,6` vs current `l1,5;l3,6;l2,7`, one-off direct-Pro black spirit sibling `l0,4;l1,3` vs `l0,4;l1,4`, one-off Normal black spirit rerank `l2,4;l0,5;l1,5` vs `l2,4;l4,2;l3,2`, one-off Normal white engine-disabled tie `l8,7;l8,8` vs `l8,7;l9,8`, and one-off Fast black bridge plus white forced-prepass drift. Cleaner buckets are still weaker than one repeated retained seam.
- Do not spend from `pro_turn_planner_reliability_v30` just because direct Pro and Fast finally finish clean. On Apr 9, 2026 direct Pro finished `0` regressions / `4` improvements / `8` flat, Normal `3` / `3` / `6`, and Fast `0` / `2` / `10`, but every exact move pair still stayed at count `1`. The only regressions were one-off Normal white `ManaTempo` reranks `l7,4;l8,3` vs `l7,6;l8,7`, `l6,3;l7,3` vs `l6,7;l7,7`, and `l9,4;l8,3` vs `l5,2;l4,1`. Clean Pro/Fast buckets are still weaker than one repeated retained Normal seam.
- Do not spend from `pro_turn_planner_reliability_v31` just because Normal repeats black spirit-bridge `l1,5;l1,7;l0,7` vs current `l4,1;l5,0;mb` twice. On Apr 9, 2026 direct Pro finished `1` regression / `5` improvements / `6` flat, Normal `2` / `3` / `7`, and Fast `3` / `0` / `9`; Normal did repeat that old bridge seam twice, but Fast only added one old black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb` plus one-off white and black drift, while direct Pro only had a one-off black `ManaTempo` rerank `l0,5;l1,4` vs `l1,5;l2,4`. That is still the already-killed `l4,1;l5,0;mb` bridge fallback story, not a new code-ready family.
- Do not spend from `pro_turn_planner_reliability_v32` just because Normal surfaces both retained `l4,1;l5,0;mb` bridge variants in the same seed. On Apr 9, 2026 direct Pro finished `2` regressions / `5` improvements / `5` flat, Normal `3` / `0` / `9`, and Fast `2` / `3` / `7`, but every exact move pair still stayed at count `1`. Normal only mixed the two old bridge variants `l0,5;l1,4` and `l1,5;l1,7;l0,7` against current `l4,1;l5,0;mb`, Fast remixed `l0,5;l1,4` against current `l1,6;l0,6` plus the old white forced-prepass `l9,4;l8,5` vs `l9,4;l8,3`, and direct Pro added one-off black `l1,5;l2,7;l3,6` vs `l1,5;l2,7;l1,8` plus white `engine_disabled` tie `l8,4;l8,5` vs `l8,4;l9,4`. Related baselines are still weaker than one repeated exact family with a retained match.
- Do not spend from `pro_turn_planner_reliability_v33` just because it replays several known families at once. On Apr 9, 2026 direct Pro finished `3` regressions / `3` improvements / `6` flat, Normal `4` / `2` / `6`, and Fast `1` / `2` / `9`, but every exact move pair still stayed at count `1`. Direct Pro only mixed one old early-black `negative_deny` replay `l0,5;l1,6` vs current `l1,5;l3,6;l2,7` with two unrelated white reranks `l9,4;l8,5` vs `l9,6;l8,7` and `l8,7;l8,8` vs `l8,7;l9,8`; Normal only mixed the two old `l4,1;l5,0;mb` bridge variants with local sibling ties `l10,4;l9,3` vs `l9,4;l9,3` and `l2,7;l2,8` vs `l2,7;l1,8`; Fast added only one old `l0,5;l1,4` mana-bridge replay. Mixed known families are still weaker than one repeated exact seam.
- Do not spend from `pro_turn_planner_reliability_v34` just because Normal repeats the old mana-bridge seam `l0,5;l1,4` vs current `l4,1;l5,0;mb` twice. On Apr 9, 2026 direct Pro finished `1` regression / `3` improvements / `8` flat, Normal `2` / `2` / `8`, and Fast `1` / `1` / `10`. Direct Pro only had one white `ManaTempo` rerank `l6,7;l7,7` vs `l6,4;l7,3`, Fast only had one white `ManaTempo` tie `l9,4;l8,3` vs `l9,4;l8,4`, and the only repeated exact pair in the whole seed was the already-killed black mana-bridge fallback family. A clean Pro/Fast frame around the same dead bridge seam is still weaker than a genuinely new repeated retained family.
- Do not spend from `pro_turn_planner_reliability_v35` just because Normal repeats the retained white safe-progress rerank `l9,4;l8,3` vs current `l5,2;l4,1` twice. On Apr 9, 2026 direct Pro finished `2` regressions / `3` improvements / `7` flat, Normal `3` / `2` / `7`, and Fast `1` / `3` / `8`. The repeated Normal pair is real and the existing white safe-progress probe confirms it matches `primary_white_safe_progress_rerank_ply27`, but direct Pro only had one-off white reranks `l10,6;l9,5` vs `l10,5;l9,4` and `l8,5;l7,6` vs `l8,5;l7,4`, Fast only had one-off white `l10,4;l9,3` vs `l9,5;l7,6;l8,7`, and the only black regression was a one-off early `negative_deny` replay `l0,5;l1,6` vs `l1,5;l3,6;l2,7`. A repeated retained seam in Normal alone is still weaker than a shared cross-bucket duel story with a clear new lever.
- Do not spend from `pro_turn_planner_reliability_v37` just because direct Pro stays mostly clean while Normal and Fast each replay one old bridge seam. On Apr 9, 2026 direct Pro finished `1` regression / `3` improvements / `8` flat, Normal `2` / `2` / `8`, and Fast `1` / `3` / `8`, but every exact move pair still stayed at count `1`. Direct Pro only had one white opening rerank `l10,6;l9,5` vs `l10,5;l9,4`; Normal mixed one old black spirit-bridge `l1,5;l1,7;l0,7` vs current `l4,1;l5,0;mb` with one white rerank `l10,5;l9,4` vs `l9,6;l7,6;l7,7`; Fast only added one copy of that same old black bridge seam. Cleaner counts are still weaker than one repeated exact family with a fresh shared lever.
- Do not spend from `pro_turn_planner_reliability_v38` just because Fast finally repeats black mana-bridge `l0,5;l1,4` vs current `l4,1;l5,0;mb` three times. On Apr 9, 2026 direct Pro finished `1` regression / `5` improvements / `6` flat, Normal `5` / `1` / `6`, and Fast `6` / `1` / `5`. The repeated Fast seam is still the already-killed broad bridge-fallback family, and the rest of the seed stayed fragmented: direct Pro only replayed early-black `negative_deny` `l0,5;l1,6` vs `l1,5;l3,6;l2,7`; Normal mixed white forced-prepass `l9,6;l9,4;l8,5` vs `l9,6;l7,4;l8,4`, white engine-disabled `l9,3;l8,3` vs `l9,5;l8,5`, white spirit rerank `l9,5;l7,4;l8,3` vs `l9,5;l7,6;l7,7`, old black mana-bridge, and black spirit rerank `l2,5;l0,6;l1,6` vs `l2,5;l2,3;l1,2`; Fast added only one-off black spirit-bridge `l1,5;l1,7;l0,7`, white `l8,5;l8,6` vs `l8,5;l7,4`, and black spirit sibling `l0,4;l1,3` vs `l0,4;l1,4`. A repeated old seam is still not enough when the rest of the replay does not line up behind the same branch story.
- Do not spend from `pro_turn_planner_reliability_v39` just because Fast gets quiet again after a noisy seed. On Apr 9, 2026 direct Pro finished `2` regressions / `4` improvements / `6` flat, Normal `4` / `1` / `7`, and Fast `1` / `2` / `9`, but every exact move pair still stayed at count `1`. Direct Pro already split across one-off black spirit rerank `l1,5;l2,3;l3,3` vs current `l1,5;l2,7;l1,8` and one-off black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`; Normal mixed one-off white `l8,5;l7,4` vs `l8,5;l9,4`, one-off white forced-prepass `l9,4;l8,5` vs `l9,4;l8,3`, one-off black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb`, and one-off black action+mana `l1,6;l2,7` vs `l3,2;l4,1`; Fast only added one-off black `ManaTempo` rerank `l2,7;l2,8` vs `l7,1;l6,1`. A quieter Fast bucket is still not enough when Pro and Normal keep mixing unrelated count-`1` seams.
- Do not spend from `pro_turn_planner_reliability_v40` just because direct Pro gets back down to one old regression. On Apr 9, 2026 direct Pro finished `1` regression / `4` improvements / `7` flat, Normal `3` / `1` / `8`, and Fast `2` / `2` / `8`, but every exact move pair still stayed at count `1`. Direct Pro only replayed early-black `negative_deny` `l0,5;l1,6` vs current `l1,5;l3,6;l2,7`; Normal mixed one-off white safe-progress `l10,7;l9,6` vs `l9,8;l8,7`, one-off white `l9,5;l8,5` vs `l9,5;l7,6;l7,7`, and one-off black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`; Fast added one-off white forced-prepass `l9,6;l8,5` vs `l9,6;l8,7` and one-off white `ManaTempo` sibling `l6,3;l7,2` vs `l6,3;l7,3`. A cleaner direct-Pro bucket is still not enough when Normal and Fast do not line up behind the same exact family.
- Do not spend from `pro_turn_planner_reliability_v41` just because direct Pro stays near-clean while Fast improves more often than it regresses. On Apr 9, 2026 direct Pro finished `1` regression / `5` improvements / `6` flat, Normal `4` / `1` / `7`, and Fast `3` / `4` / `5`, but every exact move pair still stayed at count `1`. Direct Pro only had one-off white spirit-own-setup rerank `l9,6;l7,4;l8,3` vs current `l9,6;l7,6;l7,7`; Normal mixed one-off black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb` with three separate white `ManaTempo` boards `l7,4;l7,3` vs `l7,4;l7,5`, `l9,2;l9,1` vs `l9,2;l10,1`, and `l9,8;l8,9` vs `l9,2;l8,1`; Fast added one-off white `l10,4;l9,3` vs `l9,5;l7,6;l8,7`, one-off black `ManaTempo` rerank `l2,7;l2,8` vs `l2,7;l1,8`, and one-off white `l10,3;l9,2` vs `l7,3;l7,2`. Better improvement counts are still weaker than one repeated exact family with a retained foothold and a fresh shared lever.
- Do not spend from `pro_turn_planner_reliability_v42` just because direct Pro finally repeats early-black `negative_deny` again. On Apr 9, 2026 direct Pro finished `3` regressions / `3` improvements / `6` flat and repeated `l0,5;l1,6` vs current `l1,5;l3,6;l2,7` twice, but Normal still mixed that with one-off black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb` and one-off white spirit-own-setup rerank `l9,6;l7,4;l8,3` vs `l9,6;l7,6;l7,7`, while Fast added the old white forced-prepass shell `l8,4;l8,5` vs `l8,4;l8,3` plus one-off black `l0,5;l1,4` vs `l3,2;l4,1`. A repeated Pro seam is still not enough when the losing buckets do not line up behind the same exact family.
- Do not spend from `pro_turn_planner_reliability_v43` just because Normal finally goes clean. On Apr 9, 2026 direct Pro finished `1` regression / `4` improvements / `7` flat, Normal `0` / `4` / `8`, and Fast `3` / `3` / `6`. The only Pro regression was the old black action+mana seam `l1,6;l2,7` vs current `l3,2;l4,1`, but Fast still split across one-off black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb`, one-off white `l10,4;l9,3` vs `l9,5;l7,6;l8,7`, and one-off white `l8,3;l8,2` vs `l8,3;l9,2`. A clean Normal bucket is still not enough when Pro and Fast do not line up behind one repeated exact family.
- Do not spend from `pro_turn_planner_reliability_v44` just because direct Pro stays relatively quiet and Normal nearly goes clean. On Apr 9, 2026 direct Pro finished `2` regressions / `4` improvements / `6` flat, Normal `1` / `3` / `8`, and Fast `3` / `3` / `6`, with every exact move pair still at count `1`. The seed split across one-off direct-Pro black spirit sibling `l0,4;l1,3` vs current `l0,4;l1,4`, one-off direct-Pro accepted safe-progress `l3,5;l4,4` vs `l1,5;l3,5;l3,6`, one-off Normal white `l9,4;l8,5` vs `l9,4;l8,3` where runtime-faithful `selected` and `pre_accept` already matched current, plus one-off Fast black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb`, black action+mana `l1,6;l2,7` vs `l3,2;l4,1`, and white spirit head `l4,9;l4,7;l3,7` vs `l4,9;l3,9`. Cleaner Pro/Normal counts are still weaker than one repeated exact family with a retained foothold and a shared lever.
- Do not spend from `pro_turn_planner_reliability_v45` just because Fast goes clean while Normal repeats the old black spirit-bridge seam twice. On Apr 9, 2026 direct Pro finished `3` regressions / `2` improvements / `7` flat, Normal `4` / `2` / `6`, and Fast `0` / `4` / `8`. Normal did repeat `l1,5;l1,7;l0,7` vs current `l4,1;l5,0;mb` twice, but that is still the already-killed bridge-fallback family; direct Pro simultaneously split across one-off black spirit sibling `l0,4;l1,3` vs current `l0,4;l1,5`, one-off black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`, and one-off black spirit rerank `l1,5;l2,3;l3,3` vs `l1,5;l2,7;l1,8`. A clean Fast bucket is still not enough when Pro stays mixed and the only repeated losing seam is an already-killed family.
- Do not spend from `pro_turn_planner_reliability_v46` just because direct Pro and Normal both get quieter again. On Apr 9, 2026 direct Pro finished `2` regressions / `5` improvements / `5` flat, Normal `1` / `0` / `11`, and Fast `3` / `5` / `4`, but every exact move pair still stayed at count `1`. Direct Pro only had one-off white `l9,6;l8,7` vs current `l9,5;l7,6;l8,7` and one-off early-black `negative_deny` `l0,5;l1,6` vs `l1,5;l3,6;l2,7`; Normal only had one-off white spirit-own-setup `l9,5;l7,4;l8,3` vs `l9,5;l7,6;l7,7`; Fast mixed one-off black spirit sibling `l0,4;l1,3` vs `l0,4;l1,4`, one-off black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb`, and one-off black `ManaTempo` tie `l2,7;l2,8` vs `l2,7;l1,8`. Better bucket counts are still weaker than one repeated exact family with a retained foothold and a fresh shared lever.
- Do not spend from `pro_turn_planner_reliability_v47` just because direct Pro drops to one regression again. On Apr 9, 2026 direct Pro finished `1` regression / `4` improvements / `7` flat, Normal `3` / `3` / `6`, and Fast `4` / `2` / `6`. The only Pro regression was a one-off black accepted `ManaTempo` rerank `l0,5;l1,5` vs current `l2,2;l3,3`. Normal did repeat the old black spirit-bridge seam `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb` twice, but that is still the already-killed bridge-fallback family. Fast then split across unrelated one-offs: black drainer-safety `l1,4;l0,5` vs `l4,1;l5,0;mb`, black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb`, white `l10,4;l9,3` vs `l9,5;l7,6;l8,7`, and black `ManaTempo` tie `l2,7;l2,8` vs `l7,1;l6,1`. A cleaner Pro bucket is still not enough when the only repeated loser is an already-killed family and Fast fragments again.
- Do not spend from `pro_turn_planner_reliability_v48` just because Normal and Fast both go clean. On Apr 9, 2026 direct Pro finished `4` regressions / `4` improvements / `4` flat, while Normal finished `0` / `0` / `12` and Fast `0` / `4` / `8`. Every exact move pair still stayed at count `1`, and all four losses lived only in direct Pro: black `ManaTempo` tie `l2,3;l2,2` vs current `l2,3;l1,2`, white `l10,5;l9,4` vs `l9,8;l7,6;l7,7`, white `l4,9;l2,7;l2,8` vs `l4,9;l2,7;l3,6`, and white `l6,3;l7,2` vs `l6,5;l7,4`. Clean Normal and Fast are still not enough when the only losing bucket fractures into unrelated count-`1` one-offs.
- Do not spend from `pro_turn_planner_reliability_v49` just because direct Pro nearly cleans up again. On Apr 9, 2026 direct Pro finished `1` regression / `6` improvements / `5` flat, but Normal cratered to `7` / `0` / `5` and Fast still had `1` / `3` / `8`, with every exact move pair still at count `1`. The lone Pro loss was a one-off black bridge-like rerank `l1,5;l1,7;l0,7` vs current `l5,1;l6,0`, while Normal mixed one-off black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb`, one-off black `l1,5;l1,7;l0,7` vs `l4,2;l4,1`, one-off black `l2,5;l0,6;l1,6` vs `l2,5;l2,3;l1,2`, one-off black spirit sibling `l0,4;l1,3` vs `l0,4;l1,4`, one-off black `ManaTempo` tie `l2,7;l2,8` vs `l2,7;l1,8`, one-off white `l8,7;l7,8` vs `l10,7;l10,8`, and one-off white spirit-own-setup `l9,5;l7,4;l8,3` vs `l9,5;l7,6;l7,7`. Fast only added one-off black spirit sibling `l0,4;l1,3` vs `l0,4;l1,5`. A cleaner Pro bucket is still not enough when the other buckets explode into unrelated count-`1` seams.
- Do not spend from `pro_turn_planner_reliability_v50` just because Normal finally repeats a new white `ManaTempo` tie. On Apr 9, 2026 direct Pro finished `3` regressions / `4` improvements / `5` flat, Normal `3` / `3` / `6`, and Fast `3` / `2` / `7`. Normal did repeat `l6,5;l6,6` vs current `l6,5;l7,4` twice, but Pro and Fast did not line up behind it: direct Pro split across one-off white `l10,5;l9,4` vs `l9,2;l8,1`, one-off white `l9,4;l8,5` vs `l8,1;l7,0`, and one-off white forced-prepass `l9,5;l8,3;l7,2` vs `l9,2;l8,1`, while Fast split across one-off black `l2,7;l1,8` vs `l2,3;l3,2`, one-off white `l9,6;l8,5` vs `l9,5;l8,5`, and one-off black `l0,6;l1,6` vs `l1,5;l2,5`. A repeated Normal-only seam is still not enough when Pro and Fast do not share the same exact family.
- Do not spend from `pro_turn_planner_reliability_v51` just because Fast repeats the old black spirit-bridge seam twice. On Apr 9, 2026 direct Pro finished `3` regressions / `2` improvements / `7` flat, Normal `3` / `2` / `7`, and Fast `3` / `5` / `4`. Fast did repeat `l1,5;l1,7;l0,7` vs current `l4,1;l5,0;mb` twice, but that family already failed both the broad bridge fallback and the later spirit-only bridge fallback; direct Pro still split across one-off black spirit-bridge, one-off early-black `negative_deny` `l0,5;l1,6` vs `l1,5;l3,6;l2,7`, and one-off white spirit-own-setup `l9,7;l8,5;l7,4` vs `l9,7;l7,6;l7,7`; Normal split across one-off black drainer-safety `l1,6;l0,5` vs `l4,1;l5,0;mb`, one-off white engine-disabled tie `l8,7;l8,8` vs `l9,5;l8,6`, and the old white forced-prepass shell `l9,4;l8,5` vs `l9,4;l8,3`. A repeated Fast seam is still not enough when Pro and Normal keep breaking across unrelated one-offs and the repeated family is already a dead fallback path.
- Do not spend from `pro_turn_planner_reliability_v52` just because it replays several retained black families across all three buckets. On Apr 9, 2026 direct Pro finished `3` regressions / `3` improvements / `6` flat, Normal `3` / `5` / `4`, and Fast `4` / `4` / `4`, but every exact move pair still stayed at count `1`. Direct Pro split across one-off early-black `negative_deny` `l0,5;l1,6` vs current `l1,5;l3,6;l2,7`, one-off black action+mana `l1,6;l2,7` vs `l2,3;l3,2`, and one-off black spirit sibling `l0,4;l1,4` vs `l0,4;l1,5`. Normal split across one-off white engine-disabled tie `l6,3;l7,3` vs `l7,7;l8,8`, one-off white forced-prepass `l9,6;l8,5` vs `l9,6;l8,7`, and one-off black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`. Fast split across one-off black spirit sibling `l0,4;l1,3` vs `l0,4;l1,4`, one-off early-black `negative_deny` `l0,5;l1,6` vs `l1,5;l3,6;l2,7`, one-off black drainer-safety `l1,4;l0,5` vs `l4,1;l5,0;mb`, and one-off black `ManaTempo` tie `l2,7;l2,8` vs `l2,7;l1,8`. Replaying several known seams at once is still not enough when none of them repeats exactly and no single family leads the seed.
- Do not spend from `pro_turn_planner_reliability_v53` just because Fast goes clean while direct Pro drops to one loss again. On Apr 9, 2026 direct Pro finished `1` regression / `4` improvements / `7` flat, Normal `5` / `2` / `5`, and Fast `0` / `4` / `8`. The only Pro loss was the old black mana-bridge seam `l0,5;l1,4` vs current `l4,1;l5,0;mb`, but Normal still exploded into count-`1` seams across that same bridge family, the old black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb`, black action+mana `l1,6;l2,7` vs `l3,2;l4,1`, black `ManaTempo` tie `l2,7;l2,8` vs `l2,7;l1,8`, and a white forced-prepass rerank `l9,6;l10,5` vs `l9,6;l8,7`. Clean Fast and a single Pro loss are still not enough when the remaining losing bucket fractures into unrelated one-offs and the lone Pro family is already a dead bridge-fallback path.
- Do not spend from `pro_turn_planner_reliability_v54` just because it surfaces a new bridge-like baseline on one direct-Pro board. On Apr 9, 2026 direct Pro finished `4` regressions / `4` improvements / `4` flat, Normal `4` / `1` / `7`, and Fast `5` / `4` / `3`, with every exact move pair still at count `1`. Direct Pro mixed one-off black bridge-like `l1,5;l1,7;l0,7` vs current `l5,1;l6,0`, one-off early-black `negative_deny` `l0,5;l1,6` vs `l1,5;l3,6;l2,7`, and two one-off white `ManaTempo` reranks `l9,3;l8,2` vs `l8,5;l9,4` and `l8,5;l8,6` vs `l8,5;l9,4`. Normal mixed one-off black spirit rerank `l2,5;l0,6;l1,6` vs `l2,5;l2,3;l1,2`, one-off black `ManaTempo` tie `l2,7;l2,8` vs `l2,7;l1,8`, and one-off white reranks `l8,6;l7,7` vs `l8,6;l8,5` and `l9,5;l7,4;l8,3` vs `l9,5;l7,6;l7,7`. Fast mixed one-off white forced-prepass and `ManaTempo` drifts `l8,4;l8,5` vs `l8,4;l9,3`, `l8,4;l8,5` vs `l8,4;l8,3`, and `l9,4;l8,3` vs `l9,4;l8,4`, plus one-off black drainer-safety `l1,6;l0,5` vs `l4,1;l5,0;mb` and one-off black `ManaTempo` tie `l2,7;l2,8` vs `l2,7;l1,8`. A new baseline on one board is still not enough when the rest of the seed fractures into unrelated one-offs.
- Do not spend from `pro_turn_planner_reliability_v55` just because it surfaces two fresh direct-Pro white reranks at once. On Apr 9, 2026 direct Pro finished `3` regressions / `5` improvements / `4` flat, Normal `4` / `5` / `3`, and Fast `4` / `2` / `6`, but every exact move pair still stayed at count `1`. Direct Pro mixed one-off black spirit sibling `l0,4;l1,3` vs current `l0,4;l1,5`, plus one-off white reranks `l10,5;l9,4` vs `l9,2;l8,1` and `l10,6;l9,5` vs `l10,5;l9,4`. Normal mixed one-off black spirit rerank `l2,5;l0,6;l1,6` vs `l2,5;l2,3;l1,2`, one-off black `ManaTempo` tie `l2,7;l2,8` vs `l2,7;l1,8`, one-off black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`, and one-off white spirit-own-setup `l9,5;l7,4;l8,3` vs `l9,5;l7,6;l7,7`. Fast mixed one-off black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb`, one-off black action+mana `l1,6;l2,7` vs `l2,3;l3,2`, and one-off white forced-prepass drifts `l9,4;l8,5` vs `l8,4;l7,6;l6,6` and `l9,4;l8,5` vs `l9,4;l8,3`. New direct-Pro white reranks are still not enough when Normal and Fast do not line up behind the same exact family.
- Do not spend from `pro_turn_planner_reliability_v56` just because direct Pro repeats one white pair twice. On Apr 9, 2026 direct Pro finished `3` regressions / `3` improvements / `6` flat, Normal `2` / `4` / `6`, and Fast `3` / `0` / `9`. Direct Pro did repeat `l10,5;l9,4` vs current `l9,5;l7,4;l8,3` twice, but Normal and Fast did not line up behind that exact family, and every non-Pro exact move pair still stayed at count `1`. The rest of the seed mixed one-off direct-Pro black `ManaTempo` tie `l2,7;l2,6` vs `l2,7;l3,8`, one-off Normal black spirit rerank `l2,5;l0,6;l1,6` vs `l2,5;l2,3;l1,2`, one-off Normal white spirit-own-setup `l9,5;l7,4;l8,3` vs `l9,5;l7,6;l7,7`, one-off Fast black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`, one-off Fast black `ManaTempo` tie `l2,7;l2,8` vs `l2,7;l1,8`, and one-off Fast white forced-prepass `l8,4;l8,5` vs `l8,4;l8,3`. A repeated Pro-only pair is still not enough when the other buckets do not share the same exact seam.
- Do not spend from `pro_turn_planner_reliability_v57` just because the first direct-Pro trace looks like a repeated white family. On Apr 9, 2026 direct Pro finished `2` regressions / `3` improvements / `7` flat, Normal `2` / `2` / `8`, and Fast finished clean at `0` / `2` / `10`, with every exact move pair across all three buckets still at count `1`. Direct Pro only mixed one-off white reranks `l9,5;l7,4;l7,3` vs current `l9,5;l7,6;l7,7` and `l8,1;l7,0` vs `l9,4;l8,3`. Normal only mixed one-off white `l9,6;l8,5` vs `l9,7;l7,6;l7,7` and one-off black `l3,2;l3,4;l2,3` vs `l3,2;l4,1`. Fast had no regressions at all. An apparently repeated opening trace is still not enough when the final summaries show no exact pair above count `1`.
- Do not spend from `pro_turn_planner_reliability_v58` just because direct Pro repeats retained early-black `negative_deny` twice and Fast repeats the old black spirit-bridge seam twice. On Apr 9, 2026 direct Pro finished `3` regressions / `5` improvements / `4` flat, Normal `5` / `2` / `5`, and Fast `4` / `3` / `5`. Direct Pro did repeat `l0,5;l1,6` vs current `l1,5;l3,6;l2,7` twice, and Fast did repeat `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb` twice, but Normal still fractured entirely into count-`1` white seams: `l9,5;l8,5` vs `l9,5;l7,6;l7,7`, forced-prepass `l9,4;l8,5` vs `l9,4;l8,3`, spirit-own-setup `l9,5;l7,4;l8,3` vs `l9,5;l7,6;l7,7`, `ManaTempo` tie `l8,5;l8,4` vs `l8,5;l9,4`, and `l9,5;l8,4` vs `l9,6;l7,7;l7,8`. Repeating one retained black seam in Pro and one already-killed bridge seam in Fast is still weaker than one shared exact family that carries through the losing buckets.
- Do not spend from `pro_turn_planner_reliability_v59` just because it replays one retained black seam in Pro and one retained bridge seam in Normal. On Apr 9, 2026 direct Pro finished `2` regressions / `2` improvements / `8` flat, Normal `1` / `1` / `10`, and Fast `5` / `3` / `4`, with every exact move pair still at count `1`. Direct Pro only mixed one-off retained early-black `negative_deny` `l0,5;l1,6` vs current `l1,5;l3,6;l2,7` and one-off white forced-prepass `l8,4;l8,5` vs `l8,4;l9,3`. Normal only added one-off black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb`. Fast then fractured across one-off white `l10,4;l9,3` vs `l9,5;l7,6;l8,7`, one-off black `l2,7;l1,8` vs `l2,3;l3,2`, one-off white forced-prepass `l8,4;l8,5` vs `l8,4;l9,3`, one-off white forced-prepass `l9,4;l8,5` vs `l9,4;l8,3`, and one-off white safe-progress `l9,5;l8,6` vs `l10,6;l9,6`. Cleaner Pro and Normal buckets are still not enough when Fast breaks into unrelated white and black one-offs.
- Do not spend from `pro_turn_planner_reliability_v60` just because Fast nearly goes clean while direct Pro and Normal surface familiar retained black seams. On Apr 9, 2026 direct Pro finished `3` regressions / `3` improvements / `6` flat, Normal `3` / `2` / `7`, and Fast `1` / `6` / `5`, with every exact move pair still at count `1`. Direct Pro split across one-off white `l8,4;l9,3` vs current `l8,5;l6,7;l7,7`, one-off retained early-black `negative_deny` `l0,5;l1,6` vs `l1,5;l3,6;l2,7`, and one-off black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`. Normal split across one-off black action+mana `l1,6;l2,7` vs `l3,2;l4,1`, one-off white forced-prepass `l9,4;l8,5` vs `l9,4;l8,3`, and one-off black `ManaTempo` rerank `l0,10;l0,9` vs `l1,5;l2,5`. Fast only added one-off black action+mana `l1,6;l2,7` vs `l2,3;l3,2`. Cleaner Fast evidence is still not enough when Pro and Normal do not line up behind the same exact family.
- Do not spend from `pro_turn_planner_reliability_v61` just because direct Pro repeats retained early-black `negative_deny` twice again. On Apr 9, 2026 direct Pro finished `2` regressions / `5` improvements / `5` flat, Normal `2` / `2` / `8`, and Fast `4` / `1` / `7`, with every exact move pair still at count `1` outside that one Pro pair. Direct Pro only repeated `l0,5;l1,6` vs current `l1,5;l3,6;l2,7`. Normal only added one-off black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb` and one-off white `l9,5;l8,5` vs `l9,5;l7,6;l7,7`. Fast then fractured across one-off white forced-prepass `l9,4;l8,5` vs `l9,4;l8,3`, one-off white forced-prepass `l8,4;l8,5` vs `l8,4;l9,3`, one-off black spirit-bridge `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb`, and one-off black spirit variant `l1,5;l0,7;l1,7` vs `l3,2;l4,1`. Repeating the retained early-black seam in Pro is still not enough when Normal and Fast split across separate white and bridge-like families.
- Do not spend from `pro_turn_planner_reliability_v62` just because direct Pro nearly goes clean while the other buckets stay modest. On Apr 9, 2026 direct Pro finished `1` regression / `2` improvements / `9` flat, Normal `3` / `0` / `9`, and Fast `1` / `3` / `8`, with every exact move pair still at count `1`. Direct Pro only had one-off black `ManaTempo` sibling `l2,3;l2,2` vs current `l2,3;l1,2`. Normal split across one-off white `l8,5;l8,6` vs `l10,6;l9,6`, one-off black action+mana `l1,6;l2,7` vs `l3,2;l4,1`, and one-off white spirit-own-setup `l9,5;l7,4;l7,3` vs `l9,5;l7,6;l7,7`. Fast only added one-off bridge-adjacent black spirit variant `l1,5;l1,7;l2,6` vs `l5,0;l4,1`. Cleaner Pro and Fast buckets are still not enough when every exact family remains count-`1`.
- Do not spend from `pro_turn_planner_reliability_v63` just because Normal repeats the old black spirit-bridge seam twice again. On Apr 9, 2026 direct Pro finished `2` regressions / `3` improvements / `7` flat, Normal `5` / `1` / `6`, and Fast `3` / `5` / `4`. Normal did repeat `l1,5;l1,7;l0,7` vs current `l4,1;l5,0;mb` twice, but direct Pro stayed split across one-off black `l1,5;l3,4;l2,3` vs `l1,5;l3,6;l2,7` and one-off late white safe-progress `l10,5;l9,6` vs `l10,6;l9,5`. Fast then split across one-off white `l10,4;l9,3` vs `l9,5;l7,6;l8,7`, one-off white forced-prepass `l8,6;l7,5` vs `l8,6;l7,7`, and one-off black accepted `ManaTempo` rerank `l1,5;l1,4` vs `l3,2;l4,1`. Repeating the already-killed bridge seam in one bucket is still weaker than one exact family that actually carries through the other losing buckets.
- Do not spend from `pro_turn_planner_reliability_v64` just because direct Pro nearly goes clean again. On Apr 9, 2026 direct Pro finished `1` regression / `4` improvements / `7` flat, Normal `2` / `2` / `8`, and Fast `1` / `3` / `8`, with every exact move pair still at count `1`. Direct Pro only had one-off black drainer-safety `l0,5;l1,4` vs current `l2,8;l3,8`. Normal then split across one-off white spirit-own-setup `l9,5;l7,4;l8,3` vs `l9,5;l7,6;l7,7` and one-off black spirit rerank `l2,5;l0,6;l1,6` vs `l2,5;l2,3;l1,2`. Fast only added one-off black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`. Cleaner Pro and quieter Normal/Fast buckets are still not enough when no exact family repeats.
- Do not spend from `pro_turn_planner_reliability_v65` just because direct Pro nearly goes clean and the surviving seams look like tidy `ManaTempo` ties. On Apr 9, 2026 direct Pro finished `1` regression / `3` improvements / `8` flat, Normal `2` / `3` / `7`, and Fast `3` / `4` / `5`, with every exact move pair still at count `1`. Direct Pro only had one-off black `ManaTempo` tie `l1,2;l1,1` vs current `l1,2;l0,1`. Normal then split across one-off black accepted `ManaTempo` rerank `l0,9;l0,10` vs `l2,5;l4,3;l3,3` and one-off white `ManaTempo` tie `l8,3;l8,2` vs `l8,3;l9,2`. Fast added one-off black `ManaTempo` tie `l1,8;l1,9` vs `l1,8;l0,9`, one-off white `ManaTempo` tie `l8,5;l7,4` vs `l8,5;l9,4`, and one-off black bridge-adjacent drainer-safety `l0,5;l1,4` vs `l4,1;l5,0;mb`. Cleaner surface shape is still not enough when no exact family repeats.
- Do not spend from `pro_turn_planner_reliability_v66` just because Normal repeats the white spirit-own-setup rerank twice. On Apr 9, 2026 direct Pro finished `1` regression / `4` improvements / `7` flat, Normal `4` / `1` / `7`, and Fast `3` / `2` / `7`. Normal did repeat `l9,5;l7,4;l8,3` vs current `l9,5;l7,6;l7,7` twice, but direct Pro stayed on one-off black accepted spirit `l1,4;l0,6;l1,6` vs `l5,10;l6,9`, and Fast still split across one-off black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`, one-off white forced-prepass `l7,4;l8,5` vs `l7,4;l8,3`, and one-off black `ManaTempo` rerank `l1,5;l2,5` vs `l1,6;l0,6`. A repeated Normal-only white seam is still not enough when Pro and Fast do not line up behind it.
- Do not spend from `pro_turn_planner_reliability_v68` just because it revives the white forced-prepass shell in both Normal and Fast. On Apr 9, 2026 direct Pro finished `1` regression / `3` improvements / `8` flat, Normal `2` / `4` / `6`, and Fast `5` / `2` / `5`, with every exact move pair still at count `1`. Normal surfaced `l9,4;l8,5` vs current `l9,4;l8,3` and Fast surfaced `l8,4;l8,5` vs `l8,4;l8,3`, but both are still the old `search_only_forced_prepass` live-only drainer-safety family with `pre_accept` already on current. The remaining losses split across one-off later-black `l1,5;l2,4` vs `l1,6;l2,7`, one-off later-black `l1,6;l2,7` vs `l2,3;l3,4`, one-off black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`, one-off white `l8,7;l8,6` vs `l8,7;l7,8`, one-off white `l9,8;l8,8` vs `l9,6;l8,7`, and one-off black `l2,7;l2,8` vs `l2,7;l1,8`. Cross-bucket neighborhood alone is still not enough without one exact family that repeats and lands on a retained cheap surface.
- Do not spend from `pro_turn_planner_reliability_v69` just because it replays both black bridge variants in Normal and revives `l1,6;l2,7` vs current `l2,3;l3,4` in Fast. On Apr 9, 2026 direct Pro finished `2` regressions / `6` improvements / `4` flat, Normal `2` / `2` / `8`, and Fast `3` / `1` / `8`, with every exact move pair still at count `1`. Normal only mixed the already-killed `l4,1;l5,0;mb` mana-bridge and spirit-bridge seams, direct Pro still split across one-off early-black `negative_deny` and the old white spirit cluster `l4,9;l4,7;l5,7` vs `l9,4;l8,3`, and the widened black runtime probe showed the Fast `l1,6;l2,7` board is another distinct current-`l2,3;l3,4` branch rather than the retained action+mana seam. A cross-bucket replay is still not enough when the exact pair count stays `1` and the probe expands the seam map instead of collapsing it.
- Do not spend from `pro_turn_planner_reliability_v70` just because direct Pro nearly cleans up. On Apr 9, 2026 direct Pro finished `1` regression / `5` improvements / `6` flat, but Normal still sat at `4` / `3` / `5` and Fast at `2` / `4` / `6`, with every exact move pair still at count `1`. The only Pro loss was a white `engine_disabled` rerank `l10,6;l9,5` vs current `l10,5;l9,4`, while Normal fractured across the old white forced-prepass shell `l9,6;l8,5` vs `l9,6;l8,7`, one-off white accepted spirit `l8,5;l6,5;l6,4` vs `l7,6;l8,7`, one-off black mana-bridge `l0,5;l1,4` vs `l4,1;l5,0;mb`, and one-off early-black `negative_deny` `l0,5;l1,6` vs `l1,5;l3,6;l2,7`. Fast only added one-off black spirit sibling `l0,4;l1,3` vs `l0,4;l1,4` plus another one-off white forced-prepass `l9,6;l8,5` vs `l9,6;l8,7`. Cleaner Pro evidence is still not enough when Normal and Fast remain fragmented across already-classified seams.
- Do not spend from `pro_turn_planner_reliability_v71` just because Normal narrows to two losses and Fast nearly goes clean. On Apr 9, 2026 direct Pro finished `2` regressions / `2` improvements / `8` flat, Normal `2` / `0` / `10`, and Fast `1` / `4` / `7`, with every exact move pair still at count `1`. Direct Pro only mixed the old black spirit sibling `l0,4;l1,3` vs current `l0,4;l1,4` and a white `engine_disabled` `ManaTempo` tie `l8,7;l8,8` vs `l9,6;l8,5`. Normal only replayed two distinct black variants on the old `l4,1;l5,0;mb` baseline, `l0,5;l1,4` and `l2,5;l0,5;l1,5`. Fast only replayed the old white forced-prepass shell `l8,4;l8,5` vs `l8,4;l9,3`. Narrower losing buckets are still not enough when they still point only at already-killed bridge, spirit-sibling, and forced-prepass families.
- Do not reopen the retained white harvest score-route fix just because a fresh replay lands on another `l9,6;...` vulnerable spirit rerank. On Apr 8, 2026 the widened white score-route probe showed the `v6` board `l9,6;l8,4;l7,4` vs `l9,6;l7,4;l7,3` is another accepted spirit-sibling rerank with `pre_accept` already on a nearby spirit root, while the retained harvest fixture remains a different rejected-head surface where current already matches.
- The repeated white `l5,0;*` family now has a retained foothold: `primary_white_mana_sibling_ply9` plus its probe. It is an `engine_disabled`, non-vulnerable `ManaTempo` sibling rerank where current/pre-accept stay on `l5,0;l4,1` and the challenger picks another same-family sibling. Keep the seam, but do not cut production code until there is a bounded selector explanation for why v30 prefers the lower-ranked sibling.
- Do not keep a new shared negative-deny selector override just because it closes a repeated black duel seam and its retained fixture. If the cheap gate still snaps back to the same stale `human_win_pro_c`-only churn, kill the production change and keep only the new retained fixture/probe.
- Do not keep that same shared negative-deny override just because it also clears `runtime-preflight`. On Apr 8, 2026 the full-loop retry still failed direct duels and merely moved the wall onto a later black accepted-head family, so the right outcome was still to kill the production change and keep only the retained foothold plus the lesson.
- Do not treat late white full-resource current-Pro wrappers as a safe way to clear `human_win_pro_c`; if the fixture does not move immediately, kill the idea.
- Do not retain a late-white omitted-root rescue that only drives `pro-triage(primary_pro)` to `0/52`; matching current everywhere on the cheap surface is still a dead line.
- Do not retain a same-lane own-setup-vs-progress override just because it clears `human_win_pro_c`; if it reopens black reliability fixtures or tanks direct duels, kill it immediately.
- Do not retain white-only or black-only local seam repairs that fail to move the broader `vs current Normal` wall.
- Do not treat the relaxed `700ms` move-time cap as permission to reopen parity-preserving speed regressions.
- Do not retain new scratch profile IDs when the shared fix can live under `runtime_pro_turn_engine_v30` and the scratch line still fails direct duel evidence.

## Current Durable Direction

- Speed is already acceptable on the retained Pro challenger. Promotion is blocked by quality, especially versus current Normal, not by the `700ms` move-time budget.
- The remaining work should favor broader in-path selector and root-choice changes over wrapper fallbacks, fallback widening, or exact replay fixes.
- The most credible future wins are shared selector/search families that move direct `runtime_pro_turn_engine_v30` vs `runtime_current` results, not local acceptance or wrapper repairs that only clear one traced seam.
- `human_win_pro_c` may still be a retained triage drift, but it is not a hotspot-backed duel seam today. Do not reopen shared ProV2 selector work on that fixture alone without fresh direct duel or hotspot evidence.
- The current human drift and sampled fast black turn-four duel seam still look separate. Treat them as different hypotheses unless a fresh duel replay proves they have converged onto the same projection surface.
- The current white turn-three wall is split in two: the mana-only mid-turn wrapper family is closed again as too local, while the repeated `action+mana` accepted-head seam (`l9,4;l8,4` vs `l8,7;l7,8`) remains live.
- The repeated white `action+mana` seam still does not have a retained cheap-surface foothold today. Do not spend another production split on it until a fresh target-surface probe shows the same accepted-head family on `primary_pro`.
- The fresh black normal-duel `l0,5;l1,6` vs `l1,5;l3,6;l2,7` seam is not a retained foothold today. It is a one-off `negative_deny` spirit-preference drift, not the retained human followup-progress seam, so it does not justify a new production split by itself.
- The lone `vs current Pro` white `l9,2;l8,3` vs `l10,7;l9,7` miss is not a new foothold today either. It is another member of the already-closed white `turn=3`, `action=false`, `mana=true` wrapper family, so it does not justify reopening that production split.
- The later black normal-duel `l2,4;l3,3` vs `l3,6;l2,7` seam is not a retained foothold today either. It is a one-off forced-head shortlist distortion with no retained `primary_pro` counterpart outside the already-closed `primary_pvs_sensitive_search` family.
- The white fast-duel `l9,5;l8,5` vs `l7,5;l6,4` seam is not a retained foothold today either. It is another member of the already-closed white `turn=3`, `action=false`, `mana=true` wrapper family, so it does not justify reopening that production split.
- The white normal-duel `l10,4;l9,4` vs `l8,6;l7,7` seam is not a retained foothold today either. It is another member of the already-closed white `turn=3`, `action=false`, `mana=true` wrapper family, so it does not justify reopening that production split.
- The fresh `pro_turn_planner_reliability_v2` duel replay also does not supply a retained foothold today. It produced only one-off move pairs across direct Pro, Normal, and Fast, so neither the later black `SpiritImpact` takeover nor the resurfaced early black `negative_deny` seam is strong enough yet to justify production code.
- The repeated black `negative_deny` family now does have a retained foothold today: `primary_black_negative_deny_ply4` plus its selector probe. What it still does not have is a promotable production fix; the first shared override closed that new seam but still left the overall cheap surface at stale human-only churn.
- The later black accepted-head family `l1,5;l1,7;l0,7` vs current `l3,2;l4,1` is now the stronger live wall than the retained early-black seam. On Apr 8, 2026 the full-loop retry of the early `negative_deny` fix repeated that later seam `2x` in direct Pro and `3x` in Normal, while Fast added only one copy plus one-off white forced-prepass drifts, so future spend needs either a retained foothold for that later family or proof that it is only another wrapper-shaped artifact.
- That later black family now does have a retained foothold today, but it is not the dominant live wall after the combined black retry. On Apr 8, 2026 the first retained later-black fixture/probe made the seam durable, yet a full-loop branch that closed both retained black seams still replayed mostly white wrapper and accepted-head churn, with only one-off black drift outside Fast.
- The repeated white fast accepted-head seam is no longer the dominant live wall today. On Apr 8, 2026 the retained white deferred-recovery guard removed that repeated `l9,4;l8,4` vs `l8,7;l7,8` pair entirely from the default reliability replay; Fast improved to `0.7500`, and the remaining regressions across Pro/Normal/Fast were all one-offs. Future spend should target the surviving Normal one-offs or wait for a fresh repeated family rather than reopening that white accepted-head seam.
- The latest retained shared win was a selector hygiene fix, not a new branch family: reject non-concrete one-chunk progress heads when they only override an unsafe non-progress selected root without a completed plan.
- Reducing `primary_pro` churn is not enough on its own. The retained line now reaches `2/52` changed fixtures on `primary_pro`, but `pro-reliability` is still flat at `0.8333` vs current Pro, `0.5000` vs current Normal, and `0.6667` vs current Fast. Future spend needs a direct duel-quality hypothesis.
- Closing the late `primary_pvs_sensitive_search` seam was still not enough. The retained line is now down to `1/52` changed `primary_pro` fixtures, but `pro-reliability` remained flat at `0.8333` vs current Pro, `0.5000` vs current Normal, and `0.6667` vs current Fast on Apr 8, 2026.
- Fixing the retained duel-accept seams and clearing `human_win_pro_c` is still not enough on its own. The latest retained attempt kept `primary_pro` at `1/52` changed fixtures and stayed preflight-clean, but `pro-reliability` still failed at `0.7500` vs current Pro, `0.6667` vs current Normal, and `0.5000` vs current Fast.
