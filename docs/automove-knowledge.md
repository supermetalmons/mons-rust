# Automove Knowledge

This document keeps only durable lessons that should outlast the current session.

Use `HOW_TO_ITERATE_ON_AUTOMOVE.md` for the operator loop and `AUTOMOVE_IDEAS.md` for the next live split.

## Stable Runtime Truths

- Shipping Pro is `runtime_current`.
- `runtime_pro_turn_engine_v30` is the only live Pro challenger.
- `runtime_pro_turn_engine_v1` is reference-only regression history.
- `runtime_pro_turn_engine_v30` is a guarded `ProV2` path with deliberate opening-book and early-white fallbacks, not a raw always-on engine.
- Full-turn planning can beat per-input expansion, but only if it survives the real ranked-root surface and direct duel evidence.
- Opportunity-context extraction is worth keeping separate from raw input search. It remains useful shared structure for planner seeds and selector guards.
- Continuation reuse and no-plan reuse matter, but cache keys must include a config fingerprint.
- Drainer safety, root reply-risk guards, and efficiency tie-breaks still earn their keep because they cheaply reject fake-good roots.
- Opening-specific latency still matters. A candidate that stalls on the first real black reply is not promotable.
- Hybrid fallbacks must respect retained opening and eligibility guards before they call expensive plan probes.
- Wrapper-only tuning saturates quickly. Once the obvious knobs are in place, the next gain usually has to come from shared engine or search code.
- Global `SmartSearchConfig` knob space is effectively exhausted. Future gains need new code or a sharper selector/search hypothesis, not another broad retune.
- Weak deferred progress heads are a repeat failure mode. Keep blocking them when the real selected root already offers a safer immediate pickup, a concrete setup window, or an unsafe non-progress root that the head does not concretely improve.
- Production wasm still needs single-shot, predictable search. Deferred or post-return work is not release-safe.
- The retained churn probe is worth keeping. For live ProV2 misses, distinguish `pre_accept` search choice from final `engine_post_search` output before changing shared heuristics.
- The runtime-faithful retained churn probe is worth keeping too. It must inject forced engine inputs before `focused_root_candidates_with_forced_inputs(...)`, or it can misclassify injected-root seams as selector churn.
- The compare-oriented hotspot probe is worth keeping. On Apr 5, 2026 it showed the bounded reliability hotspot corpus was move-identical to `runtime_current` on every real duel hotspot, with only a synthetic `quiet_positional` difference.
- The duel-replay probe is worth keeping too. On Apr 8, 2026 `smart_automove_pro_reliability_duel_trace_probe` replayed the exact `pro-reliability` seeds, exposed a repeated real fast-duel `engine_post_search` `SafeSupermanaProgress` override, and showed that the hotspot corpus was no longer sufficient by itself to explain the live wall.
- Re-running the hotspot compare after the retained `primary_pvs_sensitive_search` repair did not expose a new real duel seam. On Apr 8, 2026 every real hotspot was still move-identical to `runtime_current`, so `human_win_pro_c` remained a triage-only selector drift rather than promotion evidence.
- The current retained seam map is stable enough to plan around: `primary_pvs_sensitive_search` now has a retained late `engine_post_search` fix, `human_win_pro_c` remains a retained `pre_accept` safe-progress bias, `primary_black_turn_four_action_mana_ply15` is now the retained later-black forced-engine seam, and the previously live `primary_white_harvest_loss_c_ply24`, `primary_spirit_setup`, and `primary_black_reliability_opening_3_ply4` seams still hold their retained fixes.
- `primary_spirit_setup` was a two-step bug, not one seam: the engine was force-pinning an existing low-ranked plain `SpiritImpact` head into the focused shortlist, then a completed-plan override was still allowed to replace an equivalent stronger selected plain spirit sibling. Both checks have to stay aligned.
- The black turn-two low-budget clamp was too broad. On `turn=2`, `mons_moves=1`, `action+mana` states it could suppress a stronger spirit-own-setup root; the clamp should only fire on truly resource-constrained turn-two black states.
- White `turn=3`, mana-only mid-turn wrapper regressions are real, but still not enough by themselves. On Apr 8, 2026 routing those traced boards back to the current Pro surface fixed the replayed duel choices, yet `pro-triage(primary_pro)` stayed flat at `1/52`, so the guard repair was still too local to earn more spend.
- Even a broader duel-backed wrapper bundle can still be too local. On Apr 8, 2026 routing all traced white `turn=3` mid-turn boards plus black `turn=2`/`turn=4` one-move `action+mana` boards back to the current Pro surface fixed the targeted replay boards and cleared `guardrails`, but `pro-triage(primary_pro)` still stayed flat at `1/52`.
- Simple speculative immediate-score non-regression clamps and setup-gain-only spirit-setup promotion are not enough. They can reshuffle `primary_pro` fixtures and regress direct Pro-vs-Pro without moving the `vs current Normal` wall.
- Broad same-lane `spirit_own_mana_setup_now` overrides are too blunt as well. On Apr 8, 2026 a shared ProV2 override collapsed `human_win_pro_c` and fixed one traced normal-duel board, but it reopened `primary_black_reliability_opening_3_ply4` and regressed `pro-reliability` to `0.7500` vs current Pro and `0.4167` vs current Normal/Fast.
- Late-white full-resource current-Pro guards are not a useful `human_win_pro_c` lever either. On Apr 8, 2026 a late white turn-start safe-supermana wrapper did not change the selected move on the live `human_win_pro_c` fixture.
- A late-white omitted-root reply-risk rescue is too local as well. On Apr 8, 2026 it cleared `human_win_pro_c` without reopening black reliability, but that only collapsed `pro-triage(primary_pro)` to `0/52`, which still fails because the cheap target surface must move.
- Soft followup-tolerance for `spirit_own_mana_setup_now` roots and close quiet-root normal-safety blocks are also not enough by themselves. If the runtime-faithful seam stays unchanged, kill the split before spending the canonical loop.
- Eval-only progress-head wins are too soft for unsafe late overrides. When both the selected root and the `Safe*Progress` head stay unsafe, do not let a lower-scored progress head replace the selected non-progress root unless it brings a non-eval strategic gain or a forced `score_delta` jump.
- Even a real duel-traced acceptance seam can still be too local. On Apr 8, 2026 a bounded fast-duel `Safe*Progress` override clamp matched the traced seed, but it left `pro-triage(primary_pro)` unchanged at `1/52`, so the production split was killed before `runtime-preflight` and `pro-reliability`.
- Once the wrapper-local misses are gone, the live wall can shift cleanly into later `engine_post_search` drift. On Apr 8, 2026 the wrapper bundle plus late-white omitted-root rescue left `vs current Pro` at `0` regressions / `2` improvements, but `vs current Normal` still sat at `2` regressions / `1` improvement and `vs current Fast` at `3` regressions / `3` improvements, so the next spend would have to attack post-search head acceptance rather than more wrapper or human-only fixes.
- When debugging `engine_post_search` head acceptance, print both `turn_engine_selected_override_utility(...)` and the raw selected-root `turn_engine_root_plan_utility(...)`. On Apr 8, 2026 a retained fast white turn-seven seam looked like a weak raw selected root, but the real gate was comparing against a much stronger projected `selected_override_utility`.
- A multi-chunk `ImmediateScore` near-tie `ManaTempo` clamp can still be too local. On Apr 8, 2026 it cleared `human_win_pro_c`, fixed the bounded duel-accept seams, and passed `guardrails`, `pro-triage(primary_pro)=1/52`, and `runtime-preflight`, but `pro-reliability` still failed at `0.7500` vs current Pro, `0.6667` vs current Normal, and `0.5000` vs current Fast. A sampled duel trace still diverged on a later black turn-four `engine_post_search` choice (`l1,6;l2,7` vs current `l2,3;l3,2`), so future spend has to target the broader later duel wall rather than another near-tie accept clamp.
- The sampled fast-duel black turn-four seam is wrapper-shaped but still too local on its own. On Apr 8, 2026 an isolated `turn=4`, `mons_moves=1`, `action+mana` current-Pro guard did route the live board back from `l1,6;l2,7` to current `l2,3;l3,2`, but `pro-triage(primary_pro)` still stayed unchanged at the same `human_win_pro_c`-only `1/52`.
- The remaining human drift and the sampled fast black turn-four duel seam do not currently share one projection-family fix. On Apr 8, 2026 the shared probe showed `human_win_pro_c` winning through a higher followup floor on a root whose post-root projection became `SpiritImpact -> ImmediateScore`, while the fast duel board stayed an injected vulnerable `ManaTempo` root whose post-root projection became `SafeSupermanaProgress -> ImmediateScore`.
- A repeated later-black duel seam can justify its own retained cheap-surface foothold too. On Apr 8, 2026 the sampled `l1,6;l2,7` vs current `l2,3;l3,2` family became `primary_black_turn_four_action_mana_ply15` plus a widened probe, and the cheap gate moved to `2/57` with only that new seam plus `human_win_pro_c` changed.
- That later-black action+mana seam is not a final accept-after-search clamp. On Apr 8, 2026 the widened retained probe showed raw ranked roots and current still prefer `l2,3;l3,2`, but runtime-faithful v30 injects `l1,6;l2,7` and already collapses selected, `pre_accept`, and head onto the same vulnerable `ManaTempo` root under a `SafeSupermanaProgress -> ImmediateScore` plan.
- Compare retained black seams at injection time before assuming they share one production rule. On Apr 8, 2026 the black forced-root comparison probe showed both retained black families inject a root that was absent from raw roots and jump it straight to rank `0`, but the action+mana seam injects a vulnerable non-progress `ManaTempo` root while the later black family injects a genuine `SpiritImpact` progress root.
- A fresh traced black replay can still sit between those two retained families without justifying one shared injected-root rule. On Apr 8, 2026 the widened black forced-root probe added a traced fast `v7` board `l0,5;l1,4` vs current `l4,1;l5,0;mb`; it also injected an absent raw root, but only to injected rank `1` before focused rank `0`, with `head_family=SafeSupermanaProgress` and `goal_family=SpiritImpact`.
- Do not keep a shared black injected non-progress progress-head block just because it closes the retained action+mana seam and nearby traced fast replay boards. On Apr 8, 2026 that shared guard passed `guardrails`, returned `pro-triage(primary_pro)` to `1/57` with only `human_win_pro_c`, and passed `runtime-preflight`, but `pro-reliability` still failed at `0.5833` vs current Pro, `0.4167` vs current Normal, and `0.6667` vs current Fast.
- Even with the retained foothold, the isolated black wrapper repair is still not promotable. On Apr 8, 2026 a narrow current-Pro reroute closed `primary_black_turn_four_action_mana_ply15`, passed `guardrails`, returned `pro-triage(primary_pro)` to `1/57` with only `human_win_pro_c`, and passed `runtime-preflight`, but `pro-reliability` still failed at `0.7500` vs current Pro, `0.5000` vs current Normal, and `0.5833` vs current Fast.
- A fresh replay can stay non-actionable even after a new retained black foothold lands. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v7` still produced only count-`1` move pairs across Pro, Normal, and Fast; the later-black accepted-head seam resurfaced only once in Normal, while the other regressions were all new white or black one-offs, so there was still no repeated duel family worth code.
- A fresh replay can also surface attractive new top-ranked engine-post-search drifts and still be a kill. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v8` exposed a Pro black `SafeSupermanaProgress` root `l1,4;l2,5` over current `l2,3;l3,4`, a Normal white spirit-own-setup rerank `l3,8;l1,7;l2,8` over `l3,8;l3,6;l4,7`, and two Fast black spirit families, but every move pair still stayed at count `1` across Pro, Normal, and Fast.
- A cleaner duel seed is still not enough if the remaining wall fragments only in Fast. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v9` produced `0` regressions vs current Pro and only `1` regression vs current Normal, but Fast still broke into six unrelated count-`1` misses across black spirit families, black `ManaTempo` sibling reranks, and white spirit/forced-prepass reranks. That is still a diagnostics-only kill, not a code-ready family.
- The inverse is also true: Fast going clean does not make a duel replay actionable by itself. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v12` finished with `0` regressions vs current Fast, but direct Pro and Normal still each contained only count-`1` one-offs, including black spirit reranks `l0,4;l1,3` vs `l0,4;l1,5` and `l2,5;l0,5;l1,6` vs `l3,2;l4,1`, a black `ManaTempo` rerank `l1,5;l2,5` vs `l1,6;l0,6`, and a white safe-progress rerank `l9,5;l8,5` vs `l10,7;l9,8`. Kill that replay at diagnostics too.
- The recurring black `l0,5;l1,4` vs `l4,1;l5,0;mb` family now has a retained foothold. On Apr 8, 2026 `primary_black_mana_bridge_ply20` matched the old traced fast `v7` board exactly: the forced root was absent from raw roots, injected to rank `1`, then promoted to focused rank `0`, with `head_family=SafeSupermanaProgress` and `goal_family=SpiritImpact`.
- Compare retained black seams at runtime-faithful selection time too, not only at injection time. On Apr 8, 2026 `primary_black_turn_four_action_mana_ply15` and `primary_black_mana_bridge_ply20` both ended as `engine_post_search` accepted `ManaTempo` roots with `selected=pre_accept=head=forced` under a `SafeSupermanaProgress` head, while current stayed on a nearby `ManaTempo` baseline. That means the remaining difference is a narrow utility and risk preference, not a clean shared current-forcing repair.
- Family resemblance across duel buckets is still weaker than exact pair repetition. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v10` resurfaced the retained early-black `negative_deny` seam in direct Pro, the later-black accepted-head seam in Normal and Fast, and the known white forced-prepass family in Normal and Fast, but every exact move pair still stayed count `1`. Treat that as another diagnostics-only kill, not as permission to merge those families into one production branch.
- A repeated move pair inside `vs current Normal` still is not a retained Pro foothold by itself. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v11` repeated white `l9,5;l7,4;l8,3` vs current `l9,5;l7,6;l7,7` twice in Normal, but probing that exact board in Pro mode showed `selected=pre_accept=baseline=l10,4;l9,4`, `forced_inputs=None`, `accepted=false`, and both repeated Normal targets absent from the Pro shortlist. Kill that branch at diagnostics rather than retaining a fake `primary_pro` seam.
- Shared current baselines are still weaker than runtime-faithful stage matching. On Apr 8, 2026 a fresh Fast black drift `l1,5;l1,4` vs current `l3,2;l4,1` looked adjacent to the retained later-black accepted-head seam because both boards share current `l3,2;l4,1`, but the widened black forced-runtime probe showed a different story: `pre_accept` stayed on current, then `runtime_pro_turn_engine_v30` accepted a non-vulnerable `ManaTempo` head `l1,5;l1,4` under `goal_family=SpiritImpact`. That is a separate live rerank, not the retained late-black rejected-head surface.
- Not every white accepted-head board with `goal_family=ImmediateScore` belongs to the retained harvest score-route family. On Apr 8, 2026 `primary_white_safe_progress_rerank_ply27` retained a fresh Normal board where `runtime_pro_turn_engine_v30` already had `selected=pre_accept=head=l9,4;l8,3`, `accepted=true`, `drainer_vulnerable=false`, and a vulnerable `ManaTempo` root over a non-vulnerable `SafeSupermanaProgress` current baseline `l5,2;l4,1`. That is a separate accepted white rerank, not the retained harvest surface with `accepted=false` and vulnerable spirit siblings.
- The live duel wall can shift into a broader white turn-three family without touching the retained cheap surface. On Apr 8, 2026 a fresh duel trace showed three white `turn=3`, `mons_moves>0`, `action=false`, `mana=true` boards across Pro/Normal/Fast were all broad fallback drifts, and one repeated white `turn=3`, `mons_moves=1`, `action=true`, `mana=true` board was still a separate accepted-head seam.
- Even a broader white turn-three mana-only reroute can still be too local. On Apr 8, 2026 routing all white `turn=3`, `mons_moves>0`, `action=false`, `mana=true` states back to the current Pro surface fixed three live duel boards, but `pro-triage(primary_pro)` still stayed unchanged at the same `human_win_pro_c`-only `1/52`.
- A repeated live accepted-head seam still is not enough without a retained target-surface foothold. On Apr 8, 2026 the white `turn=3`, `mons_moves=1`, `action+mana` fast-duel seam (`l9,4;l8,4` vs `l8,7;l7,8`) repeated twice, but the runtime-faithful retained churn probe still showed `accepted=false` on every retained `primary_pro` fixture.
- A fresh early black spirit-own-setup drift can still be the wrong merge. On Apr 8, 2026 the new normal-duel board `l0,5;l1,6` vs current `l1,5;l3,6;l2,7` looked superficially like the retained `human_win_pro_c` setup-vs-progress bias, but the focused selector probe showed `negative_deny_competes=true` and `followup_progress_competes=false`, so it was a separate negative-deny spirit-preference seam rather than the retained human followup-progress story.
- A lone `vs current Pro` white regression can still be only wrapper churn. On Apr 8, 2026 the board `l9,2;l8,3` vs current `l10,7;l9,7` looked like a new Pro-only selector miss, but the focused probe showed the configured v30 path still pre-accepted the current-style root and the outer `runtime_pro_turn_engine_v30_guarded_inputs(...)` wrapper was the piece rerouting the board through the broad white `turn=3`, `action=false`, `mana=true` fast fallback.
- A later black `ManaTempo` drift can still be only a one-off forced shortlist seam. On Apr 8, 2026 the normal-duel board `l2,4;l3,3` vs current `l3,6;l2,7` injected a terrible `ManaTempo` head `l3,6;l3,7`, left it unaccepted, and still pre-accepted `l2,4;l3,3`; the retained runtime-faithful churn surface still had no matching forced-input seam outside the already-closed `primary_pvs_sensitive_search` case.
- A white fast-duel one-off can still be only wrapper churn too. On Apr 8, 2026 the board `l9,5;l8,5` vs current `l7,5;l6,4` looked like a new opening-family selector drift, but the focused probe showed configured v30 still pre-accepted current while the live path returned a move that was not even present in the configured root shortlist, so it was another outer white `turn=3`, `action=false`, `mana=true` wrapper miss.
- A white normal-duel one-off can still be only wrapper churn too. On Apr 8, 2026 the board `l10,4;l9,4` vs current `l8,6;l7,7` looked like a new opening-family selector drift, but the focused probe showed configured v30 still pre-accepted current on another white `turn=3`, `action=false`, `mana=true` board, so it was again only wrapper-level drift rather than a retained selector seam.
- A retained white mana-sibling seam can still reduce to wrapper churn too. On Apr 8, 2026 `primary_white_mana_sibling_ply9` looked like a new selector foothold until the focused wrapper probe showed the board was really `turn=3`, `mons_moves=3`, `action+mana`: guarded `runtime_pro_turn_engine_v30` fell through to `runtime_release_safe_pre_exact` and picked `l5,0;l5,1`, while current Pro still chose `l5,0;l4,1`. Lowering the existing white turn-three full-resource current-Pro fallback from `mons_moves>=5` to `mons_moves>=3` safely closed the retained seam and passed `guardrails`, `pro-triage`, and `runtime-preflight`, but the default `pro-reliability` wall stayed unchanged one-off churn.
- A fresh duel replay with only count-`1` move pairs is still a kill signal. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v2` produced no repeated move pair across direct Pro, Normal, or Fast duels; even the apparent later black `SpiritImpact` takeover and the resurfaced early black `negative_deny` seam each appeared only once per duel, so the right move was to stop at diagnostics and wait for a replay that also repeats on a retained `primary_pro` surface.
- A repeated live candidate can still be only a `search_only_forced_prepass` shell with no retained foothold. On Apr 8, 2026 the fresh `pro_turn_planner_reliability_v3` replay surfaced white fast `l8,4;l8,5` twice in `vs current Fast`, but the focused probe showed current and `pre_accept` both already prefer `l8,4;l7,3`; only the early `search_only_forced_prepass` shell overrides that to `l8,4;l8,5`, and the nearby `primary_white_fast_screen_opening_0_ply9` fixture did not share the same targets or stage.
- A later fresh replay can still collapse back into pure count-`1` churn immediately after a promising diagnostic family. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v4` produced only one-off accepted-head, spirit-setup, and mana-tempo reranks across direct Pro, Normal, and Fast, so none of those new-looking seams justified a retained fixture or production split.
- A direct-Pro black spirit-vs-spirit drift is not automatically a retained black seam. On Apr 8, 2026 the one-off `l1,5;l2,3;l3,3` vs current `l1,5;l2,7;l1,8` board looked adjacent to the retained black families, but the focused probe showed all spirit-pref competition flags stayed `false`, the forced head `l1,5;l1,7;l0,7` was still rejected, and the whole candidate shortlist stayed a vulnerable plain-spirit cluster. That means it is not the retained early-black `negative_deny` story and not the retained later black accepted-head story either.
- A direct-Pro white spirit-own-setup drift is not automatically `human_win_pro_c` in reverse. On Apr 8, 2026 the one-off `l4,9;l4,7;l5,7` vs current `l9,4;l8,3` board looked superficially like the retained human spirit-vs-progress surface, but the focused probe showed `followup_progress_competes=false`, `progress_competes=false`, and `risky_score_competes=true` while both roots remained vulnerable. That makes it a separate risky-score spirit cluster, not the retained human followup-floor seam.
- When the retained cheap surface is down to `human_win_pro_c` alone, that is not permission to reopen a human-only production split. On Apr 8, 2026 the refreshed `human_win_pro_c` selector probe still showed the same safe-progress / followup-floor story, while the fresh direct-Pro white one-off stayed a separate risky-score spirit cluster. With the live duel wall still off-surface count-`1` churn, another human-only fix would still be a dead line.
- A repeated live family can justify a retained fixture addition even when the old retained surface misses it. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v3` repeated the early black `l0,5;l1,6` vs current `l1,5;l3,6;l2,7` seam `3x` in direct Pro, `2x` in Normal, and `1x` in Fast, so the right durable move was to add `primary_black_negative_deny_ply4` plus a focused selector probe rather than keep treating it as replay-only.
- The repeated black early seam is an over-broad plain-spirit `negative_deny` story, not a followup-progress story. On Apr 8, 2026 the new retained selector probe showed `negative_deny_competes=true` while `progress_competes`, `followup_progress_competes`, `risky_score_competes`, `score_competes`, `projection_competes`, and `risky_recovery_competes` all stayed `false`; the losing plain-spirit projections were the piece blocking stronger spirit-own-setup roots.
- Even that shared selector cut can still be too local. On Apr 8, 2026 a narrow override that ignored the plain-spirit `negative_deny` block when a spirit-setup root already beat every non-spirit challenger fixed `primary_black_negative_deny_ply4` and passed `guardrails`, but `pro-triage(primary_pro)` still fell back to the stale `human_win_pro_c`-only `1/53`, so the production change was killed before `runtime-preflight`.
- Even a retained seam that survives the full cheap loop can still be the wrong branch. On Apr 8, 2026 the same early-black `negative_deny` override was pushed through `guardrails`, `pro-triage(primary_pro)=1/53`, and `runtime-preflight`, but `pro-reliability` still failed at `0.8333` vs current Pro, `0.4167` vs current Normal, and `0.5833` vs current Fast; a fresh `v3` duel trace showed the live wall had shifted to a later black accepted-head seam `l1,5;l1,7;l0,7` vs current `l3,2;l4,1` repeated `2x` in Pro and `3x` in Normal, while the old early-black family still resurfaced once in Pro.
- The later black accepted-head family now has a retained foothold too. On Apr 8, 2026 `primary_black_late_accepted_head_ply4` plus `smart_automove_pro_black_late_accepted_head_probe` showed current and `pre_accept` both prefer `l3,2;l4,1`, while a forced plain `SpiritImpact` head `l1,5;l1,7;l0,7` can still be accepted on projected-plan utility even though the head root is lower-scored and brings only non-concrete spirit progress.
- Closing both retained black seams together is still not enough. On Apr 8, 2026 the combined early-black `negative_deny` override plus late-black plain-spirit-progress block passed `guardrails`, moved `pro-triage(primary_pro)` to `1/54` with only `human_win_pro_c`, and passed `runtime-preflight`, but `pro-reliability` still failed at `0.8333` vs current Pro, `0.5000` vs current Normal, and `0.5833` vs current Fast. The default duel replay then fell back to one white wrapper miss in direct Pro, three one-off Normal misses, and the old repeated white fast accepted-head seam, so future spend should not keep black-only production cuts without a broader white live-wall story.
- The repeated white fast accepted-head family now has a retained foothold too. On Apr 8, 2026 `primary_white_fast_accepted_head_ply13` plus `smart_automove_pro_white_fast_accepted_head_probe` showed current and `pre_accept` both prefer `l8,7;l7,8`, while a forced `SafeSupermanaProgress -> DrainerSafetyRecovery` head `l9,4;l8,4` could still be accepted even though the head root stayed unsafe and brought no concrete immediate recovery.
- Closing the retained white fast accepted-head seam is useful, but still not promotable. On Apr 8, 2026 the combined retained branch that fixed early-black `negative_deny`, later-black accepted-head drift, and the new white deferred-recovery accepted head passed `guardrails`, moved `pro-triage(primary_pro)` to `1/55` with only `human_win_pro_c`, and passed `runtime-preflight`, but `pro-reliability` still failed at `0.8333` vs current Pro, `0.5000` vs current Normal, and `0.7500` vs current Fast. The default duel replay showed the repeated white fast seam was gone and Fast regressions were down to three one-offs, yet Pro still had one wrapper one-off and Normal still had the same three one-offs.

## Durable Workflow Rules

- Keep the active frontier small: one live idea, one retained candidate, one canonical path.
- For Pro work, compare directly against `runtime_current`.
- The canonical Pro path is `guardrails -> pro-triage(primary_pro) -> runtime-preflight -> pro-reliability`.
- `opening_reply` is a narrow fallback-order and opening-regression surface, not the default Pro surface.
- `pro-triage` only passes when the target surface changes and off-target churn stays at `<= 1`.
- `runtime-preflight` is the required stamp before duel stages unless the run is intentionally diagnostic.
- `pro-reliability` is the first real promotion gate. It must clear current `Pro`, current `Normal`, and current `Fast` at `win_rate >= 0.90`, `confidence >= 0.99`, and `candidate_avg_move_ms <= 700`.
- `pro-reliability-confirm` is the final promotion proof. Do not promote on smaller-corpus evidence.
- Prefer a fresh live `pro-reliability` sample over diagnostic probes when the wall is unclear.
- Use `triage-calibrate` only when a retained triage surface is new or no longer calibrated.
- Use `pro-opening-speed-probe` only for opening-specific regressions.
- Use the hotspot probe only after a real duel stall, and only to narrow the next code surface.
- When the hotspot corpus stays flat but the direct wall is still unclear, use the duel-replay probe before changing shared code.
- If the compare hotspot probe shows decision parity on the real hotspot cases, kill the line immediately. Selector/exact counter deltas without candidate-vs-current move differences are not promotion evidence.
- Logs, stamps, and process samples are disposable evidence, not durable memory.
- Keep ignored harness test names unique; `cargo test` substring filters can hit the wrong stage.

## Mistakes Not To Repeat

- Do not reopen archived profile IDs or retired branch families without a brand-new hypothesis.
- Do not spend another loop on wrapper-only reroutes, current-Normal fallbacks, or replay-specific acceptance clamps just because they fix a traced exact.
- Do not retain a traced white turn-three mana-only wrapper reroute unless it also moves `primary_pro`; fixing replay boards alone was not enough.
- Do not retain a broader white-turn-three plus black one-move wrapper bundle if it still leaves `pro-triage(primary_pro)` unchanged at `1/52`, even when multiple traced duel boards now match current.
- Do not reopen cache-size, memo-shape, reserve-heavy, or hasher experiments without a direct quality story tied to the live duel wall.
- Do not reopen broad search-budget, reply-budget, or generic search-knob clamps without evidence that the real wall lives on that surface.
- Do not keep branches just because disagreement counts shrink or hotspot counters move. If direct duel quality stays flat or regresses, kill the line.
- Do not retain a duel-traced acceptance clamp just because it fixes one repeated seed. If `pro-triage(primary_pro)` does not move, kill the production split and keep only the probe or lesson.
- Do not assume `human_win_pro_c` and the sampled fast black turn-four divergence can be fixed by one shared projection clamp. If the probe says the human drift is a followup-floor `SpiritImpact` story and the duel board is a vulnerable `ManaTempo` progress-head story, kill the shared idea before code edits.
- Do not attack `primary_black_turn_four_action_mana_ply15` with another acceptance-only clamp. If the retained probe shows raw ranked roots still prefer current while forced engine injection already makes `l1,6;l2,7` the selected/pre-accept/head root, the live lever is earlier than the final accept gate.
- Do not merge the retained black action+mana seam and the retained later-black spirit-head seam into one shared injection fix just because both forced roots jump from absent raw roots to injected rank `0`. If one injected root is still a vulnerable non-progress `ManaTempo` root and the other is a genuine spirit-progress root, keep the probe and kill the shared production idea.
- Do not reopen from a fresh duel replay like `pro_turn_planner_reliability_v7` just because it shows both the retained later-black accepted-head move and a new batch of white reranks. If every move pair still stays count-`1` inside its duel bucket, keep only the lesson and wait for a replay that repeats on a retained surface.
- Do not retain a broader white `turn=3`, `mons_moves>0`, `action=false`, `mana=true` reroute just because it fixes several live duel boards. If `pro-triage(primary_pro)` does not move off the stale `human_win_pro_c`-only `1/52`, kill it before `runtime-preflight`.
- Do not retain a repeated white `turn=3`, `mons_moves=1`, `action+mana` accepted-head clamp just because it repeats in fast duels. If the retained churn probe still shows `accepted=false` on every retained `primary_pro` fixture, kill the idea before code edits.
- Do not merge an early black `negative_deny_competes` spirit-setup drift with `human_win_pro_c` just because both are spirit-vs-non-spirit selector stories. If the black board shows `followup_progress_competes=false`, treat it as a separate one-off seam and kill the shared idea before code edits.
- Do not treat a lone `vs current Pro` white mana-only regression as a new selector seam until the probe proves it is not just another `runtime_pro_turn_engine_v30_guarded_inputs(...)` fast-fallback reroute. If configured v30 already pre-accepts the current root, kill the idea before code edits.
- Do not reopen a later black `ManaTempo` shortlist repair unless the retained runtime-faithful churn probe shows a matching forced-input seam on `primary_pro`. A one-off injected head with `accepted=false` is still not enough by itself.
- Do not treat a white fast-duel one-off as a new selector seam if the live move is missing from the configured root shortlist. That is another wrapper-level drift, not a retained in-path surface.
- Do not treat a white normal-duel one-off as a new selector seam if configured v30 already pre-accepts current on another `turn=3`, `action=false`, `mana=true` board. That is still wrapper churn, not a retained in-path surface.
- Do not stretch a fresh duel seed with only count-`1` move pairs into a production split. If Pro, Normal, and Fast all come back as one-off seams, keep only the lesson and wait for a replay that repeats on a retained surface.
- When the canonical cheap gate explicitly reconfirms `opening_reply=0/3`, `primary_pro=1/55`, `off_target_changed=0`, and the only changed fixture is still `human_win_pro_c`, treat the retained frontier as exhausted for now. Do not reopen a human-only selector split from that state while fresh duel seams remain off-surface count-`1` churn.
- Do not reopen from a fresh duel replay just because previously known black families resurface. On Apr 8, 2026 `SMART_PRO_RELIABILITY_SEED_TAG=pro_turn_planner_reliability_v5` brought back the early-black `negative_deny` seam, the later-black accepted-head seam, and a new white forced-prepass drift, but every move pair still stayed count-`1` inside its duel bucket, so the replay was still non-actionable churn.
- Do not reopen a white forced-prepass production split just because a new duel replay lands on another `DrainerSafetyRecovery` root. On Apr 8, 2026 the widened white forced-prepass probe showed the new Normal `l9,4;l8,5` vs `l9,4;l8,3` seam is the same live-only `search_only_forced_prepass` family as the old Fast board: the live boards are `drainer_vulnerable=true` and override vulnerable `ManaTempo` pre-accept/current roots, while the retained comparison fixture still stays `drainer_vulnerable=false` and `engine_disabled`.
- Do not reopen a white score-route production split just because `l7,4;l8,3` reappears in a fresh direct-Pro replay. On Apr 8, 2026 the dedicated white score-route probe showed the traced `l7,4;l8,3` vs `l9,7;l7,6;l8,7` seam is an accepted `SafeSupermanaProgress -> ImmediateScore` override on a vulnerable board, while the retained harvest fixture `primary_harvest_white_score_route_win_a` stays a different `engine_post_search` surface where current already matches and the plain `l7,4;l8,3` mana route is only a lower-ranked sibling.
- Do not reopen the retained late-black accepted-head fix just because `l1,5;l1,7;l0,7` reappears in a fresh duel replay. On Apr 8, 2026 the widened black late accepted-head probe showed the new Normal `l1,5;l1,7;l0,7` vs `l4,1;l5,0;mb` seam is a different root-selection case: `pre_accept` already chooses the spirit head, `accepted=true`, and `goal_family=SpiritImpact`, while the retained fixture remains a rejected `SpiritImpact -> ImmediateScore` head over a `ManaTempo` root.
- Do not reopen from a white `l8,3;l8,2` vs `l8,3;l9,2` replay without a retained surface. On Apr 8, 2026 the traced board showed an `engine_disabled` live-only `ManaTempo` sibling tie: forced head/current already matched `l8,3;l9,2`, candidate differed only by picking the equal-utility sibling `l8,3;l8,2`, and both roots stayed identically vulnerable.
- Do not cut production code from a fresh replay like `pro_turn_planner_reliability_v6` when every duel bucket still stays count-`1`. Even if the seed reintroduces multiple already-classified shapes at once, such as white forced-prepass drainer-safety, white `ManaTempo` sibling reranks, the non-retained late-black spirit-head, and a one-off black mana rerank, that is still non-actionable churn until one family repeats on a retained surface.
- Do not reopen the retained white harvest score-route fix just because a fresh replay lands on another `l9,6;...` vulnerable spirit rerank. On Apr 8, 2026 the widened white score-route probe showed the `v6` board `l9,6;l8,4;l7,4` vs `l9,6;l7,4;l7,3` is another accepted spirit-sibling rerank with `pre_accept` already on a nearby spirit root, while the retained harvest fixture remains a different rejected-head surface where current already matches.
- The repeated white `l5,0;*` family now has a retained foothold: `primary_white_mana_sibling_ply9` plus its probe. It is an `engine_disabled`, non-vulnerable `ManaTempo` sibling rerank where current/pre-accept stay on `l5,0;l4,1` and the challenger picks another same-family sibling. Keep the seam, but do not cut production code until there is a bounded selector explanation for why v30 prefers the lower-ranked sibling.
- Do not keep a new shared negative-deny selector override just because it closes a repeated black duel seam and its retained fixture. If the cheap gate still snaps back to the same stale `human_win_pro_c`-only churn, kill the production change and keep only the new retained fixture/probe.
- Do not keep that same shared negative-deny override just because it also clears `runtime-preflight`. On Apr 8, 2026 the full-loop retry still failed direct duels and merely moved the wall onto a later black accepted-head family, so the right outcome was still to kill the production change and keep only the retained foothold plus the lesson.
- Do not treat late white full-resource current-Pro wrappers as a safe way to clear `human_win_pro_c`; if the fixture does not move immediately, kill the idea.
- Do not retain a late-white omitted-root rescue that only drives `pro-triage(primary_pro)` to `0/52`; matching current everywhere on the cheap surface is still a dead line.
- Do not retain a same-lane own-setup-vs-progress override just because it clears `human_win_pro_c`; if it reopens black reliability fixtures or tanks direct duels, kill it immediately.
- Do not retain white-only or black-only local seam repairs that fail to move the broader `vs current Normal` wall.
- Do not treat the relaxed `700ms` move-time cap as permission to reopen parity-preserving speed regressions.
- Do not retain new scratch profile IDs when the shared fix can live under `runtime_pro_turn_engine_v30` and the scratch line still fails direct duel evidence.

## Current Durable Direction

- Speed is already acceptable on the retained Pro challenger. Promotion is blocked by quality, especially versus current Normal, not by the `700ms` move-time budget.
- The remaining work should favor broader in-path selector and root-choice changes over wrapper fallbacks, fallback widening, or exact replay fixes.
- The most credible future wins are shared selector/search families that move direct `runtime_pro_turn_engine_v30` vs `runtime_current` results, not local acceptance or wrapper repairs that only clear one traced seam.
- `human_win_pro_c` may still be a retained triage drift, but it is not a hotspot-backed duel seam today. Do not reopen shared ProV2 selector work on that fixture alone without fresh direct duel or hotspot evidence.
- The current human drift and sampled fast black turn-four duel seam still look separate. Treat them as different hypotheses unless a fresh duel replay proves they have converged onto the same projection surface.
- The current white turn-three wall is split in two: the mana-only mid-turn wrapper family is closed again as too local, while the repeated `action+mana` accepted-head seam (`l9,4;l8,4` vs `l8,7;l7,8`) remains live.
- The repeated white `action+mana` seam still does not have a retained cheap-surface foothold today. Do not spend another production split on it until a fresh target-surface probe shows the same accepted-head family on `primary_pro`.
- The fresh black normal-duel `l0,5;l1,6` vs `l1,5;l3,6;l2,7` seam is not a retained foothold today. It is a one-off `negative_deny` spirit-preference drift, not the retained human followup-progress seam, so it does not justify a new production split by itself.
- The lone `vs current Pro` white `l9,2;l8,3` vs `l10,7;l9,7` miss is not a new foothold today either. It is another member of the already-closed white `turn=3`, `action=false`, `mana=true` wrapper family, so it does not justify reopening that production split.
- The later black normal-duel `l2,4;l3,3` vs `l3,6;l2,7` seam is not a retained foothold today either. It is a one-off forced-head shortlist distortion with no retained `primary_pro` counterpart outside the already-closed `primary_pvs_sensitive_search` family.
- The white fast-duel `l9,5;l8,5` vs `l7,5;l6,4` seam is not a retained foothold today either. It is another member of the already-closed white `turn=3`, `action=false`, `mana=true` wrapper family, so it does not justify reopening that production split.
- The white normal-duel `l10,4;l9,4` vs `l8,6;l7,7` seam is not a retained foothold today either. It is another member of the already-closed white `turn=3`, `action=false`, `mana=true` wrapper family, so it does not justify reopening that production split.
- The fresh `pro_turn_planner_reliability_v2` duel replay also does not supply a retained foothold today. It produced only one-off move pairs across direct Pro, Normal, and Fast, so neither the later black `SpiritImpact` takeover nor the resurfaced early black `negative_deny` seam is strong enough yet to justify production code.
- The repeated black `negative_deny` family now does have a retained foothold today: `primary_black_negative_deny_ply4` plus its selector probe. What it still does not have is a promotable production fix; the first shared override closed that new seam but still left the overall cheap surface at stale human-only churn.
- The later black accepted-head family `l1,5;l1,7;l0,7` vs current `l3,2;l4,1` is now the stronger live wall than the retained early-black seam. On Apr 8, 2026 the full-loop retry of the early `negative_deny` fix repeated that later seam `2x` in direct Pro and `3x` in Normal, while Fast added only one copy plus one-off white forced-prepass drifts, so future spend needs either a retained foothold for that later family or proof that it is only another wrapper-shaped artifact.
- That later black family now does have a retained foothold today, but it is not the dominant live wall after the combined black retry. On Apr 8, 2026 the first retained later-black fixture/probe made the seam durable, yet a full-loop branch that closed both retained black seams still replayed mostly white wrapper and accepted-head churn, with only one-off black drift outside Fast.
- The repeated white fast accepted-head seam is no longer the dominant live wall today. On Apr 8, 2026 the retained white deferred-recovery guard removed that repeated `l9,4;l8,4` vs `l8,7;l7,8` pair entirely from the default reliability replay; Fast improved to `0.7500`, and the remaining regressions across Pro/Normal/Fast were all one-offs. Future spend should target the surviving Normal one-offs or wait for a fresh repeated family rather than reopening that white accepted-head seam.
- The latest retained shared win was a selector hygiene fix, not a new branch family: reject non-concrete one-chunk progress heads when they only override an unsafe non-progress selected root without a completed plan.
- Reducing `primary_pro` churn is not enough on its own. The retained line now reaches `2/52` changed fixtures on `primary_pro`, but `pro-reliability` is still flat at `0.8333` vs current Pro, `0.5000` vs current Normal, and `0.6667` vs current Fast. Future spend needs a direct duel-quality hypothesis.
- Closing the late `primary_pvs_sensitive_search` seam was still not enough. The retained line is now down to `1/52` changed `primary_pro` fixtures, but `pro-reliability` remained flat at `0.8333` vs current Pro, `0.5000` vs current Normal, and `0.6667` vs current Fast on Apr 8, 2026.
- Fixing the retained duel-accept seams and clearing `human_win_pro_c` is still not enough on its own. The latest retained attempt kept `primary_pro` at `1/52` changed fixtures and stayed preflight-clean, but `pro-reliability` still failed at `0.7500` vs current Pro, `0.6667` vs current Normal, and `0.5000` vs current Fast.
